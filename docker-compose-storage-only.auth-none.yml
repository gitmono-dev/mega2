# Opt-in override: remount push_auth=none sample (plan-20260906 ADR-SO-06).
# Must be composed with the base file and recreate mega2:
#
#   docker compose -p mega2-trunk \
#     -f docker-compose-storage-only.yml \
#     -f docker-compose-storage-only.auth-none.yml \
#     up -d --wait --force-recreate mega2
#
# Revert to token: drop the second -f and --force-recreate mega2 again.

services:
  mega2:
    volumes:
      - ./config/config-storage-only.none.toml:/etc/mega2/config.toml:ro
