# Git 使用场景测试补全计划（2026-08-03）

**模板版本:** v2（成稿符合 `docs/plan/plan-template.md` v2）

## 文档职责

本文解决 `plan-long.md` **PT-04**（Git 协议集成测试夹具与真实 CLI 兼容矩阵）相对 PT-01 已交付 HTTP 最小矩阵仍存在的用户场景缺口。目标是：

1. 冻结以真实用户命令为轴的场景覆盖表，并区分 cargo-native、shell smoke 与 DEFER；
2. 补齐 HTTP 字面 `git pull`、关闭匿名读后的真实客户端拒绝、HTTP LFS 往返；
3. 以 cargo-native 自启 `service ssh` 的唯一拓扑补齐 SSH clone/pull/push 与坏密钥拒绝；
4. 审计 Mega 协议夹具，在固定类型的 implementation / handoff 卡之间做一次 go/no-go 选择；
5. 把新增 target 纳入具体 CI job，并同步 PT-04、历史 DEFER 与开发文档。

本文只规划任务，不宣称产品或测试代码已经实现。每卡开工前须按 ER-02 刷新锚点。

### 适用范围

- `bin/tests/integration_git_cli.rs`、新 target `bin/tests/integration_git_lfs.rs` / `bin/tests/integration_git_ssh.rs`，以及 `bin/tests/common/` helper。
- LFS runner：`Dockerfile.git-cli`、`docker-compose.test.yml`。
- CI：仅 GM-11A 可修改 `.github/workflows/config-validation.yml`；仅 GM-11C 可修改 `.github/workflows/git-protocol-smoke.yml`。
- 文档：`docs/refactoring/protocol.md`、`integration.md`、`test-infra.md`、`mega-git-fixtures-audit.md`、`docs/plan/plan-long.md`、`plan-20260727.md`、`README.md` 与本计划。
- Mega 只读审计：`../mega/tests/{data,diff,objects,refs,scripts}` @ `42cd288d266dade371aed03917d222eb57f1d5a4`。

### 非目标

- 默认以补测为主，不主动扩产品范围；但若 ADR-GM-07 测试基线因本卡用例实际行使的产品路径失败，必须扩写 `Implementation write set`、按 ER-10/G-* 重跑后就地修码至基线绿。仅当根因越出本卡被测路径时才移交 DEP-05 / `FIX-GM-*`。
- 不追求 Git 官方测试套件级覆盖。
- 不整树复制 Mega 夹具；仅允许 GM-09 审计后启用的 GM-10A 最小 allowlist。
- 不增加 compose SSH 服务；SSH 只允许 ADR-GM-05 的 cargo-native 自启拓扑。
- 不承诺 ImportRepo 全矩阵、pure SSH LFS、protocol v2 写路径与真多进程拓扑。

### 成功定义

- 场景表每格都有具体值（例如 `cargo:integration_git_cli_http_pull_cl_ref_round_trip`、`smoke:lfs_smoke_http`）、`DEFER-GM-*` 或 `N/A+理由`，无幽灵覆盖。
- HTTP 字面 `git pull` 与 `anonymous_access=false` 真实客户端拒绝进入 `integration_git_cli`。
- LFS push → 定向取回 → `git lfs pull` 字节往返进入 `integration_git_lfs`，runner 缺 LFS 时硬失败。
- SSH 使用每 case 独立 host/client key、临时端口、`ssh_keys` 指纹播种、固定 `known_hosts` 与 `GIT_SSH_COMMAND`，进入 `integration_git_ssh`。
- GM-09 复审后 GM-10A/GM-10B 恰好一张可执行，另一张标记 `cancelled`；没有双 Task type。
- 两个新增 target 进入明确 workflow/job；`plan-long.md` PT-04 关联日期计划由 `无` 更新为 `plan-20260803.md`。

## 事实基线

> 核对日：2026-08-03。执行时仍须按 ER-02 刷新。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| HTTP cargo 黑盒 | `integration_git_cli` 已覆盖 clone→认证 push→定向 fetch/checkout、未认证 push、failpath/liveness；没有字面 `git pull` | `bin/tests/integration_git_cli.rs:298,523,639,787` |
| runner/helper | compose `git-cli` pin `git version 2.49.1`；已有 access token SQL helper、端口/PID证据与进程 guard | `bin/tests/common/git_cli.rs:18-25,621-640`；`integration_git_cli.rs:163-291` |
| Git 配置 | `config/config.toml` **没有** `[git]`；`GitConfig::default()` 在模型中把 `anonymous_access` 设为 `true` | `src/config/model.rs:952-975`；2026-08-03 直接检查样例配置 |
| SSH 服务 | CLI 为 `service ssh --host 127.0.0.1 --ssh-port PORT`；host key 由 Vault `ssh_server_key` 生成/保存 | `src/commands/service/ssh.rs`；`src/server/ssh_server.rs:25-103` |
| SSH 鉴权 | 服务计算 SHA-256 fingerprint，并要求 `ssh_keys.finger` 命中且 username 唯一 | `src/contract/git_protocol/ssh.rs:228-270`；`src/callisto/ssh_keys.rs` |
| LFS/SSH smoke | LFS 仅 shell smoke；SSH 依赖外部 URL 且默认 CI 不跑 | `scripts/git_protocol_smoke.sh:176-277` |
| CI | `config-validation.yml` job=`validate-config`，push main，paths 已含 `bin/tests/**`/compose/test-infra；`git-protocol-smoke.yml` job=`git-protocol-smoke`，push main，paths 含 protocol/compose 但不含 `bin/tests/**` | 两个 workflow，核对日 2026-08-03 |
| Mega pin | live sibling HEAD 为 `42cd288d266dade371aed03917d222eb57f1d5a4` | `cd ../mega && libra rev-parse HEAD`，2026-08-03 R4 再核对 |
| 长期计划 | PT-04 状态为「已验证」，关联日期计划仍为 `无` | `docs/plan/plan-long.md:105` |
| 日期索引 | README 已登记本计划为「进行中」 | `docs/plan/README.md:25` |

### 当前缺口

| ID | 缺口 | 影响 | 计划动作 |
|---|---|---|---|
| GAP-01 | 无字面 `git pull` | 用户主读路径无回归证据 | GM-02 |
| GAP-02 | `anonymous_access=false` 无真实 CLI | 配置可静默失效 | GM-03 |
| GAP-03 | LFS 不在 cargo 分层门 | DEFER-IT-02 未关闭 | GM-05（GM-04 已取消并入） |
| GAP-04 | SSH 无确定性 cargo 拓扑与指纹播种 | 认证矩阵不可复现 | GM-06、GM-06A、GM-07、GM-08（各独立 patch） |
| GAP-05 | Mega 夹具许可/体积/生成方式未审计 | 无法选择移植或重建 | GM-09、GM-10A/GM-10B |
| GAP-06 | 场景表与 target/CI 归属漂移 | 会产生幽灵格 | GM-01、GM-11A、GM-11C、GM-11B |
| GAP-07 | DEFER-IT-12 missing-repo 边界未收口 | PT-04 故障路径口径不清 | GM-01、GM-11B |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` PT-04 | 本计划是日期执行层 | GM-11B 更新关联日期计划与完成/剩余边界 |
| `plan-long.md` PT-03 | 协议实现承接方 | 本计划只移交补测暴露的产品缺陷 |
| `plan-20260727.md` | 提供 PT-01 基线与 DEFER-IT-01/02/12 | GM-11B 统一回写，行为卡不写历史计划 |
| `protocol.md` / `integration.md` | 场景表与覆盖索引 | 仅 GM-01→GM-11B 串行修改 |
| `test-infra.md` | runner/SSH 生命周期事实源 | 仅 GM-11B 修改；GM-06 只实现可验证拓扑 |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | pull/LFS/SSH 是 PT-04 用户可见缺口 | 保留三条行为轴 |
| 粒度 | 空 GM-04 合入 GM-05；GM-10/11 分裂为固定类型卡 | 见审计表 |
| 可执行性 | SSH 只有 cargo 自启方案；config 与密钥播种写死 | ADR-GM-05 / GM-06 |
| 并发 | 文档写集归 GM-01→GM-11B；其余交叉写集均用依赖边串行 | G-10 无豁免 |
| 发布 | 每张 implementation/release 卡完成即独立 patch；docs 等 no-release 由 GM-12 承载；无家族合并发布 | ADR-GM-08 / ER-12 |
| 证据 | focused 命令固定 test fn、禁 SKIP、证明恰好运行一次 | Verification 判定口径 |
| 完成/基线 | 每卡完成定义重写；测试基线红则轴内修码至绿 | ADR-GM-07 / 完成定义表 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响引用 |
|---|---|---|---|---|
| 2026-08-12 | 状态同步（post-completion） | 计划外索引对齐：`docs/plan/README.md` 与 `docs/plan/plan-long.md` 将本计划状态由「进行中/实施中」更正为「已完成」（GM-12 已于 v0.2.11 发布、D 组绿、v0.2.15 完成度复审收口），PT-04 总览由「已排期」升为「实施中」（`DEFER-GM-01..05` 仍为 follow-up）；GM-11B VER-4 的 README「进行中」断言系 GM-12 发布前快照，补执行后注记 | `N/A` | `docs/plan/README.md`、`docs/plan/plan-long.md`、GM-11B 注记 |
| 2026-08-06 | 完成度复审（post-completion review） | ① SSH harness 可移植性修复：`container_path_for_host` 对尚不存在的路径改用「最深存在祖先 canonicalize + 拼回余段」，消除 macOS `/tmp`→`/private/tmp` symlink 下 4 个 `integration_git_ssh_*` 用例的前缀断言假失败（Linux 目标行为不变）；同文件修复 `expected_init_monorepo_fixture` 文档注释后空行（`clippy -p monoengine -D warnings` 编译失败）；② GM-03/05/06/06A/07 补勾收口时漏勾的 AC/VER（静态逐项复核 + 动态重跑全绿）；③ GM-08 粒度审计表行 `1/0`→`1/4`（与卡内 Granularity 一致）；④ GM-09 登记 `../mega` HEAD 前进一格的 pin 漂移注记（审计结论锚定 pin 不变）；⑤ GM-10B VER-2 第二正则改为终态 `done`/`complete`；⑥ GM-11B VER-3 改逐关键词判定，消除 rg 15.2 多 pattern CJK 漏报假 FAIL | `N/A` | GM-03/05/06/06A/07/08/09/10B/11B、`bin/tests/common/git_cli.rs`、`bin/tests/integration_git_ssh.rs`、`docs/refactoring/mega-git-fixtures-audit.md` |
| 2026-08-03 | P2 清理 | 删除任务卡节内模板占位默认值/示例审计行，改指向上文权威默认节 | 无卡拆分 | Review log |
| 2026-08-03 | Codex R11 remediation | GM-12 状态解析兼容 N/A；post_d 含 GM-12=done/complete | GM-12 | Review log |
| 2026-08-03 | Codex R10 remediation | GM-12 仅 D 后提升 done/complete；post_d 校验 Lifecycle/Acceptance | GM-12 | Review log |
| 2026-08-03 | Codex R9 remediation | 调和 ADR-GM-07 与非目标；GM-12 拆 pre_c/post_c 消除 complete 环 | GM-12 | Review log |
| 2026-08-03 | 用户修订 | 完成定义重写；ADR-GM-07 基线失败即修；ADR-GM-08 废止 REL-GM-01/GM-R1，SSH 子卡改独立 patch；GM-12 allowlist 去 GM-R1 | `GM-R1 → cancelled`；`GM-06..08 → independent` | DAG、发布、D 表、里程碑、Review log |
| 2026-08-03 | 成稿 | 首版 Git 使用场景测试补全计划 | `N/A` | 全文 |
| 2026-08-03 | G-08 自审 | SSH 家族发布点拆为行为less GM-R1 | `GM-08 → GM-08, GM-R1` | DAG、发布、里程碑 |
| 2026-08-03 | Codex R7 remediation | GM-R1 R-only 排序+收引号；GM-11A/C jobs: 限定与 docs 脏树；GM-11B DEFER GO/NO-GO；GM-11C AC/VER=6/8 | 多卡 | Review log |
| 2026-08-03 | Codex R6 remediation | GM-R1 R-only=release commit 路径；审计同步；GM-11A/C VER；GM-11B DEFER 行字段；去掉 `|| true` | 多卡 | Review log |
| 2026-08-03 | Codex R5 remediation | 子卡门改 Acceptance=`locally-accepted`；版本面含 maj/min；GM-06/10A/11B/R1 单次相等+R-only VER；GM-12 排除自身 | 多卡 | Review log |
| 2026-08-03 | Codex R4 remediation | Mega pin→live `42cd288…`；GM-06/10A/11B/R1/12 按 ADR-GM-06 单相等谓词；CI paths 保留既有触发并超集新增；GM-12 按最大轮次判定 PASS | 多卡 | Review log |
| 2026-08-03 | Codex R3 remediation | Publisher=`eli`；GM-11A/C/R1/12 改 Deliverables 集合/序列相等；审计同步；GM-12 最新 PASS 门；GM-06 cleanup 合并；ADR-GM-06；R4 前再同步 AC/VER 摘要与 GM-06A/GM-09 allowlist 压缩 | 多卡 | Review log |
| 2026-08-03 | Codex R2 remediation | 关闭 R2 残留 P1：嵌入模板 GC/ER/G/Verification 正文；全卡补 Current evidence/Out of scope；具名 Publisher `eli`；GM-R1 AC/VER 按独立谓词重计为 4/12 与 3/12 | `GM-R1` | Review log、审计表、强制章节 |
| 2026-08-03 | Codex R1 remediation | 关闭 R1 全部 P1：恢复 v2 规则节；取消 GM-04 并合入 GM-05；拆 GM-06 生命周期/认证、GM-10A/B、GM-11 workflow/docs；冻结 SSH；统一 DAG/写集/C-D/计数/命令/Mega pin/config 事实/回滚 | `GM-04 → cancelled + GM-05`；`GM-06 → GM-06, GM-06A`；`GM-10 → GM-10A, GM-10B`；`GM-11 → GM-11A, GM-11B, GM-11C` | 全文；等待 R2 复审 |

## 已决议设计决策

### ADR-GM-01: 场景表是文档唯一事实源

- **Status:** Accepted
- **Decision:** `docs/refactoring/protocol.md` 保存唯一完整表；`integration.md` 只保存 target 索引与链接。单元格只能是 exact cargo test fn、smoke case、DEFER 或带理由 N/A。
- **Consequence:** GM-01 建表，GM-11B 在所有实现终态后收口；中间行为卡不写这两份文档。

### ADR-GM-02: monorepo pull 定向 CL tip

- **Status:** Accepted
- **Decision:** 测试执行 `git pull origin refs/cl/CASE_ID` 到明确本地分支，并断言字节树；默认 main 仍保持种子树。只断言 exit 0 不合格。

### ADR-GM-03: Mega 夹具 go/no-go 与固定类型分支

- **Status:** Accepted
- **Decision:** GM-09 只审计。GM-10A 永远是 `implementation`，GM-10B 永远是 `handoff`。GM-09 结束前必须修订本计划并通过 review：go/partial 仅启用 GM-10A、取消 GM-10B；no-go 仅启用 GM-10B、取消 GM-10A。未完成修订与 review 时两卡均禁止开工。
- **Consequence:** GM-11A 的 join gate 为：GO 时 GM-10A=`complete`、GM-10B=`cancelled`；NO-GO 时 GM-10A=`cancelled`、GM-10B 至少 `locally-accepted` + review PASS（最终随 GM-12 变为 `complete`）。不存在运行时 Task type 变化或 no-release→GM-12 的验收环。

### ADR-GM-04: 独立 cargo targets

- **Status:** Accepted
- **Decision:** LFS 固定为 `bin/tests/integration_git_lfs.rs`；SSH 固定为 `bin/tests/integration_git_ssh.rs`；HTTP pull/匿名读及启用时的最小 Mega refs fixture 守卫留在 `integration_git_cli.rs`。

### ADR-GM-05: SSH 唯一拓扑——cargo-native self-start

- **Status:** Accepted
- **Decision:** 禁止 compose SSH 备选。`integration_git_ssh` 复用 HTTP harness 的 per-case DB/Redis/object storage/process guard；helper 先绑定 `127.0.0.1:0`、读取 `local_addr().port()`、释放探测 listener，随后立即以该具体高位端口启动：
  `CARGO_BIN_EXE_monoengine --config CASE/config.toml service ssh --host 127.0.0.1 --ssh-port PORT`。
- **Host key:** 每 case 的 `MEGA_BASE_DIR` 固定为 `CASE/ssh/base`；Vault core key 位于 `CASE/ssh/base/vault/core_key.json`，生成的 `ssh_server_key` 密文只写入该 case 的临时数据库，明文私钥只在服务内存中。case guard 结束时删除临时数据库与 `CASE/ssh`，不得声称 Vault secret 明文是工作区文件。
- **Client key:** 每 case 执行 `ssh-keygen -q -t ed25519 -N '' -f CASE/ssh/client_ed25519`；私钥权限 `0600`。
- **DB seed:** migration 完成且启动前，helper 读取 `.pub`，用 `ssh-keygen -lf CASE/ssh/client_ed25519.pub -E sha256 | awk '{print $2}'` 取得服务端同格式 `SHA256:*`，向 `ssh_keys(id,username,title,ssh_key,finger,created_at)` 插入一次性行，语义与 `seed_access_token` 相同；TCP ready 后才执行 Git 命令。
- **Host trust:** 同一 git-cli runner 执行 `ssh-keyscan -p PORT 127.0.0.1` 写 `CASE/ssh/known_hosts`；所有 Git 命令固定：
  `GIT_SSH_COMMAND='ssh -i /work/CASE/ssh/client_ed25519 -o IdentitiesOnly=yes -o UserKnownHostsFile=/work/CASE/ssh/known_hosts -o StrictHostKeyChecking=yes -p PORT'`。
- **Lifecycle:** 分配具体端口后立即记录；ready 上限 90s；Drop 先 SIGINT、等待 10s，再 kill/wait；最后断言该端口拒绝连接、临时数据库已 drop、client private key 与 SSH case dir 已清理。坏密钥用第二个未播种 keypair，不改 known_hosts。

### ADR-GM-06: G-03 对固定 allowlist 的集合/序列相等计为单一谓词

- **Status:** Accepted
- **Context:** 复审曾把 allowlist 成员展开成多谓词，导致 CI/发布卡无法落在上限内。
- **Decision:** 当 AC 明文写与 Deliverables 固定 allowlist **集合相等**或**序列相等**，且 Verification 用**一次** `test "$actual" = "$expected"`（或等价双向差集为空 / 单一脚本单一 exit）断言时，该 AC 与对应 VER **各计 1** 条 G-03 谓词；不得把 allowlist 成员、`for` needle、或同一脚本内中间 `test` 再拆成独立谓词。成员枚举只在 Deliverables。口径对齐 `plan-20260727` IT-04。
- **Alternatives considered:** 每成员一 AC（拒绝）；G-03 EX waiver（拒绝）。
- **Consequences:** 改 allowlist 必须改 Deliverables 与同一条相等谓词；VER 须先构造 `actual`/`expected` 再单次比较。
- **Revisit when:** 模板修订 G-03 时。


### ADR-GM-07: 测试基线建立后失败即修

- **Status:** Accepted
- **Context:** 用户要求：测试基线建立后若失败则修正代码，直到基线完全通过；不得用 SKIP/`#[ignore]`/削弱断言换绿。
- **Decision:** 每张会改代码或测试的卡在宣称 A 组通过前，必须先建立并守住**测试基线**：
  1. **基线组成（累加）:** 本卡 `Verification` 全部命令 + 适用 A 表面 focused 门 + `AGENTS.md` 三门（`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all`）+ `cargo build` / `cargo build --tests` 零警告。docs/audit/spike/handoff 卡无代码改动时，基线退化为 B 组结构/链接门 + 写集零越界，不跑全量 cargo 三门（仍由承载发布点 C 覆盖）。
  2. **失败分类（以被测路径为准）:**
     - **轴内:** 根因位于本卡行为轴，或位于本卡 Verification/exact 用例实际调用的生产路径（即使初始写集未列出）→ 先按 ER-10 扩写集、更新 AC/VER、重跑全部 `G-*`，再本卡内修码/修测直至基线全绿。
     - **轴外:** 根因不在上述被测路径 → 移交 DEP-05 / `FIX-GM-*` / PT-03，本卡 `blocked`；禁止为换绿删断言或改无关代码。
  3. **禁止:** `SKIP:`、零匹配 filter、`#[ignore]`、放宽断言、关闭匿名/鉴权守卫来换绿；禁止把轴内产品失败默认甩给 DEP-05。
- **Consequence:** 「完成」以基线全绿为前提；实现卡不得在基线红时进入 C 组 bump。
- **Revisit when:** 模板修订 ER-04 基线口径。

### ADR-GM-08: 每张可发布卡独立 patch（废止 REL-GM-01）

- **Status:** Accepted
- **Context:** 用户要求每完成一张任务卡 bump 一个 patch。模板 G-07 禁止 docs/audit/spike/handoff bump；G-08 家族合并发布与「每卡一 patch」冲突。
- **Decision:**
  1. **废止** `REL-GM-01` 与行为less 发布点 `GM-R1`（编号保留为 `cancelled`，同 GM-04）。
  2. 原 SSH 子卡 `GM-06`/`GM-06A`/`GM-07`/`GM-08` 全部改为 `Release boundary=independent`、`Version increment=patch`、`C/D coverage from=self`；各自 A/B+review PASS 后由 Publisher 串行执行完整 C 组（含 patch bump）并取得适用 D。
  3. 其它 implementation / release 卡保持「完成 → 独立 patch」：`GM-02`/`GM-03`/`GM-05`/`GM-10A`（启用时）/`GM-11A`/`GM-11C`/`GM-12`。
  4. docs/audit/spike/handoff（`GM-01`/`GM-09`/`GM-10B`/`GM-11B`）受 G-07 约束**不 bump**，仍 `no-release`，统一由 `GM-12` 承载 C/D；进入 GM-12 前必须为 `locally-accepted`。GM-12 先 bump/push 取得 C，再等 D 全绿；**仅在 D 绿之后**把它们（及本卡）的 `Lifecycle/Acceptance` 升为 `done`/`complete` 并写入本计划。C 已取得而 D 未绿时至多标 `remote-pending`，禁止在 D 前标 `complete`。
- **Alternatives considered:** 保留 GM-R1 家族一点发布（拒绝，违背每卡 patch）；强制 docs 卡 bump（拒绝，违背 G-07 且不可 waiver）。
- **Consequence:** 发布窗口按卡串行；SSH 链上每卡一 patch；DAG 去掉 `GM-R1` 边。
- **Revisit when:** 用户改回家族发布或模板允许 docs bump。

## 全局工程约束

以下约束对本文所有任务生效。任务条目不再逐条重复，违反任一项即视为任务未完成。

- **GC-01 现状核实前置:** 每个任务开工前重新核对计划、相关开发文档、当前代码和测试。如果已实现，则任务改为补测试、补文档、更新状态或关闭，不重复实现。
- **GC-02 单一事实源:** entity 定义、配置解析、API schema、权限策略、错误类型和共享 helper 必须有单一事实源。禁止 CLI handler、HTTP handler、migration 和测试 fixture 各自复制等价逻辑。
- **GC-03 Mega 目标项目与 monoengine 扩展边界:** 直接从 Mega 移植的代码表面必须标注来源和差异；monoengine-only 表面必须说明替代方案、用户影响和机器接口。
- **GC-04 输出与错误契约:** 用户可见错误使用 `MegaError` 稳定变体并同步 `docs/errors.md`。HTTP 状态码、JSON 响应、CLI 退出码和人读输出必须分别验收。注意 `MegaError` 目前**没有**数值错误码注册表，变体→HTTP 状态的映射只存在于 `src/common/errors/api.rs` 的转换实现里；改动该映射必须同时更新 `docs/errors.md` 或在卡内说明为何不需要。
- **GC-05 文档同步:** 命令、配置、HTTP API 或公开行为变化必须同步对应 `docs/` 下文档、`config/config.toml` 注释与 `README.md`。本仓 `docs/` 为中文单语，无 EN/zh 双份要求；OpenAPI 由 `utoipa` 在运行时聚合、**磁盘上没有落盘的 spec 文件**，因此 API schema 证据只能取自运行中的 `/api/openapi.json`。
- **GC-06 测试覆盖:** 新增 entity / storage / migration 必须附带 `#[cfg(test)] mod tests`，使用 `test_db_connection` + `apply_migrations` 集成测试。新增 CLI 子命令必须附解析测试，并覆盖 `builtin()` / `builtin_exec()` / `load_mode()` 三处注册。新增集成 test target 直接在 `bin/tests/<name>.rs` 建文件（cargo 自动发现，无需 `[[test]]` 声明），但必须同步本计划「测试矩阵」与 `docs/refactoring/integration.md` 的覆盖矩阵。
- **GC-07 安全默认值:** 未满足认证、授权（Cedar）、路径归属、schema 版本、对象闭包或 secret redaction 前置时默认 fail-closed。任何 fail-open 必须有显式用户选择、日志和测试。已知现状：Cedar guard 当前使用硬编码 permit-all 且 `EntityStore` 启动时为空，任何依赖「授权已生效」的验收判据都必须先验证该前提，不得假定。
- **GC-08 原子性与恢复:** 修改 DB 事务、redis 状态、对象存储、配置、vault secret 或发布状态时，必须定义事务边界、幂等键、崩溃窗口和回滚/前滚策略。
- **GC-09 并发与资源生命周期:** DB 连接池、redis 连接、文件句柄、异步任务队列和临时目录必须有释放/恢复语义；测试不得依赖未隔离的全局状态（改环境变量的测试必须使用 `src/config/testing.rs` 的 `env_lock` / `EnvVarGuard`）。
- **GC-10 性能预算:** HTTP 热路径、DB 查询、对象存储读写、Git 协议操作和后台任务不得引入无界扫描、无界内存或 N+1 DB/网络调用。需要时写出数据规模和断言。
- **GC-11 生产 panic 禁止:** 生产路径不得新增裸 `unwrap()`、`expect()`、`panic!()`；必须用 `MegaResult`、`anyhow::Context` 或领域错误返回可操作信息。
- **GC-12 精确提交:** 提交前只 `libra add <相关路径>`（本仓由 libra 管理，无 `.git`，任何 `git` 命令在本仓不可用），不得使用 `commit -a`。发现无关脏状态时保留并报告，不得清理、重置或混入提交。


### 本计划 GC 补充

- **GC-GM-01 文档单写者:** `protocol.md` / `integration.md` 只由 GM-01→GM-11B 串行写；`test-infra.md`、`plan-long.md`、`plan-20260727.md`、计划 README 只由 GM-11B 写。行为卡不得顺带修改这些文档。
- **GC-GM-02 无 SKIP 证据:** `MONOENGINE_IT_SKIP_GIT_CLI=1`、`SKIP:` 输出、零匹配 cargo filter 均为失败。
- **GC-GM-03 测试密钥:** host/client key 只存在 per-case workdir，不得提交；证据只保留 SHA-256 fingerprint 的脱敏摘要。
- **GC-GM-04 Mega pin:** 审计只对 full SHA `42cd288d266dade371aed03917d222eb57f1d5a4` 有效，漂移即阻塞 GM-09。

## 执行检查必备需求（强制）

任一要求未满足，对应任务不得标记完成。条目使用稳定 ID，正文引用时用 ID 而不是序号，便于后续插入条目而不破坏交叉引用。

1. **ER-01 开工前安全检查:** 必须完成下列四项，缺一不可。
   - `libra status --short --branch`（`--branch` 才会输出 `## <branch>...<upstream>` 行；不带它只有文件状态），确认当前分支与计划指定分支一致、工作区脏状态、目标文件是否已有无关改动。若目标文件已有未确认用户改动，先报告并避免覆盖。已知现状：本仓 upstream 行可能显示 `## main...origin/main [gone]`，而 `origin/main` ref 实际存在，因此**不要**把 status 的 ahead/behind 当作推送判据。
   - 确认路径依赖 `../orbit` 已就位（`Cargo.toml` 的 `orbit-api` 与 `bin/Cargo.toml` 的 `orbit` 均为 path 依赖）。缺失时任何 `cargo` 命令都会在依赖解析阶段失败，必须先补齐 sibling checkout，不得把该失败当作代码缺陷排查。
   - 确认 `.env.test` 是否存在（仓库只提供 `.env.test.example`，`.env.test` 本身被忽略）。缺失时按 `AGENTS.md` 的规定停下来确认，**不得**静默降级为不 source 的 `cargo test --all`。
   - 确认需要的测试服务是否已启动：`docker compose -f docker-compose.test.yml up -d --wait`（Postgres `15432`、Redis `16379`、Mailpit `11025/18025`、RustFS `19000/19001`；RustFS 桶初始化需要额外的 `--profile init run --rm rustfs-init`）。Postgres 缺失会让相关用例直接 panic 而不是跳过。
2. **ER-02 先核对后实现:** 刷新本任务相关源码锚点、文档锚点、测试 target 和外部参照 revision，再决定实现、补测、补文档、关闭或降级。
3. **ER-03 粒度门禁:** 开工前按粒度规则 `G-*` 逐条复核本任务卡，并逐字段核对该卡的 `Granularity` 摘要行。若核对后发现范围已扩大（新增行为轴、AC/Verification 超限、scope 升到 L、写集与其它在跑任务重叠），先修改计划拆卡再开工，不得在实现中静默扩张任务范围。
4. **ER-04 每卡验收门:** 门由 **A 表面 focused 门**（按实际改动的表面）+ **B 类型门**（按 `Task type`）+ **C 发布收口门**（覆盖要求对所有非延后卡生效，执行归属见下）+ **D 远端后置门**（有不可本地复现的 CI 语义时）四组组成，**所有适用行累加**，全部通过才算验收。权威口径分层：`AGENTS.md`「Required Checks Before Submitting Code Changes」的三门（`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all`）是**任何会提交的改动**的完成契约，模板不得削弱；`AGENTS.md` 另强制 `cargo build` 与 `cargo build --tests` 均 0 错误 0 警告。任务卡指定的 focused 用例是在此之上的**附加**门，用来证明本卡行为，不是三门的替代品。权威口径变更时必须同批同步 `AGENTS.md`、`docs/plan/README.md`，以及**采用本模板当前版本的计划**（存量计划按「模板版本与迁移政策」处理，不因本条被追认为违规）。

   **门不计入条目上限：** 本条列出的门是全局强制门，**不计入**任务卡 `Verification` 的 G-03 条目计数；`Verification` 只登记本卡特有的判据（指定用例、新增守卫、手工证据）。

   **两个正交状态字段（都在任务卡登记）:**
   - `Lifecycle`（执行生命周期）：`pending` | `in-progress` | `blocked`（ER-10 的越界故障置此值）| `done`。
   - `Acceptance`（验收状态）：空 | `locally-accepted` | `remote-pending`（仅当本卡有适用的 D 组远端后置门）| `complete`。与 `AGENTS.md` 的完成契约对齐 —— 任何改动只有三门全绿才算 done，因此：
     - `locally-accepted` = 本卡适用的 **A 组 + B 组**门已过，但本卡的 **C 组覆盖**（自行执行或从承接卡继承，见下）尚未取得。此状态下**不得**对外报告「完成 / done」。
     - `remote-pending` = A/B 已过且 C 组覆盖已取得（含一次三门全绿的运行，其被测树状态包含本卡最终变更），但本卡适用或继承的 **D 组**远端后置门尚未全绿。此状态同样**不得**报告完成。
     - `complete` = 本卡的 **A + B** 已过、**C 组覆盖**已取得、且适用或继承的 **D 组**已全绿。无 D 时 C 覆盖到手即可 `complete`；有 D（含继承的 D）时必须先经 `remote-pending`。
     - 唯一状态转移路径：A/B 通过 → `locally-accepted` → ER-05 review PASS → 取得 C 组覆盖 →（无 D：`complete`；有 D：`remote-pending` → D 全绿 → `complete`）。
   - 两者独立取值：`blocked` 卡的 `Acceptance` 可以已是 `locally-accepted` 甚至 `complete`（例如变更已被三门覆盖，但仍卡在外部前置）。`Lifecycle=done` 必须以 `Acceptance=complete` 为前提；`blocked` 必须先回到 `in-progress` 并完成剩余动作才能进入 `done`，**不允许**从 `blocked` 直接标 `done`。计划完成门另要求所有非延后任务都到 `done`（见「完成判据」）。`Granularity` 里的 `complete=yes` 是 G-02 的结构完整性判据，与本字段无关，不可混用。

   **C 组覆盖与执行归属（每张非延后卡都必须取得 C 覆盖，但不都自己执行）:**
   - **独立发布卡（`Release boundary = independent`）与发布点卡（`release` / `family release point`）**：自行执行完整 C 组门。
   - **`family child`**：不 bump、不构建发布产物、不推送，**继承**其家族唯一发布点的 C 覆盖——前提是该发布点的三门运行其被测树状态包含本子卡的最终变更；同时继承该发布点适用的 D 组。
   - **`no-release` 卡（`docs` / `audit` / `spike` / `handoff`）**：**继承**任务卡显式声明的承载发布点（或计划收口点）的 C 覆盖与其 D 组；该承载点必须在卡内写明 ID，不得留空。
   - 继承 D 组的卡同样要经过 `remote-pending`，直到被继承的 D 组证据全绿。
   - **不存在「用零命中守卫替代三门」的通道**——零命中守卫只用于证明这类卡未改代码，不改变其在取得 C 覆盖前仍是 `locally-accepted` 的事实。

   门分四组，全部适用者累加：**A 表面 focused 门**（按实际改动的表面，每个表面唯一命中一行，命中几行加几行）+ **B 类型门**（按 `Task type` 取一行）+ **C 发布收口门**（由会推送的卡执行，其覆盖可被 `family child` / `no-release` 卡继承）+ **D 远端后置门**（只在存在不可本地复现的 CI 语义时适用）。A/B/C 是本地可完成的门；D 只能在推送之后取得证据，因此**不阻塞** `locally-accepted` 与 ER-05 的 review 顺序。

   **A 表面 focused 门**（覆盖全部合法生产表面；`family child` 复用同一映射）：

   | 实际改动的表面 | focused 门 |
   |---|---|
   | `monoengine-core` 纯单元逻辑（测试写在 `src/**` 的 `#[cfg(test)]` 里） | `source .env.test && cargo test -p monoengine-core '<mod::path::tests>'` |
   | `monoengine-core` 需要真实 Postgres 的逻辑（storage / entity / migration / config secret） | 先起测试栈，再 `source .env.test && cargo test -p monoengine-core '<mod::path::tests>'`；用例必须走 `test_db_connection` + `apply_migrations` |
   | 进程级黑盒 / CLI 集成行为（`bin/tests/**`） | `source .env.test && cargo test -p monoengine --test <target> -- --test-threads=1 [<filter>]`（`-p monoengine` 不可省略：默认选中的是 `monoengine-core`，它没有集成 target） |
   | `bin/src/**`（composition root：orbit provider 注入、global allocator、`parse` 分发；本仓只有 `bin/src/main.rs`） | `source .env.test && cargo test -p monoengine --test <target> -- --test-threads=1`（经 `CARGO_BIN_EXE_monoengine` 黑盒覆盖真实二进制）**加** `cargo clippy -p monoengine --all-targets -- -D warnings`（不可省略：全局 clippy 门不覆盖 `bin` 包，见下） |
   | CLI 解析与注册（`src/cli.rs`、`src/commands/**`） | `cargo test -p monoengine-core 'cli::tests'` + `cargo test -p monoengine-core 'commands::'`；新增/改名子命令必须同时断言 `builtin()`、`builtin_exec()`、`load_mode()` 三处 |
   | HTTP API 路由 / handler / OpenAPI 注解（`src/api/**`、`src/server/http_server.rs`） | `cargo test -p monoengine-core 'api::'` + 启动服务后拉取 `/api/openapi.json` 的 sanitized 证据（无落盘 spec，只能取运行时输出） |
   | `src/callisto/**`、`src/jupiter/migration/**` | `migrations()` 注册列表已登记的断言 + `apply_migrations(&db, true)` 集成用例；若该迁移的 `down` 是 no-op，必须在 `Rollback mode` 写 `forward-only`，不得声称可回滚 |
   | `config/config.toml`、`src/config/**` | config 校验链本地等价：`cargo run -p monoengine -- --config config/config.toml config validate`，按改动追加 `config init --output <tmp> --force`、`config validate --deny-warnings`、`--profile <name> config validate --show-sources`，以及坏配置的非零退出与 secret 不泄漏断言 |
   | Git 协议 / LFS（`src/ceres/protocol/**`、`src/contract/git_protocol/**`、`src/ceres/lfs/**`、`src/api/router/lfs_router.rs`、`src/server/http_server.rs`） | `scripts/git_protocol_smoke.sh` 本地等价，完整前置以 `.github/workflows/git-protocol-smoke.yml` 现场读取为准（本格只给骨架，不是快照事实源）：① `docker compose -f docker-compose.test.yml up -d --wait postgres redis`；② `cargo build --release -p monoengine`；③ **起 `service http` 前必须覆盖数据面**——仓库默认 `config/config.toml` 指向 `postgres://localhost:5432/...` 与 `redis://127.0.0.1:6379`，与测试栈的 `15432` / `16379` 不符，用默认配置起服务必然连不上；须 `source .env.test` 或显式导出 `MEGA_DATABASE__DB_URL`（建议独立 smoke 库）、`MEGA_REDIS__URL`、`MEGA_BASE_DIR`，并按 workflow 用同一 `MEGA_BASE_DIR` 预置 `mail.password` secret（mail 启动路径 fail-closed，缺 secret 时进程直接退出且不绑定端口）；④ 起服务后轮询 `/api/openapi.json` 就绪；⑤ 只读用例（ls-remote / clone / fetch / protocol v2 / shallow / blob:none）匿名即可通过（`git.anonymous_access` 默认 `true`），此时 `MONOENGINE_HTTP_REPO_URL=http://127.0.0.1:9000/` 足够；⑥ **push / tag / LFS 用例需要鉴权**：receive-pack 无有效 Mono access token 一律 401，而脚本只能通过 URL 传凭据，因此必须先向 smoke 库 `access_token` 表播种一次性 token，并写成 `http://<user>:<token>@127.0.0.1:9000/`（token 按 ER-11 脱敏，不得进验收证据）；漏做这一步会让全部 push/tag/LFS 用例以 401 失败，**不得**判为协议实现回归；⑦ **LFS 卡必须同时设 `MONOENGINE_GIT_SMOKE_PUSH=1 MONOENGINE_GIT_SMOKE_LFS=1`**——LFS 矩阵嵌在 push 分支内，只设 `MONOENGINE_GIT_SMOKE_LFS=1` 时脚本只打印 `SKIP: HTTP LFS push/clone also requires MONOENGINE_GIT_SMOKE_PUSH=1` 且仍以退出码 0 结束，该 skip **不得**作为 LFS 证据（属「Verification 判定口径」禁止的幽灵验收）；LFS 验收必须断言输出出现 `PASS: HTTP LFS push and clone` 且末行 summary 为 `0 failed`，并预装 `git-lfs`（缺失直接判 FAIL） |
   | Cedar 策略与守卫（`src/contract/policy/**`） | 对应单元/集成用例 + 明确断言当前 permit-all 与空 `EntityStore` 前提是否被本卡改变 |
   | 仓库配置与 CI（`Cargo.toml` / `bin/Cargo.toml` 非版本行、`rustfmt.toml`、`docker-compose.test.yml`、`scripts/**`、`.github/workflows/**`） | 受影响 CI job 的本地等价命令，按下文「仓库配置与 CI 展开规则」现场提取；不可本地复现的部分归 D 组 |
   | 只改文档 / 索引（无代码、无配置） | 无表面 focused 门，只走 B 组的结构与链接门 |

   **Rust 行的修饰规则（不是独立表面）:** env 约束与线程约束是上面几条 Rust 行的**修饰条件**，不构成独立表面：先按测试归属唯一选中 `-p monoengine-core` 或 `-p monoengine --test <target>` 中的一行，再把该 target 实际需要的 env 变量与线程约束**并入同一条命令**，例如 `source .env.test && cargo test -p monoengine --test integration_vault -- --test-threads=1`（`source .env.test &&` 前缀不可省略）。当前工作区**没有任何 cargo feature**（两个包的 `[features]` 均不存在），因此 `--all-features` 不改变本仓编译内容，不要用它冒充一条独立的 focused 门；它只保留在 clippy 全局门里。

   **默认包选择（本仓事实，直接影响门的覆盖面）:** 根 `Cargo.toml` 同时是 workspace 根与 root package `monoengine-core`，且**没有** `default-members`，因此不带 `-p` 的 cargo 命令只选中 `monoengine-core`——`cargo build`、`cargo build --tests`、`cargo clippy --all-targets --all-features -- -D warnings` 均**不覆盖 `bin` 包**；只有 `cargo +nightly fmt --all --check` 与 `source .env.test && cargo test --all` 覆盖整个 workspace。所以改动 `bin/src/**` 的卡必须显式写 `-p monoengine`，否则该改动不会被任何 clippy 门检查（`bin/src/main.rs` 仍会被 `cargo test -p monoengine --test <target>`、`cargo run -p monoengine`、`cargo build --release -p monoengine` 编译，只是不会被 lint）。

   **不得**为了凑一条 Rust 命令而制造与本卡无关的用例；也不得因为「C 组已有全量测试」就跳过 A 组某个表面的行。

   **仓库配置与 CI 展开规则:** 受影响 job 必须**从本卡实际改动的那个 workflow 文件现场提取**。模板不保存 job→命令的映射快照（必然漂移）。两步：

   ① 列出 job，且必须限定在 `jobs:` 节点内（裸 `rg "^  [a-z...]:"` 会把 `on:` 下的 `push`/`pull_request`、`permissions:` 下的 `contents` 误报为 job）：

   ```bash
   awk '/^jobs:/{in_jobs=1; next} in_jobs && /^[^[:space:]]/{exit} in_jobs && /^  [A-Za-z0-9_-]+:/{print FNR ":" $0}' .github/workflows/<file>.yml
   ```

   ② 读取受影响 job 的**完整定义**——`permissions`、`strategy` / `matrix`、job 与 step 级 `env`、`if`、`working-directory`、`run`、`uses`、`with`——再据此写判据。只看 `run:` 不够：`claude-review.yml` 当前**没有任何 `run:` 步骤**，其行为完全由 action 定义。分流：
   - **可本地复现**的步骤 → 抄成命令进 A 组，保留其 `env`、`working-directory`、线程与 URL 约束。**依赖 GitHub checkout 的步骤不得照抄**（本仓无 `.git`）：`actions/checkout` 对应的本地事实是「当前工作树即 checkout」，私有 `orbit` 仓的 checkout 对应「sibling `../orbit` 已就位」（见 ER-01）。`${RUNNER_TEMP}` 换成本地临时目录，`>> "$GITHUB_ENV"` 换成同一 shell 会话内的 `export`。
   - **不可本地复现**的语义（`::add-mask::`、secret 存在性校验、私有仓 token checkout、`anthropics/claude-code-action`）→ 不得凭空造命令，归入下文 **D 组远端后置门**。

   参考：2026-07-29 快照下 `.github/workflows/` 只有三个文件、每个文件恰好一个 job——`claude-review.yml` → `claude-review-with-tracking`（仅 `issue_comment` / `pull_request_review*` / `issues` 事件触发，**不由 push 触发**，且 100% action-only）、`config-validation.yml` → `validate-config`、`git-protocol-smoke.yml` → `git-protocol-smoke`（后两者均为 `pull_request` + `push: branches:[main]`，且都带 `paths:` 过滤器）。仅核对日快照，判定一律以现场读取的 workflow 文件为准。

   **B 类型门**（按 `Task type` 取一行）：

   | Task type | 类型门 |
   |---|---|
   | `implementation` / `migration` / `removal` | 无额外类型门（由 A 组 + C 组构成完整验收；`family child` 自跑 A 组 + fmt/clippy，C 覆盖继承自家族唯一发布点） |
   | `docs` / `audit` / `handoff` | 结构与链接门：本卡产物文件存在且章节完整、内部链接与 `file:line` 锚点可解析、新引入的 `docs/*.md` 路径全部真实存在（本仓已有多处悬空文档引用，不得新增）、`libra status --short --branch` 无越界改动。这三类**必须保持 no-code / no-config**：一旦发现需要改动代码或配置，不得「就地升级门」，必须先按 ER-03 把卡重分类为 `implementation` / `migration` / `removal`，同步 `Release boundary`、`Version increment`、`Release write set`，重跑粒度门后再执行 |
   | `spike` | 产物门：结论文档或 ADR 已落盘、go/no-go 已判定、承接卡已登记；**allowlist diff 门**——`libra status --short --branch` 的全部变更必须落在本卡 `Deliverables` 声明的产物内，且生产表面零改动（至少覆盖 `src/**`、`bin/src/**`、`bin/tests/**`、`config/**`、`scripts/**`、`Cargo.toml`、`bin/Cargo.toml`、`Cargo.lock`、`rustfmt.toml`、`docker-compose.test.yml`、`.github/workflows/**`），用「Verification 判定口径」的退出码模板逐条守卫 |
   | `release` | 聚合守卫（本组引入的全部新守卫用例）+ release note / 兼容证据 |

   **C 发布收口门（由会推送的卡执行，顺序强制）:** ① 版本面 parity 预检（ER-08）→ ② 按 `Version increment` bump **两处** `version`（`Cargo.toml` 的 `monoengine-core`、`bin/Cargo.toml` 的 `monoengine`）+ 让工具链刷新 `Cargo.lock` 的两个对应条目（不手改）→ ③ 在**已 bump 的状态**上跑 `AGENTS.md` 三门（fmt、clippy、`source .env.test && cargo test --all`）→ ④ `cargo build` 与 `cargo build --tests` 均 0 错误 0 警告；需要可执行产物时另跑 `cargo build --release -p monoengine` → ⑤ `libra add <相关路径>` + `libra commit -s -m`（ER-07 的签名预检与提交后校验）→ ⑥ 推送并确认 branch ref：`libra push origin main` 成功且远端 ref 已更新 → ⑦ **tag 与 release artifact：默认 `N/A`**。本仓当前**没有任何 release 流水线**：三个 workflow 都不由 tag 触发，没有 artifact 上传，仓库也没有任何已存在的 tag。因此不得凭空写「推 tag 触发发布」；确需产出用户可获取产物时，必须先用独立卡引入 release workflow 并同步修订本模板。

   三门必须覆盖 bump 后的最终状态——bump 与 `Cargo.lock` 刷新本身可能引入格式、lint 或编译回归，`cargo build` 不能替代 clippy 与全量测试。

   **C / D 边界（唯一口径）:** C 组**截止到已验证的 branch 推送**；推送之后由远端流水线产生的一切证据全部归 **D 组**。任务卡必须为每个 D 组项登记：workflow 文件、job 名、**触发事件与 ref**、**paths 过滤器是否命中本卡改动**、以及判据。

   **D 远端后置门（post-push，仅在有不可本地复现的 CI 语义时适用）:**
   - 本仓 D 组的实际内容是：`config-validation.yml` 与 `git-protocol-smoke.yml` 在 `push: branches:[main]` 上的远端结论——**当且仅当本卡改动命中各自的 `paths:` 过滤器**；以及这两个 workflow 中本地无法复现的部分（私有 `gitmono-dev/orbit` token checkout、`::add-mask::` 日志脱敏、secret 存在性校验）。
   - `claude-review.yml` **不由 push 触发**（只响应 issue/PR 评论与 review 事件），因此直推 `main` 的卡不得把它登记为 D 组项；只有走 PR 流程的卡才可能命中。
   - 若本卡改动不命中任何 `paths:` 过滤器，D 组写 `N/A` 并说明「改动路径不在两个 workflow 的 paths 列表内」，不得虚构一个远端门。
   - D 组**不属于**本地验收，不阻塞 `locally-accepted`，也不改变 ER-05「先本地验收再 review」与 C 组「review 通过后才提交推送」的顺序。
   - 只有当适用的 D 组门也全绿，该卡的 `Acceptance` 才能到 `complete`；此前停在中间态 `remote-pending`。
   - D 组失败一律**前滚修复**（新提交 / 新版本），不得回退已推送提交；修复卡按 ER-10 的越界规则处理。

   「完成判据」的计划级门是最后一次总检查，不替代每张会推送的卡各自跑过的发布收口门。
5. **ER-05 代码 review 闭环:** 实现和本地验收完成后进行代码 review；review 问题修复后重跑相关验收，直到 review 明确给出 `PASS`。P0/P1 必须关闭，不得以「residual risk 已接受」替代 `PASS`（仅 P2 可由具名责任人书面接受）。
6. **ER-06 文档与兼容同步:** 涉及公开行为的任务必须同步用户文档、开发文档、`config/config.toml` 示例、错误契约、运行时 OpenAPI 证据和测试矩阵。
7. **ER-07 Libra-native 工作流与提交签名:** 本仓库使用 Libra 工作流：`libra status`、`libra add <相关路径>`、`libra commit -s -m "<scope>: <summary>"`、`libra push origin main`。不要把仓库当普通 Git 仓库处理——本仓没有 `.git`，任何 `git` 命令都会失败。
   - `libra commit` **没有** `-S`（Libra 未暴露 Git 的正向 `-S`/`--gpg-sign`），`-s` 只添加 `Signed-off-by`。签名策略优先级为 `--no-gpg-sign`（最高）> `commit.gpgSign`（Git 级联，`false` 直接关闭签名）> `vault.signing` 默认（为 `true` 且 vault unseal key 可用时签名）。
   - 预检必须**先读 `commit.gpgSign`**（`libra config get commit.gpgSign`），未设置时再回退 `libra config get vault.signing`；`commit.gpgSign=false` 时不得当作「已启用签名」。
   - 每次提交后强制校验：`libra cat-file -p HEAD | rg -q '^gpgsig'`。校验失败不得推送。
   - **本仓已知现状（2026-07-29 核对）:** `commit.gpgSign` 未设置、`vault.signing=true`，但近期历史提交**均不带 `gpgsig` 头**。这说明「配置为签名」与「实际产生签名」之间存在缺口。首次执行本条时若校验失败，必须先查明原因（vault 未 unseal、签名密钥不可用等）并记录，不得静默跳过；在查明修复前若确需以 sign-off-only 方式提交，必须在「字段全局默认与例外」的 waiver 表登记 `豁免项 = ER-07 签名要求` 的 `EX-*`（含 Approver、Review round、证据、有效期）。「事实基线」最多链接该 `EX-*`，不构成独立授权路径。
   - **与 `README.md` / `AGENTS.md` 的已知漂移及优先级（必须按此执行）:** `README.md` 的 Contributing 一节写的是「Sign your commits (`git commit -s -S …`)」——该命令在本仓**双重不可执行**（没有 `.git`；`libra commit` 也没有 `-S`）。`AGENTS.md` 则**完全没有**提交/分支/签名指引，同时含若干过期事实（把本仓描述为单一 binary crate、`src/main.rs`、`src/common/config/loader.rs`、`cargo test` 作为全量测试、子命令 executor 签名）。计划执行时**以 ER-07 + GC-12 为准**：`libra add <相关路径>` → `libra commit -s -m` →（按上三条做签名预检与提交后 `gpgsig` 校验）。这些漂移应作为独立的文档修复项承接，不得在计划执行中两套并行。
   - 提交信息沿用本仓已观察到的两种既有风格：发布类改动用 `v<version>: <summary>`；文档/测试/CI 类改动用 `<type>(<scope>): <summary>`。
8. **ER-08 版本与发布:** 版本权威源是根 `Cargo.toml` 的 `version`（包 `monoengine-core`）。发布前先做版本面 parity 预检：`Cargo.toml` 与 `bin/Cargo.toml`（包 `monoengine`）的 `version` 必须一致；**不一致时先建立修复卡对齐，禁止直接 bump**。对齐后按任务卡的 `Version increment` 递增并同步这两处，让工具链刷新 `Cargo.lock` 中两个包的版本条目，其余步骤与顺序按 ER-04 的「发布收口门」执行（bump 后必须重跑三门）。开工时须重新核对版本面文件数量是否仍为两处。
   - `Version increment` 取值：`patch`（默认）| `minor` | `major` | `N/A`。
   - 删除公开 surface、破坏兼容的 schema/协议变更必须用 `minor` 或 `major`，且递增级别由 ADR + 兼容窗口证据决定，不得用 patch 夹带；家族卡（G-08）的递增级别写在唯一发布点卡上，子卡为 `N/A`。
   - `docs` / `audit` / `spike` / `handoff` 卡为 `N/A`，但必须说明产物随哪次提交进入仓库。
   - 本仓没有安装脚本、没有 release artifact、没有 tag 自动化，因此「安装证据」「artifact 可获取性」默认写 `N/A`；不得照抄其它仓库的发布步骤。
9. **ER-09 push 失败策略:** 非 fast-forward 需要 pull/merge 后重新验收再推；认证、权限、网络或服务端失败不 blind retry，记录原因，待下一次修复/发布窗口处理。
10. **ER-10 内部服务错误（有界重试）:** Redis、Postgres、对象存储、SMTP、AI provider 等错误不得直接把任务宣告完成。先分类：确定性错误（4xx 参数/权限、schema 不符、编译或配置缺陷）不重试，按范围决定归属——只有当修复落在**本卡行为轴内**，且先更新本卡 `Acceptance criteria` / `Verification` / `Implementation write set` / `Granularity` 后**重跑 ER-03 的全部 `G-*` 仍然通过**（含 G-10 写集不与在跑卡新冲突）时，才作为本卡修复项就地修；任一条不满足就新建修复卡 `FIX-*`、加一条 `FIX-* -> 当前卡` 的依赖边，并把当前卡置为 `blocked`，不得为了「顺手修完」突破粒度；暂时性错误（超时、5xx、限流、网络中断）按指数退避重试，并写明**最大尝试次数与总时间预算**（计划未另行规定时默认 ≤ 5 次、总计 ≤ 30 分钟）。超预算后把任务置为 `blocked` 并记录 sanitized 证据与升级对象，不得静默空转。发布类动作（push）不自动重试，按 ER-09 处理。
11. **ER-11 证据卫生:** 验收证据不得保存 secret、API key、token、PII、未脱敏 transcript、绝对私有路径或原始 tool payload。需要留存时只写 sanitized summary。测试栈的口令（如 `monoengine_test_password`、`smtp-test-password`、RustFS 测试凭据 `rustfs` / `rustfs_secret`）虽为公开测试值，记录时同样按脱敏处理。
12. **ER-12 并发边界与串行发布:** 并发只适用于**实现与 review 阶段**：只有 `Implementation write set` 不相交（G-10）的卡可以并发推进。**发布动作一律串行，且由单一发布者执行**：
    - 计划必须在「发布分组与并发窗口」声明发布者（哪个 Agent/人负责 C 组的 bump、构建、提交、推送，以及推送后跟踪 D 组远端证据）。同一时刻只允许一个卡处于「已 bump 未完成推送」状态。
    - 进入发布前重新读取 `Cargo.toml` 权威版本（ER-08 的 parity 预检），按顺序做完整套发布动作后才轮到下一张卡。
    - **禁止多 Agent 并发发布。** 本仓库当前**没有**仓库级发布锁：`libra push origin main` 推送的是本地 `main` 的整个 ref tip，无法只发布一条协调记录，也无法在 push 之外提供 CAS 仲裁；靠纯文档约定实现的 lease 无法验证，属于未经实现验证的协议。若某计划确实需要并发发布，必须先用独立 ADR + 独立计划落地一个仓库级发布锁（含原子认领、fence 校验、超时回收、崩溃恢复与测试），并在本计划以 `DEFER-*` 登记；在该机制落地并通过验收之前，一律按本条串行执行。
    - 并发实现期间仍受 I–R 约束（G-10）：发布者持有发布窗口时，其它卡不得修改 `Release write set` 内的文件。


### 本计划 ER 补记

- **ER-GM-01 runner 前置:** `source .env.test` 后，执行 `docker compose -p monoengine-it -f docker-compose.test.yml --profile git up -d --wait postgres redis git-cli`；共享 workdir 先 `mkdir -p`、`chmod 1777` 并导出当前 UID/GID。
- **ER-GM-02 发布者:** `Publisher: eli`（具名责任人）。C 组 bump/commit/push/D 跟踪只能由 **eli** 本人执行，或由 eli 在 Review log 书面点名的单一委派 Agent 执行；同一时刻只允许一个委派记录生效，禁止第二发布者。
- **ER-GM-03 no-release carrier:** 本计划所有 docs/audit/spike/handoff 卡统一继承 GM-12。不得写“最近发布点”或可变 carrier。SSH 卡不再有 family child（ADR-GM-08）。
- **ER-GM-04 测试基线:** 实现/测试卡在进入 C 组前必须满足 ADR-GM-07；基线红且缺陷在轴内时就地修码，禁止 SKIP/削弱。
- **ER-GM-05 每卡 patch:** 每张 `Release boundary=independent` 的卡在 ER-05 review PASS 后，由 Publisher 执行一次且仅一次 patch bump + C 组；禁止把多张卡的行为合并进同一次 bump（ADR-GM-08）。


## 任务完成定义（强制）

「完成」一律指 `Lifecycle=done` 且 `Acceptance=complete`（ER-04）。下表是每卡对外报告完成前的**充分必要条件**摘要；卡内 `**完成定义:**` 字段必须与本表一致。测试基线口径见 ADR-GM-07；patch 口径见 ADR-GM-08。

| 任务 | 完成时必须为真 | patch bump | C/D 归属 |
|---|---|---|---|
| GM-01 | 场景表+索引+GAP 映射通过 AC/VER；A/B+review → `locally-accepted`；GM-12 的 D 全绿后再升为 `done`/`complete` | 否（no-release） | GM-12 |
| GM-02 | exact pull fn 基线绿；A/B+review；C：patch+三门+push；适用 D 绿 → `complete` | 是（+1 patch） | self |
| GM-03 | exact 匿名拒绝 fn 基线绿；同上独立发布切片 → `complete` | 是 | self |
| GM-04 | `cancelled`（不执行） | N/A | N/A |
| GM-05 | LFS target+runner+exact 往返基线绿；独立 patch → `complete` | 是 | self |
| GM-06 | SSH lifecycle exact fn 基线绿；独立 patch → `complete` | 是 | self |
| GM-06A | SSH authenticated clone exact fn 基线绿；独立 patch → `complete` | 是 | self |
| GM-07 | SSH pull exact fn 基线绿；独立 patch → `complete` | 是 | self |
| GM-08 | SSH push + wrong-key 两 exact fn 基线绿；独立 patch → `complete` | 是 | self |
| GM-R1 | `cancelled`（ADR-GM-08 废止家族发布点） | N/A | N/A |
| GM-09 | 审计+唯一 Decision 写入并通过 review → `locally-accepted`；GM-12 的 D 全绿后再升为 `done`/`complete` | 否 | GM-12 |
| GM-10A | （仅 GO）最小夹具+exact 守卫基线绿；独立 patch → `complete`；NO-GO 时 `cancelled` | 是（仅启用时） | self |
| GM-10B | （仅 NO-GO）DEFER-GM-03 handoff 通过 AC/VER → `locally-accepted`；GM-12 的 D 全绿后再升为 `done`/`complete`；GO 时 `cancelled` | 否 | GM-12 |
| GM-11A | config-validation workflow allowlist 全绿；独立 patch → `complete` | 是 | self |
| GM-11C | git-protocol-smoke workflow allowlist 全绿；独立 patch → `complete` | 是 | self |
| GM-11B | 七份文档/DEFER/索引收口通过 AC/VER → `locally-accepted`；GM-12 的 D 全绿后再升为 `done`/`complete` | 否 | GM-12 |
| GM-12 | 开工前：实现卡 `complete`、no-release=`locally-accepted`；patch+push 取得 C→D；**D 绿后**再把 no-release/本卡升为 `done`/`complete`；Review log 最大轮次=`PASS` | 是 | self |

## 实施顺序

下列是唯一权威 DAG；`A -> B` 与每卡 `Dependencies`、粒度审计表 `deps` 完全一致。no-release 前置的边在该卡 A/B + review PASS、达到 `locally-accepted` 后即可消费，其最终 C/D 仍由 GM-12 承载，不构成等待 GM-12 的执行环。GM-04 与 GM-R1 不在 DAG（均 cancelled）；GM-10A/10B 按 ADR-GM-03 的分支 join 口径参与后继，其中一张启用、另一张 `cancelled`。每张 independent 实现卡在自身 complete 前完成一次 patch（ADR-GM-08）。

- `DEP-01 -> GM-01`
- `DEP-03 -> GM-02`
- `GM-09 -> GM-02`
- `GM-02 -> GM-03`
- `GM-03 -> GM-05`
- `GM-05 -> GM-06`
- `GM-06 -> GM-06A`
- `GM-06A -> GM-07`
- `GM-07 -> GM-08`
- `GM-01 -> GM-09`
- `DEP-04 -> GM-09`
- `GM-03 -> GM-10A`
- `GM-08 -> GM-10A`
- `GM-09 -> GM-10A`
- `GM-09 -> GM-10B`
- `GM-08 -> GM-11A`
- `GM-10A -> GM-11A`
- `GM-10B -> GM-11A`
- `GM-11A -> GM-11C`
- `GM-11C -> GM-11B`
- `DEP-01 -> GM-11B`
- `DEP-02 -> GM-11B`
- `GM-11B -> GM-12`

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 可用性判据 | 证据 | 失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | incoming | 跨计划 | `plan-20260727.md` DEFER-IT-01/02/12 | 本计划 | 三项均关闭或明确改挂 DEFER-GM | 历史计划延后表 | 未收口则 GM-11B 不得完成 |
| DEP-02 | incoming | 长期计划 | `plan-long.md` PT-04 | GM-11B | 完成判据与关联日期计划可编辑 | `plan-long.md:295-326` | 冲突先修长期计划再收口 |
| DEP-03 | incoming | 测试环境 | `../orbit`、`.env.test`、postgres/redis/git-cli | 执行 Agent | ER-01/ER-GM-01 全部满足 | workspace manifests / compose | 缺任一项即停止，不降级 SKIP |
| DEP-04 | incoming | 上游 revision | Mega tests @ `42cd288d266dade371aed03917d222eb57f1d5a4` | GM-09 | `rev-parse` 全 SHA 相等且五目录可读 | 2026-08-03 live sibling | 漂移时阻塞并先修订 pin |
| DEP-05 | outgoing | 协议缺陷（轴外） | PT-03 / 新 `FIX-GM-*` | PT-03 owner | 缺陷有复现、接收 ID、失败测试 | Review log / handoff | 仅当根因越出本卡 Verification 被测路径时移交；轴内（含被测产品路径）按 ADR-GM-07 本卡修 |
| DEP-06 | outgoing | 延后范围 | ImportRepo / pure SSH LFS / 官方套件级 | PT-03/PT-04 后续计划 | DEFER 行含 owner、日期、重启条件 | 本计划延后表 | 不得写成已覆盖 |

### 发布分组与并发窗口

本计划**无** `REL-*` 家族合并发布（ADR-GM-08）。下列独立发布卡各自是自己的唯一发布点。

**并发声明:** 本计划在共享 Libra checkout 中不并发执行任务卡；GM-01→GM-09→GM-02 的前置链先冻结所有 no-release 基线，再进入行为/发布链。文档写集 GM-01→GM-09/10B→GM-11B 已有顺序边，无并发写者。

**Publisher:** `eli`（ER-12 具名责任人；委派须写进 Review log）

**发布窗口顺序（每卡一次 patch）:** `GM-02 → GM-03 → GM-05 → GM-06 → GM-06A → GM-07 → GM-08 → GM-10A（仅启用时）→ GM-11A → GM-11C → GM-12`。GM-01、GM-09、GM-10B、GM-11B 不单独 bump，由 GM-12 承载；GM-04/GM-R1 cancelled 无窗口。

### D 组命令与逐发布卡登记

命中 `config-validation.yml` 时执行：

```bash
bash -c 'set -euo pipefail
pushed_sha="$(libra rev-parse HEAD)"
run_id="$(gh run list --workflow config-validation.yml --branch main --event push --commit "$pushed_sha" --limit 20 --json databaseId,conclusion --jq '\''map(select(.conclusion == "success")) | .[0].databaseId // empty'\'')"
test -n "$run_id"
test "$(gh run view "$run_id" --json conclusion --jq .conclusion)" = success'
```

命中 `git-protocol-smoke.yml` 时执行：

```bash
bash -c 'set -euo pipefail
pushed_sha="$(libra rev-parse HEAD)"
run_id="$(gh run list --workflow git-protocol-smoke.yml --branch main --event push --commit "$pushed_sha" --limit 20 --json databaseId,conclusion --jq '\''map(select(.conclusion == "success")) | .[0].databaseId // empty'\'')"
test -n "$run_id"
test "$(gh run view "$run_id" --json conclusion --jq .conclusion)" = success'
```

| 发布卡 | workflow | job | event/ref | paths hit | 成功判据 |
|---|---|---|---|---|---|
| GM-02 | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**` | 上述 config `gh run list` + `gh run view`，`conclusion=success` |
| GM-02 | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：当前 paths 不含 `bin/tests/**` | N/A |
| GM-03 | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**` | config run `conclusion=success` |
| GM-03 | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：当前 paths 不含 `bin/tests/**` | N/A |
| GM-05 | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**`, `docker-compose.test.yml` | config run `conclusion=success` |
| GM-05 | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | `docker-compose.test.yml` | git-protocol run `conclusion=success` |
| GM-06 | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**` | config run `conclusion=success` |
| GM-06 | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：现行 paths 不含 `bin/tests/**`（至 GM-11C 前） | N/A |
| GM-06A | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**` | config run `conclusion=success` |
| GM-06A | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：现行 paths 不含 `bin/tests/**`（至 GM-11C 前） | N/A |
| GM-07 | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**` | config run `conclusion=success` |
| GM-07 | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：现行 paths 不含 `bin/tests/**`（至 GM-11C 前） | N/A |
| GM-08 | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**` | config run `conclusion=success` |
| GM-08 | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：现行 paths 不含 `bin/tests/**`（至 GM-11C 前） | N/A |
| GM-10A | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `bin/tests/**` | config run `conclusion=success` |
| GM-10A | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：GM-11A 尚未把 `bin/tests/**` 加入该 paths | N/A |
| GM-11A | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `.github/workflows/config-validation.yml` | config run `conclusion=success` |
| GM-11A | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | N/A：该 workflow 的 paths 不含 `config-validation.yml` | N/A |
| GM-11C | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | N/A：该 workflow 的 paths 不含 `git-protocol-smoke.yml` | N/A |
| GM-11C | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | `.github/workflows/git-protocol-smoke.yml` | git-protocol run `conclusion=success` |
| GM-12 | `.github/workflows/config-validation.yml` | `validate-config` | `push: branches:[main]` | `docs/refactoring/test-infra.md` | config run `conclusion=success` |
| GM-12 | `.github/workflows/git-protocol-smoke.yml` | `git-protocol-smoke` | `push: branches:[main]` | `docs/refactoring/protocol.md` | git-protocol run `conclusion=success` |

## Phase 0: 场景与依赖冻结

**进入条件:** DEP-01/DEP-04 可用。  
**退出条件:** GM-01 场景表与 GM-09 唯一分支修订均 `locally-accepted` 且 review PASS。

## Phase 1: HTTP 与 LFS

**进入条件:** GM-09 完成唯一分支修订，且 DEP-03 可用。  
**退出条件:** GM-02、GM-03、GM-05 `complete`。

## Phase 2: SSH 独立发布链

**进入条件:** GM-05 complete；ADR-GM-05 / ADR-GM-08 未被修订。  
**退出条件:** GM-06、GM-06A、GM-07、GM-08 各自 `complete`（每卡一次独立 patch + 适用 D）。

## Phase 3: Mega 唯一分支、CI 与文档收口

**进入条件:** GM-08 complete，且 GM-09 的唯一分支 gate 保持有效。  
**退出条件:** GM-10A/10B 一张 complete、一张 cancelled；GM-11A/11C/11B 与 GM-12 complete。

## 字段全局默认与例外

- 实现卡默认 `Release boundary=independent`、`Version increment=patch`、`Rollback mode=revert`、`C/D coverage from=self`。
- 本计划**无** family child / family release point（ADR-GM-08）。
- docs/audit/spike/handoff 的 `Release boundary=no-release`、`Version increment=N/A`、`Release write set=N/A`、`C/D coverage from=GM-12`。
- release 卡不承载行为 I；`Implementation write set=N/A`、`Release write set=Inherited`。
- **规则 waiver:** 无。

## 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GM-01 | docs | 场景表结构 | revert docs | yes | yes | 6/20 | 4/20 | 2/0 | S | DEP-01 | 序列化于 GM-11B | no-release→GM-12 | N/A | N/A |
| GM-02 | implementation | HTTP pull | revert | yes | yes | 6/8 | 1/8 | 1/0 | S | DEP-03,GM-09 | 序列化于 GM-03/GM-10A | independent | N/A | N/A |
| GM-03 | implementation | 关匿名读 | revert | yes | yes | 6/8 | 2/8 | 2/0 | S | GM-02 | 序列化于 GM-02/GM-06/GM-06A/GM-10A | independent | N/A | N/A |
| GM-04 | implementation | cancelled：空 LFS scaffold | N/A（并入 GM-05） | N/A | N/A | N/A | N/A | 0/0 | N/A | N/A | N/A | cancelled | N/A | N/A |
| GM-05 | implementation | LFS cargo 往返 | revert | yes | yes | 8/8 | 3/8 | 4/2 | M | GM-03 | 序列化于 GM-06/GM-06A | independent | GM-04 | N/A |
| GM-06 | implementation | SSH service/host-key 生命周期 | revert | yes | yes | 2/8 | 2/8 | 3/0 | M | GM-05 | 序列化于 GM-03/GM-05/GM-06A/GM-07/GM-08 | independent | N/A | N/A |
| GM-06A | implementation | SSH client auth + clone | revert | yes | yes | 3/8 | 1/8 | 4/1 | M | GM-06 | 序列化于 GM-03/GM-05/GM-06/GM-07/GM-08 | independent | GM-06 | N/A |
| GM-07 | implementation | SSH pull | revert | yes | yes | 5/8 | 1/8 | 1/0 | S | GM-06A | 序列化于 GM-06/GM-06A/GM-08 | independent | N/A | N/A |
| GM-08 | implementation | SSH push/坏密钥 | revert | yes | yes | 4/8 | 2/8 | 1/4 | S | GM-07 | 序列化于 GM-06/GM-06A/GM-07 | independent | N/A | N/A |
| GM-R1 | release | cancelled：原 SSH family 发布点 | N/A（ADR-GM-08） | N/A | N/A | N/A | N/A | 0/0 | N/A | N/A | N/A | cancelled | N/A | N/A |
| GM-09 | spike | Mega 审计决策 | revert docs | yes | yes | 5/8 | 4/8 | 2/0 | S | GM-01,DEP-04 | 序列化于 GM-10B/GM-11B | no-release→GM-12 | N/A | N/A |
| GM-10A | implementation | 最小夹具移植 | revert | yes | yes | 5/8 | 2/8 | 3/0 | M | GM-03,GM-08,GM-09 | 序列化于 GM-02/GM-03 | independent | GM-10 | N/A |
| GM-10B | handoff | no-go 移交 | revert docs | yes | yes | 5/20 | 2/20 | 1/0 | S | GM-09 | 序列化于 GM-09/11B | no-release→GM-12 | GM-10 | N/A |
| GM-11A | implementation | config-validation CI 面 | revert | yes | yes | 6/8 | 6/8 | 1/1 | S | GM-08,GM-10A,GM-10B | no-overlap | independent | GM-11 | N/A |
| GM-11C | implementation | git-protocol-smoke CI 面 | revert | yes | yes | 6/8 | 6/8 | 1/1 | S | GM-11A | no-overlap | independent | GM-11 | N/A |
| GM-11B | docs | PT-04 文档收口 | revert docs | yes | yes | 6/20 | 6/20 | 7/0 | M | GM-11C,DEP-01,DEP-02 | 序列化于 GM-01/GM-09/GM-10B | no-release→GM-12 | GM-11 | N/A |
| GM-12 | release | 最终文档/版本发布 | immutable-release | yes | yes | 6/12 | 6/12 | 0/0 | S | GM-11B | R-only | independent | N/A | N/A |

## 任务卡

### 任务卡粒度规则（强制）

粒度是任务卡质量的第一判据：卡过大则无法 review、无法回滚、无法交给单个 Agent 完成；卡过碎则实现、测试与文档脱节，且每片都要付一次发布成本。新增或修改任务卡时必须逐条满足下列 `G-*` 规则。任一条不满足即为「粒度不合格」，必须在开工前拆分或合并，并同步实施顺序、依赖登记表、发布分组、追溯表、测试矩阵、里程碑、风险表的任务归属和修订历史。

- **G-01 单一行为轴与恢复模式（上限）:** 一张卡只承担一个**可独立恢复**的行为轴——该卡失败或需要撤回时，存在**单一已声明的恢复动作**，执行后系统停在一个自洽状态，不留半吊子中间态。这里的「恢复」不等于「一次 revert」：不可逆变更同样合格，只要恢复路径是单一且已写明的。禁止把「schema 变更 + 存储层接入 + API 暴露」「删 A + 删 B + 删 C」「新增能力 + 顺带重构既有实现」压进同一张卡。快速判据：`Description` 中出现两个以上并列的「并且 / 同时 / 顺带 / 以及」，先按「推荐拆分维度」拆。恢复形式必须在 `Rollback mode` 字段声明为四种模式之一：
  - `revert`：纯本地代码/文档变更，一次 revert 即完整撤销（默认）。
  - `forward-only`：已产生不可逆数据或迁移（Postgres schema、对象存储、vault secret），只能前滚修复；必须写出数据不变量、恢复验证命令和用户影响，**不得**为了凑「可 revert」而设计不安全的 down migration。注意本仓有相当一部分既有迁移的 `down` 是空实现（开工时以实际迁移文件为准），且运行期没有任何 `migrate down` 入口——`src/jupiter/migration/runner.rs` 只暴露 `Migrator::up` 与破坏性的 `Migrator::refresh`。因此涉及既有迁移的卡默认按 `forward-only` 处理，除非本卡自己实现并验证了真实的 `down`。
  - `compensating`：对外部服务已有副作用（远端写入、邮件发送、外部对象删除），撤销靠补偿动作；必须写出补偿命令与幂等键。
  - `immutable-release`：已推送的提交不可撤回，只能前滚新版本；必须写出降级指引与兼容窗口。
- **G-02 完整可交付（下限）:** 一张卡必须是一个自洽的可验收增量。同一行为轴的实现、测试、文档与索引同步是**同一张卡**的验收内容，禁止拆成「实现卡 / 补测试卡 / 补文档卡」。只有当被拆出的部分本身就是独立可恢复的行为轴（G-01 意义上：有单一已声明的恢复动作——独立迁移、独立 deprecation 收口、独立性能门、独立 API 切面、跨计划移交）时才允许单独成卡。
- **G-03 条目上限与计数口径:** 上限按 `Task type` 取值，计数按**独立判据**而非行数。ER-04 的强制门（fmt / clippy / 全量测试 / 表面门）**不计入**本条计数，`Verification` 只登记本卡特有的判据：

  | Task type | AC 上限 | Verification 上限 |
  |---|---|---|
  | `implementation` / `migration` / `removal` | 8 | 8 |
  | `spike` | 8 | 8 |
  | `docs` / `audit` / `handoff` | 20 | 20 |
  | `release` | 12 | 12（只计聚合守卫、release note、兼容证据等本卡特有项） |

  计数细则：
  - AC 按「独立 pass/fail 谓词」计。一条 checklist 内用「且 / 并且 / 以及 / 同时」连接的多个可分别失败的断言按多条计；嵌套子列表逐项计；表格行逐行计。
  - Verification 按「独立验证门」计。判据是「是否构成一次独立的通过/失败判定」：环境准备前缀（`source .env.test`、`MEGA_*=…` 赋值、`docker compose … up -d --wait`、`export`）与其后的命令合计为**一门**；一条命令中的多个 `--test` target 分别计；`&&` 串联两个都会独立判定的验收命令按两门计；手工证据按项计。
  - 超限视为多轴信号，必须拆卡，**不得**通过合并长句、塞进表格或改写成「等等」来规避。
  - 文档 / 审计 / 索引-only 卡的条目是清单项、不构成独立行为轴，故适用 20 条上限；这类卡仍受 G-01 约束，且必须在任务卡 `Deliverables` 字段登记产物范围（具体文件清单）——这是常规登记，不是例外，无需进 waiver 表。需要突破本表上限时，只能在 waiver 白名单登记 `EX-*`（具名审批），不得私自改写分母。
- **G-04 规模上限（可计数）:** `Estimated scope` 的开工态只允许 `S` 或 `M`。`L`/`XL` 只能作为「必须再拆」的中间标注，计划成稿后不得存在 L/XL 卡。计数**只统计行为实现落点与生产文件**，不统计「随附同步集」：
  - **计入**：承载本卡行为变更的生产代码落点与文件（`src/**`、`bin/src/**`、`config/**`、`scripts/**` 等）。
  - **不计入（随附同步集）**：本卡自己的测试文件、按 GC-05/ER-06 强制同步的文档集（`docs/**`、`README.md`、`config/config.toml` 注释）、以及 ER-08 的版本面两处。这些是每张卡的固定成本，不构成粒度信号；但仍要在写集字段中如实列出（文档/测试进 `Implementation write set`，版本面两处进 `Release write set`）。
  - **仓库根文件**（`Cargo.toml`、`bin/Cargo.toml`、`rustfmt.toml`、`docker-compose.test.yml` 等）按「单个文件」计，不各占一个落点；若某张卡的行为变更**就发生在**根文件本身（例如改 `docker-compose.test.yml` 的服务拓扑），则该文件计为一个落点。
  - `S`：≤ 2 个行为落点、≤ 3 个生产文件，无 schema / 协议 / 公开接口变更。
  - `M`：≤ 4 个行为落点、≤ 12 个生产文件，最多一处公开行为或接口变化，仍是单一行为轴。
  - 超出 `M` 的计数即为 L：默认必须拆分。确实不可拆的机械变更（全仓重命名、批量删除、格式化）可在「字段全局默认与例外」的 waiver 白名单中登记 `EX-*`（需具名审批人与 review 轮次），写明为何不可拆、如何 review、如何恢复；此时该卡 `Estimated scope` 写 `L-exception:EX-<n>`，这是全文唯一允许出现 `L` 字样的形式，`XL` 永不允许。
  - 把 `src/`、`bin/`、`docs/` 或仓库根算作「一个落点」是规避行为，按「粒度反模式速查」的「落点注水」处理。
- **G-05 Agent 可独立执行:** 一张卡必须能在不阅读其它卡正文的前提下被执行：`Current evidence` 给出可核对的 `file:line` 锚点，`Acceptance criteria` 自洽可判定，`Verification` 是可直接复制执行的确切命令，`Dependencies` 只引用「依赖登记表」中的 `DEP-*` / 任务 ID。禁止「见上文」「同上一卡」式跨卡隐式约定；确属跨卡共享的约定要提升为全局工程约束或 ADR。
- **G-06 依赖闭合且无环:** 依赖必须有向无环。本计划内依赖直接引用任务 ID；跨计划与外部前置必须先在「依赖登记表」登记为 `DEP-*` 再引用，不得在卡内自由描述。互相等待、循环依赖、以及「等某个 Phase 整体完成」都是拆分错误——把依赖收敛到具体前置卡。「实施顺序」的依赖边与各卡 `Dependencies` 必须一致；不一致时以「实施顺序」为准并当场修正卡片。
- **G-07 发布切片对齐（按任务类型）:** 默认「一张卡 = 一个发布切片」（独立 review + ER-04 门 + 版本 + 提交 + 推送）。适用范围按 `Task type`（G-11）区分：`implementation` / `migration` / `removal` 必须走完整发布切片；`docs` / `audit` / `spike` / `handoff` 卡不 bump 版本，`Release boundary` 写 `no-release` 并说明其产物随哪次提交进入仓库；`release` 卡本身就是发布点（家族卡的唯一发布点必须是 `release` 卡，见 G-08）。任何「多卡合并发布」都是例外，必须在「发布分组与并发窗口」登记 `REL-*`：成员、唯一发布点、窗口期禁止插入的内容、失败时的逆序回滚顺序。例外必须先修订计划并通过 review 才可开工，**不得**在开工时凭笔记临时合并。
- **G-08 家族卡（不可分割变更的唯一出路）:** 当一次公开 surface 删除、或 schema 与 reader 必须同时上线这类变更确实无法切成可独立发布的切片时，用「家族卡」表达：拆成多张各自 review、各自通过全部适用 ER-04 门、各自本地提交的子卡，共用一个唯一发布点卡；**该发布点卡的 `Task type` 必须是 `release`**（不引入新行为，只做版本、构建、聚合守卫与发布证据），以保证它在 ER-04 的 B 组中唯一命中 `release` 行。家族内子卡仍受除 G-07 外的全部 `G-*` 约束；子卡 `Release boundary` 写 `family child`，发布点卡写 `family release point`，家族边界与「不推送窗口」写进 `REL-*` 登记。
- **G-09 拆分协议:** 拆分已被引用的卡时，原编号保留给主轴，新子卡在所属 Phase 末尾追加新编号，不重排既有编号。原卡必须写明「拆出 `<ID>`、`<ID>`」，新卡写明「自 `<ID>` 拆出」，并同步实施顺序、依赖登记表、「发布分组与并发窗口」、追溯表、测试矩阵、里程碑、风险表，以及「修订历史」中的一行（日期、原因、原卡、新卡、受影响引用）。
- **G-10 写集与并发:** 写集分三类，每张卡必须声明前两类（第三类由 ER-12 统一定义，卡内不重复）：
  - **`Implementation write set`（I）**：承载本卡行为的代码、测试、文档文件。
  - **`Release write set`（R）**：ER-08 的版本面两处（`Cargo.toml`、`bin/Cargo.toml`）+ `Cargo.lock`。对所有发布卡相同；`family child` 与 `no-release` 卡写 `N/A`（它们不 bump、不推送）。
  - **协调写集（C）**：计划级的发布顺序与窗口记录（「发布分组与并发窗口」的发布者、发布顺序、`REL-*` 登记）。由 ER-12 的单一发布者串行维护，**不计入**任何卡的 I 或 R，也不参与并发判定。

  冲突规则：
  - **I–I 相交** → **禁止并发，无豁免通道**（G-10 不在 waiver 白名单内）：只有两个合法出路——补一条顺序依赖边，或把相交部分合并到唯一集成卡。
  - **I–R 相交**（某卡把 `Cargo.toml`、`bin/Cargo.toml` 等当行为落点，而它同时属于别的卡的 R）→ 在**已声明的串行发布窗口**内（ER-12），该窗口对 R 内文件是写锁：其它卡不得在此期间修改这些文件，必须等窗口结束或补顺序边。
  - **R–R 相交** → 由 ER-12 的串行发布窗口顺序化，不构成并发禁止条件。
  - `Files likely touched` 是估计值，并发判定以 `Implementation write set` 为准。
- **G-11 任务类型:** 每张卡必须声明 `Task type`，不同类型适用不同粒度口径：
  - `implementation`：默认类型，全部 `G-*` 条款全量适用。
  - `migration`：数据/schema 迁移，`Rollback mode` 通常为 `forward-only`，必须有 up/down 或前滚验证与故障注入用例，并在 `src/jupiter/migration/mod.rs` 的 `migrations()` 列表登记顺序。
  - `removal`：公开 surface 删除，通常进入家族卡（G-08），必须先有 deprecation 窗口证据。
  - `spike`：探索/验证，**不得**改动生产代码。必须写出待回答的问题、时间箱、产物（结论 + ADR 或缺口登记）、go/no-go 退出标准与后续承接卡；不适用 G-04 的文件计数，`Estimated scope` 按时间箱判定：`S` ≤ 0.5 人日、`M` ≤ 2 人日，超出即拆成多个问题或直接转 ADR / `implementation` 卡。
  - `audit` / `docs`：只读核对或文档收敛，按 G-03 的文档-only 口径执行。规模上限按**产物文件数或人日**判定（不适用 G-04 的生产文件计数）：`S` ≤ 5 个产物文件或 ≤ 0.5 人日；`M` ≤ 15 个产物文件或 ≤ 2 人日；超出即拆卡。随代码卡强制同步的文档仍按 G-04 的随附同步集处理，不计入这里。
  - `release`：发布点卡，不引入新行为，只做版本、构建、聚合守卫与发布证据。
  - `handoff`：跨计划移交，默认 `no-release`。**移入**（本计划承接他人）在「依赖登记表」登记 `direction: incoming`；**移出**（本计划把范围交给别的计划或 `plan-long.md` 的 PT 项）登记 `direction: outgoing`，并写明接收方、移交日期、本计划不再重做的部分，以及接收方未接手时的回落处理。

#### 推荐拆分维度

超限卡按下列维度之一切开；切完每片仍须独立满足 G-01（单一行为轴 + 已声明的恢复模式）。

| 维度 | 切法 | 典型结果 |
|---|---|---|
| 数据 / 状态轴 | entity + migration → storage 写入与幂等 → 读取投影与恢复 | 3 张卡 |
| 协议轴 | 协议版本与协商 → 容量 / 背压与性能门 → 消费端接入 | 3 张卡 |
| 表面轴 | 后端 service/storage → 机器接口（HTTP JSON / OpenAPI / 错误映射） → CLI 或客户端接入 | 2–3 张卡 |
| 生命周期轴 | 新实现上线 → 默认切换 → deprecation shim → 物理删除 | 按发布窗口分卡 |
| 安全轴 | 身份与请求边界（Cedar / token） → 路径与对象归属 → 敏感信息 redaction | 每轴一卡 |
| 清理轴 | 公开 surface 删除（家族卡） → 内部模块退场 → 依赖摘除 | 家族卡 + 普通卡 |

#### 粒度反模式速查

| 反模式 | 症状 | 处理 |
|---|---|---|
| 巨型卡 | `Estimated scope` = L；AC > 8；Description 含多个并列目标 | 按「推荐拆分维度」拆分（G-01/G-03/G-04） |
| 碎片卡 | 「补测试」「补文档」「改个字段名」单独成卡 | 合并回所属行为轴（G-02） |
| 多轴伪装 | 把多条 AC 合成一条长句、塞进表格或写「等等」以压到 8 条以内 | 按独立谓词还原计数后重新判定（G-03） |
| 落点注水 | 把 `src/` 或仓库根算作「一个落点」以保住 S/M | 按目录级落点重新计数（G-04） |
| 隐式依赖 | Description 写「按 X 卡的约定」而 X 卡未交付该约定 | 写进本卡，或提升为全局约束 / ADR（G-05） |
| 幽灵验收 | `Verification` 只写 `cargo test --all`，或零命中守卫不区分 `rg` 退出码 `1` 与 `>1` | 指定 package/target 与 test fn；按「Verification 判定口径」的退出码模板重写（G-05） |
| 悬空依赖 | `Dependencies` 写「Phase N 完成」或自由描述外部前置 | 收敛到具体前置卡 ID / `DEP-*`（G-06） |
| 假回滚 | 已推送或已迁移数据的卡仍写「一次 revert 撤销」 | 按实际选 `forward-only` / `compensating` / `immutable-release`（G-01） |
| 并发冲撞 | 两张无依赖的卡实现写集相交 | 只有两条出路：补顺序边，或合并到唯一集成卡（G-10 不可豁免）。版本面争用不算并发冲突，由 ER-12 的串行发布窗口处理 |
| 顺手合并 | 多张卡凭开工笔记合成一次发布 | 登记为 `REL-*` 家族卡，或拆回独立发布切片（G-07/G-08） |

#### 字段全局默认与例外

本计划的权威默认值与 waiver 声明见上文「## 字段全局默认与例外」（ADR-GM-08：实现卡独立 patch；docs/audit/spike/handoff → GM-12；无 family child；**规则 waiver: 无**）。此处不再重复模板占位行。

#### Verification 判定口径

- **零命中守卫必须区分「无命中」与「命令失败」。** `rg` 的退出码是 `0` = 有命中、`1` = 零命中、`>1` = 执行失败（非法正则、路径不存在、I/O 错误）。`! rg …` 和 `if rg …; then exit 1; fi` 都会把 `>1` 误判为通过，**不得**单独作为证据。裸 `rg …; rc=$?` 也不行——在 `set -e` 下零命中会让脚本在取 `$?` 前就退出。使用下列模板：

  ```bash
  if rg -n "<pattern>" <paths>; then
    echo "FAIL: forbidden pattern found"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- 「只允许 allowlist 命中」类守卫必须逐条比对固定 allowlist，并在任务记录中附命中 diff。
- 仅用于定位符号的 `rg` 必须注明「锚点定位用，非判据」。
- 本任务新增的 test fn / 场景过滤必须标 `(new)`，并确保其归属明确：`monoengine-core` 的 `#[cfg(test)]` 用例写 `-p monoengine-core '<mod::path::tests::fn>'`；集成用例写 `-p monoengine --test <target>`，新 target 直接在 `bin/tests/<target>.rs` 建文件（cargo 自动发现），并同步测试矩阵与 `docs/refactoring/integration.md` 覆盖矩阵。
- `cargo test --all` 不能替代任务指定用例（ER-04）；反过来，指定用例也不能替代计划完成前的全量门（见「完成判据」）。
- 依赖真实服务的用例必须在 Verification 里写清前置：Postgres 缺失会让相关用例 **panic**（不是跳过），Mailpit 在 `monoengine-core` 侧是优雅跳过、在 `bin/tests` 侧是硬断言。把「跳过」当成「通过」属于幽灵验收。


### 本计划粒度补记

- GM-04 是取消记录，不是可执行空 scaffold；行为、target、测试与 runner 全并入 GM-05。
- GM-06 只验收 service/host-key/port 生命周期；GM-06A 只验收 client-key/DB seed/strict-host-trust 后的 authenticated clone；二者均为独立 patch 发布卡（ADR-GM-08），不再共享家族发布点。
- GM-10A/GM-10B 固定 Task type；GM-09 的计划修订 gate 只改变启用/取消状态。
- GM-11A 只编辑 `config-validation.yml`，GM-11C 只编辑 `git-protocol-smoke.yml`，GM-11B 是无 workflow 写集的 docs/handoff。
- AC/VER 按独立 pass/fail 谓词计数；集合相等（allowlist / paths set）计为单一谓词。
- 本计划无 `EX-*` waiver。

### Task GM-01: 冻结 Git 使用场景覆盖表

**Task type:** `docs`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 在 `protocol.md` 建唯一场景表，在 `integration.md` 建链接/target 索引，不实现测试。
**完成定义:** 场景表与索引满足全部 AC/VER 且 review PASS 后标 `locally-accepted`；不得在 GM-12 的 D 全绿前标 `complete`。由 GM-12 在 D 绿后写入 `done`/`complete`。不 bump。

**Out of scope:**

- 测试实现 —— 由 GM-02+ 承接
- CI workflow —— 由 GM-11A/GM-11C 承接

**Current evidence:**

| 事实 | 证据 |
|---|---|
| protocol 建议清单 | `docs/refactoring/protocol.md:600-633` |
| integration 覆盖仅一行 | `docs/refactoring/integration.md:28` |


**Acceptance criteria:**

- [x] 一张结构表整体覆盖 ls-remote、clone、fetch、pull、push、branch/tag delete、LFS push/pull/locks 与 HTTP/SSH/auth/repo-shape 列。
- [x] 每格恰为 cargo exact fn、smoke case、DEFER 或 N/A+理由，无空格。
- [x] 一张 GAP 映射表整体把 GAP-01..07 映射到任务或 DEFER。
- [x] 明文写“clone/fetch 不等于覆盖字面 git pull”。
- [x] `integration.md` 只链接场景表，不复制矩阵。
- [x] DEFER-IT-12 裁定为 GM-11B 收口的 DEFER-GM-05 或明确最小探针。

**Verification:**

- [x] `bash -c 'set -euo pipefail; rg -n "场景覆盖表|git pull|cargo:|smoke:|DEFER-GM-|N/A" docs/refactoring/protocol.md'`
- [x] `bash -c 'set -euo pipefail; awk -F"|" '\''BEGIN{found=0;bad=0} /^\| 用户命令 \|/{found=1;next} found && /^\|---/{next} found && (/^#### / || /^### /){exit} found && /^\|/ { n=0; for (i=3;i<NF;i++) { cell=$i; gsub(/^[[:space:]]+|[[:space:]]+$/,"",cell); if (cell=="") bad=1; else if (cell !~ /^(cargo:[A-Za-z0-9_]+|smoke:[A-Za-z0-9_-]+|DEFER-GM-[0-9]+|N\/A\+.+)$/) bad=1; n++ } if (n!=4) bad=1 } END { if (!found || bad) exit 1; exit 0 }'\'' docs/refactoring/protocol.md'`
- [x] `bash -c 'set -euo pipefail; f=docs/refactoring/protocol.md; rg -n "GAP-01.*GM-02" "$f"; rg -n "GAP-02.*GM-03" "$f"; rg -n "GAP-03.*GM-05" "$f"; rg -n "GAP-04.*GM-06" "$f"; rg -n "GAP-05.*GM-09" "$f"; rg -n "GAP-06.*GM-11B" "$f"; rg -n "GAP-07.*DEFER-GM-05" "$f"'`
- [x] `bash -c 'set -euo pipefail; rg -n "protocol.md.*场景覆盖表" docs/refactoring/integration.md'`

**Dependencies:** `DEP-01`  
**Deliverables:** `docs/refactoring/protocol.md`, `docs/refactoring/integration.md`  
**Implementation write set:** 同 Deliverables  
**Release write set:** `N/A`  
**Files likely touched:** 同 Deliverables  
**Docs and compatibility impact:** 文档 only  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** `N/A`  
**Performance budget:** `N/A`  
**Estimated scope:** `S`  
**Version increment:** `N/A`  
**Release boundary:** `no-release（GM-12）`  
**C/D coverage from:** `GM-12`  
**Granularity:** `type=docs; axis=场景表结构; recovery=revert docs; complete=yes; self-contained=yes; AC=6/20; VER=4/20; landing=2; prod-files=0; scope=S; deps=DEP-01; writeset=序列化于GM-11B; release=no-release; split-from=N/A; exception=N/A`

---

### Task GM-02: HTTP 字面 pull 协作往返

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 新增 reserved exact test `(new) integration_git_cli_http_pull_cl_ref_round_trip`，按 ADR-GM-02 对 CL ref 执行字面 pull 并比较字节树。
**完成定义:** exact pull 测试与 AGENTS 三门构成的基线全绿 → A/B+review → 独立 patch bump + push → 适用 D 绿后 `complete`/`done`。基线红则轴内修码（ADR-GM-07）。

**Out of scope:**

- LFS —— GM-05
- SSH —— GM-07
- 关匿名读 —— GM-03

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 既有 clone/push/fetch 往返 | `bin/tests/integration_git_cli.rs:298` |
| 无字面 pull | `rg "\"pull\"" bin/tests/integration_git_cli.rs` |


**Acceptance criteria:**

- [x] cargo 列表中 exact fn `(new) integration_git_cli_http_pull_cl_ref_round_trip` 恰出现一次。
- [x] 被测客户端实际执行字面 `pull`。
- [x] pull 的 remote ref 是本 case 的具体 `refs/cl/CASE_ID`。
- [x] pull 后工作树与发送端快照逐字节相等。
- [x] 默认 main clone 仍等于种子树。
- [x] 无 runner/SKIP 绿路径。

**Verification:**

- [x] `bash -c 'set -euo pipefail; name=integration_git_cli_http_pull_cl_ref_round_trip; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_cli "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`

**Dependencies:** `DEP-03`, `GM-09`  
**Deliverables:** `N/A`  
**Implementation write set:** `bin/tests/integration_git_cli.rs`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** 由 GM-11B 同步  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** token 仅 credential helper 传递  
**Performance budget:** ≤180s  
**Estimated scope:** `S`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-02 两行）  
**Granularity:** `type=implementation; axis=HTTP pull; recovery=revert; complete=yes; self-contained=yes; AC=6/8; VER=1/8; landing=1; prod-files=0; scope=S; deps=DEP-03,GM-09; writeset=序列化于GM-03,GM-10A; release=independent; split-from=N/A; exception=N/A`

---

### Task GM-03: anonymous_access=false 真实 CLI 拒绝

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 扩展 full-config helper，为本 case 的完整 TOML 追加唯一 `[git]\nanonymous_access = false`，启动 HTTP 服务并验证匿名 clone 拒绝、token clone 成功。
**完成定义:** exact 匿名拒绝测试基线全绿 → A/B+review → 独立 patch → 适用 D 绿后 `complete`/`done`。基线红则轴内修码。

**Out of scope:**

- Cedar deny —— 永久非目标于本卡
- SSH 匿名策略 —— DEFER-GM-02 外

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 策略单测 | `src/contract/git_protocol/mod.rs:104-133` |
| 默认 true | `src/config/model.rs:962-973` |
| 样例无 [git] | `config/config.toml` |


**Acceptance criteria:**

- [x] cargo 列表中 exact fn `(new) integration_git_cli_auth_anonymous_disabled_rejects_clone` 恰出现一次。
- [x] `write_full_config` 生成的 per-case 完整 TOML 经真实 loader 解析后 `git.anonymous_access=false`。
- [x] 无凭据 clone 非零并匹配认证失败类别。
- [x] 同一服务、同一 repo 的 token 对照 clone 成功。
- [x] `config/config.toml` 保持无 `[git]`。
- [x] `src/config/model.rs` 的默认值保持 `true`。

**Verification:**

- [x] `bash -c 'set -euo pipefail; name=integration_git_cli_auth_anonymous_disabled_rejects_clone; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_cli "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`
- [x] `bash -c 'set -euo pipefail; if rg -n "^\[git\]|anonymous_access" config/config.toml; then exit 1; else rc=$?; test "$rc" -eq 1; fi'`

**Dependencies:** `GM-02`  
**Deliverables:** `N/A`  
**Implementation write set:** `bin/tests/integration_git_cli.rs`, `bin/tests/common/mod.rs`, `src/config/validate.rs`（ER-10/ADR-GM-07：known_fields 缺 `git` 导致 per-case override 无法加载）  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** 由 GM-11B 同步  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** fail-closed；输出不得含 token  
**Performance budget:** ≤120s  
**Estimated scope:** `S`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-03 两行）  
**Granularity:** `type=implementation; axis=关匿名读; recovery=revert; complete=yes; self-contained=yes; AC=6/8; VER=2/8; landing=2; prod-files=0; scope=S; deps=GM-02; writeset=序列化于GM-02,GM-06,GM-06A,GM-10A; release=independent; split-from=N/A; exception=N/A`

---

### Task GM-04: 已取消的 LFS 空 scaffold

**Task type:** `implementation`（固定；取消记录）  
**Lifecycle / Acceptance:** `cancelled` / `N/A`

GM-04 因 G-02 不构成独立可交付增量而取消；不得执行、发布或作为依赖。其 target 建立、runner pin 与真实行为全部并入 GM-05。此编号仅为 G-09 引用稳定性保留。

---

### Task GM-05: HTTP LFS cargo 往返（含原 GM-04）

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 一次交付非空 `integration_git_lfs`、pin 到 git-lfs 3.7.1 的 runner，以及 push→CL ref→lfs pull 字节往返；runner 继承现有 profile=`git`、host network、UID/GID 与共享 workdir 契约，不另起拓扑轴。
**完成定义:** LFS target/runner/exact 往返基线全绿 → A/B+review → 独立 patch → 适用 D 绿后 `complete`/`done`。基线红则轴内修码。

**Out of scope:**

- SSH LFS —— DEFER-GM-02
- 与本卡 LFS 往返用例无关的产品路径 —— DEP-05（若基线失败根因落在被测 LFS 产品路径，按 ADR-GM-07 扩写集就地修，不移交）

**Current evidence:**

| 事实 | 证据 |
|---|---|
| smoke LFS | `scripts/git_protocol_smoke.sh:176-236` |
| 无 lfs target | `ls bin/tests/integration_git_lfs.rs` |


**Acceptance criteria:**

- [x] cargo 列表中 target 唯一 exact fn `(new) integration_git_lfs_http_round_trip` 恰出现一次。
- [x] compose 启动的 git-cli runner 报告 exact `git-lfs/3.7.1`。
- [x] LFS push 成功并产生新 CL ref。
- [x] 对端执行 `git lfs pull` 后二进制字节等于源文件。
- [x] runner 或 git-lfs 不可用时测试进程非零。
- [x] focused 输出不含 `SKIP:`。
- [x] case 结束时无 LFS 临时凭据残留。
- [x] case 结束时无 owned 进程残留。

**Verification:**

- [x] `bash -c 'set -euo pipefail; docker compose -p monoengine-it -f docker-compose.test.yml --profile git build git-cli; docker compose -p monoengine-it -f docker-compose.test.yml --profile git up -d --wait git-cli; test "$(docker compose -p monoengine-it -f docker-compose.test.yml exec -T git-cli git lfs version | sed -n "s|^git-lfs/\([^ ]*\).*|\1|p")" = 3.7.1'`
- [x] `bash -c 'set -euo pipefail; name=integration_git_lfs_http_round_trip; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_lfs "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`
- [x] `bash -c 'set -euo pipefail; docker compose -p monoengine-it -f docker-compose.test.yml --profile git down -v; test -z "$(docker ps -aq --filter label=com.docker.compose.project=monoengine-it)"'`

**Dependencies:** `GM-03`  
**Deliverables:** `N/A`  
**Implementation write set:** `bin/tests/integration_git_lfs.rs`, `bin/tests/common/git_cli.rs`, `Dockerfile.git-cli`, `docker-compose.test.yml`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B 关闭 DEFER-IT-02 分层部分  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** LFS token 不写 URL/证据  
**Performance budget:** ≤300s  
**Estimated scope:** `M`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-05 两行）  
**Granularity:** `type=implementation; axis=LFS cargo往返; recovery=revert; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=4; prod-files=2; scope=M; deps=GM-03; writeset=序列化于GM-06,GM-06A; release=independent; split-from=GM-04; exception=N/A`

---

### Task GM-06: SSH service 与 host-key 生命周期

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

> **执行后注记（2026-08-06 完成度复审，适用 GM-06..GM-08 全链）：** 复审在 macOS checkout 上发现两处 harness 缺陷并已修复：① `common/git_cli.rs::container_path_for_host` 对尚不存在的路径（如 keyscan 前的 `known_hosts`）canonicalize 失败回退原始路径，而 workdir 侧成功规范化，`/tmp`→`/private/tmp` symlink 主机上前缀断言假失败——改为对两侧统一「最深存在祖先 canonicalize + 拼回余段」（Linux 目标 OS 行为不变）；② `integration_git_ssh.rs` fixture 函数文档注释后空行触发 `clippy::empty_line_after_doc_comments`，致 `cargo clippy -p monoengine --all-targets -- -D warnings` 编译失败（该门不在全局 clippy 覆盖内，收口时未被抓到）。修复后 5 个 SSH 用例全绿。

**Description:** 自 GM-06 拆出服务侧唯一轴：按 ADR-GM-05 用 cargo-native self-start 证明具体端口、per-case Vault host key 与进程清理可重复；不做 client auth/clone，不改 compose 与文档。
**完成定义:** SSH lifecycle exact fn 基线全绿 → A/B+review → **本卡独立 patch**（不再等 GM-R1）→ 适用 D 绿后 `complete`/`done`。

**Out of scope:**

- client seed/clone —— GM-06A
- push —— GM-08

**Current evidence:**

| 事实 | 证据 |
|---|---|
| service ssh | `src/commands/service/ssh.rs` |
| fingerprint lookup | `src/contract/git_protocol/ssh.rs:228-270` |


**Acceptance criteria:**

- [x] cargo 列表中 exact fn `(new) integration_git_ssh_service_lifecycle_isolated` 恰出现一次。
- [x] `bin/tests/integration_git_ssh.rs` 中 lifecycle 契约标记集合与 Deliverables「SSH service lifecycle allowlist」**集合相等**（ADR-GM-06；标记为测试源内固定字符串，由 focused 用例强制）。

**Verification:**

- [x] `bash -c 'set -euo pipefail; name=integration_git_ssh_service_lifecycle_isolated; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_ssh "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`
- [x] `bash -c 'set -euo pipefail; f=bin/tests/integration_git_ssh.rs; expected="$(printf "%s\n" ephemeral_port_from_127.0.0.1:0_to_--ssh-port listen_within_90s vault_core_key=CASE/ssh/base/vault/core_key.json ssh_server_key_ciphertext_in_temp_db_only SIGINT_then_kill_wait_le_10s cleanup_invariant=port_refuses_and_temp_db_and_CASE/ssh_absent | sort)"; actual="$(rg -o --fixed-strings -f <(printf "%s\n" "$expected") "$f" | sort -u)"; test "$actual" = "$expected"'`

**Dependencies:** `GM-05`  
**Deliverables:** SSH service lifecycle allowlist=`{ephemeral_port_from_127.0.0.1:0_to_--ssh-port, listen_within_90s, vault_core_key=CASE/ssh/base/vault/core_key.json, ssh_server_key_ciphertext_in_temp_db_only, SIGINT_then_kill_wait_le_10s, cleanup_invariant=port_refuses_and_temp_db_and_CASE/ssh_absent}`  
**Implementation write set:** `bin/tests/integration_git_ssh.rs`, `bin/tests/common/git_cli.rs`, `bin/tests/common/mod.rs`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B 登记 test-infra  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** host private key 明文不得写盘/输出；证据只含路径状态  
**Performance budget:** ≤180s  
**Estimated scope:** `M`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-06）  
**Granularity:** `type=implementation; axis=SSH service/host-key生命周期; recovery=revert; complete=yes; self-contained=yes; AC=2/8; VER=2/8; landing=3; prod-files=0; scope=M; deps=GM-05; writeset=序列化于GM-03,GM-05,GM-06A,GM-07,GM-08; release=independent; split-from=N/A; exception=N/A`

---

### Task GM-06A: SSH client auth 与首个 clone

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 自 GM-06 拆出客户端认证轴：生成 per-case client key，经 SQL helper 播种 `ssh_keys`，固定 strict host trust，并用 authenticated clone 证明身份链闭合。
**完成定义:** SSH authenticated clone exact fn 基线全绿 → A/B+review → 本卡独立 patch → 适用 D 绿后 `complete`/`done`。

**Out of scope:**

- host key lifecycle —— GM-06
- pull/push —— GM-07/08

**Current evidence:**

| 事实 | 证据 |
|---|---|
| fingerprint DB | `src/contract/git_protocol/ssh.rs:240` |
| HTTP token seed 先例 | `bin/tests/common/git_cli.rs:74` |


**Acceptance criteria:**

- [x] cargo 列表中 exact fn `(new) integration_git_ssh_authenticated_clone` 恰出现一次。
- [x] client auth fixture 与 Deliverables「SSH client fixture allowlist」**集合相等**（含 key mode 0600、ssh_keys 行自洽、finger=`ssh-keygen -lf ... -E sha256` 第二列、known_hosts 仅本 case 端口、GIT_SSH_COMMAND 模板；单一集合相等谓词）。
- [x] authenticated clone 的工作树逐字节等于完整种子树。

**Verification:**

- [x] `bash -c 'set -euo pipefail; name=integration_git_ssh_authenticated_clone; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_ssh "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`

**Dependencies:** `GM-06`  
**Deliverables:** SSH client fixture allowlist=`{client_key_mode=0600, ssh_keys_row_matches_keypair, finger=ssh-keygen_-lf_sha256_col2, known_hosts=case_port_only, GIT_SSH_COMMAND=ssh -i CASE/ssh/client_ed25519 -o IdentitiesOnly=yes -o UserKnownHostsFile=CASE/ssh/known_hosts -o StrictHostKeyChecking=yes -p PORT}`  
**Implementation write set:** `bin/tests/integration_git_ssh.rs`, `bin/tests/common/git_cli.rs`, `bin/tests/common/mod.rs`, `Dockerfile.git-cli`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B 登记 test-infra  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 私钥只在 case workdir；日志只允许 fingerprint 摘要  
**Performance budget:** ≤180s  
**Estimated scope:** `M`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-06A）  
**Granularity:** `type=implementation; axis=SSH client auth+clone; recovery=revert; complete=yes; self-contained=yes; AC=3/8; VER=1/8; landing=4; prod-files=1; scope=M; deps=GM-06; writeset=序列化于GM-03,GM-05,GM-06,GM-07,GM-08; release=independent; split-from=GM-06; exception=N/A`

---

### Task GM-07: SSH pull CL ref 往返

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 在同一 SSH target 新增 exact CL-ref pull 字节往返。
**完成定义:** SSH pull exact fn 基线全绿 → A/B+review → 本卡独立 patch → 适用 D 绿后 `complete`/`done`。

**Out of scope:**

- push —— GM-08
- HTTP pull —— GM-02

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 脚本 SSH 只读 opt-in | `scripts/git_protocol_smoke.sh:254-261` |


**Acceptance criteria:**

- [x] cargo 列表中 exact fn `(new) integration_git_ssh_pull_cl_ref_round_trip` 恰出现一次。
- [x] 被测客户端使用字面 `pull`。
- [x] remote ref 是本 case 的具体 `refs/cl/CASE_ID`。
- [x] pull 后字节树等于发送端快照。
- [x] 默认 main 仍是种子树。

**Verification:**

- [x] `bash -c 'set -euo pipefail; name=integration_git_ssh_pull_cl_ref_round_trip; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_ssh "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`

**Dependencies:** `GM-06A`  
**Deliverables:** `N/A`  
**Implementation write set:** `bin/tests/integration_git_ssh.rs`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 使用播种 client key  
**Performance budget:** ≤180s  
**Estimated scope:** `S`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-07）  
**Granularity:** `type=implementation; axis=SSH pull; recovery=revert; complete=yes; self-contained=yes; AC=5/8; VER=1/8; landing=1; prod-files=0; scope=S; deps=GM-06A; writeset=序列化于GM-06,GM-06A,GM-08; release=independent; split-from=N/A; exception=N/A`

---

### Task GM-08: SSH push 与坏密钥拒绝

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 新增认证 push 创建 CL ref 与未播种第二密钥拒绝两个 exact tests。
**完成定义:** SSH push 与 wrong-key 两 exact fn 基线全绿 → A/B+review → 本卡独立 patch → 适用 D 绿后 `complete`/`done`。

**Out of scope:**

- SSH LFS —— DEFER-GM-02
- CI workflow —— GM-11A/GM-11C

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 脚本 SSH push opt-in | `scripts/git_protocol_smoke.sh:271-272` |


**Acceptance criteria:**

- [x] 正路径 fn 固定为 `(new) integration_git_ssh_authenticated_push_creates_cl_ref`。
- [x] push 后 `ls-remote` 可见新 CL ref。
- [x] 失败路径 fn 固定为 `(new) integration_git_ssh_wrong_key_is_rejected`。
- [x] 第二个未播种 key 非零退出且服务保持存活。

**Verification:**

- [x] `bash -c 'set -euo pipefail; name=integration_git_ssh_authenticated_push_creates_cl_ref; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_ssh "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`
- [x] `bash -c 'set -euo pipefail; name=integration_git_ssh_wrong_key_is_rejected; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_ssh "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`

**Dependencies:** `GM-07`  
**Deliverables:** `N/A`  
**Implementation write set:** `bin/tests/integration_git_ssh.rs`, `src/server/ssh_server.rs`, `src/server/http_server.rs`, `src/ceres/api_service/cache.rs`, `src/contract/git_protocol/ssh.rs`, `src/ceres/protocol/smart.rs`, `docker-compose.test.yml`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B；`MEGA_GIT_OBJECT_CACHE_PREFIX` 可选覆盖（默认 `git-object-rkyv:v1`；`disabled`/空串关闭缓存）；git-cli compose `init: true` 回收僵尸进程；SSH upload-pack 按 pkt flush 攒包后再处理  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 坏密钥内容不进日志  
**Performance budget:** ≤240s  
**Estimated scope:** `S`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-08）  
**Granularity:** `type=implementation; axis=SSH push/坏密钥; recovery=revert; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=1; prod-files=4; scope=S; deps=GM-07; writeset=序列化于GM-06,GM-06A,GM-07; release=independent; split-from=N/A; exception=N/A`

---

### Task GM-R1: 已取消的 SSH family 发布点

**Task type:** `release`（固定；取消记录）  
**Lifecycle / Acceptance:** `cancelled` / `N/A`

GM-R1 因 ADR-GM-08（每卡独立 patch）废止家族合并发布而取消；不得执行、bump 或作为依赖。原 REL-GM-01 成员 GM-06/06A/07/08 均改为独立发布切片。此编号仅为 G-09 引用稳定性保留。

### Task GM-09: Mega tests 夹具审计与唯一分支 gate

**Task type:** `spike`  
**Lifecycle / Acceptance:** `done` / `complete`

> **执行后注记（2026-08-06 完成度复审）：** sibling `../mega` 的 HEAD 已前进一格至 `a8abbbc5a9d43997c7f53b7346f4a6984babfdad`（pin `42cd288d…` 的直接子提交，`HEAD~1` = pin 可核）。按 GC-GM-04，本卡审计结论只对 pin 有效且**不受此漂移影响**（审计于收口日在 pin 上完成，Decision: NO-GO、零移植不变）；VER-1 的 HEAD 相等判据按收口日快照阅读（等价复核见该 VER 注记）。若未来启动任何夹具移植，须按 DEP-04 先修订 pin 并重跑审计。`mega-git-fixtures-audit.md` 已加同日复核注记。

**Decision state:** `NO-GO; enabled=GM-10B; cancelled=GM-10A`

**Description:** 在 0.5 人日内审计固定 Mega SHA，并以一次计划修订只启用 GM-10A 或 GM-10B。状态 allowlist 只额外容纳前置 GM-01 已验收但由 GM-12 承载的两份 dirty docs；GM-09 review 必须确认其相对卡入口未变化。
**完成定义:** 审计与唯一 Decision 写入并通过 review 后 `locally-accepted`；不得在 GM-12 的 D 全绿前标 `complete`。由 GM-12 在 D 绿后写入。不 bump。

**Out of scope:**

- 移植 —— GM-10A
- handoff —— GM-10B

**Current evidence:**

| 事实 | 证据 |
|---|---|
| Mega tests 存在 | `../mega/tests/{data,diff,objects,refs,scripts}` |
| pin | `42cd288d266dade371aed03917d222eb57f1d5a4` |


**Acceptance criteria:**

- [x] 审计报告 headings 集合与 Deliverables「audit heading allowlist」**集合相等**，且含 pin SHA 与 `Decision: GO|NO-GO`。
- [x] `rev-parse` 与 DEP-04 full SHA **相等**。
- [x] `Decision state` 终态属于 `{GO; enabled=GM-10A; cancelled=GM-10B, NO-GO; enabled=GM-10B; cancelled=GM-10A}`（单一成员资格谓词；partial 仅当有逐文件 allowlist 时可归一为 GO）。
- [x] 唯一分支计划修订在后继开工前取得 review PASS。
- [x] spike dirty 路径集合 ⊆ Deliverables「spike path allowlist」。

**Verification:**

- [x] `bash -c 'set -euo pipefail; expected=42cd288d266dade371aed03917d222eb57f1d5a4; actual="$(cd ../mega && libra rev-parse HEAD 2>/dev/null)"; test "$actual" = "$expected"'`（2026-08-06 完成度复审注记：sibling `../mega` HEAD 已前进至 `a8abbbc5…`（pin 的直接子提交），本命令仅在收口日快照下成立；审计结论按 GC-GM-04 继续锚定 pin `42cd288d…`（NO-GO 不变），后续任何夹具工作须先按 DEP-04 重审 pin。等价复核命令：`cd ../mega && libra rev-parse HEAD~1` = pin）
- [x] `bash -c 'set -euo pipefail; file=docs/refactoring/mega-git-fixtures-audit.md; test -f "$file"; expected="$(printf "%s\n" "## Revision" "## Tree and size" "## License and provenance" "## Generation" "## Decision" "## Minimal allowlist" "## Handoff" | sort)"; actual="$(rg -n "^## " "$file" | sed "s/^[0-9]*://" | sort)"; test "$actual" = "$expected"; rg -n "42cd288d266dade371aed03917d222eb57f1d5a4|Decision: (GO|NO-GO)" "$file"'`
- [x] `bash -c 'set -euo pipefail; rg -n "^\*\*Decision state:\*\* \x60(GO; enabled=GM-10A; cancelled=GM-10B|NO-GO; enabled=GM-10B; cancelled=GM-10A)\x60" docs/plan/plan-20260803.md'`
- [x] `bash -c 'set -euo pipefail; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; libra status --short | awk '\''{print $2}'\'' >"$out"; if unexpected="$(rg -v "^(docs/refactoring/(protocol|integration|mega-git-fixtures-audit)\.md|docs/plan/plan-20260803\.md)$" "$out")"; then printf "FAIL: out-of-allowlist path: %s\n" "$unexpected" >&2; exit 1; else rc=$?; test "$rc" -eq 1; fi'`

**Dependencies:** `GM-01`, `DEP-04`  
**Deliverables:** audit heading allowlist=`{## Revision, ## Tree and size, ## License and provenance, ## Generation, ## Decision, ## Minimal allowlist, ## Handoff}`；spike path allowlist=`{docs/refactoring/mega-git-fixtures-audit.md, docs/plan/plan-20260803.md}`（可另含 GM-01 已验收且未变的 protocol/integration）  
**Implementation write set:** 同 Deliverables  
**Release write set:** `N/A`  
**Files likely touched:** 同 Deliverables  
**Docs and compatibility impact:** 审计/决策 only  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 不复制秘密/绝对私有路径  
**Performance budget:** ≤0.5 人日  
**Estimated scope:** `S`  
**Version increment:** `N/A`  
**Release boundary:** `no-release（GM-12）`  
**C/D coverage from:** `GM-12`  
**Granularity:** `type=spike; axis=Mega审计决策; recovery=revert docs; complete=yes; self-contained=yes; AC=5/8; VER=4/8; landing=2; prod-files=0; scope=S; deps=GM-01,DEP-04; writeset=序列化于GM-10B,GM-11B; release=no-release; split-from=N/A; exception=N/A`

---

### Task GM-10A: Mega 最小夹具移植（GO 分支）

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `cancelled` / `N/A`（decision-gated；GM-09 NO-GO @ 42cd288d266dade371aed03917d222eb57f1d5a4）

**Execution gate:** 已关闭——GM-09 Decision=`NO-GO; enabled=GM-10B; cancelled=GM-10A`。本卡不得开工。

**Description:** 仅移植审计 allowlist 到 `test/fixtures/mega-git/`，在既有 HTTP target 增加一条 refs/object 守卫。
**完成定义:** 仅 GO：夹具+exact 守卫基线全绿 → A/B+review → 独立 patch → D 绿后 `complete`。NO-GO 时本卡 `cancelled`，不发布。

**Out of scope:**

- no-go —— GM-10B
- 整树复制 —— 永久非目标

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 审计 gate | `本文件 GM-09 Decision state` |
| test/project 先例 | `test/project/` |


**Acceptance criteria:**

> **本卡 `cancelled`，以下 AC/VER 一律**保持未勾选**（2026-09-15 核对）。** GM-09 判定 `NO-GO`（`enabled=GM-10B; cancelled=GM-10A`），
> GO 分支从未开工、`test/fixtures/mega-git/` 从未入库，勾选它们会伪造未发生的验收。**不是待回填的数据落差。**

- [ ] exact fn 固定为 `(new) integration_git_cli_mega_fixture_refs_round_trip`。
- [ ] 仓内文件集合与审计文逐文件 allowlist **集合相等**。
- [ ] README/MANIFEST 字段集合与 Deliverables「fixture meta allowlist」**集合相等**（单一集合相等谓词）。
- [ ] exact test 实际读取夹具并分别验证 refs 与 object（不做存在性假测；单一行为轴）。
- [ ] 无大于 5 MiB 的文件，且无 allowlist 外二进制（单一负向守卫）。

**Verification:**

- [ ] `bash -c 'set -euo pipefail; name=integration_git_cli_mega_fixture_refs_round_trip; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; source .env.test; env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_cli "$name" -- --exact --test-threads=1 --nocapture 2>&1 | tee "$out"; if rg -n "SKIP:" "$out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi; test "$(awk -v n="$name" '\''$0 == "test " n " ... ok" {c++} END {print c+0}'\'' "$out")" -eq 1'`
- [ ] `bash -c 'set -euo pipefail; file=test/fixtures/mega-git/README.md; manifest=test/fixtures/mega-git/MANIFEST.sha256; needles="$(printf "%s\n" 42cd288d266dade371aed03917d222eb57f1d5a4 "Source path:" "License:" "Regenerate:" | sort)"; hit="$(rg -o --fixed-strings -f <(printf "%s\n" "$needles") "$file" | sort -u)"; big="$(find test/fixtures/mega-git -type f -size +5M -printf "%P\n" | sort)"; man_files="$(awk '\''{print $2}'\'' "$manifest" | sed '\''s|^\./||'\'' | sort)"; tree_files="$(cd test/fixtures/mega-git && find . -type f ! -name README.md ! -name MANIFEST.sha256 -printf "%P\n" | sort)"; man_hashes="$(sort "$manifest")"; tree_hashes="$(cd test/fixtures/mega-git && sha256sum $(printf "%s\n" "$man_files" | sed '\''s|^|./|'\'') | sort)"; expected="$(printf "%s\n" "NEEDLES"; printf "%s\n" "$needles"; printf "%s\n" "BIG"; printf "%s\n" ""; printf "%s\n" "FILES"; printf "%s\n" "$man_files"; printf "%s\n" "HASHES"; printf "%s\n" "$man_hashes")"; actual="$(printf "%s\n" "NEEDLES"; printf "%s\n" "$hit"; printf "%s\n" "BIG"; printf "%s\n" "$big"; printf "%s\n" "FILES"; printf "%s\n" "$tree_files"; printf "%s\n" "HASHES"; printf "%s\n" "$tree_hashes")"; test "$actual" = "$expected"'`

**Dependencies:** `GM-03`, `GM-08`, `GM-09`  
**Deliverables:** fixture meta allowlist=`{README:Mega_full_SHA, README:Source_path, README:License, README:Regenerate, MANIFEST.sha256:allowlist_hashes}`  
**Implementation write set:** `test/fixtures/mega-git/**`, `test/fixtures/mega-git/README.md`, `bin/tests/integration_git_cli.rs`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上；实际逐文件清单由 gate 修订  
**Docs and compatibility impact:** 来源记录随 fixture  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 许可/来源合规  
**Performance budget:** ≤120s  
**Estimated scope:** `M`  
**Version increment:** `patch`  
**Release boundary:** `independent`（NO-GO 时任务取消，但本字段与 Task type 不变）  
**C/D coverage from:** `self`（仅启用时；D 表 GM-10A）  
**Granularity:** `type=implementation; axis=最小夹具移植; recovery=revert; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=3; prod-files=0; scope=M; deps=GM-03,GM-08,GM-09; writeset=序列化于GM-02,GM-03; release=independent; split-from=GM-10; exception=N/A`

---

### Task GM-10B: Mega 夹具 no-go handoff

**Task type:** `handoff`  
**Lifecycle / Acceptance:** `done` / `complete`

**Execution gate:** GM-09 Decision=`NO-GO; enabled=GM-10B; cancelled=GM-10A` 已写入；本卡在 GM-09 review PASS 后开工。

**Description:** 把 NO-GO 结论登记为 active `DEFER-GM-03`，明确接收方与重启条件，不写代码/workflow。
**完成定义:** 仅 NO-GO：DEFER-GM-03 handoff 满足 AC/VER 后 `locally-accepted`；不得在 GM-12 的 D 全绿前标 `complete`。由 GM-12 在 D 绿后写入。GO 时 `cancelled`。不 bump。

**Out of scope:**

- go 移植 —— GM-10A

**Current evidence:**

| 事实 | 证据 |
|---|---|
| GM-10B DEFER 槽位 | `本文件延后表` |


**Acceptance criteria:**

- [x] DEFER-GM-03 状态从 `inactive-reserve` 改为 `active`。
- [x] 行内记录 `NO-GO @ 42cd288d266dade371aed03917d222eb57f1d5a4` 与日期 2026-08-03。
- [x] 接收方固定为 `plan-long.md PT-03/PT-04 follow-up`。
- [x] 重启条件包含许可澄清或对象层差分需求之一。
- [x] GM-10A=`cancelled`、GM-10B=`locally-accepted` 的 join 状态写入计划；GM-12 D 绿后再改为 `done / complete`。

**Verification:**

- [x] `bash -c 'set -euo pipefail; rg -n "^\| DEFER-GM-03 \| active \|.*NO-GO @ 42cd288d266dade371aed03917d222eb57f1d5a4.*2026-08-03.*PT-03/PT-04 follow-up" docs/plan/plan-20260803.md'`
- [x] `bash -c 'set -euo pipefail; sed -n "/^### Task GM-10A:/,/^### Task GM-10B:/p" docs/plan/plan-20260803.md | rg -n "^\*\*Lifecycle / Acceptance:\*\* \x60cancelled\x60 / \x60N/A\x60"; sed -n "/^### Task GM-10B:/,/^### Task GM-11A:/p" docs/plan/plan-20260803.md | rg -n "^\*\*Lifecycle / Acceptance:\*\* \x60done\x60 / \x60complete\x60"'`（2026-08-06 完成度复审：第二个正则原断言 `in-progress`/`locally-accepted`，那是 GM-12 D 绿之前的中间态；AC-5 本就要求 D 绿后升为 `done`/`complete`，VER 文本当时未随终态更新。现按终态改写，中间态断言仅在 GM-12 D 绿前的历史窗口有效）

**Dependencies:** `GM-09`  
**Deliverables:** `docs/plan/plan-20260803.md` 的 DEFER-GM-03 active handoff  
**Implementation write set:** `docs/plan/plan-20260803.md`  
**Release write set:** `N/A`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B 后续同步长期计划  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** `N/A`  
**Performance budget:** `N/A`  
**Estimated scope:** `S`  
**Version increment:** `N/A`  
**Release boundary:** `no-release（GM-12）`（GO 时任务取消，但本字段与 Task type 不变）  
**C/D coverage from:** `GM-12`（仅启用时）  
**Granularity:** `type=handoff; axis=no-go移交; recovery=revert docs; complete=yes; self-contained=yes; AC=5/20; VER=2/20; landing=1; prod-files=0; scope=S; deps=GM-09; writeset=序列化于GM-09,GM-11B; release=no-release; split-from=GM-10; exception=N/A`

---

### Task GM-11A: config-validation 新 target 执行面

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 只编辑 `.github/workflows/config-validation.yml`，使 `validate-config` 的 push/main 执行面与 paths 分别等于本卡 Deliverables 两个固定 allowlist。

**完成定义:** config-validation.yml 的 job/branches/cargo/paths/cond/diff 全部 VER 绿 → A/B+review → 独立 patch → D 绿后 `complete`。

**Out of scope:**
- smoke workflow —— GM-11C
- 文档 —— GM-11B

**Current evidence:**

| 事实 | 证据 |
|---|---|
| paths 已含 bin/tests | `.github/workflows/config-validation.yml:16` |
| 执行面含 integration_git_cli | `config-validation.yml` 测试步骤 |

**Acceptance criteria:**

- [x] job id 集合等于 `{validate-config}`。
- [x] push 事件 branches 集合等于 `{main}`。
- [x] cargo 执行命令有序列表与 Deliverables「config cargo allowlist」**序列相等**（cli→lfs→ssh；含 `env -u MONOENGINE_IT_SKIP_GIT_CLI`）。
- [x] push paths 集合与 Deliverables「config paths allowlist」**集合相等**（既有触发超集，含本 workflow 路径）。
- [x] workflow 中不存在 target 条件分支（`test -f` / `if ... integration_git_lfs|ssh` 零命中）。
- [x] 本卡 diff 路径集合等于 `{.github/workflows/config-validation.yml}`。

**Verification:**

- [x] `bash -c 'set -euo pipefail; f=.github/workflows/config-validation.yml; expected="$(printf "%s\n" validate-config)"; actual="$(awk '\''/^jobs:/{in_jobs=1; next} in_jobs && /^[^[:space:]]/{exit} in_jobs && /^  [A-Za-z0-9_-]+:/{gsub(/:/,"",$1); print $1}'\'' "$f" | sort)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/config-validation.yml; push="$(awk "/^  push:/{p=1;next} p && /^  [a-z]/{exit} p{print}" "$f")"; expected="$(printf "%s\n" main)"; actual="$(printf "%s\n" "$push" | awk "/^[[:space:]]*branches:/{b=1;next} b && /^[[:space:]]*-/{gsub(/[\"'\'' ]/,\"\",\$2); print \$2; next} b && NF && \$1 !~ /^-/{exit}")"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/config-validation.yml; expected="$(printf "%s\n" "env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_cli -- --test-threads=1" "env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_lfs -- --test-threads=1" "env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_ssh -- --test-threads=1")"; actual="$(sed -n "/env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --test integration_git_\\(cli\\|lfs\\|ssh\\)/{s/^[[:space:]]*//;p}" "$f")"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/config-validation.yml; push="$(awk "/^  push:/{p=1;next} p && /^  [a-z]/{exit} p{print}" "$f")"; expected="$(printf "%s\n" ".env.test.example" ".github/workflows/config-validation.yml" "Dockerfile" "Dockerfile.git-cli" "Dockerfile.it-runtime" "README.md" "bin/tests/**" "config/**" "docker-compose.test.yml" "docs/refactoring/config.md" "docs/refactoring/mail.md" "docs/refactoring/test-infra.md" "docs/refactoring/vault.md" "src/cli.rs" "src/commands/config.rs" "src/commands/service/ssh.rs" "src/config/**" "src/contract/vault/integration/vault_core.rs" "src/jupiter/storage/notification_storage.rs" "src/notification/**" "src/server/ssh_server.rs" | sort)"; actual="$(printf "%s\n" "$push" | sed -n "s/^[[:space:]]*- \\"\\([^\"]*\\)\\"$/\\1/p" | sort)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/config-validation.yml; if rg -n "test -f.*integration_git_(lfs|ssh)|if .*integration_git_(lfs|ssh)" "$f"; then exit 1; else rc=$?; test "$rc" -eq 1; fi'`
- [x] `bash -c 'set -euo pipefail; expected="$(printf "%s\n" .github/workflows/config-validation.yml)"; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; libra status --short | awk '\''{print $2}'\'' >"$out"; if hits="$(rg -v "^docs/" "$out")"; then actual="$(printf "%s\n" "$hits" | sort)"; else rc=$?; test "$rc" -eq 1; actual=""; fi; test "$actual" = "$expected"'`

**Dependencies:** `GM-08`, `GM-10A`, `GM-10B`  
**Deliverables:** config cargo allowlist = 三条固定 `env -u ... integration_git_{cli,lfs,ssh}` 命令（顺序 cli→lfs→ssh）；config paths allowlist = `{.env.test.example, .github/workflows/config-validation.yml, Dockerfile, Dockerfile.git-cli, Dockerfile.it-runtime, README.md, bin/tests/**, config/**, docker-compose.test.yml, docs/refactoring/config.md, docs/refactoring/mail.md, docs/refactoring/test-infra.md, docs/refactoring/vault.md, src/cli.rs, src/commands/config.rs, src/commands/service/ssh.rs, src/config/**, src/contract/vault/integration/vault_core.rs, src/jupiter/storage/notification_storage.rs, src/notification/**, src/server/ssh_server.rs}`（既有 push paths 超集 ∪ SSH/git-cli 新增；含本 workflow 路径）  
**Implementation write set:** `.github/workflows/config-validation.yml`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B 后置同步  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 保留 secret mask/check；不输出 key/token  
**Performance budget:** 现有 60m 上限内  
**Estimated scope:** `S`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-11A）  
**Granularity:** `type=implementation; axis=config-validation CI面; recovery=revert; complete=yes; self-contained=yes; AC=6/8; VER=6/8; landing=1; prod-files=1; scope=S; deps=GM-08,GM-10A,GM-10B; writeset=no-overlap; release=independent; split-from=GM-11; exception=N/A`

---

### Task GM-11B: PT-04 文档与 handoff 收口

**Task type:** `docs`  
**Lifecycle / Acceptance:** `done` / `complete`

> **执行后注记（2026-08-12 状态同步）：** VER-4 对 `README.md`「`plan-20260803.md` | 日期计划 | 进行中」字样与 `plan-long.md` PT-04 链接行的断言，是 GM-12 发布（D 组绿）之前的收口日快照；本计划全卡终态后，两处索引已于 2026-08-12 更正为「已完成」，PT-04 总览升为「实施中」（`DEFER-GM-01..05` 仍为 follow-up）。事后重跑 VER-4 须按收口日快照阅读。

**Description:** 在实现与 CI 终态后，由唯一文档 owner 同步协议矩阵、测试索引、SSH infra、长期计划、历史 DEFER 与日期索引；不编辑 workflow。
**完成定义:** 七份文档与 DEFER 快照满足 AC/VER 后 `locally-accepted`；不得在 GM-12 的 D 全绿前标 `complete`。由 GM-12 在 D 绿后写入。不 bump、不改 workflow。

**Out of scope:**

- workflow —— GM-11A/11C

**Current evidence:**

| 事实 | 证据 |
|---|---|
| README 已登记本计划 | `docs/plan/README.md:25` |
| PT-04 日期计划仍为无 | `docs/plan/plan-long.md PT-04 行` |


**Acceptance criteria:**

- [x] `protocol.md` 场景表 exact fn 集合 ⊇ Deliverables「protocol fn allowlist」（集合包含经 `comm -23` 空差集一次判定；ADR-GM-06）。
- [x] `integration.md` target 引用集合与 Deliverables「integration target allowlist」**集合相等**，并含 `protocol.md` 链接（链接为同一 VER 脚本末尾单次 `rg` 成功并入 exit；不另计谓词）。
- [x] `test-infra.md` 关键词集合与 Deliverables「test-infra keyword allowlist」**集合相等**，且 compose+SSH 组合零命中（零命中并入同一脚本 exit）。
- [x] `plan-long.md`/`README.md` PT-04 关联与状态行与 Deliverables「index link allowlist」**集合相等**。
- [x] `plan-20260727.md` DEFER-IT 关闭/改挂集合与 Deliverables「DEFER-IT close allowlist」**集合相等**。
- [x] DEFER-GM-01..05 行字段快照与 Deliverables「DEFER-GM row allowlist」**集合相等**；本卡 diff 不含 `.github/workflows/**`（并入同一脚本）。

**Verification:**

- [x] `bash -c 'set -euo pipefail; expected="$(printf "%s\n" integration_git_cli_http_pull_cl_ref_round_trip integration_git_cli_auth_anonymous_disabled_rejects_clone integration_git_lfs_http_round_trip integration_git_ssh_service_lifecycle_isolated integration_git_ssh_authenticated_clone integration_git_ssh_pull_cl_ref_round_trip integration_git_ssh_authenticated_push_creates_cl_ref integration_git_ssh_wrong_key_is_rejected | sort)"; actual="$(rg -o "integration_git_(cli|lfs|ssh)_[a-z0-9_]+" docs/refactoring/protocol.md | sort -u)"; missing="$(comm -23 <(printf "%s\n" "$expected") <(printf "%s\n" "$actual"))"; test -z "$missing"'`
- [x] `bash -c 'set -euo pipefail; file=docs/refactoring/integration.md; expected="$(printf "%s\n" integration_git_cli integration_git_lfs integration_git_ssh protocol.md | sort)"; actual="$( { rg -o "integration_git_(cli|lfs|ssh)" "$file"; rg -o "protocol\.md" "$file"; } | sort -u)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; file=docs/refactoring/test-infra.md; missing=""; while IFS= read -r kw; do rg -q --fixed-strings "$kw" "$file" || missing="$missing[$kw]"; done < <(printf "%s\n" "cargo-native self-start" "service ssh" ssh_keys known_hosts GIT_SSH_COMMAND 端口 清理); test -z "$missing" || { echo "missing keywords: $missing" >&2; exit 1; }; if rg -q "compose.*service ssh|service ssh.*compose" "$file"; then echo "compose+service ssh combo found" >&2; exit 1; fi'`（2026-08-06 完成度复审：原命令用 `rg -o --fixed-strings -f <(…)` 多 pattern 提取做集合相等，rg 15.2 在多 pattern 下会漏报个别 CJK pattern（`清理`）导致假 FAIL；改为逐关键词 `rg -q` 存在性判定 + 组合零命中，语义不变，仍为单脚本单 exit（ADR-GM-06 计 1 条））
- [x] `bash -c 'set -euo pipefail; expected="$(printf "%s\n" "plan-long_ok" "readme_ok" | sort)"; actual="$( { rg -n "^\| PT-04 \|.*\[plan-20260803\.md\]\(plan-20260803\.md\)" docs/plan/plan-long.md >/dev/null && echo plan-long_ok; rg -n "^\| \x60plan-20260803\.md\x60 \| 日期计划 \| 进行中" docs/plan/README.md >/dev/null && echo readme_ok; } | sort)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; expected="$(printf "%s\n" DEFER-IT-01 DEFER-IT-02 DEFER-IT-12 | sort)"; actual="$(rg "DEFER-IT-(01|02|12).*(关闭|DEFER-GM)" docs/plan/plan-20260727.md -o | rg -o "DEFER-IT-(01|02|12)" | sort -u)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; file=docs/plan/plan-20260803.md; dec="$(awk '\''/^### Task GM-09:/{p=1;next} p && /^### Task /{exit} p && /^\*\*Decision state:\*\*/{print; exit}'\'' "$file")"; row03_go="DEFER-GM-03|inactive-reserve|Mega 大规模夹具移植|仅 GM-09 NO-GO 后由 GM-10B 激活|许可澄清或对象层差分需求|PT-03/PT-04 follow-up"; row03_nogo="DEFER-GM-03|active|Mega 大规模夹具移植|NO-GO @ 42cd288d266dade371aed03917d222eb57f1d5a4 · 2026-08-03|许可澄清或对象层差分需求|PT-03/PT-04 follow-up"; if printf "%s\n" "$dec" | rg -q "enabled=GM-10B"; then row03="$row03_nogo"; elif printf "%s\n" "$dec" | rg -q "enabled=GM-10A"; then row03="$row03_go"; else echo "GM-09 Decision state must be GO or NO-GO before GM-11B" >&2; exit 1; fi; expected="$(printf "%s\n" "DEFER-GM-01|active|ImportRepo 全命令矩阵|本计划聚焦 Monorepo|ImportRepo 产品验收排期|PT-04 follow-up" "DEFER-GM-02|active|Pure SSH LFS|endpoint/client 发现另轴|产品承诺 SSH LFS|PT-03/PT-04 follow-up" "$row03" "DEFER-GM-04|active|shell SSH 广度强制 CI|cargo 最小集优先|cargo SSH 稳定且时长预算允许|PT-04 follow-up" "DEFER-GM-05|active|形态合法 missing-repo 客户端失败（原 DEFER-IT-12）|monorepo 空仓广告语义限制|产品改变广告语义或独立探针设计|PT-04 follow-up" "workflow_diff=" | sort)"; rows="$(awk -F'|' '\''/^\| DEFER-GM-0[1-5] /{gsub(/^ +| +$/,"",$2); gsub(/^ +| +$/,"",$3); gsub(/^ +| +$/,"",$4); gsub(/^ +| +$/,"",$5); gsub(/^ +| +$/,"",$6); gsub(/^ +| +$/,"",$7); print $2"|"$3"|"$4"|"$5"|"$6"|"$7}'\'' "$file" | sort)"; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; libra status --short | awk '\''{print $2}'\'' >"$out"; wf=""; if hits="$(rg "^\\.github/workflows/" "$out")"; then wf="$(printf "%s\n" "$hits" | paste -sd, -)"; else rc=$?; test "$rc" -eq 1; wf=""; fi; actual="$(printf "%s\n%s\n" "$rows" "workflow_diff=$wf" | sort)"; test "$actual" = "$expected"'`
**Dependencies:** `GM-11C`, `DEP-01`, `DEP-02`  
**Deliverables:** 文件=`docs/refactoring/protocol.md, docs/refactoring/integration.md, docs/refactoring/test-infra.md, docs/plan/plan-long.md, docs/plan/plan-20260727.md, docs/plan/README.md, docs/plan/plan-20260803.md`；protocol fn allowlist=`{integration_git_cli_http_pull_cl_ref_round_trip, integration_git_cli_auth_anonymous_disabled_rejects_clone, integration_git_lfs_http_round_trip, integration_git_ssh_service_lifecycle_isolated, integration_git_ssh_authenticated_clone, integration_git_ssh_pull_cl_ref_round_trip, integration_git_ssh_authenticated_push_creates_cl_ref, integration_git_ssh_wrong_key_is_rejected}`；integration target allowlist=`{integration_git_cli, integration_git_lfs, integration_git_ssh}`；test-infra keyword allowlist=`{cargo-native self-start, service ssh, ssh_keys, known_hosts, GIT_SSH_COMMAND, 端口, 清理}`；index link allowlist=`{plan-long_ok, readme_ok}`；DEFER-IT close allowlist=`{DEFER-IT-01, DEFER-IT-02, DEFER-IT-12}`；DEFER-GM row allowlist=`完整行快照 ID|status|延后内容|原因/证据|重启条件|Owner/承接；DEFER-GM-03 随 GM-09 Decision：GO→inactive-reserve 冻结行，NO-GO→active 且原因含 NO-GO @ 42cd288d266dade371aed03917d222eb57f1d5a4 · 2026-08-03；+ workflow_diff=`  
**Implementation write set:** 同 Deliverables（不含 workflow）  
**Release write set:** `N/A`  
**Files likely touched:** 同 Deliverables  
**Docs and compatibility impact:** 本卡主轴  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 文档无 key/token  
**Performance budget:** `N/A`  
**Estimated scope:** `M`  
**Version increment:** `N/A`  
**Release boundary:** `no-release（GM-12）`  
**C/D coverage from:** `GM-12`  
**Granularity:** `type=docs; axis=PT-04文档收口; recovery=revert docs; complete=yes; self-contained=yes; AC=6/20; VER=6/20; landing=7; prod-files=0; scope=M; deps=GM-11C,DEP-01,DEP-02; writeset=序列化于GM-01,GM-09,GM-10B; release=no-release; split-from=GM-11; exception=N/A`

---

### Task GM-11C: git-protocol-smoke 新 target 执行面

**Task type:** `implementation`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 只编辑 `.github/workflows/git-protocol-smoke.yml`，使 job 执行命令序列与 push paths 分别等于 Deliverables 两个固定 allowlist。

**完成定义:** git-protocol-smoke.yml 的 job/branches/cargo/paths/cond/diff 全部 VER 绿 → A/B+review → 独立 patch → D 绿后 `complete`。

**Out of scope:**
- config-validation —— GM-11A
- 文档 —— GM-11B

**Current evidence:**

| 事实 | 证据 |
|---|---|
| smoke paths 含 protocol.md | `.github/workflows/git-protocol-smoke.yml:22` |
| 已跑 integration_git_cli | `git-protocol-smoke.yml` |

**Acceptance criteria:**

- [x] job id 集合等于 `{git-protocol-smoke}`。
- [x] push 事件 branches 集合等于 `{main}`。
- [x] cargo 执行命令有序列表与 Deliverables「smoke cargo allowlist」**序列相等**（`--release`；cli→lfs→ssh）。
- [x] push paths 集合与 Deliverables「smoke paths allowlist」**集合相等**（既有触发超集，含本 workflow 路径）。
- [x] 无 target 条件分支（`test -f` / `if ... integration_git_{cli,lfs,ssh}` 零命中）。
- [x] 本卡 diff 路径集合等于 `{.github/workflows/git-protocol-smoke.yml}`（允许 `docs/**` 仍为 GM-12 承载的 dirty）。
**Verification:**

- [x] `bash -c 'set -euo pipefail; f=.github/workflows/git-protocol-smoke.yml; expected="$(printf "%s\n" git-protocol-smoke)"; actual="$(awk '\''/^jobs:/{in_jobs=1; next} in_jobs && /^[^[:space:]]/{exit} in_jobs && /^  [A-Za-z0-9_-]+:/{gsub(/:/,"",$1); print $1}'\'' "$f" | sort)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/git-protocol-smoke.yml; push="$(awk "/^  push:/{p=1;next} p && /^  [a-z]/{exit} p{print}" "$f")"; expected="$(printf "%s\n" main)"; actual="$(printf "%s\n" "$push" | awk "/^[[:space:]]*branches:/{b=1;next} b && /^[[:space:]]*-/{gsub(/[\"'\'' ]/,\"\",\$2); print \$2; next} b && NF && \$1 !~ /^-/{exit}")"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/git-protocol-smoke.yml; expected="$(printf "%s\n" "env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --release --test integration_git_cli -- --test-threads=1" "env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --release --test integration_git_lfs -- --test-threads=1" "env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --release --test integration_git_ssh -- --test-threads=1")"; actual="$(sed -n "/env -u MONOENGINE_IT_SKIP_GIT_CLI cargo test -p monoengine --release --test integration_git_\\(cli\\|lfs\\|ssh\\)/{s/^[[:space:]]*//;p}" "$f")"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/git-protocol-smoke.yml; push="$(awk "/^  push:/{p=1;next} p && /^  [a-z]/{exit} p{print}" "$f")"; expected="$(printf "%s\n" ".github/workflows/git-protocol-smoke.yml" "Dockerfile.git-cli" "bin/tests/**" "docker-compose.test.yml" "docs/refactoring/protocol.md" "docs/refactoring/test-infra.md" "scripts/git_protocol_smoke.sh" "src/api/router/lfs_router.rs" "src/ceres/lfs/**" "src/ceres/pack/**" "src/ceres/protocol/**" "src/commands/service/ssh.rs" "src/contract/git_protocol/**" "src/jupiter/storage/git_db_storage.rs" "src/jupiter/storage/lfs_db_storage.rs" "src/jupiter/storage/mono_storage.rs" "src/server/http_server.rs" "src/server/ssh_server.rs" | sort)"; actual="$(printf "%s\n" "$push" | sed -n "s/^[[:space:]]*- \\"\\([^\"]*\\)\\"$/\\1/p" | sort)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; f=.github/workflows/git-protocol-smoke.yml; if rg -n "test -f.*integration_git_(cli|lfs|ssh)|if .*integration_git_(cli|lfs|ssh)" "$f"; then exit 1; else rc=$?; test "$rc" -eq 1; fi'`
- [x] `bash -c 'set -euo pipefail; expected="$(printf "%s\n" .github/workflows/git-protocol-smoke.yml)"; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; libra status --short | awk '\''{print $2}'\'' >"$out"; if hits="$(rg -v "^docs/" "$out")"; then actual="$(printf "%s\n" "$hits" | sort)"; else rc=$?; test "$rc" -eq 1; actual=""; fi; test "$actual" = "$expected"'`
**Dependencies:** `GM-11A`  
**Deliverables:** smoke cargo allowlist = 三条固定 `--release` target 命令（cli→lfs→ssh）；smoke paths allowlist = `{.github/workflows/git-protocol-smoke.yml, Dockerfile.git-cli, bin/tests/**, docker-compose.test.yml, docs/refactoring/protocol.md, docs/refactoring/test-infra.md, scripts/git_protocol_smoke.sh, src/api/router/lfs_router.rs, src/ceres/lfs/**, src/ceres/pack/**, src/ceres/protocol/**, src/commands/service/ssh.rs, src/contract/git_protocol/**, src/jupiter/storage/git_db_storage.rs, src/jupiter/storage/lfs_db_storage.rs, src/jupiter/storage/mono_storage.rs, src/server/http_server.rs, src/server/ssh_server.rs}`（既有 push paths 超集 ∪ bin/tests/SSH/git-cli 新增；含本 workflow 路径）  
**Implementation write set:** `.github/workflows/git-protocol-smoke.yml`  
**Release write set:** `Inherited`  
**Files likely touched:** 同上  
**Docs and compatibility impact:** GM-11B 后置同步  
**Rollback mode:** `revert`  
**Migration and rollback:** `N/A`  
**Security and privacy:** 保留 secret mask/check  
**Performance budget:** 现有 60m 上限内  
**Estimated scope:** `S`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-11C）  
**Granularity:** `type=implementation; axis=git-protocol-smoke CI面; recovery=revert; complete=yes; self-contained=yes; AC=6/8; VER=6/8; landing=1; prod-files=1; scope=S; deps=GM-11A; writeset=no-overlap; release=independent; split-from=GM-11; exception=N/A`

---

### Task GM-12: 最终文档与版本发布

**Task type:** `release`  
**Lifecycle / Acceptance:** `done` / `complete`

**Description:** 不引入行为；由具名发布者 `eli` 提交已验收 no-release 产物，bump patch，执行 C 组并取得两项 D；D 全绿后再把承载的 no-release 卡与本卡 `Lifecycle/Acceptance` 升为 `done`/`complete`（写入本计划）。

**完成定义:** 开工前实现卡已 `complete`、no-release 卡已 `locally-accepted`；提交 no-release 产物并独立 patch+push 取得 C，再等双 D 全绿；**仅 D 绿后**把 no-release 卡与本卡写入 `done`/`complete`；Review log 最大轮次=`PASS`。

**Out of scope:**
- 新行为 —— 各实现卡

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 版本面 | `Cargo.toml:6` / `bin/Cargo.toml:3` |

**Acceptance criteria:**

- [x] 开工前，Acceptance 映射与 Deliverables「pre_c card-state allowlist」**集合相等**（实现卡=`complete`；no-release 卡=`locally-accepted`；取消分支卡 Acceptance=`N/A`；不含 GM-12；ADR-GM-06）。
- [x] 版本面与 Deliverables「version face allowlist」**集合相等**（major/minor 不变且 patch+1）。
- [x] 本卡 diff 路径集合 ⊆（Release write set ∪ 已验收 no-release Deliverables ∪ 本计划状态提升行）。
- [x] D 表 GM-12 两行均为 `success`（paths 命中时）。
- [x] **仅在上述 D 全绿之后**，把承载的 no-release 卡与本卡（及已发布实现卡）的 `Lifecycle/Acceptance` 写入为 `done`/`complete`（取消卡保持 `cancelled`/`N/A`），且映射与 Deliverables「post_d life/acc allowlist」**集合相等**（含 GM-12；ADR-GM-06；用反引号字段解析，兼容 Acceptance=`N/A`）。
- [x] Review log **最大轮次号**对应 Result 精确等于 `PASS`（按 `R{n}` 数值取 max；后轮 FAIL 使前轮 PASS 失效）。

**Verification:**

- [x] `bash -c 'set -euo pipefail; file=docs/plan/plan-20260803.md; rg -n "^\*\*Decision state:\*\* \x60(GO; enabled=GM-10A; cancelled=GM-10B|NO-GO; enabled=GM-10B; cancelled=GM-10A)\x60" "$file" >/dev/null; acc_of(){ line="$(awk -v id="$1" '\''$0 ~ \"^### Task \" id \":\" {p=1;next} p && /^### Task /{exit} p && /^\*\*Lifecycle \/ Acceptance:\*\*/ { print; exit }'\'' "$file")"; printf "%s\n" "$line" | sed -E '\''s/^\*\*Lifecycle \/ Acceptance:\*\*[[:space:]]*\x60[^\x60]*\x60[[:space:]]*\/[[:space:]]*\x60([^\x60]*)\x60.*/\1/; t; s/.*/EMPTY/'\'' ; }; if rg -q "enabled=GM-10A" <<<"$(rg "^\*\*Decision state:\*\*" "$file")"; then expected="$(printf "%s\n" GM-01=locally-accepted GM-02=complete GM-03=complete GM-05=complete GM-06=complete GM-06A=complete GM-07=complete GM-08=complete GM-09=locally-accepted GM-10A=complete GM-10B=N/A GM-11A=complete GM-11B=locally-accepted GM-11C=complete | sort)"; else expected="$(printf "%s\n" GM-01=locally-accepted GM-02=complete GM-03=complete GM-05=complete GM-06=complete GM-06A=complete GM-07=complete GM-08=complete GM-09=locally-accepted GM-10A=N/A GM-10B=locally-accepted GM-11A=complete GM-11B=locally-accepted GM-11C=complete | sort)"; fi; ids="GM-01 GM-02 GM-03 GM-05 GM-06 GM-06A GM-07 GM-08 GM-09 GM-10A GM-10B GM-11A GM-11B GM-11C"; actual=""; for id in $ids; do actual+="$id=$(acc_of "$id")"$'\n'; done; actual="$(printf "%s" "$actual" | sort -u)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; previous="$(libra show HEAD:Cargo.toml | awk -F\" '\''/^version = / {print $2; exit}'\'')"; root="$(awk -F\" '\''/^version = / {print $2; exit}'\'' Cargo.toml)"; bin="$(awk -F\" '\''/^version = / {print $2; exit}'\'' bin/Cargo.toml)"; lock_mono="$(awk '\''$0 == "name = \"monoengine\"" {getline; gsub(/\"/, "", $3); print $3; exit}'\'' Cargo.lock)"; lock_core="$(awk '\''$0 == "name = \"monoengine-core\"" {getline; gsub(/\"/, "", $3); print $3; exit}'\'' Cargo.lock)"; IFS=.; set -- $previous; pmaj=$1; pmin=$2; ppatch=$3; IFS=.; set -- $root; expected="$(printf "%s\n" "maj=$pmaj" "min=$pmin" "patch=$((ppatch+1))" "root=$pmaj.$pmin.$((ppatch+1))" "bin=$pmaj.$pmin.$((ppatch+1))" "monoengine=$pmaj.$pmin.$((ppatch+1))" "monoengine-core=$pmaj.$pmin.$((ppatch+1))" | sort)"; actual="$(printf "%s\n" "maj=$1" "min=$2" "patch=$3" "root=$root" "bin=$bin" "monoengine=$lock_mono" "monoengine-core=$lock_core" | sort)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; out="$(mktemp)"; trap '\''rm -f "$out"'\'' EXIT; libra status --short | awk '\''{print $2}'\'' >"$out"; allowed="^(Cargo\\.toml|bin/Cargo\\.toml|Cargo\\.lock|docs/refactoring/(protocol|integration|test-infra)\\.md|docs/plan/(plan-long|plan-20260727|README|plan-20260803)\\.md|docs/refactoring/mega-git-fixtures-audit\\.md)$"; if unexpected="$(rg -v "$allowed" "$out")"; then printf "FAIL: %s\n" "$unexpected" >&2; exit 1; else rc=$?; test "$rc" -eq 1; fi'`
- [x] `bash -c 'set -euo pipefail; pushed_sha="$(libra rev-parse HEAD)"; for wf in config-validation.yml git-protocol-smoke.yml; do run_id="$(gh run list --workflow "$wf" --branch main --event push --commit "$pushed_sha" --limit 20 --json databaseId,conclusion --jq "map(select(.conclusion == \"success\")) | .[0].databaseId // empty")"; test -n "$run_id"; test "$(gh run view "$run_id" --json conclusion --jq .conclusion)" = success; done'`
- [x] `bash -c 'set -euo pipefail; file=docs/plan/plan-20260803.md; rg -n "^\*\*Decision state:\*\* \x60(GO; enabled=GM-10A; cancelled=GM-10B|NO-GO; enabled=GM-10B; cancelled=GM-10A)\x60" "$file" >/dev/null; la_of(){ line="$(awk -v id="$1" '\''$0 ~ \"^### Task \" id \":\" {p=1;next} p && /^### Task /{exit} p && /^\*\*Lifecycle \/ Acceptance:\*\*/ { print; exit }'\'' "$file")"; printf "%s\n" "$line" | sed -E '\''s/^\*\*Lifecycle \/ Acceptance:\*\*[[:space:]]*\x60([^\x60]*)\x60[[:space:]]*\/[[:space:]]*\x60([^\x60]*)\x60.*/\1\/\2/; t; s/.*/EMPTY\/EMPTY/'\'' ; }; if rg -q "enabled=GM-10A" <<<"$(rg "^\*\*Decision state:\*\*" "$file")"; then expected="$(printf "%s\n" GM-01=done/complete GM-02=done/complete GM-03=done/complete GM-05=done/complete GM-06=done/complete GM-06A=done/complete GM-07=done/complete GM-08=done/complete GM-09=done/complete GM-10A=done/complete GM-10B=cancelled/N/A GM-11A=done/complete GM-11B=done/complete GM-11C=done/complete GM-12=done/complete | sort)"; else expected="$(printf "%s\n" GM-01=done/complete GM-02=done/complete GM-03=done/complete GM-05=done/complete GM-06=done/complete GM-06A=done/complete GM-07=done/complete GM-08=done/complete GM-09=done/complete GM-10A=cancelled/N/A GM-10B=done/complete GM-11A=done/complete GM-11B=done/complete GM-11C=done/complete GM-12=done/complete | sort)"; fi; ids="GM-01 GM-02 GM-03 GM-05 GM-06 GM-06A GM-07 GM-08 GM-09 GM-10A GM-10B GM-11A GM-11B GM-11C GM-12"; actual=""; for id in $ids; do actual+="$id=$(la_of "$id")"$'\n'; done; actual="$(printf "%s" "$actual" | sort -u)"; test "$actual" = "$expected"'`
- [x] `bash -c 'set -euo pipefail; file=docs/plan/plan-20260803.md; result="$(awk -F"|" '\''/^\| R[0-9]+ / { r=$2; gsub(/^ +| +$/,"",r); n=substr(r,2)+0; res=$4; gsub(/^ +| +$/,"",res); gsub(/\x60/,"",res); if (n>=maxn){maxn=n; out=res} } END { print out }'\'' "$file")"; test "$result" = "PASS"'`

**Dependencies:** `GM-11B`  
**Deliverables:** pre_c card-state allowlist=`实现卡 Acceptance=complete + no-release Acceptance=locally-accepted + 取消分支 Acceptance=N/A（GO:10B；NO-GO:10A）`；post_d life/acc allowlist=`含 GM-12：启用卡=`done`/`complete`；取消卡=`cancelled`/`N/A`；须 D 全绿后写入；解析取两个 \x60...\x60 字段以兼容 N/A`；version face allowlist=`maj/min unchanged + patch+1 + Cargo.toml==bin/Cargo.toml==Cargo.lock(monoengine)==Cargo.lock(monoengine-core)`；release note 说明 PT-04 交付/剩余 DEFER  
**Implementation write set:** `N/A`  
**Release write set:** `Inherited`  
**Files likely touched:** `Cargo.toml`, `bin/Cargo.toml`, `Cargo.lock`，以及已验收 no-release docs  
**Docs and compatibility impact:** 日期索引与 PT-04  
**Rollback mode:** `immutable-release`  
**Migration and rollback:** 推送后只前滚新 patch  
**Security and privacy:** release evidence 脱敏  
**Performance budget:** `N/A`  
**Estimated scope:** `S`  
**Version increment:** `patch`  
**Release boundary:** `independent`  
**C/D coverage from:** `self`（D 表 GM-12）  
**Granularity:** `type=release; axis=最终文档版本发布; recovery=immutable-release后前滚patch; complete=yes; self-contained=yes; AC=6/12; VER=6/12; landing=0; prod-files=0; scope=S; deps=GM-11B; writeset=R-only; release=independent; split-from=N/A; exception=N/A`

## 测试矩阵

| 类别 | 必须覆盖 | Exact target / command |
|---|---|---|
| HTTP pull | CL-ref 字面 pull + 字节树 | `integration_git_cli::integration_git_cli_http_pull_cl_ref_round_trip` |
| HTTP auth | anonymous false 拒绝 + token 对照 | `integration_git_cli::integration_git_cli_auth_anonymous_disabled_rejects_clone` |
| LFS | push / lfs pull / cmp | `integration_git_lfs::integration_git_lfs_http_round_trip` |
| SSH topology | service/host-key/port lifecycle | `integration_git_ssh::integration_git_ssh_service_lifecycle_isolated` |
| SSH auth/read | seeded client auth clone | `integration_git_ssh::integration_git_ssh_authenticated_clone` |
| SSH pull | CL-ref pull 字节树 | `integration_git_ssh::integration_git_ssh_pull_cl_ref_round_trip` |
| SSH write/security | push + wrong key | `integration_git_ssh_authenticated_push_creates_cl_ref`; `integration_git_ssh_wrong_key_is_rejected` |
| Mega fixture（GO） | refs/object fixture | `integration_git_cli::integration_git_cli_mega_fixture_refs_round_trip` |
| CI | 两个 jobs 固定 targets | GM-11A（config）+ GM-11C（protocol）exact workflow commands |

## 追溯表

| 任务 | 来源 | 落点 | 文档 owner | 指定测试/证据 |
|---|---|---|---|---|
| GM-01 | GAP-06/07 | protocol/integration | self→GM-11B | 文档表守卫 |
| GM-02 | GAP-01 | integration_git_cli | GM-11B | exact pull fn |
| GM-03 | GAP-02/config baseline | integration_git_cli/common | GM-11B | exact auth fn |
| GM-04 | R1/G-02 | cancelled | N/A | N/A |
| GM-05 | GAP-03/DEFER-IT-02 | LFS target/runner | GM-11B | exact LFS fn |
| GM-06 | GAP-04/ADR-GM-05 | SSH service/host-key lifecycle | GM-11B | exact lifecycle fn |
| GM-06A | GAP-04/ADR-GM-05 | SSH client auth/helpers | GM-11B | exact clone fn |
| GM-07 | SSH pull | SSH target | GM-11B | exact SSH pull fn |
| GM-08 | SSH push/auth | SSH target | GM-11B | two exact fns |
| GM-R1 | ADR-GM-08 | cancelled | N/A | N/A |
| GM-09 | DEP-04 | audit doc/plan gate | self | SHA + audit headings |
| GM-10A | GO | minimal fixture | fixture README | exact fixture fn |
| GM-10B | NO-GO | DEFER-GM-03 | GM-11B | active handoff row |
| GM-11A | CI gap | config-validation workflow | GM-11B | job/paths/commands |
| GM-11C | CI gap | git-protocol-smoke workflow | GM-11B | job/paths/commands |
| GM-11B | DEP-01/02 | seven docs | self | exact links/status |
| GM-12 | ER-08 | version R + no-release docs | self | parity + two D |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 恢复 |
|---|---|---|---|
| M0 | GM-01 场景表与 GM-09 唯一分支修订 review PASS | no-release→GM-12 | revert docs |
| M1 | GM-02/03/05 complete | 三个独立 patch + D | 已推送则 forward patch |
| M2 | GM-06/06A/07/08 各自 complete | 四个独立 SSH patch + 各自 config D | 已推送则 forward patch only |
| M3 | GM-10A/B 唯一启用分支达到其 join gate | impl 或 handoff | 推送前按启用卡 revert；任何 push 后只允许 forward patch |
| M4 | GM-11A/11C/11B、GM-12 complete | 两 workflows D success | forward patch only |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| cargo filter 零匹配 | 输出文件 | exact `... ok` count 必须为 1 | cargo exit 0 即 PASS |
| runner/LFS/SSH 不可用 | sanitized log | 修复前置后重跑 | `SKIP:` 当 PASS |
| SSH 启动/退出失败 | owned child/case dir | SIGINT→10s→kill/wait→删 SSH dir | orphan PID/private key |
| Mega SHA 漂移 | 审计草稿 | 阻塞、修订 DEP-04、review | 对新 SHA 沿用旧结论 |
| push 后 D 失败 | 已推送 main | 新提交/新 patch 前滚 | revert 已推送历史 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| monorepo pull 语义误判 | 测试假绿 | ADR-GM-02 + 字节树 | GM-02/07 |
| LFS runner 漂移 | 本地/CI 不一致 | image/digest + 3.7.1 exact pin | GM-05/11A/11C |
| SSH key/port 残留 | 安全与不稳定 | ADR-GM-05 生命周期 | GM-06/06A/07/08 |
| Mega 许可/体积 | 合规风险 | SHA pin + go/no-go gate | GM-09/10A/B |
| 文档并发覆盖 | 矩阵漂移 | GM-01→GM-11B 单写者 | GM-01/11B |

## 性能与容量摘要

| 操作 | 上限 | 验证 |
|---|---|---|
| HTTP pull | 180s | GM-02 exact test |
| HTTP auth | 120s | GM-03 exact test |
| LFS round-trip | 300s，单文件≤5MiB | GM-05 |
| SSH lifecycle/clone/pull | 每例≤180s | GM-06/06A/07 |
| SSH push/failure | 每例≤240s | GM-08 |
| Mega spike | 0.5 人日 | GM-09 |

## 兼容与文档收口

> 本节于 2026-09-15 据实回填勾选（GM-01..GM-12 早已全部终态，GM-12 发布于 v0.2.11、复审收口于 v0.2.15，仅本清单遗漏）。

- [x] `docs/errors.md`: N/A（不改产品错误契约）。
- [x] `config/config.toml`: N/A；保持无 `[git]`，GM-03 用 per-case 完整 TOML override。 —— `rg '^\[git\]' config/config.toml` 零命中
- [x] OpenAPI / migration / schema: N/A。
- [x] `protocol.md`、`integration.md`、`test-infra.md` 由 GM-11B 单写收口。 —— `protocol.md:628`「场景覆盖表（权威，plan-20260803 / ADR-GM-01）」+ `:653` GAP 映射 + `:651` `DEFER-GM-05` 行；`integration.md:38,46,48` 引用该权威表与 GM-05/GM-06..08 target
- [x] `plan-long.md` PT-04 关联日期计划不再为 `无`。 —— PT-04 行「日期计划」列已为 [`plan-20260803.md`](plan-20260803.md)（GM-* / GM-R1），状态「实施中」，剩余 `DEFER-GM-01/02/04/05` 为 follow-up
- [x] README 已有本计划行；仅按真实发布状态更新 wording。 —— `docs/plan/README.md:26`，措辞已含 GM-04/GM-R1/GM-10A 取消与 GM-10B handoff 承接

## Review log

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R0 | 成稿自审 | FAIL | 未识别后由 R1 暴露的结构缺陷 | 需 R1 remediation | 初稿 |
| R1 | Codex prior review | FAIL | 14 项 P1 | README/PT cross-link 纳入 GM-11B | `/tmp/monoengine-plan-review/gm-r1.txt` |
| R2 | 复审（嵌入前） | `FAIL` | 4×P1：强制章节正文 / Current evidence / Publisher / G-03 | — | `/tmp/monoengine-plan-review/gm-r2.txt` |
| R3 | 嵌入后复审 | `FAIL` | Publisher/G-03/GM-12 门 | eli+集合相等 | `/tmp/monoengine-plan-review/gm-r3.txt` |
| R4 | R3 整改后复审 | `FAIL` | G-03 / GM-12 max-round / CI paths / Mega pin | pin+超集 paths | `/tmp/monoengine-plan-review/gm-r4.txt` |
| R5 | R4 整改后复审 | `FAIL` | ADR 相等谓词落地 / 子卡 Acceptance 环 / patch maj-min / R-only VER | Acceptance=locally-accepted 等 | `/tmp/monoengine-plan-review/gm-r5.txt` |
| R6 | R5 整改后复审 | `FAIL` | GM-R1 R-only 脏树 / 审计不同步 / GM-11A/C awk / GM-11B DEFER 字段 | 已改 commit 路径相等+审计同步+可执行 VER+DEFER 行快照；待 R7 | `/tmp/monoengine-plan-review/gm-r6.txt` |
| R7 | R6 整改后复审 | `FAIL` | R-only 引号/排序 / jobs: 未限定 / DEFER-03 与 NO-GO 冲突 / GM-11C 漏计 | 已改；待 R8 | `/tmp/monoengine-plan-review/gm-r7.txt` |
| R8 | R7 整改后复审 | `PASS` | P0/P1=0 | 模板脚手架占位已在提交前清理 | `/tmp/monoengine-plan-review/gm-r8.txt` |
| — | 用户结构性修订（ADR-GM-07/08） | `superseded` | R8 PASS 因发布模型变更作废，需重审 | 见修订历史 | 本文件 |
| R9 | 结构性修订后复审 | `FAIL` | ADR-GM-07 vs 非目标/DEP-05；GM-12 complete 环 | 已改被测路径口径 + pre/post_c allowlist；待 R10 | `/tmp/monoengine-plan-review/gm-r9.txt` |
| R10 | R9 整改后复审 | `FAIL` | GM-12 在 D 前提升 complete；Lifecycle 未校验 | 已改为 D 后 post_d life/acc；待 R11 | `/tmp/monoengine-plan-review/gm-r10.txt` |
| R11 | R10 整改后复审 | `FAIL` | cancelled/N/A 解析失败；post_d 漏 GM-12 | 已改反引号字段解析并纳入 GM-12；待 R12 | `/tmp/monoengine-plan-review/gm-r11.txt` |
| R12 | R11 整改后复审 | `PASS` | P0/P1=0 | 模板脚手架占位已在提交前清理 | `/tmp/monoengine-plan-review/gm-r12.txt` |
| — | 执行委派 | `note` | Publisher `eli` 委派本会话 Agent 执行本计划卡的 C 组 bump/commit/push 与 D 跟踪（ER-GM-02；单一委派） | 生效至计划完成或 eli 撤销 | 本文件 |

**下一轮:** 无（最新轮次 `R12` = `PASS`；P2 脚手架已清理）。

## 非目标与延后项

| ID | 状态 | 延后内容 | 原因/证据 | 重启条件 | Owner/承接 |
|---|---|---|---|---|---|
| DEFER-GM-01 | active | ImportRepo 全命令矩阵 | 本计划聚焦 Monorepo | ImportRepo 产品验收排期 | PT-04 follow-up |
| DEFER-GM-02 | active | Pure SSH LFS | endpoint/client 发现另轴 | 产品承诺 SSH LFS | PT-03/PT-04 follow-up |
| DEFER-GM-03 | active | Mega 大规模夹具移植 | NO-GO @ 42cd288d266dade371aed03917d222eb57f1d5a4 · 2026-08-03 | 许可澄清或对象层差分需求 | PT-03/PT-04 follow-up |
| DEFER-GM-04 | active | shell SSH 广度强制 CI | cargo 最小集优先 | cargo SSH 稳定且时长预算允许 | PT-04 follow-up |
| DEFER-GM-05 | active | 形态合法 missing-repo 客户端失败（原 DEFER-IT-12） | monorepo 空仓广告语义限制 | 产品改变广告语义或独立探针设计 | PT-04 follow-up |

## 完成判据

> 本节于 2026-09-15 据实回填勾选。最后三条的「三门 / focused test / D 组」为**落地当时**（GM-12 v0.2.11、复审 v0.2.15）的结论，本次回填只改本文件、未重跑。

- [x] 所有非取消/非延后任务满足 G-01..G-11，审计表与卡内 Granularity 一致。
- [x] DAG 边、Dependencies、审计 deps、发布顺序一致且无环。
- [x] GM-10A/GM-10B 恰好一 complete、一 cancelled；所有 Task type 固定。 —— GM-10A `cancelled` / `N/A`（GM-09 NO-GO）、GM-10B `done` / `complete`（`handoff`）
- [x] 所有 focused test 证明 exact fn 恰好运行一次且无 `SKIP:`。 —— 落地当时结论（GM-10A 的两条 VER 因本卡 `cancelled` 不适用）
- [x] C 组三门、`cargo build`、`cargo build --tests` 以及适用 D 全绿。 —— 落地当时结论
- [x] Review 最终为带 `PASS` 的最新轮次，P0/P1=0。 —— R12 `PASS`，「下一轮: 无」
- [x] `plan-long.md` PT-04、历史 DEFER、README 与实际发布状态一致。 —— PT-04 行已关联本计划；`DEFER-GM-01/02/03/04/05` 五行 `active` 并注明承接方；`docs/plan/README.md:26` 措辞一致
