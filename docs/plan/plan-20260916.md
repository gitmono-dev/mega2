# monorepo 路径 → GitHub 单向同步基础设施计划（2026-09-16）

## 文档职责

本文解决「把 `/project` 下的 monorepo 路径内容单向推送到 GitHub 所需的**已定型基础设施**」——配置面、自举凭证与出站 SSH 传输——并用一张 spike 卡冻结全部尚未定型的设计问题，再把其余实现移交后续计划。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

> **成稿背景（R1/R2 评审响应）**：初稿（12 张卡）覆盖完整出站链路。R1（`FAIL`）确认其中 5 项同步语义属未解决设计问题；R2（`FAIL`）进一步确认 `incremental_pack` 硬化同样**未定型**——它需要改动 `RepoHandler` trait 的返回类型（`src/ceres/pack/mod.rs:254` 无流内错误通道），而该 trait 的消费端是**线上 clone/fetch 路径**（`src/ceres/protocol/smart.rs:277`、`src/ceres/protocol/v2.rs:284`）；且「任意缺失的 `have` 返回错误」会破坏 upload-pack 的正常协商语义（服务端只 ACK 自己拥有的 commit，`src/ceres/protocol/smart.rs:267`）。因此本文再次收敛：只保留**不触碰既有协议路径**的基础设施，全部未定型问题进 `GS-09`（spike）。逐条关闭矩阵见「Review log」。

### 适用范围

- `[github_sync]` 配置段：schema、加载注册（含白名单、构造器、重载分类）、启动期 fail-closed 校验
- 自举的 Ed25519 SSH 金钥：生成、跨副本收敛、持有、公钥暴露边界、私钥封闭性
- `src/ceres/github_sync/` 的出站 SSH 连接与 receive-pack 客户端（**新增模块，不改动既有协议路径**）
- `docs/refactoring/github-sync.md` 契约文档（各卡增量写入）
- 一张 spike 卡冻结全部未定型设计问题，一张 handoff 卡移交剩余实现

### 非目标

- **`incremental_pack` 硬化与 pack 错误通道设计**：需改动 `RepoHandler` trait 并触及线上 clone/fetch 路径，未定型。由 `GS-09` Q6 冻结、`GS-10` 移交（`DEFER-GS-07`）。
- **出站同步执行链路**（outbox 触发、推送 worker、游标、事件、AI 交接、轮询、命令 API）：语义未冻结，由 `GS-09` Q1–Q5/Q7 冻结、`GS-10` 移交（`DEFER-GS-01`）。
- **真实 GitHub 的 live 验收**：本计划只做本地回环验收，不产生任何 GitHub 侧副作用（`DEFER-GS-08`）。
- **GitHub → mega2 入站同步**：永久非目标（`DEFER-GS-02`）。
- **mega2 内建合并**：ADR-GS-04。
- **`main` 以外的分支、tag、release**；**`gh` CLI 与 GitHub REST API**（ADR-GS-03）；**`/third-party` 路径**（`DEFER-GS-03`）。

### 成功定义

- 配置 `[github_sync]` 后，启动期即可拒绝全部已知的不可执行组合，且 `enabled=false` 时对既有部署零影响。
- 启动一次即可从日志取得公钥并完成 GitHub 侧准备；私钥在日志、错误文本、`Debug` 三个面均不可见；多副本并发初始化收敛到同一把金钥。
- 可对一个**本地** SSH 服务端完成一次 receive-pack，并在被拒时取得有界的 side-band 诊断。
- `GS-09` 产出的 ADR 冻结 7 项待解设计问题并给出 go / no-go / 研究未完成三分支判定；`GS-10` 按对应分支完成移交或替代方向登记。
- 所有非延后任务 `Lifecycle=done` 且 `Acceptance=complete`，review 最终 `PASS`。

## 事实基线

> 行号以 2026-09-16 现场核对为准。**工作树为脏且正在被并行修改**（本轮 `libra status --short` 有 18 行，涵盖 `Cargo.toml`、`README.md`、`config/`、`docs/`、`src/` 多处，归属 `plan-20260912` 的 WH-03 等并行工作）。行号随时位移，**开工当天必须逐条重新核对**；越界守卫一律以开工前的基线快照逐行扣除（见「本计划引用的模板规则」）。
>
> 对 `docs/plan/plan-20260912.md` 的引用采用「引文 + 行号」双锚定：该文件正被并行编辑，R1 给出的更正行号本身也已偏移。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| **pack trait（本计划不改动）** | `incremental_pack` 返回 `Result<ReceiverStream<Vec<u8>>, GitError>`，**无流内错误/完成通道** | `src/ceres/pack/mod.rs:254`–`:258` |
| pack trait 的消费端（线上 clone/fetch） | **1 个 trait 声明、2 个实现、4 个调用点** | 声明 `src/ceres/pack/mod.rs:254`；实现 `src/ceres/pack/monorepo.rs`、`src/ceres/pack/import_repo.rs`；调用 `src/ceres/protocol/smart.rs:277`、`src/ceres/protocol/v2.rs:284`、`src/ceres/pack/monorepo.rs:256`（`full_pack` 委派）、`src/ceres/pack/mod.rs:284`（`filtered_pack` 默认） |
| upload-pack `have` 协商语义 | 服务端只对**自己拥有**的 commit 回 ACK；客户端广告服务端没有的 `have` 是正常协商 | `src/ceres/protocol/smart.rs:267`（`check_commit_exist`） |
| pack 内 unwrap 的可达性 | `get_trees_by_hashes` **已被** `incremental_pack → traverse_for_count` 调用，其实现含 unwrap | `src/ceres/pack/mod.rs:388`；`src/ceres/pack/monorepo.rs:616` |
| pack 字节分配点 | 单 blob 经 `try_fold(Vec::new(), …)` 整体累积，并发度 16 | `src/ceres/pack/mod.rs:443` |
| 路径子树语义 | 路径 ref 的 commit tree **就是该路径子树** | `src/ceres/pack/materialize.rs:262`（`w.subtree.id`） |
| 一次 push 的 ref 影响面 | 同事务内还会更新**祖先**路径 ref 与推进/删除**后代**路径 ref | `src/ceres/api_service/mono_api_service.rs:3493`；`src/jupiter/service/push_queue_service.rs:2093`、`:2475`；`src/jupiter/storage/mono_storage.rs:713` |
| trunk N=1 落地语义 | N=1 时落地 commit **即客户端 `cmd.new_id`**（两侧 hash 可能相同） | `src/jupiter/service/push_queue_service.rs:1995`；`docs/refactoring/trunk-push.md:429` |
| **形态推论** | `git.push_auth` 一经设置即**强制** `push_policy="trunk"` | `src/config/validate.rs:635`–`:640` |
| trunk `/api/v1` 中间件 | **有** session / CORS / Trace / 解压 / trace-context；缺少的是 review 分支的 `cedar_guard` | `src/server/http_server.rs:774`–`:782`；`:792` |
| 配置结构与全部注册点 | 字段表 / 完整构造器 / 根段白名单 / restart-required 分类 / 测试模块 | `src/config/model.rs:10`；`src/config/testing.rs:157`（`Config {`）；`src/config/validate.rs:1741`（白名单）；`src/config/reload.rs:678`；`src/config/mod.rs:677`（`mod tests;`，独立文件 `src/config/tests.rs`） |
| 配置校验（同形态先例） | `validate_storage_events_config`；**disabled 时仍做字符串解析与命名空间校验**，只跳过 vault 取值 | `src/config/validate.rs:693`；`:755`–`:760`（注释明写 "string-level only; vault resolution happens at service startup"） |
| 配置校验（SecretRef / 形态门） | `validate_config_secret_ref`；`validate_trunk_config_surface` | `src/config/validate.rs:444`、`:628` |
| hash kind | `MonoConfig::object_hash_kind()` 支持 sha1/sha256/blake3 | `src/config/model.rs:379` |
| 金钥自举先例（**含跨副本互斥**） | `ensure_server_signing_key`：首次初始化用 **RedLock** 互斥，**锁内重读指针**使并发初始化收敛到单一赢家 | `src/contract/vault/server_signing.rs:250`–`:255`（doc comment）；语义测试 `:546` |
| 协议 helper | `add_pkt_line_string` / `try_read_pkt_line`（已对非十六进制 header 硬化） | `src/ceres/protocol/smart.rs:871`、`:885` |
| side-band 解复用**参考**（不可直接复用） | `process_pack_stream` 以发现 `PACK` 为进入条件、返回 pack 数据；receive-pack 返回的是 report-status | `src/ceres/model/third_party.rs:216`、`:244` |
| 既有 GitHub 出站表面 | **已存在**第三方仓库同步入口（HTTPS、匿名、**拉取**方向） | `src/ceres/api_service/mono_api_service.rs:4364`、`:4371` |
| 依赖 | `russh = "0.63.3"`（**仅用 server 端**，`rg 'russh::client' src/` 零命中）、`rand = "0.10.2"` | `Cargo.toml:73`、`:64` |
| 版本 | 开工当天以 `Cargo.toml` 的 `version` 现场读取为准（成稿期间已从 `0.10.32` 漂移到 **`0.10.36`**，可见该文件被并行修改的频率） | `Cargo.toml:3` |
| 仓库发布规则 | **每卡 commit+push**；`Version increment=N/A` 的 docs/audit 卡**仍要 commit+push**，只是不 bump；**不得捆绑后续卡** | `.cursor/rules/task-card-release.mdc:19`–`:26`；`AGENTS.md:255`–`:258` |
| 工作区前置 | `.env.test` **存在**；测试栈状态**未核实**（`docker compose ps` 返回空，不作为已核实事实） | 2026-09-16 现场 |
| CI | **`.github/workflows/` 为空目录**（0 个文件，2026-09-16 现场），故本计划全部卡的 **D 组一律 `N/A`**，无需逐卡现场判定 | `ls -A .github/workflows` |
| VCS | **`.libra` 存在、`.git` 不存在**；`git status` 报 `not a git repository`；`libra status --short` 为 `XY path` 两列格式 | 2026-09-16 现场；`AGENTS.md:251` |
| 外部参照 | 无外部 repo 参照；本计划不引用任何未 pin 的外部事故论据 | N/A |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | 无 `[github_sync]` 配置段及其注册点 | 功能无配置入口 | `rg '\[github_sync\]' config/ src/config/` 零命中（2026-09-16） | GS-03 |
| GAP-02 | 无形态与 hash kind 启动闸 | GitHub 仅支持 SHA-1；非 trunk 形态下 `push_auth` 组合非法 | `src/config/model.rs:379`；`src/config/validate.rs:635` | GS-04 |
| GAP-03 | 无 binding 条目合法性校验 | 非法 path/remote 会在运行期深处失败，或推错仓库造成静默数据破坏 | 无既有校验（`rg 'github_sync' src/config/` 零命中） | GS-13 |
| GAP-04 | 无任何出站 SSH 凭证 | 既有 GitHub 表面（`mono_api_service.rs:4364`）是 HTTPS 匿名**拉取**，方向与传输均不适用 | `rg 'russh::client' src/` 零命中 | GS-05, GS-06 |
| GAP-05 | 无出站 SSH 连接与 receive-pack 客户端 | 无法推送 | 同上 | GS-07, GS-08 |
| GAP-06 | 7 项设计问题未冻结（同步语义 5 项 + pack 错误通道/字节预算 + 操作授权与策略判据） | 均无法在任务卡层面补 AC 解决，会导致实现返工或触及线上协议路径 | R1 P0-04..P0-07/P0-09；R2 P0-01/P0-02；证据见事实基线相应行 | GS-09 |
| GAP-07 | 模板 GC-12 / ER-07 声称本仓由 Git 管理、`.libra` 已删除，与当前 checkout 不符 | 按模板执行提交会直接失败 | `.git` 不存在；`AGENTS.md:251` | GS-11 登记漂移；DEP-02 移交模板维护者 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | 无对应 PT/SB 项（Mega 移植范围之外的新方向） | 由 GS-11 在日期计划索引登记一行，不新增 PT 编号 |
| [`plan-20260912.md`](plan-20260912.md) | **并行**：其 WH-03 等正在修改 `push_queue_service.rs` 及多处 | 本计划**不触碰**任何既有 `src/` 文件（只新增 `src/ceres/github_sync/` 与 `src/config/` 的追加字段），无行为写集冲突；配置文件的共享由顺序边与基线扣除处理 |
| [`plan-20260910.md`](plan-20260910.md) / [`plan-20260905.md`](plan-20260905.md) / [`plan-20260904.md`](plan-20260904.md) | 事实源：`MonoWriteQueue`、trunk 形态、`land_api_tip_push`、`authorize_trunk_api_write` | 只读消费 |
| `docs/monorepo.md` | 契约事实源：唯一公开分支、禁 tag、路径子树语义 | 只读消费 |
| `docs/refactoring/trunk-push.md` | 契约事实源：ADR-TP-17 线性历史；N=1 落地语义 | 只读消费；GS-09 的冻结结论必须与其 `:429` 一致 |
| `.cursor/rules/task-card-release.mdc` | **发布规则事实源**：每卡 commit+push，docs 卡不 bump 但仍推送，不得捆绑后续卡 | 本计划据此**不设家族卡**，每张卡独立发布切片 |
| `AGENTS.md` / `README.md` | 工程约束基线 | **登记漂移**（GAP-07）：按 `AGENTS.md:251` 的 Libra 流程执行，由 GS-11 登记、DEP-02 移交 |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | 值得做：monorepo 内容对外发布无任何自动化路径 | 无 |
| 可行性 | 二次收敛后 10 张活动卡均为 S/M，且**不触碰既有协议路径**；零新增依赖 | R2 后移出 pack 硬化 |
| 任务卡粒度 | R2 指出 AC 仍按 checklist 行数计 | 按独立谓词逐条展开；GS-04 超限后拆出 GS-13 |
| 依赖与顺序 | R2 指出共享写集顺序未表达 | 全串行；共享 `src/config/**` 的卡逐条补顺序边 |
| 完整性 | 测试/文档/回滚齐全；本计划无 schema 迁移 | 迁移随 GS-10 移交 |
| 安全性 | 四处 fail-closed：非 sha1、非 trunk、私钥零泄漏、主机金钥不匹配 | GS-04 / GS-06 / GS-07 |
| 功能正确性 | R2 指出 `have` 语义与 trait 错误通道未定型 | 移出本计划，进 GS-09 Q6 |
| 接口兼容 | 新增配置段默认关闭；新增模块不改既有协议路径 | GS-03 / GS-07 / GS-08 |
| 数据流与控制流 | 本计划不触及事务与持久状态；金钥自举补跨副本收敛 | GS-05 |
| 性能与容量 | 本计划不引入无界扫描；诊断输出有显式字节预算 | GS-08 |
| 可靠性与容错 | 金钥自举 forward-only 且恢复验证自足；传输失败可分阶段定位 | GS-05 / GS-07 / GS-08 |
| 可维护性 | 单一事实源：凭证只在 vault，binding 只在配置文件 | 无 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-16 | 用户发布要求 | 每次版本 bump 后创建并推送同名 `v<version>` tag，并以最终 diff 为依据人工撰写标准 release note 后发布同名 GitHub Release；禁止自动生成 note | 卡号不变 | ER-08、完成判据、发布者职责 |
| 2026-09-16 | 自审（粒度） | SSH 连接/认证 与 receive-pack 协议是两个可独立恢复的行为轴 | `GS-04`（初稿） → `GS-07`, `GS-08` | 实施顺序、追溯表、测试矩阵、粒度审计表 |
| 2026-09-16 | R1（`FAIL`，P0-01/P0-02） | 修正 9 项事实错误 | 无卡拆分 | 事实基线、当前缺口、全部 ADR |
| 2026-09-16 | R1（P0-04..P0-07、P0-09） | 5 项同步语义属未解决设计问题 | 初稿 `GS-07`..`GS-11` → `GS-09`(spike) + `GS-10`(handoff) | 适用范围、非目标、DEFER 表 |
| 2026-09-16 | R1（P0-08）+ R2（P1-01） | 本仓规则要求 docs 卡亦 commit+push 且不得捆绑后续卡，家族卡模型不适用 | `REL-00` 登记**撤销**；`GS-12`（release 点）**取消** | 发布分组、各卡 `Release boundary` / `C/D coverage from` |
| 2026-09-16 | R2（P0-01/P0-02） | pack 硬化需改 `RepoHandler` trait 并触及线上 clone/fetch，且 `have` 语义需重设计 —— 未定型 | `GS-01`, `GS-02` **取消**，替代为 `GS-09` Q6 + `GS-10` 移交 | 适用范围、非目标、实施顺序、DEFER 表、风险表、测试矩阵 |
| 2026-09-16 | R2（P1-02） | GS-04 按独立谓词计为 14 条，超 G-03 上限 | `GS-04` → `GS-04`, `GS-13` | 实施顺序、粒度审计表、追溯表 |
| 2026-09-16 | R2（P0-04） | 5 段守卫中 3 段实测假通过 | 全部守卫重写并**本地实测**（正例/负例/命令失败三态） | 各卡 `Verification` |
| 2026-09-16 | R2（P1-04） | live GitHub 验收与「不产生副作用」自相矛盾 | live 验收移出本计划 | GS-08 `Verification`、测试矩阵、DEFER 表 |
| 2026-09-16 | review R3（`FAIL`，P0-01/P1-01） | 守卫改内容哈希基线；守卫交付顺序死结 —— 新增引导卡承接守卫实现 | 新增 `GS-14`（守卫脚本）；`GS-11` 去除脚本产物回归纯 docs | 实施顺序、发布窗口、追溯表、测试矩阵、粒度审计表、Phase 0 |
| 2026-09-16 | review R3（P1-02） | `GS-05` 按独立谓词计为 ≥10，超 G-03 上限 | `GS-05` → `GS-05`, `GS-16` | 实施顺序、依赖登记、粒度审计表、里程碑 M1 |
| 2026-09-16 | review R3（P1-02/P1-05） | `GS-08` 按独立谓词计为 ≥14，且终止契约有遗漏 | `GS-08` → `GS-08`, `GS-15` | 实施顺序、依赖登记、粒度审计表、里程碑 M2、测试矩阵 |
| 2026-09-16 | review R3（P1-02/P1-07） | `GS-09` 合卡（Q1–Q7）按独立谓词计为 ≥14 | `GS-09` → `GS-09`(Q1–Q5), `GS-18`(Q6–Q7) | 实施顺序、依赖登记、粒度审计表、里程碑 M3、风险表、DEFER 表 |
| 2026-09-16 | review R4（P0-01） | 守卫比较集合不完整：基线路径从 status 消失时不被比较，可删除他人未提交工作而通过 | 无卡拆分；`GS-14` 契约改为比较「基线路径 ∪ 当前脏路径」 | 「本计划引用的模板规则」、`GS-14` AC/VER |
| 2026-09-16 | review R4（P1-03） | vault 层无 compare-and-set 原语，`GS-16` 的条件写入不可实现 | 无卡拆分；`GS-16` 降为「既有原语能力范围内」，丢锁窗口登记为残余风险并移交 `GS-28` | `GS-16` AC/VER/安全/恢复、`GS-18` 问题清单、风险表 |
| 2026-09-16 | review R4（P2-01） | 版本、WH-03 状态、outbox 行号在成稿期间漂移；`.github/workflows/` 为空 | 无卡拆分；事实基线刷新，D 组一律判定为 `N/A` | 事实基线、各卡 `C/D coverage from` |
| 2026-09-16 | review R4（P1-02） | `GS-14` 按独立谓词计为 ≥11 | `GS-14` → `GS-14`, `GS-19` | 实施顺序、发布窗口、追溯表、粒度审计表、Phase 0、里程碑 M0 |
| 2026-09-16 | review R4（P1-02） | `GS-15` 按独立谓词计为 ≥15 | `GS-15` → `GS-15`, `GS-20` | 实施顺序、发布窗口、粒度审计表、里程碑 M2、测试矩阵 |
| 2026-09-16 | review R4（P1-02） | `GS-18` 按独立谓词计为 ≥15 | `GS-18` → `GS-18`, `GS-21` | 实施顺序、发布窗口、粒度审计表、DEFER 表、风险表 |
| 2026-09-16 | review R4（P1-04） | `GS-10` 未定义三张 spike 结果的组合处置 | 无卡拆分；`GS-10` 新增组合决策表 AC | `GS-10` AC、里程碑 M4 |

## 已决议设计决策

### ADR-GS-01: 只同步 `/project` 下路径的 `main`，映射显式声明

- **Status:** Accepted
- **Context:** 路径 ref 的 commit tree 就是该路径子树（`materialize.rs:262`），故路径与 GitHub 仓库是 1:1 tree 映射。但取叶节点名会碰撞（`/project/a/b/c/foo` 与 `/project/x/y/foo` 都得到 `foo`），且仓库内无 slugifier 或碰撞解析器。
- **Decision:** binding 显式声明 `path` 与 `remote`，不从路径推导；启动校验强制两者各自唯一。只同步 `refs/heads/main`。
- **Alternatives considered:** 路径末段自动推导（拒绝——碰撞后果是两个路径互相覆盖同一仓库，单向推送下为静默数据破坏）；多分支（拒绝——`docs/monorepo.md:14`）。
- **Consequences:** 配置啰嗦；换取冲突在启动期即被拒绝。
- **Revisit when:** monorepo 放开多公开分支时。

### ADR-GS-02: 本计划只支持 trunk 形态，且不新增任何 HTTP 表面

- **Status:** Accepted
- **Context:** `git.push_auth` 一经设置即强制 `push_policy="trunk"`（`src/config/validate.rs:635`–`:640`），而本计划的启动闸要求 `push_auth` 存在，故 trunk 是**推论出的确定边界**，不是待定问题。另一方面，trunk 缺少统一的路由授权层（`http_server.rs:792` 的 `cedar_guard` 不挂载），`push_auth="none"` 时 `authorize_trunk_api_write` 返回匿名（`api_write_auth.rs:34`）。
- **Decision:** 本计划只支持 trunk 形态；**不新增任何 HTTP 路由**。操作接口及其授权设计由 `GS-09` Q7 冻结、`GS-10` 移交。
- **Alternatives considered:** 同时支持 review 形态（拒绝——与 `push_auth` 的强制推论冲突）；在本计划内加命令 API（拒绝——授权设计未定型）。
- **Consequences:** 本计划交付物无任何 HTTP 表面，运行时 OpenAPI 证据为 `N/A`。
- **Revisit when:** trunk 引入统一认证层时。

### ADR-GS-03: 单一凭证——自举的 SSH 金钥，不引入 `gh` 与 REST API

- **Status:** Accepted
- **Context:** SSH 金钥无法认证 GitHub REST API；任何 REST-only 能力都要求第二种凭证，带来第二条轮换路径与第二套审计面。仓库内已有的 GitHub 表面（`mono_api_service.rs:4364`）是 HTTPS 匿名**拉取**，方向与传输均不适用。
- **Decision:** 只用一把 SSH 金钥，启动时自动生成（照 `ensure_server_signing_key`，含 RedLock 跨副本收敛），私钥写入 vault 且不离开进程，公钥经启动日志输出供人工贴入 GitHub 机器账号。不做 branch protection 自动预检。
- **Alternatives considered:** deploy key（不能跨仓库复用）；fine-grained PAT（需 HTTPS 与第二凭证）；`gh auth token`（scope 为该用户全部仓库）。
- **Consequences:** 失去上线前 branch protection 检查，改由首次推送失败承担——因此 GS-08 必须在能力允许时收下 side-band 诊断（ADR-GS-05）。
- **Revisit when:** 需要 REST-only 能力时。

### ADR-GS-04: mega2 不实现任何合并

- **Status:** Accepted
- **Context:** 在 mega2 内做三方合并需要四样东西而均不存在：merge-base 计算（`rg 'merge_base|common_ancestor|three_way' src/` 零命中；`git-internal 0.9.0` 模块只有 `index`/`metadata`/`object`/`pack`/`zlib`）、三方 tree 合并与冲突检测（`CheckType::MergeConflict` 无注册 checker）、2-parent merge commit（`src/ceres/pack/push_chain.rs` 拒绝 merge commit）、从 GitHub fetch（违反单向范围）。
- **Decision:** 非快进时不在 mega2 内合并。处置策略（`abort` / `force`）的判定条件与外部移交协议由 `GS-09` Q7 冻结后随 `GS-10` 实现。
- **Alternatives considered:** `reparent`（产物存不进 mega2）；真三方合并（上述四条阻断）。
- **Consequences:** 本计划不实现任何非快进处置逻辑。
- **Revisit when:** monorepo 模型允许 merge commit 时。

### ADR-GS-05: receive-pack 的能力协商与最低能力集

- **Status:** Accepted
- **Context:** 客户端**只能请求服务端已声明的能力**。取消 branch protection 预检后（ADR-GS-03），失败诊断成为主要信息来源，而详细诊断走 side-band channel 2/3。
- **Decision:** 先解析 advertisement 的能力集；`report-status` 为**最低能力集**——未声明时在**发送任何字节前**拒绝执行；`side-band-64k` 为可选增强，已声明才请求，未声明则降级并在诊断中标注能力缺失。诊断输出有本层显式字节预算（不引用任何已移出本计划的事件字段上限）。
- **Alternatives considered:** 写死 capability（拒绝——违反协议协商规则）；无最低能力集即发送（拒绝——无报告则无法判定结果）。
- **Consequences:** `process_pack_stream`（`third_party.rs:216`）只能作参考：它以发现 `PACK` 为进入条件且返回 pack 数据，与 receive-pack 返回 report-status 的形态不同，不能直接复用。
- **Revisit when:** N/A

### ADR-GS-06: 未定型问题一律进 spike，不进实现卡

- **Status:** Accepted
- **Context:** R1 与 R2 累计确认 7 项设计层面问题（见 `GS-09` 的 Q1–Q7）。其中 Q6 尤其关键：`incremental_pack` 的错误传播需要改动 `RepoHandler` trait 的返回类型（`pack/mod.rs:254` 无流内错误通道），而消费端是线上 clone/fetch（`smart.rs:277`、`v2.rs:284`）；且「任意缺失的 `have` 返回错误」会破坏正常协商（`smart.rs:267`）。
- **Decision:** 这 7 项由 `GS-09`（`spike`）在时间箱内冻结为 ADR，产出后由 `GS-10` 按 go / no-go / 研究未完成三分支处置。本计划不实现任何依赖这些结论的代码，**也不改动任何既有 `src/` 文件的行为**。
- **Alternatives considered:** 在任务卡内补 AC（拒绝——改变的是 trait 契约与状态机，会导致返工）；直接实现并迭代（拒绝——涉及线上协议路径与不可逆远端副作用）。
- **Consequences:** 本计划交付物为纯增量基础设施，对既有功能零行为影响。
- **Revisit when:** N/A

## 全局工程约束

`docs/plan/plan-template.md` 的 `GC-01`..`GC-12` 全量适用。本计划特有的补充与偏离：

- **GC-12 偏离（GAP-07）:** 模板 GC-12 与 ER-07 声称本仓自 2026-08-27 起由 Git 管理、`.libra` 已删除。**2026-09-16 现场核对为假**：`.git` 不存在、`.libra` 存在、`git status` 报 `not a git repository`，且 `AGENTS.md:251` 明写 "VCS is Libra (no `git`)"。本计划按 `AGENTS.md:255`–`:258` 与 `.cursor/rules/task-card-release.mdc:19`–`:26` 执行：`libra add <相关路径>` → `libra commit -m` → `libra push origin main`，**每卡独立提交推送**（docs 卡不 bump 但仍推送），不得捆绑后续卡。漂移由 GS-11 登记、DEP-02 移交。
- **GC-11 补充:** 本计划新增生产代码零 `unwrap()`/`expect()`/`panic!()`；既有 unwrap 的消除已移出本计划（`DEFER-GS-07`）。
- **GC-07 补充:** 四处 fail-closed 必须有测试：非 sha1 拒绝启动、缺 `push_auth` 拒绝启动、主机金钥不匹配即中止连接、私钥在三个暴露面均不可见。
- **GC-02 补充:** 凭证的单一事实源是 vault；binding 定义的单一事实源是配置文件。
- **GC-10 补充:** 本计划不引入任何无界扫描或无界内存；诊断输出有显式字节预算（GS-08）。

## 执行检查必备需求（强制）

`docs/plan/plan-template.md` 的 `ER-01`..`ER-12` 全量适用。本计划的执行期补充：

- **ER-01 补充:** 本仓为 Libra，用 `libra status --short` 替代 `git status --short --branch`。开工前必须：①确认 `.env.test` 存在；②**实际启动并确认测试栈**（`docker compose -f docker-compose.test.yml up -d --wait`）——本轮未能核实其状态，不得假定已启动；③**保存内容基线** `bash scripts/guard/baseline.sh <baseline>`（产出每行 `<sha256>  <path>` 或 `ABSENT  <path>`，**不是** `libra status --short` 的原始输出）。工作树被并行修改，内容基线是唯一可信的扣除依据。该步骤自 `GS-19` 起适用；`GS-14` 自身按其引导例外执行（其证据来自隔离夹具）。
- **ER-07 补充:** 签名预检的 `git config` 路径在本仓不可用。开工时以 `libra` 的实际签名能力为准；若无法取得 `gpgsig` 证据，按「字段全局默认与例外」登记 `EX-*` 后方可提交。
- **ER-08 补充:** 版本面以**开工当天**现场读取 `Cargo.toml` 的 `version` 为准（该文件正被并行修改，本文记录的 `0.10.32` 仅为成稿日快照）。
- **ER-08 发布要求:** 每次实际版本 bump 的卡在提交并推送后，必须使用 `libra tag -m "v<version>: <summary>" v<version>` 创建同名标注 tag，并以 `libra push origin refs/tags/v<version>` 推送；再以 `gh release create v<version> -R gitmono-dev/mega2 --title "v<version>" --notes-file <release-notes-file>` 发布同名 GitHub Release。release note 必须基于最终 diff 人工撰写，包含 Highlights、用户可见变更、兼容性 / 配置 / 迁移说明（无则写 `N/A`）、验证结果和已知限制；禁止 `--generate-notes` 或其它自动生成内容。`Version increment=N/A` 的卡不创建 tag 或 release。
- **ER-12 发布者:** 见「发布分组与并发窗口」。

### 本计划引用的模板规则（就地摘录，避免悬空引用）

- **G-03 计数口径**（`plan-template.md:364`）：AC 按「独立 pass/fail 谓词」计，用「且/并且/以及/同时」连接的多个可分别失败的断言按多条计；Verification 按「独立验证门」计，环境准备前缀与其后命令合计为一门。ER-04 的强制门不计入。上限：`implementation`/`migration`/`spike` 为 8，`docs`/`audit`/`handoff` 为 20。
- **Verification 判定口径**（`plan-template.md:478`）：守卫必须区分「零命中」与「命令失败」；仅用于定位符号的 `rg` 须注明「锚点定位用，非判据」；新增 test fn 标 `(new)`；依赖真实服务的用例必须写清前置，把「跳过」当「通过」属幽灵验收。
- **G-01 恢复模式**（`plan-template.md:358`）：`revert` / `forward-only` / `compensating` / `immutable-release` 四选一；`compensating` 必须写出补偿命令与幂等键。

**本计划的三段公共守卫（由 `GS-14` 交付，已本地实测正例、越界负例与命令失败三态）：**

`scripts/guard/baseline.sh <out>` —— 保存**内容+元数据基线**。每个脏路径记一行 `<kind>|<mode>|<fingerprint>  <path>`：`kind` ∈ `file|symlink|absent|unsupported`；`mode` 为八进制权限位（`symlink`/`absent` 记 `-`）；`fingerprint` 为普通文件的 `sha256`、符号链接的链接目标、`absent` 的字面 `-`。**目录、FIFO、设备等类型记 `unsupported` 并在比对时一律判越界**（拒绝而非静默放行）。仅记内容哈希无法区分「只改执行位」与「替换为指向相同字节的符号链接」，故元数据必须入指纹。

`scripts/guard/allowlist.sh <baseline> <allowed-path>...` —— 比较集合为「**基线路径 ∪ 当前脏路径**」（覆盖已从 status 消失的基线路径）；对不在 allowlist 的路径以上述**完整指纹**与基线比对；重命名对**源路径与目的路径两端**分别判定；`libra status` / `sha256sum` / `stat` / `readlink` / `grep` / `sort` 任一失败即退出 2；allowlist 为空或基线缺失同样退出 2。

`scripts/guard/docrefs.sh <markdown-file>...` —— 逐文件以 `rg -o --no-filename` 提取 `docs/*.md` 引用（`--no-filename` 避免多文件前缀污染），`rg` 退出 >1 或 `sort` 失败即退出 2；悬空引用退出 1。

退出码契约（三段一致）：`0` = 通过，`1` = 判定失败（越界 / 悬空），`2` = 命令或输入错误。**各卡引用 allowlist 守卫时必须传入该卡 `Deliverables` 的确切产物路径，不得传入 `docs` 这类宽目录。**

## 实施顺序

依赖边与各卡 `Dependencies` 完全一致（G-06）。共享 `src/config/**` 的卡（GS-03/GS-04/GS-13）与共享 `src/ceres/github_sync/**` 的卡（GS-05..GS-08）均以顺序边消解 I–I 相交。

- `GS-14 -> GS-22`
- `GS-22 -> GS-19`
- `GS-19 -> GS-03`
- `GS-03 -> GS-23`
- `GS-23 -> GS-04`
- `GS-04 -> GS-13`
- `GS-13 -> GS-05`
- `GS-05 -> GS-16`
- `GS-16 -> GS-06`
- `GS-06 -> GS-07`
- `GS-07 -> GS-08`
- `GS-08 -> GS-24`
- `GS-24 -> GS-15`
- `GS-15 -> GS-25`
- `GS-25 -> GS-20`
- `GS-20 -> GS-26`
- `GS-26 -> GS-09`
- `GS-09 -> GS-18`
- `GS-18 -> GS-27`
- `GS-27 -> GS-21`
- `GS-21 -> GS-28`
- `GS-28 -> GS-10`
- `GS-10 -> GS-11`

**已取消的编号（保留，标记替代关系）:** `GS-01`（`incremental_pack` 错误传播）、`GS-02`（`incremental_pack` 资源与并发安全）、`GS-12`（release 收口点）。前两者替代为 `GS-18` Q6 + `GS-10` 移交（`DEFER-GS-07`）；后者因本仓每卡独立发布而不需要（见「发布分组与并发窗口」）。

**自拆分新增的编号:** `GS-13`（自 GS-04）、`GS-14`（新增引导守卫卡）、`GS-15`/`GS-24`（自 GS-08）、`GS-16`（自 GS-05）、`GS-18`/`GS-21`（自 GS-09）、`GS-19`/`GS-22`（自 GS-14）、`GS-20`/`GS-25`（自 GS-15）、`GS-23`（自 GS-03）、`GS-26`（自 GS-20）、`GS-27`（自 GS-18）、`GS-28`（自 GS-21）。编号按 G-09 在所属 Phase 末尾追加，**编号顺序 ≠ 执行顺序**，执行以「实施顺序」为准。

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | outgoing | 跨计划 | 出站同步执行链路 + `incremental_pack` 硬化 | 后续日期计划（由 GS-10 在其产出中具名） | GS-09 的 ADR 已冻结 Q1–Q7，且接收方计划已建档并引用该 ADR | GS-09 `Deliverables`；GS-10 `Deliverables` | **移交不等待接收方完成实现**：GS-10 只需建档并具名即可关闭。若 GS-09 判定 no-go，GS-10 改为登记替代方向，`DEFER-GS-01`/`DEFER-GS-07` 保持开放 |
| DEP-02 | outgoing | 文档修正 | 模板 GC-12 / ER-07 的 VCS 事实修正 | `docs/plan/plan-template.md` 维护者 | 模板与 `AGENTS.md:251` 一致 | GAP-07 | **不等待接收方**：本计划按 GS-11 登记漂移并按 AGENTS.md 执行即可关闭；模板何时修正不阻塞本计划 |
| DEP-03 | incoming | 设计输入 | 完整出站同步设计草案（含未采纳方案与取舍记录） | 本计划作者 | 草案已作为 GS-09 的输入登记 | GS-09 `Current evidence` | 草案不可用时，GS-09 退化为从事实基线重新推导，时间箱不变 |

### 发布分组与并发窗口

**不设家族卡，不登记 `REL-*`。** 依据：`.cursor/rules/task-card-release.mdc:19`–`:26` 要求每卡 commit+push、`Version increment=N/A` 的 docs/audit 卡**仍要 commit+push**（只是不 bump）、且**不得捆绑后续卡**。家族卡的「不推送窗口」与该规则直接冲突，故本计划每张卡都是 `independent` 且 `C/D coverage from: self`。

**并发声明:** **全串行**。24 张活动卡构成一条单链（23 条依赖边），相邻卡均存在消费依赖或共享写集（`src/config/validate.rs`、`src/ceres/github_sync/key.rs`、`src/ceres/github_sync/send_pack.rs`、`docs/refactoring/github-sync.md`），I–I 相交全部由顺序边消解，无需并发判定。

**发布者:** 单一发布者（执行本计划的 Agent），负责全部卡的 C 组与 D 组跟踪（ER-12）。

**发布窗口顺序:** `GS-14 → GS-22 → GS-19 → GS-03 → GS-23 → GS-04 → GS-13 → GS-05 → GS-16 → GS-06 → GS-07 → GS-08 → GS-24 → GS-15 → GS-25 → GS-20 → GS-26 → GS-09 → GS-18 → GS-27 → GS-21 → GS-28 → GS-10 → GS-11`（24 张活动卡，与「实施顺序」的 23 条依赖边逐边一致）。同一时刻只允许一张卡处于「已 bump 未完成推送」状态。

### Phase 0: 守卫与配置面

**目标:** 交付计划公共守卫脚本，以及默认关闭的配置结构与全部启动期 fail-closed 校验（零 runtime 行为）。

**进入条件:** 测试栈已启动并确认；`.env.test` 存在。

> **引导例外（GS-14 自身）:** `GS-14` 是本计划的**引导卡**，它交付基线与越界守卫本身，因此不受「基线快照已保存」这一进入条件约束——其自身的越界证据由 `VER-2` 的隔离夹具提供（在临时目录内构造 status 与文件夹具，不依赖仓库工作树）。**自 `GS-19` 起**的每张卡，开工前必须先用 `scripts/guard/baseline.sh` 保存基线。

**退出条件:** GS-14、GS-22、GS-19、GS-03、GS-23、GS-04、GS-13 均 `Acceptance=complete`。

### Phase 1: 凭证

**目标:** 自举 SSH 金钥并封闭私钥，使运维可完成 GitHub 侧准备。

**进入条件:** GS-13 `Acceptance=complete`。

**退出条件:** GS-05、GS-16、GS-06 均 `Acceptance=complete`；公钥可从启动日志取得。

### Phase 2: 出站传输

**目标:** 可对本地 SSH 服务端完成一次 receive-pack 并取得有界诊断。

**进入条件:** GS-06 `Acceptance=complete`。

**退出条件:** GS-07、GS-08、GS-24、GS-15、GS-25、GS-20、GS-26 均 `Acceptance=complete`。

### Phase 3: 语义冻结与移交

**目标:** 冻结 8 项未定型问题（Q1–Q5 / Q6a / Q6b / Q7 / Q8），完成范围移交与文档收口。

**进入条件:** GS-08 `Acceptance=complete`。

**退出条件:** GS-09、GS-18、GS-27、GS-21、GS-28、GS-10、GS-11 均 `Acceptance=complete`。

## 任务卡

### 字段全局默认与例外

- **Release boundary 默认:** `independent`（每张卡独立发布切片，含 docs/spike/handoff——见「发布分组与并发窗口」）
- **Task type 默认:** `implementation`
- **Rollback mode 默认:** `revert`
- **Migration and rollback 默认:** `N/A：本计划无 schema 迁移`
- **Security and privacy 默认:** 继承 GC-07、GC-11
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 各卡自带其文档写集（G-02）
- **C/D coverage from 默认:** `self`；**D 组一律 `N/A`**（`.github/workflows/` 为空目录，见事实基线「CI」行）

**默认覆盖**（非例外，无需审批）：

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| GS-05 | `Rollback mode` | `forward-only`：已写入 vault 的金钥不因 revert 代码而消失 |
| GS-09 | `Task type` / `Version increment` | `spike` / `N/A`：探索卡，不得改动生产代码 |
| GS-10 | `Task type` / `Version increment` | `handoff` / `N/A`：跨计划移交 |
| GS-11 | `Task type` / `Version increment` | `docs` / `N/A`：文档与脚本产物（脚本为守卫工具，不属生产表面） |

**规则 waiver（`EX-*`）:** 本计划无 waiver。

---

### Task GS-01: ~~`incremental_pack` 错误传播~~（已取消）

**取消理由:** R2 P0-01/P0-02 确认该行为轴未定型——`get_trees_by_hashes` 已在 `incremental_pack` 路径内（`src/ceres/pack/mod.rs:388`；`src/ceres/pack/monorepo.rs:616`），而「任意缺失的 `have` 返回错误」会破坏 upload-pack 正常协商（`src/ceres/protocol/smart.rs:267`）。

**替代关系:** 设计问题移入 `GS-18` Q6；实现移交 `GS-10` / `DEP-01`；延后登记 `DEFER-GS-07`。编号保留，不再复用。

---

### Task GS-02: ~~`incremental_pack` 资源与并发安全~~（已取消）

**取消理由:** R2 P0-02 确认该行为轴未定型——需改动 `RepoHandler` trait 的返回类型以提供流内错误/完成通道（`src/ceres/pack/mod.rs:254` 当前无此通道），消费端为线上 clone/fetch 路径（`src/ceres/protocol/smart.rs:277`、`src/ceres/protocol/v2.rs:284`）；且字节预算必须作用于真正的分配点（`src/ceres/pack/mod.rs:443` 的 16 路并发 blob 累积），而非 entry 到达后。

**替代关系:** 同 GS-01。编号保留，不再复用。

---

### Task GS-14: 计划公共守卫脚本

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 落盘 `scripts/guard/baseline.sh`，为每个脏路径产出含类型、权限位与指纹的基线行，并在底层工具失败时退出 2。本卡唯一行为轴：**基线指纹的正确性**。本卡是本计划的**引导卡**，必须最先执行。

**Out of scope:** 越界判定 `allowlist.sh`（由 `GS-22` 承接）；文档引用守卫 `docrefs.sh`（由 `GS-19` 承接）；任何 `[github_sync]` 功能代码；CI 接线（`.github/workflows/` 为空目录）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| R2/R3 实测：按 status 行豁免整个文件会漏检已脏文件的后续修改 | R3 P0-01 的实测退出码表 |
| `libra status --short` 为 `XY path` 两列格式，重命名形如 `R  old -> new` | 2026-09-16 现场 |
| 工作树被并行修改，基线是唯一可信的扣除依据 | 事实基线「工作区前置」行 |
| 仓库无既有守卫脚本目录 | `ls scripts/` 现场核对 |

**Acceptance criteria:**

- [ ] AC-1：普通文件记 `file|<八进制权限>|<sha256>  <path>`。
- [ ] AC-2：符号链接记 `symlink|-|<链接目标>  <path>`。
- [ ] AC-3：不存在的路径记 `absent|-|-  <path>`。
- [ ] AC-4：目录/FIFO/设备等记 `unsupported|-|-  <path>`。
- [ ] AC-5：`libra status` 非零退出时 `baseline.sh` 退出 2。
- [ ] AC-6：`sha256sum` 非零退出时 `baseline.sh` 退出 2。
- [ ] AC-7：`stat` 非零退出时 `baseline.sh` 退出 2。
- [ ] AC-8：`readlink` 非零退出时 `baseline.sh` 退出 2。

**Verification:**

- [ ] VER-1：`bash tests/fixtures/guard/run_baseline_shape_cases.sh` (new)。在 **`mktemp -d` 隔离目录**内以 `PATH` 替身提供 `libra status` 桩，逐例断言 AC-1..AC-4 四种 `kind` 各产出预期行，整体退出 0。
- [ ] VER-2：`bash tests/fixtures/guard/run_baseline_failure_cases.sh` (new)。同一夹具，以 `PATH` 替身逐项注入 `libra`/`sha256sum`/`stat`/`readlink` 非零退出，逐例断言退出码为 2（覆盖 AC-5..AC-8）。
- [ ] VER-3：`test -x scripts/guard/baseline.sh`，并断言脚本内无仓库外绝对路径（`rg -n '\s/(home|Users|tmp)/' scripts/guard/baseline.sh` 零命中，按「Verification 判定口径」的退出码模板）。

**Dependencies:** 无

**Deliverables:** N/A

**Implementation write set:** `scripts/guard/baseline.sh`, `tests/fixtures/guard/run_baseline_shape_cases.sh`, `tests/fixtures/guard/run_baseline_failure_cases.sh`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** **本卡创建** `docs/refactoring/github-sync.md` 并写入「计划守卫」一节（记录三段脚本的用途与退出码契约）

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 守卫输出不得打印文件内容，只打印路径与判定结果（ER-11）

**Performance budget:** 与脏路径数线性相关，脏路径数有界

**Estimated scope:** `S`（落点 1，生产文件 2）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=越界守卫的正确性; recovery=revert，回到无守卫脚本状态; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=3; scope=S; deps=none; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-22: 越界判定守卫

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 落盘 `scripts/guard/allowlist.sh`，以「基线路径 ∪ 当前脏路径」为比较集合、按完整指纹判定越界。本卡唯一行为轴：**越界判定的正确性**。自 `GS-14` 拆出（R5 P1-01：合卡按独立谓词计为 ≥10，超 G-03 上限）。

**Out of scope:** 基线生成（由 `GS-14` 承接）；文档引用守卫（由 `GS-19` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| R4 实测：只迭代当前脏路径会漏掉「基线路径从 status 消失」 | R4 P0-01 实测表 |
| R5 实测：仅内容哈希无法区分「只改执行位」与「同字节符号链接」 | R5 P1-02 |
| 基线行格式由 `GS-14` 定义 | 本计划 GS-14 AC-1..AC-4 |

**Acceptance criteria:**

- [ ] AC-1：比较集合为「基线路径 ∪ 当前脏路径」。
- [ ] AC-2：对不在 allowlist 的路径，指纹与基线不同即退出 1。
- [ ] AC-3：对重命名的**源路径**单独判定。
- [ ] AC-4：对重命名的**目的路径**单独判定。
- [ ] AC-5：`kind=unsupported` 的路径一律判越界，不静默放行。
- [ ] AC-6：`libra status` 非零退出时退出 2。
- [ ] AC-7：`grep` 非零退出时退出 2。
- [ ] AC-8：基线文件缺失时退出 2。

**Verification:**

- [ ] VER-1：`bash tests/fixtures/guard/run_allowlist_cases.sh` (new)。`mktemp -d` 隔离夹具 + `libra` 替身，逐例断言：正例 0；基线已脏文件再修改 1；还原后 0；新增未声明文件 1；**基线文件被删除而从 status 消失 1**；**已脏文件恢复到 HEAD 而从 status 消失 1**；重命名源越界 1；重命名目的越界 1；仅改执行位 1；替换为同字节符号链接 1；`unsupported` 类型 1。
- [ ] VER-2：`bash tests/fixtures/guard/run_allowlist_failure_cases.sh` (new)。同一夹具，以 `PATH` 替身注入 `libra`/`sha256sum`/`stat`/`readlink`/`grep`/`sort` 非零退出，并测试基线缺失与空 allowlist，逐例断言退出码为 2。
- [ ] VER-3：`test -x scripts/guard/allowlist.sh`，并按判定口径模板断言脚本内无仓库外绝对路径。

**Dependencies:** `GS-14`（消费其基线行格式；与其共享 `scripts/guard/` 与 `tests/fixtures/guard/`，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `scripts/guard/allowlist.sh`, `tests/fixtures/guard/run_allowlist_cases.sh`, `tests/fixtures/guard/run_allowlist_failure_cases.sh`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `github-sync.md` 的「计划守卫」一节追加 `allowlist.sh` 的比较集合与退出码契约

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 守卫输出只打印路径与判定结果，不打印文件内容（ER-11）

**Performance budget:** 与「基线路径 ∪ 当前脏路径」的规模线性相关，两者均有界

**Estimated scope:** `S`（落点 1，生产文件 1）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=越界判定的正确性; recovery=revert，回到无越界守卫状态; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=GS-14; writeset=序列化于 GS-14; release=independent; split-from=GS-14; exception=N/A`

---

### Task GS-19: 文档引用守卫

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 落盘 `scripts/guard/docrefs.sh`，逐文件解析 `docs/*.md` 引用并判定存在性，且不吞下提取工具的失败。本卡唯一行为轴：**文档引用守卫的正确性**。自 `GS-14` 拆出（R4 P1-02：合卡按独立谓词计为 ≥11，超 G-03 上限）。

**Out of scope:** 越界守卫（由 `GS-14` 承接）；文档内容本身的收口（由 `GS-11` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| R3 实测：多文件 `rg` 输出带文件名前缀会把现存路径误判为悬空 | R3 P0-04 的实测表 |
| 本计划正文引用多个 `docs/*.md`，其中 `github-sync.md` 由 GS-14 创建 | 本计划全文 |

**Acceptance criteria:**

- [ ] AC-1：使用 `rg -o --no-filename` 提取，避免多文件前缀污染。
- [ ] AC-2：存在悬空引用时退出 1。
- [ ] AC-3：全部引用可解析时退出 0。
- [ ] AC-4：无任何匹配时退出 0（空引用集合不算失败）。
- [ ] AC-5：`rg` 退出码 >1 时退出 2。
- [ ] AC-6：`sort` 非零退出时退出 2。
- [ ] AC-7：输入文件缺失时退出 2。
- [ ] AC-8：无输入参数时退出 2。

**Verification:**

- [ ] VER-1：`bash tests/fixtures/guard/run_docrefs_cases.sh` (new)。隔离夹具，逐例断言 AC-1..AC-4：多文件全部解析 0；含悬空 1；无匹配 0；多文件前缀不污染。
- [ ] VER-2：`bash tests/fixtures/guard/run_docrefs_failure_cases.sh` (new)。以 `PATH` 替身注入 `rg` 退出 2、`sort` 退出 2，并测试输入缺失与无参数，逐例断言退出码为 2（覆盖 AC-5..AC-8）。
- [ ] VER-3：`test -x scripts/guard/docrefs.sh`，并按判定口径模板断言脚本内无仓库外绝对路径。

**Dependencies:** `GS-22`（与 GS-14/GS-22 共享 `scripts/guard/` 与 `tests/fixtures/guard/`，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `scripts/guard/docrefs.sh`, `tests/fixtures/guard/run_docrefs_cases.sh`, `tests/fixtures/guard/run_docrefs_failure_cases.sh`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 的「计划守卫」一节追加 `docrefs.sh` 的退出码契约

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 守卫输出只打印路径与判定结果，不打印文件内容（ER-11）

**Performance budget:** 与输入文件数线性相关，输入文件数有界

**Estimated scope:** `S`（落点 1，生产文件 1）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=文档引用守卫的正确性; recovery=revert，回到无文档引用守卫状态; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=GS-22; writeset=序列化于 GS-14; release=independent; split-from=GS-14; exception=N/A`

---

### Task GS-03: `[github_sync]` 配置 schema 与全部注册点

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 交付 `[github_sync]` 配置结构及其在配置系统中的全部注册点，默认关闭且零 runtime 行为。本卡唯一行为轴：配置的**结构与加载注册**（不含语义校验，见 GS-04/GS-13）。

**Out of scope:** 配置注册点、示例与模板同步（由 `GS-23` 承接）；启动期语义校验（由 GS-04、GS-13 承接）；vault 取值解析（由 GS-05 在服务启动期承接）；随执行链路移交的配置项（由 GS-10 移交）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `Config` 字段表 | `src/config/model.rs:10`–`:26` |
| 完整构造器（新增字段必须同步） | `src/config/testing.rs:157`（`Config {`） |
| 根段与嵌套键白名单 | `src/config/validate.rs:1741` |
| restart-required 分类链 | `src/config/reload.rs:678`（`collect_*_restart_fields` 序列） |
| 配置测试为独立文件模块 | `src/config/mod.rs:677`（`mod tests;` → `src/config/tests.rs`） |
| 无 `[github_sync]` 段 | `rg '\[github_sync\]' config/ src/config/` 零命中（2026-09-16） |

**Acceptance criteria:**

- [ ] AC-1：`GithubSyncConfig` 结构含 `enabled`/`ssh_host`/`ssh_user`/`ssh_host_key`/`ssh_key_ref` 五个标量字段。
- [ ] AC-2：`GithubSyncConfig` 含 `bindings` 列表，元素含 `id`/`path`/`remote`。
- [ ] AC-3：`enabled` 的 `Default` 实现为 `false`。
- [ ] AC-4：`src/config/testing.rs:157` 的完整构造器已同步新字段。
- [ ] AC-5：`src/config/mod.rs:121`（`Config::mock()`）的完整构造器已同步新字段。
- [ ] AC-6：缺省整段时配置加载成功。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'config::tests::github_sync_shape_and_defaults' -- --test-threads=1` (new；覆盖 AC-1..AC-3、AC-6)
- [ ] VER-2：`cargo build --tests`（两处完整构造器未同步将直接编译失败，覆盖 AC-4/AC-5）

**Dependencies:** `GS-19`（消费 GS-14 与 GS-19 交付的三段守卫脚本）

**Deliverables:** N/A

**Implementation write set:** `src/config/model.rs`, `src/config/mod.rs`, `src/config/testing.rs`, `src/config/tests.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同 `Implementation write set`

**Docs and compatibility impact:** **本卡创建** `docs/refactoring/github-sync.md` 并写入「配置面」一节；`config/config.toml` 示例；**`README.md` 的配置入口需补本段链接**（GC-05 的触发范围含配置项）

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 本卡不解析任何 secret 值（`ssh_key_ref` 仅作为字符串承载）

**Performance budget:** Inherited

**Estimated scope:** `M`（落点：`src/config/`、`config/`；生产文件 7）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=[github_sync] 配置的结构与加载注册; recovery=revert，配置加载回到无该段状态; complete=yes; self-contained=yes; AC=6/8; VER=2/8; landing=2; prod-files=7; scope=M; deps=GS-19; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-23: 配置注册点与示例同步

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 把 `[github_sync]` 注册进白名单、重载分类，并同步示例与模板。本卡唯一行为轴：**配置的注册与示例同步**。 自 `GS-03` 拆出（R5 P1-01：合卡按独立谓词超 G-03 上限）。

**Out of scope:** 配置结构与构造器（由 `GS-03` 承接）；语义校验（由 `GS-04`/`GS-13` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 根段与嵌套键白名单 | `src/config/validate.rs:1637`、`:1720`、`:1741` |
| restart-required 分类链 | `src/config/reload.rs:678` |
| 初始化模板独立文本 | `src/config/template.rs:13` |
| README 配置入口 | `README.md:50` |

**Acceptance criteria:**

- [ ] AC-1：`src/config/validate.rs` 的**根段**白名单已注册 `github_sync`。
- [ ] AC-2：`src/config/validate.rs` 的**嵌套键**白名单已注册该段全部子键。
- [ ] AC-3：未知子键被拒绝。
- [ ] AC-4：`src/config/reload.rs` 的分类链已将该段登记为 restart-required。
- [ ] AC-5：`config/config.toml` 追加注释掉的示例段。
- [ ] AC-6：`src/config/template.rs` 生成的模板含该段且与 `config/config.toml` 一致。
- [ ] AC-7：`README.md` 的配置入口已补该段链接。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'config::tests::github_sync_rejects_unknown_key' -- --test-threads=1` (new；覆盖 AC-1..AC-3)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'config::reload::tests::github_sync_is_restart_required' -- --test-threads=1` (new；覆盖 AC-4)
- [ ] VER-3：`cargo run -p mega2 -- config init --output "$(mktemp -d)/c.toml" --force` 后比对生成模板与 `config/config.toml` 的该段一致（覆盖 AC-5/AC-6）

**Dependencies:** `GS-03`（与其共享写集，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/config/validate.rs`, `src/config/reload.rs`, `src/config/template.rs`, `src/config/tests.rs`, `config/config.toml`, `README.md`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `config/config.toml` 与 `template.rs` 示例；`README.md` 配置入口链接

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 继承 GC-07、GC-11

**Performance budget:** 继承 GC-10

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=[github_sync] 配置的注册与示例同步; recovery=revert，回到未注册状态; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=1; prod-files=6; scope=M; deps=GS-03; writeset=序列化于 GS-03; release=independent; split-from=GS-03; exception=N/A`

---

### Task GS-04: `[github_sync]` 全局前置门

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在启动期拒绝使整个功能不可执行的全局组合（形态、hash kind、凭证字段存在性）。本卡唯一行为轴：**功能级前置条件的 fail-closed 判定**。

**Out of scope:** binding 条目级合法性（由 GS-13 承接）；vault 取值解析（GS-05）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 形态门先例与 `push_auth` ⇒ trunk 的强制推论 | `src/config/validate.rs:628`（`validate_trunk_config_surface`）；`:635`–`:640` |
| SecretRef 命名空间校验 helper | `src/config/validate.rs:444` |
| disabled 仍做字符串与命名空间校验、只跳过 vault 取值 | `src/config/validate.rs:755`–`:760` |
| hash kind 解析 | `src/config/model.rs:379` |
| 校验注册序列 | `src/config/validate.rs:139` |

**Acceptance criteria:**

- [ ] AC-1：`enabled=true` 且 `git.push_auth` 未设置时拒绝启动。
- [ ] AC-2：`enabled=true` 且 `bindings` 为空时拒绝启动。
- [ ] AC-3：`enabled=true` 且 `monorepo.object_format != "sha1"` 时拒绝启动。
- [ ] AC-4：`enabled=true` 且 `ssh_host_key` 为空时拒绝启动。
- [ ] AC-5：`enabled=true` 时 `ssh_key_ref` 通过 `validate_config_secret_ref` 的命名空间校验。
- [ ] AC-6：`enabled=false` 时仍对 `ssh_key_ref` 做字符串级解析。
- [ ] AC-7：`enabled=false` 时仍对 `ssh_key_ref` 做命名空间校验。
- [ ] AC-8：`enabled=false` 时**不**解析 vault 值（与 `validate.rs:755`–`:760` 的既有分层一致）。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'config::validate::tests::github_sync_global_gates' -- --test-threads=1` (new；逐条覆盖 AC-1..AC-5 的负例，并断言每条错误文本均含可定位的原因说明)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'config::validate::tests::github_sync_disabled_validation_layers' -- --test-threads=1` (new；覆盖 AC-6..AC-8，含「disabled 时 vault 未被调用」的断言)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --lib 'config::validate::tests::github_sync_errors_redact_secret_ref' -- --test-threads=1` (new；断言全部拒绝路径的错误文本不含 `ssh_key_ref` 的值)

**Dependencies:** `GS-23`（消费 `GithubSyncConfig` 结构与白名单注册）

**Deliverables:** N/A

**Implementation write set:** `src/config/validate.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「全局前置门」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 错误文本不得回显 secret ref 值（AC-7）

**Performance budget:** Inherited（启动期一次性）

**Estimated scope:** `M`（引入公开配置校验语义，按 G-04 评为 M）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=功能级前置条件的 fail-closed 判定; recovery=revert，启动校验回到不检查该段; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=GS-23; writeset=序列化于 GS-03; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-13: binding 条目合法性校验

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在启动期校验每个 binding 条目的标识、路径与 remote 形状及其唯一性。本卡唯一行为轴：**binding 条目的合法性**。自 `GS-04` 拆出（R2 P1-02：按独立谓词计为 14 条，超 G-03 上限）。

**Out of scope:** 功能级前置门（由 GS-04 承接）；binding 的运行时使用（随 GS-10 移交）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 无既有 binding 校验 | `rg 'github_sync' src/config/` 零命中（2026-09-16） |
| `import_dir` 取值来源 | `src/config/model.rs`（`MonoConfig.import_dir`） |
| 碰撞风险依据 | ADR-GS-01（仓库内无 slugifier 或碰撞解析器） |

**Acceptance criteria:**

- [ ] AC-1：binding `id` 为 1..32 个 `[A-Za-z0-9_-]` 字符，违反即拒绝。
- [ ] AC-2：binding `id` 互不重复。
- [ ] AC-3：binding `path` 为 canonical 绝对路径（无 `.`/`..`/重复分隔符/尾随斜线），违反即拒绝。
- [ ] AC-4：binding `path` 位于 `/project` 之下（按组件边界，`/projectX` 不算）。
- [ ] AC-5：binding `path` 不位于 `monorepo.import_dir` 之下。
- [ ] AC-6：binding `path` 互不重复。
- [ ] AC-7：binding `remote` 形如 `<owner>/<repo>`，两段均匹配 `^[A-Za-z0-9_-][A-Za-z0-9_.-]*$` 且**均不等于 `.` 或 `..`**（前导字符禁点即排除 `.`/`..`/`../` 形态；空白与 shell 元字符不在字符集内）。
- [ ] AC-8：binding `remote` 互不重复。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'config::validate::tests::github_sync_binding_identity' -- --test-threads=1` (new；覆盖 AC-1/AC-2，并断言重复时错误指出冲突的两个条目序号)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'config::validate::tests::github_sync_binding_path' -- --test-threads=1` (new；覆盖 AC-3..AC-6，含 `/projectX` 组件边界负例)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --lib 'config::validate::tests::github_sync_binding_remote' -- --test-threads=1` (new；覆盖 AC-7/AC-8，负例须含 `../repo`、`./repo`、`owner/..`、`owner/.`、空格、`;`、`$()`)

**Dependencies:** `GS-04`（与其共享 `src/config/validate.rs`，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/config/validate.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「binding 合法性矩阵」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** AC-7 逐组件禁止 `.`/`..`，是路径穿越与 SSH `exec` 参数安全的第一道防线；GS-08 另做引用转义（纵深防御，引用转义**不消除** `..` 的路径语义）

**Performance budget:** Inherited（启动期一次性，binding 数量有界）

**Estimated scope:** `M`（引入公开配置校验语义，按 G-04 评为 M）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=binding 条目的合法性; recovery=revert，binding 校验回到不检查; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=GS-04; writeset=序列化于 GS-04; release=independent; split-from=GS-04; exception=N/A`

---

### Task GS-05: SSH 金钥自举与跨副本收敛

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在服务启动期自动生成并持久化一把 Ed25519 SSH 金钥，或载入既有金钥，并置于进程内单一持有点。本卡唯一行为轴：金钥的**生命周期**。

**Out of scope:** 跨副本并发收敛与故障处置（由 GS-16 承接）；公钥输出与私钥泄漏防护（由 GS-06 承接）；两段式轮换（`DEFER-GS-04`）；任何 SSH 连接行为（由 GS-07 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 自举先例：首次初始化用 **RedLock** 互斥，**锁内重读指针**使并发初始化收敛到单一赢家 | `src/contract/vault/server_signing.rs:250`–`:255`（doc comment）；语义测试 `:546` |
| 该先例的 RNG 为 `rand08::thread_rng()` | `src/contract/vault/server_signing.rs:419` |
| 本计划可用 RNG | `Cargo.toml:64`（`rand = "0.10.2"`，含 `thread_rng`） |
| 服务启动期 secret 解析位置 | `src/context/mod.rs`（`bind_*` 系列在 vault 就绪后执行） |

**Acceptance criteria:**

- [ ] AC-1：`enabled=true` 且 vault 无金钥时生成 Ed25519 并以 OpenSSH 格式写入 `ssh_key_ref` 指向的路径。
- [ ] AC-2：`enabled=true` 且 vault 已有金钥时载入该值。
- [ ] AC-3：AC-2 的载入路径**不覆写** vault 中的既有值。
- [ ] AC-4：`enabled=false` 时不生成金钥。
- [ ] AC-5：`enabled=false` 时不写 vault。
- [ ] AC-6：生成与载入两条路径填入同一个进程内持有点。
- [ ] AC-7：生成算法固定为 Ed25519，不提供其它选项。
- [ ] AC-8：删除 vault 条目后重启，自举生成的新金钥公钥与删除前不同（恢复路径的本卡自足验证）。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::ensure_initializes_once_and_reloads' -- --test-threads=1` (new；覆盖 AC-1..AC-3、AC-6、AC-7)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::disabled_does_not_generate_or_write' -- --test-threads=1` (new；覆盖 AC-4/AC-5)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::delete_then_rebootstrap_yields_new_key' -- --test-threads=1` (new；覆盖 AC-8，**本卡自足**，不依赖 GS-06 的日志)

**Dependencies:** `GS-13`（消费已完成全部校验的 `GithubSyncConfig`）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/mod.rs`, `src/ceres/github_sync/key.rs`, `src/ceres/mod.rs`, `src/context/mod.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「金钥生命周期」一节

**Rollback mode:** `forward-only`

**Migration and rollback:** 无 DB schema 变更。vault 侧不变量：`ssh_key_ref` 指向的条目至多一条合法 Ed25519 私钥。**前滚步骤**：需更换金钥时删除该 vault 条目后重启，自举生成新的一把。**恢复验证命令**（本卡自足，不依赖 GS-06）：见 AC-8 / VER-3 的 `delete_then_rebootstrap_yields_new_key`。**用户影响**：删除后需把新公钥重新加入 GitHub 机器账号，期间推送以认证失败告终。

**Security and privacy:** 私钥只存在于 vault 与进程内存；本卡不产生任何输出面（输出边界由 GS-06 定义）

**Performance budget:** 启动期一次性

**Estimated scope:** `M`（落点 3，生产文件 4）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=GitHub 同步 SSH 金钥的生命周期与跨副本收敛; recovery=forward-only，删除 vault 条目后重启重新自举; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=3; prod-files=4; scope=M; deps=GS-13; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-16: 金钥自举的跨副本收敛与故障处置

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 保证多副本正常并发初始化收敛到同一把金钥，并定义 vault 不可用与值损坏两种故障下的行为；丢锁窗口的残余风险显式登记但**不在本卡消除**。本卡唯一行为轴：金钥自举的**并发与故障安全性（在既有原语能力范围内）**。自 `GS-05` 拆出（R3 P1-02）。

**Out of scope:** 金钥的生成与载入本身（由 GS-05 承接）；公钥输出（由 GS-06 承接）；**消除丢锁窗口**——vault 层无 compare-and-set 原语（见 `Current evidence`），需要新的原子存储原语，属未定型设计，由 `GS-28` 冻结。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 先例用 RedLock 互斥且**锁内重读指针**使并发初始化收敛到单一赢家 | `src/contract/vault/server_signing.rs:250`–`:255` |
| 现有 RedLock 丢锁后**只停止续租任务**，不阻止旧持有者继续执行临界区写入 | `src/jupiter/redis/lock.rs:323` |
| `guard.unlock()` 未把「已非锁拥有者」作为失败传播 | `src/jupiter/redis/lock.rs:359` |
| **vault 写入无 compare-and-set 原语**：`VaultCoreInterface` 只提供普通读/写/删，最终落到冲突时覆盖 value 的 upsert | `src/contract/vault/integration/vault_core.rs:556`、`:1231`；`src/contract/vault/integration/jupiter_backend.rs:116`；`src/jupiter/storage/vault_storage.rs:57` |
| 既有先例 `ensure_server_signing_key` 使用同一套 RedLock，**具有同样的丢锁窗口** | `src/contract/vault/server_signing.rs:250`–`:255` |

**Acceptance criteria:**

- [ ] AC-1：首次初始化经 RedLock 跨副本互斥（与既有先例 `server_signing.rs:250`–`:255` 同一机制）。
- [ ] AC-2：**在锁内重读** vault 指针，若已有赢家则放弃本次生成并载入赢家的金钥。
- [ ] AC-3：正常并发（无丢锁）下两个 ensure 收敛到同一公钥。
- [ ] AC-4：vault 不可用时 fail-closed，不以空金钥继续。
- [ ] AC-5：vault 中已有值无法解析为合法 Ed25519 私钥时 fail-closed。
- [ ] AC-6：AC-5 的损坏值**不被覆写**，由人工决定后续处置。
- [ ] AC-7：**丢锁窗口的残余风险已在 `docs/refactoring/github-sync.md` 显式登记**，并指向 `GS-28`（Q8）作为其冻结位置（本卡不承诺消除该风险）。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::concurrent_bootstrap_converges' -- --test-threads=1` (new；覆盖 AC-1..AC-3)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::fails_closed_on_vault_error_and_corrupt_value' -- --test-threads=1` (new；覆盖 AC-4..AC-6，含「损坏值未被覆写」断言)
- [ ] VER-3：产物门 —— `docs/refactoring/github-sync.md` 的「自举残余风险」小节已登记丢锁窗口并指向 `GS-18` Q8（覆盖 AC-7）

**Dependencies:** `GS-05`（与其共享 `src/ceres/github_sync/key.rs`，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/key.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「自举的并发与故障语义」一节

**Rollback mode:** `forward-only`（与 GS-05 同一 vault 条目）

**Migration and rollback:** 无 DB schema 变更。不变量：正常并发下 `ssh_key_ref` 指向的条目至多一条合法 Ed25519 私钥。**故障后的修复动作**：若因丢锁窗口导致 vault 中的金钥与某副本内存中的不一致，修复步骤为「停止全部副本 → 删除 vault 条目 → 重启单一副本完成自举 → 再启动其余副本 → 用新公钥替换 GitHub 上的旧公钥」。恢复验证：该步骤的前三步等价于 GS-05 VER-3 的 `delete_then_rebootstrap_yields_new_key`。用户影响：修复期间推送以认证失败告终。

**Security and privacy:** RedLock + 锁内重读可消解**正常并发**下的分叉；**丢锁窗口无法用现有原语消除**（vault 无 CAS），该残余风险与既有 `ensure_server_signing_key` 同级，已按 AC-7 显式登记并移交 `GS-28`

**Performance budget:** RedLock 持有时间限于一次 vault 读 + 一次条件写

**Estimated scope:** `S`（落点 1，生产文件 1）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=金钥自举的并发与故障安全性; recovery=forward-only，条件写入保证 vault 侧不变量不被破坏; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=GS-05; writeset=序列化于 GS-05; release=independent; split-from=GS-05; exception=N/A`

---

### Task GS-06: 公钥暴露边界与私钥封闭性

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 把公钥经启动日志输出为可直接粘贴的形式，并在日志、错误文本、`Debug` 三个面上封闭私钥。本卡唯一行为轴：金钥材料的**暴露边界**（安全轴，与 GS-05 的生命周期轴分离）。

**Out of scope:** 金钥的生成与持有（由 GS-05 承接）；公钥经 HTTP 暴露（本计划无 HTTP 表面，ADR-GS-02）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 金钥持有点与生成/载入分支由 GS-05 提供 | 本计划 GS-05 AC-4 |
| SecretRef/SecretString 的既有脱敏口径 | `src/config/secret.rs`（`Debug`/`Display` 输出 `vault://secret/***#***`） |
| 校验错误不回显 secret 值的既有口径 | `src/config/validate.rs:444` |

**Acceptance criteria:**

- [ ] AC-1：生成分支的日志含可机器识别的「生成」标记。
- [ ] AC-2：生成分支的日志含完整公钥行（`ssh-ed25519 AAAA… mega2-github-sync`）。
- [ ] AC-3：生成分支的日志含一行提示，指引把该公钥加入 GitHub 机器账号。
- [ ] AC-4：载入分支的日志含可机器识别的「载入」标记。
- [ ] AC-5：载入分支输出的公钥与生成时一致。
- [ ] AC-6：私钥材料不出现在任何日志（含 error/warn 级别）。
- [ ] AC-7：私钥材料不出现在任何错误文本（含 vault 失败、解析失败等错误路径）。
- [ ] AC-8：私钥类型的 `Debug` 实现脱敏，不输出金钥字节。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::logs_generated_branch' -- --test-threads=1` (new；覆盖 AC-1..AC-3)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::logs_loaded_branch_same_public_key' -- --test-threads=1` (new；覆盖 AC-4/AC-5)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::key::tests::private_key_never_leaks' -- --test-threads=1` (new；覆盖 AC-6..AC-8。断言对象为**测试私钥的已知敏感正文**（去掉 PEM 头尾的 base64 体、其原始字节序列、以及十六进制表示三种编码形式），而非仅 OpenSSH 头；覆盖正常路径、vault 失败路径与解析失败路径的日志、错误文本与 `format!("{:?}")` 输出)

**Dependencies:** `GS-16`（与 GS-05/GS-16 共享 `src/ceres/github_sync/key.rs`，以顺序边消解 I–I 相交；消费金钥持有点与生成/载入分支信息）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/key.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「公钥获取方式」一节及运维指引

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 本卡即安全边界卡：三个暴露面（日志、错误、`Debug`）均有运行时断言，不依赖源码级正则；断言须覆盖去头尾正文、原始字节与十六进制三种编码形式（仅查 PEM 头不足以证明 AC-6..AC-8）

**Performance budget:** Inherited

**Estimated scope:** `S`（落点 1，生产文件 1）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=金钥材料的暴露边界（公钥输出 + 私钥封闭）; recovery=revert，回到无日志输出且无脱敏断言的状态; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=GS-16; writeset=序列化于 GS-05; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-07: 出站 SSH 连接与主机金钥钉住

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 建立一条经主机金钥钉住校验并完成公钥认证的出站 SSH 会话。本卡唯一行为轴：**可信 SSH 会话的建立**。自 `GS-04`（初稿）拆出。

**Out of scope:** receive-pack 协议帧与响应解析（由 GS-08 承接）；连接复用与多 channel（随 GS-10 移交）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `russh` 已是直接依赖，当前**仅用 server 端** | `Cargo.toml:73`；`src/server/ssh_server.rs`；`rg 'russh::client' src/` 零命中 |
| `russh::keys` 已在生产代码使用 | `src/api/router/user_router.rs:8` |

**Acceptance criteria:**

- [ ] AC-1：从 GS-05 的持有点取金钥，连接配置的 `ssh_host`/`ssh_user` 并完成公钥认证。
- [ ] AC-2：主机金钥与配置 `ssh_host_key` 逐字节比对，不匹配即中止连接。
- [ ] AC-3：主机金钥不匹配时**未进入认证阶段**（以「认证回调未被调用」断言）。
- [ ] AC-4：连接、主机金钥校验、认证三阶段的失败可区分，错误携带对应 `stage` 值。
- [ ] AC-5：连接设有超时上限，超时返回 `ssh_connect` 阶段错误而非无界等待。
- [ ] AC-6：金钥算法非 Ed25519 时拒绝并给出可操作错误。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::ssh::tests::host_key_mismatch_aborts_before_auth' -- --test-threads=1` (new；覆盖 AC-2/AC-3，以行为断言而非源码正则)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::ssh::tests::stage_errors_are_distinguishable' -- --test-threads=1` (new；覆盖 AC-4/AC-5)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --test integration_github_sync -- --test-threads=1 ssh_connect_authenticates` (new；覆盖 AC-1，对本地 SSH 服务端)

**Dependencies:** `GS-06`（与其共享 `src/ceres/github_sync/` 模块注册，以顺序边消解 I–I 相交；并消费 GS-05 建立的金钥持有点）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/ssh.rs`, `src/ceres/github_sync/mod.rs`, `tests/integration_github_sync.rs`, `docs/refactoring/github-sync.md`, `docs/refactoring/integration.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「传输与主机金钥钉住」一节；`docs/refactoring/integration.md` 覆盖矩阵追加 `integration_github_sync`

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 主机金钥校验 fail-closed；**以行为测试为安全判据**，不把源码正则当作判据（R2 P0-04）；日志不得含金钥材料

**Performance budget:** 连接超时上限必须显式设定（GC-10）

**Estimated scope:** `M`（落点 2，生产文件 2）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=经主机金钥校验与公钥认证的出站 SSH 会话建立; recovery=revert，回到无出站 SSH 能力; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=2; prod-files=2; scope=M; deps=GS-06; writeset=序列化于 GS-06; release=independent; split-from=GS-04（初稿）; exception=N/A`

---

### Task GS-08: receive-pack over SSH（能力协商与终止条件）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在已认证的 SSH 会话上发起 receive-pack 并解析 advertisement 与能力集，未满足最低能力集即在写出任何字节前中止。本卡唯一行为轴：**advertisement 与能力解析**。

**Out of scope:** 命令帧构造、side-band 降级与 pack 流式写入（由 `GS-24` 承接）；响应侧的终止条件与诊断收集（由 GS-15 承接）；推送的触发时机、游标与非快进策略（`GS-09` 冻结、`GS-10` 移交）；pack 的生成与其错误通道（`DEFER-GS-07`，本卡测试使用合成 pack）；真实 GitHub 验收（`DEFER-GS-08`）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| pkt-line 写入/读取 helper（已对非十六进制 header 硬化） | `src/ceres/protocol/smart.rs:871`、`:885` |
| side-band 解复用**参考**（不可直接复用） | `src/ceres/model/third_party.rs:216`；`:244` 以发现 `PACK` 为进入条件且返回 pack 数据 |
| 无任何出站 receive-pack 实现 | `rg 'git-receive-pack' src/` 仅命中服务端 |

**Acceptance criteria:**

- [ ] AC-1：在 SSH 会话上 `exec "git-receive-pack '<remote>.git'"`，`<remote>` 经引用转义。
- [ ] AC-2：解析 ref advertisement，得到 `refs/heads/main` 的远端 tip。
- [ ] AC-3：`refs/heads/main` 不存在时远端 tip 取全零。
- [ ] AC-4：解析 advertisement 的能力集。
- [ ] AC-5：`report-status` 未声明时在**发送任何字节前**中止。
- [ ] AC-6：AC-5 的中止返回可操作错误，指明缺失的能力名。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::advertisement_parsing' -- --test-threads=1` (new；覆盖 AC-2..AC-4)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::minimum_capability_gate' -- --test-threads=1` (new；覆盖 AC-5/AC-6，断言未写出任何字节)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --test integration_github_sync -- --test-threads=1 loopback_advertise` (new；覆盖 AC-1)

**Dependencies:** `GS-07`（消费已认证 SSH 会话）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/send_pack.rs`, `src/ceres/github_sync/mod.rs`, `tests/integration_github_sync.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「receive-pack 协议与能力协商」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** `<remote>` 已在 GS-13 AC-7 约束形状，本卡另做 `exec` 参数引用转义（纵深防御）；诊断输出按 ER-11 脱敏后方可记录

**Performance budget:** pack 流式传输（AC-8），驻留与 pack 大小无关

**Estimated scope:** `M`（落点 2，生产文件 2）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=receive-pack 客户端协议（协商、帧、pack 流、报告与终止判定）; recovery=revert，回到无出站推送能力; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=2; prod-files=2; scope=M; deps=GS-07; writeset=序列化于 GS-07; release=independent; split-from=GS-04（初稿）; exception=N/A`

---

### Task GS-24: receive-pack 请求构造与降级

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按协商结果构造命令帧、处理 side-band 降级，并流式写入 packfile。本卡唯一行为轴：**请求构造与 pack 写出**。 自 `GS-08` 拆出（R5 P1-01：合卡按独立谓词超 G-03 上限）。

**Out of scope:** advertisement 与能力解析（由 `GS-08` 承接）；响应侧判定（由 `GS-15` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| pkt-line 写入 helper | `src/ceres/protocol/smart.rs:871` |
| 能力集由 GS-08 解析 | 本计划 GS-08 AC-4 |

**Acceptance criteria:**

- [ ] AC-1：命令帧的 NUL 分隔、capability 前导空格、无结尾 LF 逐字节符合协议。
- [ ] AC-2：命令帧后紧随 `0000` flush。
- [ ] AC-3：`side-band-64k` 已声明时在 capability 列表中请求。
- [ ] AC-4：`side-band-64k` 未声明时不请求该能力。
- [ ] AC-5：AC-4 的降级在结果中置「能力缺失」标记。
- [ ] AC-6：pack 以流式写入 channel，驻留内存与 pack 大小无关。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::command_frame_golden' -- --test-threads=1` (new；覆盖 AC-1/AC-2 的 golden 字节串)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::sideband_negotiation_and_downgrade' -- --test-threads=1` (new；覆盖 AC-3..AC-5)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --test integration_github_sync -- --test-threads=1 loopback_receive_pack` (new；覆盖 AC-6，以合成 pack 断言驻留上界)

**Dependencies:** `GS-08`（与其共享写集，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/send_pack.rs`, `tests/integration_github_sync.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `github-sync.md` 追加「请求构造与降级」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 继承 GC-07、GC-11

**Performance budget:** 继承 GC-10

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=receive-pack 的请求构造与 pack 写出; recovery=revert，回到无请求构造能力; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=GS-08; writeset=序列化于 GS-08; release=independent; split-from=GS-08; exception=N/A`

---

### Task GS-15: receive-pack 响应侧终止条件与诊断

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 定义 receive-pack **报告层**的成功必要条件与各类失败的可区分判定。本卡唯一行为轴：**报告层的终止判定**。

**Out of scope:** SSH 终止事件的采集与失败优先级（由 `GS-25` 承接）；能力协商与请求构造（由 GS-08 承接）；四处读写期限与诊断字节预算（由 `GS-20` 承接）；真实 GitHub 验收（`DEFER-GS-08`）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| Git 报告含 unpack 状态、逐 ref 状态与终止 flush | Git pack-protocol `report-status` 规范 |
| `russh` 的 client Handler 分别暴露 stderr 与远端退出事件 | `russh` 0.63.3 `client::Handler`（`extended_data` / `exit_status` / `exit_signal`） |
| side-band 解复用**参考**（不可直接复用） | `src/ceres/model/third_party.rs:216`、`:244` |

**Acceptance criteria:**

- [ ] AC-1：收到 `unpack ok` 是成功的必要条件。
- [ ] AC-2：收到目标 ref 的 `ok` 是成功的必要条件。
- [ ] AC-3：收到终止 flush 是成功的必要条件。
- [ ] AC-4：目标 ref 收到 `ng` 时判为失败。
- [ ] AC-5：AC-4 保留 `ng` 的原因字符串。
- [ ] AC-6：报告缺失或半帧判为失败，且与 AC-4 可区分。
- [ ] AC-7：连接中断判为失败，且与 AC-4、AC-6 可区分。
- [ ] AC-8：side-band channel 3（fatal）出现即判为失败，不等待后续报告。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::success_requires_full_report' -- --test-threads=1` (new；覆盖 AC-1..AC-3)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::failure_modes_are_distinguishable' -- --test-threads=1` (new；覆盖 AC-4..AC-8)

**Dependencies:** `GS-24`（与 GS-08/GS-24 共享 `src/ceres/github_sync/send_pack.rs`，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/send_pack.rs`, `tests/integration_github_sync.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `docs/refactoring/github-sync.md` 追加「终止条件与诊断契约」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 诊断输出按 ER-11 脱敏后方可记录；stderr 同样按脱敏处理

**Performance budget:** 由 `GS-20` 承接（本卡只做状态判定，不设期限与预算）

**Estimated scope:** `M`（落点 1，生产文件 1）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=receive-pack 响应侧的终止判定与诊断; recovery=revert，回到无终止契约的状态; complete=yes; self-contained=yes; AC=8/8; VER=2/8; landing=1; prod-files=1; scope=M; deps=GS-24; writeset=序列化于 GS-08; release=independent; split-from=GS-08; exception=N/A`

---

### Task GS-25: SSH 终止事件与失败优先级

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 采集 SSH stderr、`exit-status`、`exit-signal`，并定义其相对报告层的失败优先级与成功的最终条件。本卡唯一行为轴：**传输层终止事件的判定**。 自 `GS-15` 拆出（R5 P1-01：合卡按独立谓词超 G-03 上限）。

**Out of scope:** 报告层判定（由 `GS-15` 承接）；期限与预算（由 `GS-20` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `russh` client Handler 分别暴露 stderr 与远端退出事件 | `russh` 0.63.3 `client::Handler` |
| RFC 4254 §6.10 允许 `exit-status` 在 EOF 之后到达 | RFC 4254 |

**Acceptance criteria:**

- [ ] AC-1：SSH `stderr` 被采集并纳入诊断。
- [ ] AC-2：非零 `exit-status` 判为失败。
- [ ] AC-3：任何 `exit-signal` 判为失败。
- [ ] AC-4：AC-2/AC-3 的失败优先级**高于**完整成功报告。
- [ ] AC-5：成功的最终条件为报告层成功（`GS-15` AC-1..AC-3）**加上**收到显式 `exit-status = 0`。
- [ ] AC-6：仅有 EOF/close 而未收到 `exit-status` 时不得判为成功，按 `GS-20` 的期限处理。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::remote_exit_overrides_report' -- --test-threads=1` (new；覆盖 AC-2..AC-4，含「完整成功报告 + 非零 exit-status」与「完整成功报告 + exit-signal」两例)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::eof_without_exit_status_is_not_success' -- --test-threads=1` (new；覆盖 AC-5/AC-6)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::stderr_is_collected' -- --test-threads=1` (new；覆盖 AC-1)

**Dependencies:** `GS-15`（与其共享写集，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/send_pack.rs`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `github-sync.md` 追加「传输层终止事件」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 继承 GC-07、GC-11

**Performance budget:** 继承 GC-10

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=传输层终止事件的判定与失败优先级; recovery=revert，回到只看报告层的判定; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=GS-15; writeset=序列化于 GS-15; release=independent; split-from=GS-15; exception=N/A`

---

### Task GS-20: receive-pack 的读写期限与诊断预算

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 为 receive-pack 的四处阻塞点设定独立期限并在超期时取消在途操作。本卡唯一行为轴：**传输的时间边界**。

**Out of scope:** 诊断字节预算与截断标记（由 `GS-26` 承接）；终止状态判定（由 `GS-15` 承接）；能力协商（由 `GS-08` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 四处阻塞点：advertisement 读取、报告读取、pack 写入、SSH 终止事件等待 | 本计划 GS-08 / GS-15 的协议流程 |
| 诊断来源为 side-band channel 2/3 与 SSH stderr | GS-15 AC-5、AC-8 |
| GC-10 禁止无界内存与无界等待 | 「全局工程约束」 |

**Acceptance criteria:**

- [ ] AC-1：advertisement 读取有独立期限，超期返回 `advertise` 阶段错误。
- [ ] AC-2：报告读取有独立期限，超期返回 `report` 阶段错误。
- [ ] AC-3：pack 写入有独立期限，超期返回 `send` 阶段错误。
- [ ] AC-4：SSH 终止事件等待有独立期限，超期返回 `report` 阶段错误。
- [ ] AC-5：任一超期均取消在途操作，不遗留后台任务。
- [ ] AC-6：四处期限为可配置字段。
- [ ] AC-7：四处期限各有默认值。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::four_deadlines' -- --test-threads=1` (new；四处阻塞点各注入一次停滞，逐例断言 `stage` 值，覆盖 AC-1..AC-4)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::deadline_cancels_inflight' -- --test-threads=1` (new；覆盖 AC-5)
- [ ] VER-3：`source .env.test && cargo test -p mega2 --lib 'config::tests::github_sync_deadline_defaults' -- --test-threads=1` (new；覆盖 AC-6/AC-7)

**Dependencies:** `GS-25`（与 GS-15/GS-25 共享 `src/ceres/github_sync/send_pack.rs`，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/send_pack.rs`, `src/config/model.rs`, `src/config/validate.rs`, `src/config/reload.rs`, `src/config/template.rs`, `src/config/testing.rs`, `src/config/mod.rs`, `src/config/tests.rs`, `config/config.toml`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `config/config.toml` 与 `template.rs` 追加四处期限与诊断预算的注释；`README.md` 无需新增（本卡字段挂在既有 `[github_sync]` 段下）；`github-sync.md` 追加「时间与容量边界」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 诊断按 ER-11 脱敏后方可记录

**Performance budget:** 本卡即性能边界卡：四处期限 + 诊断字节预算构成传输层的全部时间与容量上界（GC-10）

**Estimated scope:** `M`（落点 2，生产文件 9）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=receive-pack 的时间与容量边界; recovery=revert，回到无期限与预算的状态; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=2; prod-files=9; scope=M; deps=GS-25; writeset=序列化于 GS-15; release=independent; split-from=GS-15; exception=N/A`

---

### Task GS-26: receive-pack 诊断字节预算

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 为诊断输出设定字节预算并在超出时截断标记。本卡唯一行为轴：**诊断的容量边界**。 自 `GS-20` 拆出（R5 P1-01：合卡按独立谓词超 G-03 上限）。

**Out of scope:** 四处期限（由 `GS-20` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 诊断来源为 side-band channel 2/3 与 SSH stderr | 本计划 GS-24 AC-5、GS-25 AC-1 |
| GC-10 禁止无界内存 | 「全局工程约束」 |

**Acceptance criteria:**

- [ ] AC-1：诊断输出有字节预算上限。
- [ ] AC-2：该预算为可配置字段。
- [ ] AC-3：该预算有默认值。
- [ ] AC-4：诊断超出预算时截断。
- [ ] AC-5：AC-4 的截断结果置「已截断」标记。
- [ ] AC-6：截断按字节边界安全处理，不产生非法 UTF-8 片段。

**Verification:**

- [ ] VER-1：`source .env.test && cargo test -p mega2 --lib 'ceres::github_sync::send_pack::tests::diagnostic_budget_truncates' -- --test-threads=1` (new；覆盖 AC-1、AC-4..AC-6)
- [ ] VER-2：`source .env.test && cargo test -p mega2 --lib 'config::tests::github_sync_diagnostic_budget_default' -- --test-threads=1` (new；覆盖 AC-2/AC-3)

**Dependencies:** `GS-20`（与其共享写集，以顺序边消解 I–I 相交）

**Deliverables:** N/A

**Implementation write set:** `src/ceres/github_sync/send_pack.rs`, `src/config/model.rs`, `src/config/validate.rs`, `src/config/template.rs`, `config/config.toml`, `docs/refactoring/github-sync.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `config.toml` 与 `template.rs` 追加诊断预算注释；`github-sync.md` 追加「诊断容量边界」一节

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 继承 GC-07、GC-11

**Performance budget:** 继承 GC-10

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=implementation; axis=诊断输出的容量边界; recovery=revert，回到无预算的状态; complete=yes; self-contained=yes; AC=6/8; VER=2/8; landing=1; prod-files=5; scope=M; deps=GS-20; writeset=序列化于 GS-20; release=independent; split-from=GS-20; exception=N/A`

---

### Task GS-09: 冻结未定型设计问题（spike）

**Task type:** `spike`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在时间箱内回答 5 个阻断**出站同步语义**的设计问题（Q1–Q5），产出一份 ADR。本卡唯一行为轴：**同步语义的冻结**。**不得改动任何生产代码。**

**Out of scope:** pack 错误通道与操作授权（Q6/Q7，由 GS-18 承接）；任何实现（由 `DEP-01` 的接收方计划承接）；入站同步（`DEFER-GS-02`）；合并算法（ADR-GS-04 已否决）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| Q1：一次 push 同时更新祖先与后代路径 ref | `src/ceres/api_service/mono_api_service.rs:3493`；`src/jupiter/service/push_queue_service.rs:2093`、`:2475`；`src/jupiter/storage/mono_storage.rs:713` |
| Q2：`incremental_pack` 在 `have` 处停止追溯并排除其 tree 可达对象 | `src/ceres/pack/monorepo.rs:524`、`:556` |
| Q3：N=1 时落地 commit 即 `cmd.new_id`（两侧 hash 可能相同） | `src/jupiter/service/push_queue_service.rs:1995`；`docs/refactoring/trunk-push.md:429` |
| Q4/Q5：事务性 outbox 先例与其 commit 边界 | `src/jupiter/service/push_queue_service.rs:1604`、`:2536`（成稿期间由 `:1596`/`:2519` 漂移） |
| Q6：trait 无流内错误通道；消费端为线上 clone/fetch；`have` 协商语义 | `src/ceres/pack/mod.rs:254`；`src/ceres/protocol/smart.rs:277`、`:267`；`src/ceres/protocol/v2.rs:284`；`src/ceres/pack/mod.rs:388`、`:443` |
| Q7：trunk 无统一路由授权层；`push_auth=none` 返回匿名 | `src/server/http_server.rs:792`；`src/api/api_write_auth.rs:34` |
| 设计草案输入 | `DEP-03` |

**待回答的问题:**

- **Q1** outbox 触发粒度：如何在一次 B3 事务内枚举**实际变更的绑定路径**及各自 landed commit，覆盖祖先、后代与删除三种情形？
- **Q2** pack 闭包前提：何时可用 `have=[last_pushed]`？如何证明远端仍持有对应闭包？远端重建/更换 remote/历史被替换时如何使旧游标失效？
- **Q3** resume 收敛条件：AI 完成合并后如何证明分歧已解除？两侧预期 tip 如何表达与校验？
- **Q4** 轮询与推送的竞争窗口：如何避免把自身已推送但游标未更新的状态误判为远端修改？
- **Q5** 补偿身份与幂等键：如何保存「更新前远端 tip」以免被成功路径覆盖？首次创建远端分支时的补偿形式？

**退出分支（三选一，AC 按分支条件化）:**

- **go** —— Q1–Q5 全部有方案：AC-1..AC-5 全部满足，AC-6 记录 `go` 判定。
- **no-go** —— 至少一问被证明不可行：AC-1..AC-5 中被否决者改判为「已给出不可行判据与证据」，AC-7 记录否决理由与替代方向，AC-6 记录 `no-go` 判定。
- **研究未完成** —— 时间箱耗尽：AC-6 记录该判定与剩余问题清单，本卡置 `blocked` 并按 ER-10 升级；AC-1..AC-5、AC-7 不要求满足。

**Acceptance criteria:**

- [ ] AC-1：Q1 已给出方案（go）或不可行判据与证据（no-go）。
- [ ] AC-2：Q2 已给出方案（go）或不可行判据与证据（no-go）。
- [ ] AC-3：Q3 已给出方案（go）或不可行判据与证据（no-go）。
- [ ] AC-4：Q4 已给出方案（go）或不可行判据与证据（no-go）。
- [ ] AC-5：Q5 已给出方案（go）或不可行判据与证据（no-go）。
- [ ] AC-6：三分支之一已明确判定并落盘（含判定依据与时间箱消耗记录）。
- [ ] AC-7：no-go 分支下替代方向已写入**本卡产物文档**的「no-go 替代方向」小节（`DEFER-*` 的重启条件更新由 `GS-10` 统一执行，避免本卡越界修改计划文件）。

**Verification:**

- [ ] VER-1：产物门 —— `docs/refactoring/github-sync.md` 的「同步语义冻结（Q1–Q5）」一节已落盘，Q1..Q5 各有对应小节，且三分支之一有明确判定记录。
- [ ] VER-2：越界守卫 —— `bash scripts/guard/allowlist.sh <baseline> docs/refactoring/github-sync.md`（传入本卡 `Deliverables` 的**确切产物路径**，不传宽目录；基线按 ER-01 于开工前以 `scripts/guard/baseline.sh` 保存）。
- [ ] VER-3：手工证据 —— 判定分支与承接卡（`GS-10`）的登记片段。

**Dependencies:** `GS-26`（消费 GS-08/GS-24/GS-15/GS-25/GS-20/GS-26 的协议实现所暴露的真实约束）、`DEP-03`

**Deliverables:** `docs/refactoring/github-sync.md` 的「同步语义冻结（Q1–Q5）」章节（含 Q1..Q5 小节与三分支判定记录）

**Implementation write set:** `docs/refactoring/github-sync.md`

**Release write set:** `N/A`

**Files likely touched:** 同上

**Docs and compatibility impact:** 本卡即文档产物

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** 文档不得含真实 token、金钥材料或私有绝对路径

**Performance budget:** `N/A`

**Estimated scope:** `M`（时间箱 ≤ 1.5 人日）

**Version increment:** `N/A`

**Release boundary:** `independent`（按 `.cursor/rules/task-card-release.mdc:24`–`:26`，`N/A` 版本的卡仍独立 commit+push）

**C/D coverage from:** `self`

**Granularity:** `type=spike; axis=未定型设计问题的冻结; recovery=revert 文档改动，回到问题未冻结状态; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=1; prod-files=0; scope=M; deps=GS-26,DEP-03; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-18: 冻结 pack 错误通道与操作授权（spike）

**Task type:** `spike`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在时间箱内回答 `incremental_pack` 的**错误通道与取消传播**（Q6a）。本卡唯一行为轴：**pack 错误通道设计的冻结**。

**Out of scope:** 字节预算与 `have` 语义区分（由 `GS-27` 承接）；同步语义 Q1–Q5（由 GS-09 承接）；操作授权与原子存储原语 Q7/Q8（由 `GS-21` 承接）；任何实现（由 `DEP-01` 的接收方计划承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| trait 无流内错误/完成通道；1 声明、2 实现、4 调用点 | `src/ceres/pack/mod.rs:254`、`:284`；`src/ceres/protocol/smart.rs:277`；`src/ceres/protocol/v2.rs:284`；`src/ceres/pack/monorepo.rs:256` |
| 真正的字节分配点在共享遍历器，单 blob 整体累积、并发度 16 | `src/ceres/pack/mod.rs:443` |
| `get_trees_by_hashes` 已在 `incremental_pack` 路径内且含 unwrap | `src/ceres/pack/mod.rs:388`；`src/ceres/pack/monorepo.rs:616` |
| `have` 协商语义：服务端只 ACK 自己拥有的 commit | `src/ceres/protocol/smart.rs:267` |
| trunk 无统一路由授权层；`push_auth=none` 返回匿名 | `src/server/http_server.rs:792`；`src/api/api_write_auth.rs:34` |

**待回答的问题:**

- **Q6** pack 错误通道与资源预算：`RepoHandler` trait 如何提供**流内错误/完成通知**与**取消传播**而不回归线上 clone/fetch（1 声明 / 2 实现 / 4 调用点）？字节预算须覆盖哪些分配点（图枚举集合、并发 blob、输入/输出队列、编码过程）？底层存储查询失败链如何传播？「客户端广告的未知 `have`」与「必须遍历的祖先缺失」如何区分以保全正常协商语义？
- ~~**Q7 / Q8**~~（移至 `GS-21`）

**退出分支（三选一，AC 按分支条件化）:**

- **go** —— Q6 有方案：AC-1..AC-6 全部满足，AC-7 记录 `go` 判定。
- **no-go** —— Q6 被证明不可行：AC-1..AC-5 改判为「已给出不可行判据与证据」，AC-6 改为「替代方向已写入本卡产物文档的『no-go 替代方向』小节」，AC-7 记录 `no-go` 判定。
- **研究未完成** —— 时间箱耗尽：AC-7 记录该判定与剩余问题清单，本卡置 `blocked` 并按 ER-10 升级；AC-1..AC-6 不要求满足。

**Acceptance criteria:**

- [ ] AC-1：Q6 的 trait 变更形态已给出（流内错误/完成通知的表达方式）。
- [ ] AC-2：Q6 的消费端影响面已逐点评估（1 声明 / 2 实现 / 4 调用点）。
- [ ] AC-3：Q6 的取消传播语义已给出（消费者提前 drop 时**生产者**的终止规则）。
- [ ] AC-4：Q6 的取消传播语义已给出（消费者提前 drop 时**编码任务**的终止规则）。
- [ ] AC-5：Q6 的存储查询失败链传播方案已给出。
- [ ] AC-6：三分支之一已明确判定并落盘（go / no-go / 研究未完成）。

**Verification:**

- [ ] VER-1：产物门 —— `docs/refactoring/github-sync.md` 的「pack 错误通道冻结（Q6a）」一节已落盘，AC-1..AC-5 各有对应小节。
- [ ] VER-2：越界守卫 —— `bash scripts/guard/allowlist.sh <baseline> docs/refactoring/github-sync.md`。
- [ ] VER-3：手工证据 —— 三分支判定记录。

**Dependencies:** `GS-09`（与其共享 `docs/refactoring/github-sync.md`，以顺序边消解 I–I 相交）

**Deliverables:** `docs/refactoring/github-sync.md` 的「pack 错误通道与预算冻结（Q6）」章节

**Implementation write set:** `docs/refactoring/github-sync.md`

**Release write set:** `N/A`

**Files likely touched:** 同上

**Docs and compatibility impact:** 本卡即文档产物

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** 文档不得含真实 token、金钥材料或私有绝对路径

**Performance budget:** `N/A`

**Estimated scope:** `M`（时间箱 ≤ 1.5 人日）

**Version increment:** `N/A`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=spike; axis=pack 错误通道与操作授权设计的冻结; recovery=revert 文档改动，回到问题未冻结状态; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=1; prod-files=0; scope=M; deps=GS-09; writeset=序列化于 GS-09; release=independent; split-from=GS-09; exception=N/A`

---

### Task GS-27: 冻结 pack 资源预算与 have 语义（spike）

**Task type:** `spike`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在时间箱内回答 `incremental_pack` 的**资源预算与 `have` 语义**（Q6b）。本卡唯一行为轴：**pack 预算与协商语义设计的冻结**。 自 `GS-18` 拆出（R5 P1-01：合卡按独立谓词超 G-03 上限）。**不得改动任何生产代码。**

**Out of scope:** 错误通道与取消传播（由 `GS-18` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 真正的字节分配点在共享遍历器，单 blob 整体累积、并发度 16 | `src/ceres/pack/mod.rs:443` |
| `have` 协商：服务端只 ACK 自己拥有的 commit | `src/ceres/protocol/smart.rs:267` |

**Acceptance criteria:**

- [ ] AC-1：图枚举集合的字节预算方案已给出。
- [ ] AC-2：并发 blob 累积的字节预算方案已给出。
- [ ] AC-3：输入/输出队列的字节预算方案已给出。
- [ ] AC-4：编码过程的字节预算方案已给出。
- [ ] AC-5：「客户端广告的未知 `have`」与「必须遍历的祖先缺失」的区分规则已给出。
- [ ] AC-6：AC-5 的规则明确不改变 upload-pack 的正常协商行为。
- [ ] AC-7：三分支之一已明确判定并落盘。
- [ ] AC-8：**接收方必须承接清单**已逐项列出（四类预算 + `have` 区分）。

**Verification:**

- [ ] VER-1：产物门 —— `github-sync.md` 的「pack 资源预算与 have 语义冻结（Q6b）」一节已落盘，AC-1..AC-6 各有对应小节。
- [ ] VER-2：越界守卫 —— `bash scripts/guard/allowlist.sh <baseline> docs/refactoring/github-sync.md`。
- [ ] VER-3：手工证据 —— 三分支判定记录与 AC-8 承接清单的条目对照。

**Dependencies:** `GS-18`（与其共享写集，以顺序边消解 I–I 相交）

**Deliverables:** `docs/refactoring/github-sync.md` 的对应章节

**Implementation write set:** `docs/refactoring/github-sync.md`

**Release write set:** `N/A`

**Files likely touched:** 同上

**Docs and compatibility impact:** 本卡即文档产物

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** 文档不得含真实 token、金钥材料或私有绝对路径

**Performance budget:** `N/A`

**Estimated scope:** `M`

**Version increment:** `N/A`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=spike; axis=pack 资源预算与协商语义设计的冻结; recovery=revert 文档改动，回到问题未冻结状态; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=0; scope=M; deps=GS-18; writeset=序列化于 GS-18; release=independent; split-from=GS-18; exception=N/A`

---

### Task GS-21: 冻结操作授权与原子存储原语（spike）

**Task type:** `spike`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在时间箱内回答命令接口的**授权模型与非快进策略判据**（Q7）。本卡唯一行为轴：**操作授权设计的冻结**。

**Out of scope:** 原子存储原语 Q8（由 `GS-28` 承接）；同步语义 Q1–Q5（`GS-09`）；pack 错误通道 Q6（`GS-18`）；任何实现（由 `DEP-01` 的接收方计划承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| trunk 缺统一路由授权层；`push_auth=none` 返回匿名 | `src/server/http_server.rs:792`；`src/api/api_write_auth.rs:34` |
| 独立 token family 先例（身份 + 范围两处） | `src/api/router/agent_capture_router.rs:98`、`:634` |
| **vault 无 compare-and-set**：最终落到冲突覆盖 value 的 upsert | `src/contract/vault/integration/vault_core.rs:556`、`:1231`；`src/jupiter/storage/vault_storage.rs:57` |
| 现有 RedLock 丢锁后只停续租，不阻止旧持有者写入 | `src/jupiter/redis/lock.rs:323`、`:359` |
| 既有 `ensure_server_signing_key` 具有同样的丢锁窗口 | `src/contract/vault/server_signing.rs:250`–`:255` |

**待回答的问题:**

- **Q7** 操作授权与策略判据：命令接口的身份来源、凭证范围与授权模型是什么（trunk 无统一授权层）？`abort` / `force` 的判定条件与各自可观测结果分别是什么？
- **Q8** 原子存储原语：如何在不破坏既有 `ensure_server_signing_key` 语义的前提下提供条件写入以消除丢锁窗口？需要新增哪些接口、影响哪些既有调用方？若判定不可行，残余风险的可接受性依据是什么？

**退出分支（三选一，AC 按分支条件化）:**

- **go** —— Q7、Q8 均有方案：AC-1..AC-5 全部满足，AC-6 记录 `go` 判定。
- **no-go** —— 至少一问被证明不可行：被否决问题对应的 AC 改判为「已给出不可行判据与证据」，替代方向写入本卡产物文档的「no-go 替代方向」小节，AC-6 记录 `no-go` 判定。
- **研究未完成** —— 时间箱耗尽：AC-6 记录该判定与剩余问题清单，本卡置 `blocked` 并按 ER-10 升级；AC-1..AC-5 不要求满足。

**Acceptance criteria:**

- [ ] AC-1：Q7 的身份来源已给出。
- [ ] AC-2：Q7 的凭证范围已给出。
- [ ] AC-3：Q7 的授权模型已给出（含 `push_auth=none` 形态下的行为）。
- [ ] AC-4：Q7 的 `abort` 判定条件已给出。
- [ ] AC-5：Q7 的 `abort` 可观测结果已给出。
- [ ] AC-6：Q7 的 `force` 判定条件已给出。
- [ ] AC-7：Q7 的 `force` 可观测结果已给出。
- [ ] AC-8：三分支之一已明确判定并落盘。

**Verification:**

- [ ] VER-1：产物门 —— `github-sync.md` 的「操作授权冻结（Q7）」一节已落盘，AC-1..AC-7 各有对应小节。
- [ ] VER-2：越界守卫 —— `bash scripts/guard/allowlist.sh <baseline> docs/refactoring/github-sync.md`。
- [ ] VER-3：手工证据 —— 三分支判定记录与承接清单的条目对照。

**Dependencies:** `GS-27`（与 GS-18/GS-27 共享 `docs/refactoring/github-sync.md`，以顺序边消解 I–I 相交）

**Deliverables:** `docs/refactoring/github-sync.md` 的「授权与原子写入冻结（Q7–Q8）」章节

**Implementation write set:** `docs/refactoring/github-sync.md`

**Release write set:** `N/A`

**Files likely touched:** 同上

**Docs and compatibility impact:** 本卡即文档产物

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** 文档不得含真实 token、金钥材料或私有绝对路径

**Performance budget:** `N/A`

**Estimated scope:** `M`（时间箱 ≤ 1.5 人日）

**Version increment:** `N/A`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=spike; axis=操作授权与原子写入设计的冻结; recovery=revert 文档改动，回到问题未冻结状态; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=0; scope=M; deps=GS-27; writeset=序列化于 GS-18; release=independent; split-from=GS-18; exception=N/A`

---

### Task GS-28: 冻结原子存储原语（spike）

**Task type:** `spike`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 在时间箱内回答消除 RedLock 丢锁窗口所需的**原子存储原语**（Q8）。本卡唯一行为轴：**原子写入设计的冻结**。 自 `GS-21` 拆出（R5 P1-01：合卡按独立谓词超 G-03 上限）。**不得改动任何生产代码。**

**Out of scope:** 操作授权 Q7（由 `GS-21` 承接）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| vault 无 compare-and-set，最终落到冲突覆盖 value 的 upsert | `src/jupiter/storage/vault_storage.rs:57` |
| RedLock 丢锁后只停续租 | `src/jupiter/redis/lock.rs:323`、`:359` |
| 既有 `ensure_server_signing_key` 具有同样窗口 | `src/contract/vault/server_signing.rs:250`–`:255` |

**Acceptance criteria:**

- [ ] AC-1：Q8 的条件写入接口形态已给出。
- [ ] AC-2：Q8 对既有 `ensure_server_signing_key` 调用方的影响面已评估。
- [ ] AC-3：Q8 对其余 vault 写入调用方的影响面已评估。
- [ ] AC-4：若判定不可行，残余风险的可接受性依据已给出。
- [ ] AC-5：若判定不可行，残余风险的监测手段已给出。
- [ ] AC-6：三分支之一已明确判定并落盘。
- [ ] AC-7：**接收方必须承接清单**已逐项列出。

**Verification:**

- [ ] VER-1：产物门 —— `github-sync.md` 的「原子写入冻结（Q8）」一节已落盘，AC-1..AC-5 各有对应小节。
- [ ] VER-2：越界守卫 —— `bash scripts/guard/allowlist.sh <baseline> docs/refactoring/github-sync.md`。
- [ ] VER-3：手工证据 —— 三分支判定记录与 AC-7 承接清单的条目对照。

**Dependencies:** `GS-21`（与其共享写集，以顺序边消解 I–I 相交）

**Deliverables:** `docs/refactoring/github-sync.md` 的对应章节

**Implementation write set:** `docs/refactoring/github-sync.md`

**Release write set:** `N/A`

**Files likely touched:** 同上

**Docs and compatibility impact:** 本卡即文档产物

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** 文档不得含真实 token、金钥材料或私有绝对路径

**Performance budget:** `N/A`

**Estimated scope:** `M`

**Version increment:** `N/A`

**Release boundary:** `independent`

**C/D coverage from:** `self`（D 组：`N/A` —— `.github/workflows/` 为空目录，无任何 workflow 可命中，见事实基线「CI」行）

**Granularity:** `type=spike; axis=原子写入设计的冻结; recovery=revert 文档改动，回到问题未冻结状态; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=1; prod-files=0; scope=M; deps=GS-21; writeset=序列化于 GS-21; release=independent; split-from=GS-21; exception=N/A`

---

### Task GS-10: 出站执行链路与 pack 硬化的范围移交（handoff）

**Task type:** `handoff`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 `GS-09` / `GS-18` / `GS-21` 三张 spike 的分支判定**组合**，把出站执行链路、`incremental_pack` 硬化、授权与原子写入正式移交后续日期计划，或登记替代方向。本卡唯一行为轴：**范围移交**。

**Out of scope:** 任何实现（移交后由接收方承担）；`GS-09` 未冻结的问题（按分支处理，不强行移交）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 待移交范围已在本文「非目标」列明 | 本文「非目标」前两条 |
| 语义冻结产物与分支判定 | `GS-09` `Deliverables`、AC-8 |
| 移交登记位置 | `DEP-01`（`direction: outgoing`） |

**Acceptance criteria:**

- [ ] AC-1：**至少一项 go** —— 接收方计划已建档（`docs/plan/plan-<date>.md`），其文件名在本卡记录中具名。
- [ ] AC-2：**至少一项 go** —— 接收方计划逐项引用全部已 go 问题域的承接清单作为其事实基线。
- [ ] AC-3：**至少一项 go** —— `DEP-01` 的 Owner 字段已更新为接收方计划 ID。
- [ ] AC-4：**全部为 no-go 分支** —— 替代方向已登记为 `DEFER-*` 的重启条件更新，且**不创建**接收方计划。
- [ ] AC-5：**研究未完成分支** —— 剩余问题清单已登记，本卡随 `GS-09` 一同置 `blocked`，不做移交。
- [ ] AC-6：**组合决策表**已落盘：五张 spike（GS-09 / GS-18 / GS-27 / GS-21 / GS-28）各自的 go / no-go / 未完成共同决定移交范围 —— 全部 go 则整体移交；部分 go 则**只移交已 go 的问题域**并把 no-go 者写入对应 `DEFER-*` 的重启条件；任一为「研究未完成」则本卡随之 `blocked`。
- [ ] AC-7：本文「非目标」与实际处置一致，无遗漏也无重叠。
- [ ] AC-8：`docs/plan/README.md` 已按实际组合同步（含 go 的问题域登记接收方计划；其余只更新本计划状态）。

**Verification:**

- [ ] VER-1：产物门 —— 按 AC-6 的组合决策表核对：**至少一项 go** 时断言接收方计划存在，且其事实基线逐项引用**全部 go 问题域**的承接清单（`GS-09` AC-6 / `GS-27` AC-8 / `GS-28` AC-7 等各卡承接清单中已 go 者）；**全部 no-go** 时断言未创建接收方计划且对应 `DEFER-*` 已更新；**任一未完成**时断言本卡为 `blocked`。
- [ ] VER-2：越界守卫 —— `bash scripts/guard/allowlist.sh <baseline> docs/plan/plan-20260916.md docs/plan/README.md`（go 分支另加接收方计划的确切路径）。
- [ ] VER-3：手工证据 —— `DEP-01` 更新后的表格行与分支判定的对照片段。

**Dependencies:** `GS-28`（消费 GS-09、GS-18、GS-27、GS-21、GS-28 五张 spike 的分支判定与冻结产物）

**Deliverables:** go 分支：接收方 `docs/plan/plan-<date>.md`（新建）+ `docs/plan/README.md` 索引行 + 本计划 `DEP-01` 更新；其余分支：`DEFER-*` 更新 + 本计划状态更新

**Implementation write set:** `docs/plan/plan-20260916.md`, `docs/plan/README.md`, `docs/plan/plan-<date>.md`（仅 go 分支）

**Release write set:** `N/A`

**Files likely touched:** 同上

**Docs and compatibility impact:** 本卡即文档产物

**Rollback mode:** `revert`

**Migration and rollback:** `N/A`

**Security and privacy:** `N/A`

**Performance budget:** `N/A`

**Estimated scope:** `S`（≤ 3 个产物文件）

**Version increment:** `N/A`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=handoff; axis=出站执行链路与 pack 硬化的范围移交; recovery=revert 文档改动，范围回到本计划非目标状态; complete=yes; self-contained=yes; AC=8/20; VER=3/20; landing=1; prod-files=0; scope=S; deps=GS-28; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-11: 契约文档收口、守卫脚本与漂移登记（docs）

**Task type:** `docs`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 收口 `docs/refactoring/github-sync.md` 的整体一致性、补上线检查清单，并登记模板 GC-12/ER-07 的 VCS 事实漂移。本卡唯一行为轴：**文档与索引的一致性**。**本卡不产出任何脚本或代码**（守卫脚本由 `GS-14` 交付）。

**Out of scope:** 各实现卡自身的文档章节（已由 GS-03..GS-09 各自写入，本卡只做一致性收口）；修改 `docs/plan/plan-template.md`（移交 `DEP-02`）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 契约文档由 GS-03 创建、GS-04..GS-09 增量追加 | 各卡 `Implementation write set` |
| 模板称本仓由 Git 管理、`.libra` 已删除 | `docs/plan/plan-template.md` 的 GC-12、ER-07 段 |
| 实际为 Libra | `.git` 不存在、`.libra` 存在；`AGENTS.md:251` |
| 两段守卫脚本已在本文正文给出且本地实测通过 | 本文「本计划引用的模板规则」 |
| 集成覆盖矩阵位置 | `docs/refactoring/integration.md` |

**Acceptance criteria:**

- [ ] AC-1：`docs/refactoring/github-sync.md` 各章节顺序与交叉引用自洽，无重复或矛盾表述。
- [ ] AC-2：`docs/refactoring/github-sync.md` 的「计划守卫」一节与 `scripts/guard/` 下脚本的实际退出码契约一致。
- [ ] AC-4：该文档含上线检查清单，其中「人工确认 `main` 的 branch protection 不会挡直接推送」为独立一项。
- [ ] AC-5：该清单说明无自动 preflight 的原因并引用 ADR-GS-03。
- [ ] AC-6：该清单含「使用专属 GitHub 机器账号而非真人账号」一项及其理由。
- [ ] AC-7：GAP-07 的模板漂移已登记并指向 `DEP-02`。
- [ ] AC-8：`docs/refactoring/integration.md` 的覆盖矩阵含 `integration_github_sync`（由 GS-07 追加，本卡核对存在与准确）。
- [ ] AC-9：`docs/plan/README.md` 的本计划索引行状态与本文「完成判据」一致。
- [ ] AC-10：`plan-long.md` 的日期计划索引已同步，或写明 `N/A` 及理由。
- [ ] AC-11：本文与 `docs/refactoring/github-sync.md` 中的全部 `docs/*.md` 引用逐条存在。

**Verification:**

- [ ] VER-1：`bash scripts/guard/docrefs.sh docs/plan/plan-20260916.md docs/refactoring/github-sync.md`（覆盖 AC-11；该守卫已本地实测三态）
- [ ] VER-2：`bash scripts/guard/allowlist.sh <baseline> docs/refactoring/github-sync.md docs/plan/README.md docs/plan/plan-long.md docs/plan/plan-20260916.md`（传入本卡 `Deliverables` 的确切产物路径）
- [ ] VER-3：手工证据 —— `docs/plan/README.md` 新增/更新行与本文标题、状态一致的 diff 片段。

**Dependencies:** `GS-10`（消费其分支判定与接收方计划 ID 以完成索引一致性）、`DEP-02`（**不等待接收方**，见依赖登记表）

**Deliverables:** `docs/refactoring/github-sync.md`（收口）、`docs/plan/README.md`、`docs/plan/plan-long.md`、`docs/plan/plan-20260916.md`（状态回填）

**Implementation write set:** `docs/refactoring/github-sync.md`, `docs/plan/README.md`, `docs/plan/plan-long.md`, `docs/plan/plan-20260916.md`

**Release write set:** `N/A`

**Files likely touched:** 同上

**Docs and compatibility impact:** 本卡即文档收口；**不含任何代码或配置产物**（守卫脚本由 GS-14 交付），满足模板对 `docs` 类型的 no-code/no-config 要求

**Rollback mode:** `revert`（撤销文档改动；本卡无脚本产物）

**Migration and rollback:** `N/A`

**Security and privacy:** 文档不得含真实 token、金钥材料或私有绝对路径

**Performance budget:** `N/A`

**Estimated scope:** `S`（4 个产物文件，全部为文档）

**Version increment:** `N/A`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=docs; axis=文档与索引的一致性收口; recovery=revert 文档与脚本改动; complete=yes; self-contained=yes; AC=10/20; VER=3/20; landing=2; prod-files=0; scope=S; deps=GS-10,DEP-02; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

---

### Task GS-12: ~~计划收口发布点~~（已取消）

**取消理由:** 本仓 `.cursor/rules/task-card-release.mdc:19`–`:26` 要求每卡 commit+push，`Version increment=N/A` 的 docs/audit 卡仍要推送且不得捆绑后续卡。家族卡的「不推送窗口」与该规则冲突，故 `REL-00` 登记撤销，无需独立发布点。

**替代关系:** 各卡 `Release boundary=independent`、`C/D coverage from=self`。编号保留，不再复用。

---

### 任务卡粒度审计表

（本表由各卡 `Granularity` 行机械生成，逐字一致；`AC`/`VER` 分子为**单子句谓词**条目数，由脚本从 checklist 机械计数。）

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GS-01 | — | （已取消，见对应任务卡的替代关系） | — | — | — | — | — | — | — | — | — | — | — | — |
| GS-02 | — | （已取消，见对应任务卡的替代关系） | — | — | — | — | — | — | — | — | — | — | — | — |
| GS-14 | implementation | 越界守卫的正确性 | revert，回到无守卫脚本状态 | yes | yes | 8/8 | 3/8 | 1/3 | S | none | no-overlap | independent | N/A | N/A |
| GS-22 | implementation | 越界判定的正确性 | revert，回到无越界守卫状态 | yes | yes | 8/8 | 3/8 | 1/1 | S | GS-14 | 序列化于 GS-14 | independent | GS-14 | N/A |
| GS-19 | implementation | 文档引用守卫的正确性 | revert，回到无文档引用守卫状态 | yes | yes | 8/8 | 3/8 | 1/1 | S | GS-22 | 序列化于 GS-14 | independent | GS-14 | N/A |
| GS-03 | implementation | [github_sync] 配置的结构与加载注册 | revert，配置加载回到无该段状态 | yes | yes | 6/8 | 2/8 | 2/7 | M | GS-19 | no-overlap | independent | N/A | N/A |
| GS-23 | implementation | [github_sync] 配置的注册与示例同步 | revert，回到未注册状态 | yes | yes | 7/8 | 3/8 | 1/6 | M | GS-03 | 序列化于 GS-03 | independent | GS-03 | N/A |
| GS-04 | implementation | 功能级前置条件的 fail-closed 判定 | revert，启动校验回到不检查该段 | yes | yes | 8/8 | 3/8 | 1/1 | M | GS-23 | 序列化于 GS-03 | independent | N/A | N/A |
| GS-13 | implementation | binding 条目的合法性 | revert，binding 校验回到不检查 | yes | yes | 8/8 | 3/8 | 1/1 | M | GS-04 | 序列化于 GS-04 | independent | GS-04 | N/A |
| GS-05 | implementation | GitHub 同步 SSH 金钥的生命周期与跨副本收敛 | forward-only，删除 vault 条目后重启重新自举 | yes | yes | 8/8 | 3/8 | 3/4 | M | GS-13 | no-overlap | independent | N/A | N/A |
| GS-16 | implementation | 金钥自举的并发与故障安全性 | forward-only，条件写入保证 vault 侧不变量不被破坏 | yes | yes | 7/8 | 3/8 | 1/1 | S | GS-05 | 序列化于 GS-05 | independent | GS-05 | N/A |
| GS-06 | implementation | 金钥材料的暴露边界（公钥输出 + 私钥封闭） | revert，回到无日志输出且无脱敏断言的状态 | yes | yes | 8/8 | 3/8 | 1/1 | S | GS-16 | 序列化于 GS-05 | independent | N/A | N/A |
| GS-07 | implementation | 经主机金钥校验与公钥认证的出站 SSH 会话建立 | revert，回到无出站 SSH 能力 | yes | yes | 6/8 | 3/8 | 2/2 | M | GS-06 | 序列化于 GS-06 | independent | GS-04（初稿） | N/A |
| GS-08 | implementation | receive-pack 客户端协议（协商、帧、pack 流、报告与终止判定） | revert，回到无出站推送能力 | yes | yes | 6/8 | 3/8 | 2/2 | M | GS-07 | 序列化于 GS-07 | independent | GS-04（初稿） | N/A |
| GS-24 | implementation | receive-pack 的请求构造与 pack 写出 | revert，回到无请求构造能力 | yes | yes | 6/8 | 3/8 | 1/1 | M | GS-08 | 序列化于 GS-08 | independent | GS-08 | N/A |
| GS-15 | implementation | receive-pack 响应侧的终止判定与诊断 | revert，回到无终止契约的状态 | yes | yes | 8/8 | 2/8 | 1/1 | M | GS-24 | 序列化于 GS-08 | independent | GS-08 | N/A |
| GS-25 | implementation | 传输层终止事件的判定与失败优先级 | revert，回到只看报告层的判定 | yes | yes | 6/8 | 3/8 | 1/1 | M | GS-15 | 序列化于 GS-15 | independent | GS-15 | N/A |
| GS-20 | implementation | receive-pack 的时间与容量边界 | revert，回到无期限与预算的状态 | yes | yes | 7/8 | 3/8 | 2/9 | M | GS-25 | 序列化于 GS-15 | independent | GS-15 | N/A |
| GS-26 | implementation | 诊断输出的容量边界 | revert，回到无预算的状态 | yes | yes | 6/8 | 2/8 | 1/5 | M | GS-20 | 序列化于 GS-20 | independent | GS-20 | N/A |
| GS-09 | spike | 未定型设计问题的冻结 | revert 文档改动，回到问题未冻结状态 | yes | yes | 7/8 | 3/8 | 1/0 | M | GS-26,DEP-03 | no-overlap | independent | N/A | N/A |
| GS-18 | spike | pack 错误通道与操作授权设计的冻结 | revert 文档改动，回到问题未冻结状态 | yes | yes | 6/8 | 3/8 | 1/0 | M | GS-09 | 序列化于 GS-09 | independent | GS-09 | N/A |
| GS-27 | spike | pack 资源预算与协商语义设计的冻结 | revert 文档改动，回到问题未冻结状态 | yes | yes | 8/8 | 3/8 | 1/0 | M | GS-18 | 序列化于 GS-18 | independent | GS-18 | N/A |
| GS-21 | spike | 操作授权与原子写入设计的冻结 | revert 文档改动，回到问题未冻结状态 | yes | yes | 8/8 | 3/8 | 1/0 | M | GS-27 | 序列化于 GS-18 | independent | GS-18 | N/A |
| GS-28 | spike | 原子写入设计的冻结 | revert 文档改动，回到问题未冻结状态 | yes | yes | 7/8 | 3/8 | 1/0 | M | GS-21 | 序列化于 GS-21 | independent | GS-21 | N/A |
| GS-10 | handoff | 出站执行链路与 pack 硬化的范围移交 | revert 文档改动，范围回到本计划非目标状态 | yes | yes | 8/20 | 3/20 | 1/0 | S | GS-28 | no-overlap | independent | N/A | N/A |
| GS-11 | docs | 文档与索引的一致性收口 | revert 文档与脚本改动 | yes | yes | 10/20 | 3/20 | 2/0 | S | GS-10,DEP-02 | no-overlap | independent | N/A | N/A |
| GS-12 | — | （已取消，见对应任务卡的替代关系） | — | — | — | — | — | — | — | — | — | — | — | — |

## 测试矩阵

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | 配置默认与未知键、全局前置门负例、binding 合法性负例、金钥自举幂等、并发收敛与丢锁故障注入、私钥三编码零泄漏、主机金钥不匹配、能力协商、命令帧 golden、终止条件与诊断预算 | `source .env.test && cargo test -p mega2 --lib 'config::tests::github_sync'`；`… --lib 'config::validate::tests::github_sync'`；`… --lib 'ceres::github_sync::'` |
| 集成（DB） | `N/A`：本计划无 entity / storage / migration 改动（随 GS-10 移交） | `N/A` |
| 集成（进程） | SSH 连接认证、本地回环 receive-pack（合成 pack） | `source .env.test && cargo test -p mega2 --test integration_github_sync -- --test-threads=1` |
| CLI | `N/A`：本计划不新增 CLI 子命令（金钥自举在启动路径，非命令） | `N/A` |
| HTTP API | `N/A`：本计划不新增 HTTP 表面（ADR-GS-02），运行时 OpenAPI 证据为 `N/A` | `N/A` |
| 迁移 | `N/A`：本计划无迁移 | `N/A` |
| 配置 | `config validate` 通过；坏配置非零退出且 secret 不泄漏 | `cargo run -p mega2 -- --config config/config.toml config validate` |
| Git 协议 | `N/A`：本计划**不改动任何既有协议路径**（`src/ceres/protocol/**`、`src/ceres/pack/**` 均不在任何卡的写集内），只新增 `src/ceres/github_sync/` 出站客户端 | `N/A` |
| 安全 | 私钥三面零泄漏（去头尾正文/原始字节/十六进制三种编码）、主机金钥 fail-closed（行为断言）、非 sha1/缺 push_auth 拒绝启动、`remote` 逐组件禁 `.`/`..`、丢锁窗口为已登记残余风险（不在本计划消除） | `… --lib 'ceres::github_sync::key::tests::private_key_never_leaks'`；`… --lib 'ceres::github_sync::ssh::tests::host_key_mismatch_aborts_before_auth'`；`… --lib 'config::validate::tests::github_sync_binding_remote'` |
| 性能 | pack 流式传输驻留与大小无关；诊断字节预算生效；三处读写期限与连接超时上限 | `… --test integration_github_sync -- loopback_receive_pack`；`… --lib 'ceres::github_sync::send_pack::tests::diagnostic_budget_truncates'`；`… --lib 'ceres::github_sync::send_pack::tests::deadlines_and_remote_exit'` |
| 守卫工具 | 三段守卫脚本的正例、越界负例（含基线已脏文件再修改）、命令失败三态 | `bash scripts/guard/baseline.sh …`；`bash scripts/guard/allowlist.sh …`；`bash scripts/guard/docrefs.sh …`（GS-14 VER-1..VER-4） |
| live/gated | `N/A`：真实 GitHub 验收**已移出本计划**（`DEFER-GS-08`），本计划只做本地回环，不产生任何 GitHub 侧副作用 | `N/A` |

## 追溯表

| 任务 | 来源/证据 | mega2 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| GS-14 | R3 P0-01 / R4 P0-01；`src/jupiter/redis/lock.rs`、`libra status --short` 格式 | `scripts/guard/` | **创建** `docs/refactoring/github-sync.md`「计划守卫」一节 | `GS-14` VER-1..VER-4 的隔离夹具 |
| GS-19 | R3 P0-04（多文件 `rg` 前缀污染） | `scripts/guard/` | `github-sync.md`「计划守卫」追加 `docrefs.sh` 契约 | `GS-19` VER-1/VER-2 隔离夹具 |
| GS-03 | GAP-01；`src/config/model.rs:10`、`testing.rs:157`、`validate.rs:1741`、`reload.rs:678`、`mod.rs:677` | `src/config/`、`config/` | `github-sync.md` 配置面一节；`README.md` 配置入口 | `--lib 'config::tests::github_sync_defaults_disabled'` |
| GS-04 | GAP-02；`validate.rs:628`、`:635`、`:444`、`:755` | `src/config/` | `github-sync.md` 全局前置门 | `--lib 'config::validate::tests::github_sync_global_gates'` |
| GS-13 | GAP-03；ADR-GS-01 | `src/config/` | `github-sync.md` binding 合法性矩阵 | `--lib 'config::validate::tests::github_sync_binding_*'` |
| GS-05 | GAP-04；`server_signing.rs:250`–`:255` | `src/ceres/github_sync/`、`src/context/` | `github-sync.md` 金钥生命周期 | `--lib 'ceres::github_sync::key::tests::ensure_initializes_once_and_reloads'` |
| GS-16 | GAP-04；`vault_storage.rs:57`（无 CAS）、`redis/lock.rs:323` | `src/ceres/github_sync/` | `github-sync.md` 自举的并发与故障语义 + 残余风险登记 | `--lib 'ceres::github_sync::key::tests::concurrent_bootstrap_converges'` |
| GS-06 | GAP-04；`src/config/secret.rs` 脱敏口径 | `src/ceres/github_sync/` | `github-sync.md` 公钥获取方式 | `--lib 'ceres::github_sync::key::tests::private_key_never_leaks'` |
| GS-07 | GAP-05；`Cargo.toml:73`；`rg 'russh::client' src/` 零命中 | `src/ceres/github_sync/` | `github-sync.md` 传输与钉住；`integration.md` 覆盖矩阵 | `--test integration_github_sync -- ssh_connect_authenticates` |
| GS-08 | GAP-05；`smart.rs:871`、`:885`；`third_party.rs:244` | `src/ceres/github_sync/` | `github-sync.md` 协议与能力协商 | `--test integration_github_sync -- loopback_receive_pack` |
| GS-15 | GAP-05；Git `report-status` 规范；`russh` client Handler | `src/ceres/github_sync/` | `github-sync.md` 终止条件与诊断契约 | `--lib 'ceres::github_sync::send_pack::tests::failure_modes_are_distinguishable'` |
| GS-20 | GC-10；GS-08/GS-15 的四处阻塞点 | `src/ceres/github_sync/`、`src/config/` | `config.toml`/`template.rs` 期限与预算注释；`github-sync.md` 时间与容量边界 | `--lib 'ceres::github_sync::send_pack::tests::four_deadlines'` |
| GS-09 | GAP-06；`pack/mod.rs:254`、`:388`、`:443`；`smart.rs:267`、`:277`；`mono_api_service.rs:3493`；`push_queue_service.rs:1995`、`:2093` | `docs/` | `github-sync.md` 未定型问题冻结 | 产物门 + 越界守卫 |
| GS-18 | GAP-06；`pack/mod.rs:254`、`:443`；`vault_storage.rs:57`；`api_write_auth.rs:34` | `docs/` | `github-sync.md` pack 与授权设计冻结（Q6–Q8） | 产物门 + 越界守卫 |
| GS-21 | GAP-06；`api_write_auth.rs:34`；`vault_storage.rs:57`；`redis/lock.rs:323` | `docs/` | `github-sync.md` 授权与原子写入冻结（Q7–Q8） | 产物门 + 越界守卫 |
| GS-10 | 本文「非目标」；GS-09/GS-18/GS-21 的分支判定组合 | `docs/plan/` | 按组合：接收方计划或 `DEFER-*` 更新 | 产物门 + 越界守卫 |
| GS-11 | GAP-07；`AGENTS.md:251` | `docs/` | 契约收口 + 漂移登记 + 索引（**不含脚本**，脚本由 GS-14 交付） | `scripts/guard/docrefs.sh` |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | GS-14、GS-22、GS-19、GS-03、GS-23、GS-04、GS-13 完成：守卫可用，配置面就绪，不可执行组合在启动期被拒 | 坏配置的非零退出证据 | revert（GS-13 → GS-04 → GS-03 逆序） |
| M1 | GS-05、GS-16、GS-06 完成：公钥可从启动日志取得，私钥三面封闭，并发初始化收敛 | 两次启动的日志片段（sanitized）+ 并发收敛测试输出 | forward-only：删 vault 条目后重启 |
| M2 | GS-07、GS-08、GS-24、GS-15、GS-25、GS-20、GS-26 完成：可对本地 SSH 服务端完成一次 receive-pack 并对全部终止条件有判定 | 回环集成测试通过 + 一次 `ng` 诊断片段 | revert |
| M3 | GS-09、GS-18、GS-27、GS-21、GS-28 完成：Q1–Q5 / Q6a / Q6b / Q7 / Q8 **各自**三分支之一已判定并落盘（go 要求全部有方案；no-go 要求不可行判据；研究未完成则该卡 `blocked` 并按 ER-10 升级，不计入本里程碑完成） | ADR 落盘 + 五份判定记录 | revert |
| M4 | GS-10、GS-11 完成：按五张 spike 的**组合决策表**移交或登记替代方向，文档与索引一致 | 按分支的产物 + 守卫输出 | revert（GS-11 → GS-10 逆序） |

> 各里程碑的「完成」以其成员卡 `Acceptance=complete` 为准；本仓每卡独立 commit+push，故各卡的 C 覆盖在其自身发布切片内取得，不存在跨里程碑的覆盖等待。

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| 配置校验拒绝启动 | 无（进程未启动） | 修正配置后重启 | 以非法配置继续启动 |
| 金钥生成后、写入 vault 前崩溃 | 无（未持久化） | 下次启动重新生成 | 进程内持有一把 vault 中不存在的金钥 |
| 金钥写入 vault 后、日志输出前崩溃 | vault 有金钥但运维未取得公钥 | 下次启动走「载入」分支并再次输出公钥（GS-06 AC-4/AC-5） | 公钥永远无法取得 |
| 多副本并发初始化 | 至多一次多余的金钥生成计算 | RedLock 内重读指针，收敛到单一赢家（GS-05 AC-6） | 两副本各持不同私钥、vault 只留最后写入者 |
| vault 中已有值损坏 | 损坏值保留 | fail-closed 并告警，由人工决定删除后重新自举 | 静默覆写导致既有公钥失效 |
| SSH 主机金钥不匹配 | 无 | 中止连接，返回 `host_key` 阶段错误（GS-07 AC-2/AC-3） | 降级为 accept-any、或继续进入认证 |
| receive-pack 已发送、报告未读到即断线 | 远端可能已落地 | 判为失败并保留诊断；**本计划不推进任何游标**（游标语义由 GS-09 冻结） | 把未确认结果当作成功 |
| spike 时间箱耗尽而问题未全部回答 | 部分结论 | 按 GS-09 AC-8 的「研究未完成」分支处置：登记剩余清单、置 `blocked`、按 ER-10 升级 | 带着未冻结语义移交 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| RISK-01 五张 spike 的 8 个问题（Q1–Q8）在时间箱内无法全部冻结 | 中：移交受阻 | 各卡时间箱 ≤ 1.5 人日；「研究未完成」是合法终态（GS-09 AC-6 / GS-18 AC-8），不强行移交 | GS-09, GS-18, GS-27, GS-21, GS-28 |
| RISK-06 丢锁窗口无法用现有 vault 原语消除 | 低：与既有 `ensure_server_signing_key` 同级的残余风险 | GS-16 AC-7 显式登记并移交 GS-18 Q8；修复动作已写入 `Migration and rollback` | GS-16, GS-28 |
| RISK-02 凭证范围为机器账号全部仓库 | 中：金钥外泄影响面大于同步目标 | 上线清单强制专属机器账号并逐仓库授予 write；binding `remote` 唯一性为第二道防线 | GS-13, GS-11 |
| RISK-03 GitHub branch protection 挡住直接推送 | 低（本计划不做真实推送） | 无自动 preflight（ADR-GS-03）；上线清单人工确认 + 有界 side-band 诊断（ADR-GS-05）。该风险在 `DEP-01` 接收方实现真实推送时才实体化 | GS-11 |
| RISK-04 工作树被并行修改导致锚点漂移或越界误判 | 中：验收证据不可信 | 全部锚点开工当天重新核对；越界守卫一律按开工前基线快照逐行扣除（ER-01 补充） | 全局 |
| RISK-05 共享 `src/config/validate.rs` 的三张卡（GS-03/04/13）串行窗口被打断 | 低：合并冲突 | 全串行 + 顺序边；每卡独立 commit+push 后才启动下一卡（`.cursor/rules/task-card-release.mdc:22`） | GS-03, GS-04, GS-13 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| 配置校验 | O(binding 数)，binding 数有界 | 启动期一次 | 无 I/O，无网络 | GS-04/GS-13 单元测试 |
| 金钥自举 | 一次 vault 读 +（首次）一次写；RedLock 持有限于该读写 | 每进程一次 | 不阻塞启动超过既有 vault 交互预算 | GS-05 VER-3 |
| SSH 连接建立 | 一次握手 + 认证 | 每次推送一次 | 显式超时上限 | GS-07 AC-5 |
| receive-pack 发送 | pack 流式，驻留 O(1) | 每次推送一次 | 驻留与 pack 大小无关 | GS-08 AC-6 |
| 诊断输出收集 | O(min(远端输出, 预算)) | 每次失败一次 | 显式字节预算，超出截断并标记 | GS-08 AC-8 |

## 兼容与文档收口

- [ ] `docs/errors.md`：`N/A` —— 本计划新增错误经 `MegaError` 既有变体表达；若实现时确需新变体，由对应实现卡同步该文档。
- [ ] `docs/refactoring/*.md` 已同步：`docs/refactoring/github-sync.md`（GS-03 创建、GS-04..GS-09 增量、GS-11 收口）、`docs/refactoring/integration.md`（GS-07 追加覆盖矩阵）。
- [ ] `config/config.toml` 示例配置与注释已同步（GS-03）。
- [ ] 运行时 OpenAPI（`/api/openapi.json`）：`N/A` —— 本计划不新增 HTTP 表面（ADR-GS-02）。
- [ ] `README.md` / `AGENTS.md`：GAP-07 的模板漂移由 GS-11 登记、`DEP-02` 移交；`README.md` 无用户可见变化需同步。
- [ ] `src/callisto/`、`src/jupiter/storage/`、`src/jupiter/migration/`：`N/A` —— 本计划无 entity / storage / migration 改动。
- [ ] 新引入的 `docs/*.md` 引用全部指向真实存在的文件（GS-11 VER-1）。
- [ ] `plan-long.md` 日期计划索引已同步（GS-11 AC-10）。

## Review log

Result 只允许 `PASS` 或 `FAIL`。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R1 | 计划全文（初稿，12 张卡覆盖完整出站链路） | `FAIL` | P0-01..P0-09、P1-01..P1-12；逐条处置见下表 | P2-01、P2-02 随范围收敛移出（见下表） | Codex `codex exec -s read-only`；评审对全部 `file:line` 锚点独立核对 |
| R2 | 计划全文（一次收敛，12 张卡覆盖基础设施 + spike + 移交） | `FAIL` | P0-01..P0-04、P1-01..P1-09；逐条处置见下表 | — | Codex `codex exec -s read-only`；评审**实际运行**了五段守卫脚本 |
| R3 | 计划全文（二次收敛，10 张活动卡） | `FAIL` | P0-01..P0-02、P1-01..P1-09 | — | Codex；**实际运行**五段守卫并注入故障 |
| R4 | 计划全文（三次收敛，14 张活动卡 + 3 个已取消编号） | `FAIL` | P0-01、P1-01..P1-06 | P2-01 部分关闭 | Codex；守卫契约的内存复现与故障注入 |
| R5 | 计划全文（四次收敛） | `<pending>` | — | — | — |

### R1 逐条关闭矩阵

| 条目 | 原问题摘要 | 本轮处置 | 关闭证据 |
|---|---|---|---|
| P0-01 | 「trunk `/api/v1` 不挂任何 middleware」不成立 | 改为「有 session/CORS/Trace/解压，缺少的是 `cedar_guard`」 | 事实基线「trunk `/api/v1` 中间件」行；ADR-GS-02 Context |
| P0-02 | 8 项锚点/计数错误 | 逐项改正（跨计划锚点改用引文双锚定；`agent_capture_router` 分身份与范围两处；两条 NFF 消息区分；unwrap 计数；既有 GitHub 表面；disabled 校验分层；脏树归属 WH-03；N=1 hash 可相同） | 事实基线相应各行 |
| P0-03 | GS-01 未解决实际后台执行风险 | **移出范围**：确认属未定型设计（R2 P0-02 进一步确认需改 trait），进 GS-09 Q6 | GS-01 取消条目；`DEFER-GS-07` |
| P0-04 | outbox 触发范围漏祖先/后代 ref | **移出范围**：进 GS-09 Q1 | GS-09 Q1；事实基线「一次 push 的 ref 影响面」行 |
| P0-05 | `have` 不能保证远端对象闭包 | **移出范围**：进 GS-09 Q2 | GS-09 Q2 |
| P0-06 | 「两侧 hash 不可能相同」不成立；resume 无收敛协议 | 论断删除并改正；**移出范围**：进 GS-09 Q3 | 事实基线「trunk N=1 落地语义」行；GS-09 Q3 |
| P0-07 | 轮询配置自相矛盾且会误报自身推送 | **移出范围**：轮询整体移交，竞争窗口进 GS-09 Q4 | GS-09 Q4；`DEFER-GS-01` |
| P0-08 | `no-release` 卡不能继承先于其发布的 C 覆盖 | 本仓规则要求每卡 commit+push，家族卡模型不适用；`REL-00` 撤销、`GS-12` 取消 | 「发布分组与并发窗口」；`.cursor/rules/task-card-release.mdc:19`–`:26` |
| P0-09 | 补偿目标被成功路径覆盖 | **移出范围**：进 GS-09 Q5 | GS-09 Q5 |
| P1-01 | AC 按行数而非独立谓词计数 | 全部卡按谓词展开；GS-04 超限后拆出 GS-13 | 各卡 AC 编号；粒度审计表 |
| P1-02 | 多轴卡与「统一补文档」违反 G-01/G-02 | 各实现卡自带文档写集；GS-01/GS-02 取消 | 各卡 `Implementation write set` 含 `docs/refactoring/github-sync.md` |
| P1-03 | 实现写集不完整 | 补齐构造器、白名单、重载分类、测试模块、模块注册 | GS-03 写集（7 个生产文件） |
| P1-04 | 依赖边不一致、并发组不可执行 | 统一为单一依赖图；声明全串行 | 「实施顺序」与各卡 `Dependencies` |
| P1-05 | 审计表未逐字复制 `Granularity`；拆分标记自指 | 审计表 axis/recovery 与各卡逐字一致；拆分标记改为 `GS-04 → GS-13` | 粒度审计表；修订历史 |
| P1-06 | 守卫可假通过、测试过滤器不存在 | 全部守卫重写并本地实测三态；测试过滤器改为确切路径 | 「本计划引用的模板规则」的两段守卫；各卡 VER |
| P1-07 | 配置与 API 安全边界无可判定契约 | binding 合法性逐条谓词化（GS-13）；API 表面整体移出（ADR-GS-02） | GS-13 AC-1..AC-8 |
| P1-08 | 持久状态/幂等/故障恢复缺规则 | **移出范围**：进 GS-09 Q1..Q5 | GS-09 待回答问题 |
| P1-09 | 「人工复核」不构成 compensating | **移出范围**：补偿设计进 GS-09 Q5 | GS-09 Q5 |
| P1-10 | 协议验收不完整；side-band helper 不可直接复用 | 能力协商与终止条件逐条谓词化；helper 明确标注为「参考」 | GS-08 AC-2..AC-4、AC-7/AC-8；ADR-GS-05 Consequences |
| P1-11 | 事件 wire schema 与跨计划同步不完整 | **移出范围**：事件整体移交 | `DEFER-GS-01` |
| P1-12 | 模板小节缺失、D 组归属不明、外部事故论据无 pin | 就地摘录 G-03/G-01/判定口径；各卡写明 D 组判定方式；删除 `crates-sync` 事故论据 | 「本计划引用的模板规则」；各卡 `C/D coverage from` |
| P2-01 | 事件数量上限需解释 | **移出范围**：随事件移交 | `DEFER-GS-01` |
| P2-02 | 「完整诊断」措辞与截断契约不一致 | 统一为「有界诊断 + 显式截断标记」 | GS-08 AC-8；ADR-GS-05 |

### R2 逐条关闭矩阵

| 条目 | 原问题摘要 | 本轮处置 | 关闭证据 |
|---|---|---|---|
| P0-01 | GS-01 排除 `get_trees_by_hashes` 有误；AC-2 会破坏 `have` 协商语义 | GS-01 **取消**；`have` 语义区分进 GS-09 Q6 | GS-01 取消条目；GS-09 Q6；事实基线「upload-pack `have` 协商语义」行 |
| P0-02 | trait 无错误通道；字节预算未覆盖真正分配点 | GS-02 **取消**；trait 变更形态与分配点预算进 GS-09 Q6 | GS-02 取消条目；事实基线「pack trait」「pack 字节分配点」行 |
| P0-03 | GS-02/GS-03 配置写集不完整 | GS-03 写集补齐 `testing.rs`、`validate.rs` 白名单、`reload.rs`、`tests.rs`（独立文件） | GS-03 `Implementation write set`；`Current evidence` |
| P0-04 | 五段守卫中三段实测假通过 | 两段公共守卫重写并**本地实测**正例/负例/命令失败三态；GS-07 安全判据改为行为测试 | 「本计划引用的模板规则」；GS-07 `Security and privacy` |
| P1-01 | REL-00 与仓库发布规则冲突；M4 时序矛盾 | `REL-00` 撤销、`GS-12` 取消；里程碑加注说明每卡独立取得 C 覆盖 | 「发布分组与并发窗口」；里程碑表注 |
| P1-02 | AC 仍按 checklist 行数计 | 全部 AC 拆为原子谓词（GS-04 因此拆出 GS-13） | 各卡 AC 编号；粒度审计表 |
| P1-03 | spike 缺可执行的 no-go 退出标准 | GS-09 AC-8 定义 go / no-go / 研究未完成三分支及各自交付物；GS-10 AC-1..AC-5 按分支分列 | GS-09 AC-8；GS-10 AC-1..AC-5 |
| P1-04 | 真实 GitHub 验收范围自相矛盾 | live 验收**移出本计划**（`DEFER-GS-08`）；测试矩阵与完成判据同步 | 「非目标」第三条；测试矩阵 live/gated 行 |
| P1-05 | 部分移交的设计责任未进 spike | Q7 新增（操作授权与 `abort`/`force` 判据）；trunk 限制升为 ADR-GS-02 的已决议边界 | GS-09 Q7；ADR-GS-02 |
| P1-06 | 金钥自举缺并发收敛与独立恢复验证 | GS-05 AC-6 补 RedLock 锁内重读；恢复验证改为本卡即可取得证据 | GS-05 AC-6；`Migration and rollback` |
| P1-07 | 能力缺失与传输终止条件不完整 | GS-08 AC-3 定义最低能力集与发送前拒绝；AC-7/AC-8 覆盖终止条件与本层字节预算 | GS-08 AC-3、AC-7、AC-8；ADR-GS-05 |
| P1-08 | 依赖/文档/测试链未闭合 | 共享写集逐条补顺序边；`DEP-02` 明确「不等待接收方」；测试矩阵改为「不改动任何既有协议路径」 | 「实施顺序」；依赖登记表；测试矩阵 Git 协议行 |
| P1-09 | Review log 无 R1 逐条关闭矩阵 | 补 R1 与 R2 两份逐条关闭矩阵 | 本节两张表 |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-GS-01 | 出站同步执行链路（触发、worker、游标、事件、AI 交接、轮询、命令 API） | Q1–Q5（GS-09）与 Q7（GS-21）未冻结 | GS-09 与 GS-21 的对应问题域均判定为 go | `DEP-01` 的接收方计划（由 GS-10 具名） |
| DEFER-GS-07 | `incremental_pack` 硬化（错误传播、背压、字节预算） | Q6a/Q6b 未冻结；需改 `RepoHandler` trait 并触及线上 clone/fetch | GS-18 与 GS-27 均判定为 go |
| DEFER-GS-09 | 消除 RedLock 丢锁窗口的原子存储原语 | vault 层无 compare-and-set（`vault_storage.rs:57`）；与既有 `ensure_server_signing_key` 同级残余风险 | GS-28 判定为 go | 同上 | 同上 |
| DEFER-GS-08 | 真实 GitHub 的 live 验收 | 本计划承诺不产生任何 GitHub 侧副作用；且推送链路尚未实现 | 接收方计划实现推送链路时 | 同上 |
| DEFER-GS-02 | GitHub → mega2 入站同步 | 用户指定单向；入站需历史改写与授权仲裁 | 出现「以 GitHub 为内容权威」的产品需求 | 独立日期计划 |
| DEFER-GS-03 | `/third-party`（ImportRepo）路径同步 | ImportRepo 语义不同（可多分支、可 tag） | 出现第三方仓库镜像需求 | 独立日期计划 |
| DEFER-GS-04 | 两段式金钥轮换（rotate → promote） | 自举换取简洁，代价是轮换有中断窗口；当前极少轮换 | 轮换成为常态需求 | 独立任务卡 |
| DEFER-GS-05 | branch protection 自动 preflight | SSH 金钥无法认证 REST API，需第二种凭证（ADR-GS-03） | 已因其它需求引入 GitHub token | 独立任务卡 |

## 完成判据

- [ ] 所有活动任务卡满足粒度规则 `G-*`：无 L/XL 卡、无碎片卡、无合并发布例外（不设家族卡）、实现写集冲突均以顺序边消解；「任务卡粒度审计表」已填齐且与各卡 `Granularity` 行逐字一致；已取消的 `GS-01`/`GS-02`/`GS-12` 保留编号并标明替代关系。
- [ ] 所有非延后任务的 acceptance criteria 已满足，且 `Lifecycle=done` **且** `Acceptance=complete`。五张 spike 卡（`GS-09`、`GS-18`、`GS-27`、`GS-21`、`GS-28`）的 AC 按其「退出分支」条件化判定：**go** 要求 Q 全部有方案；**no-go** 要求被否决项有不可行判据与证据、且替代方向已登记；**研究未完成**则该卡按 ER-10 置 `blocked` 并升级，本计划不得带着 `blocked` 通过完成门。
- [ ] 所有任务的 Verification 命令已运行并记录结果；无「跳过即通过」的幽灵验收。
- [ ] 计划完成门：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all` 全绿，且 `cargo build`、`cargo build --tests` 均 0 错误 0 警告；不得新增 crate 级 `#[allow(...)]`。
- [ ] 必要的 docs/配置/测试矩阵更新已完成（各实现卡自带 + GS-11 收口）。
- [ ] 每张卡的 `Rollback mode` 都已被实际验证或记录为不可验证的原因：GS-05 的 `forward-only` 须实际演练一次「删 vault 条目 → 重启 → 新公钥」。
- [ ] 代码 review 最终结论为 `PASS`，P0/P1 全部关闭。
- [ ] 版本面（ER-08，开工当天现场核对处数）已按每卡 `Version increment` bump 且自洽；每次实际 bump 均已创建并推送同名 `v<version>` tag，并以人工编写的标准 release note 建立同名 GitHub Release；`Version increment=N/A` 的卡仍已 commit+push（`.cursor/rules/task-card-release.mdc:24`–`:26`），但不创建 tag 或 release。
- [ ] 「修订历史」已记录成稿后的全部规范性变更；Review log 的逐条关闭矩阵覆盖 R1/R2 全部条目。
- [ ] `docs/plan/README.md` 与 `plan-long.md` 的索引已同步本计划状态（GS-11）。
