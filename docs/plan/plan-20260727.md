# 集成测试基建扩展与统一计划（2026-07-27）

**模板版本:** v2（本计划成稿于 v2 生效日之前，2026-07-29 完成整份迁移；迁移登记见「修订历史」）

## 文档职责

本文解决 `plan-long.md` PT-01（集成测试基建扩展与统一）的落地，目标是：把测试双层规范与 docker-compose 拓扑扩展机制沉淀为单一事实源，交付真实 Git CLI 的 cargo-native 黑盒最小矩阵（往返一致性、用例隔离、认证边界、失败路径）作为示范负载，落地并发 dispatcher 端到端投递基线用例，并收口 `docs/refactoring/integration.md` 的 P2 清单与覆盖矩阵。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `docker-compose.test.yml` 测试栈拓扑扩展机制、既有 rustfs（S3-compatible）扩展的登记，以及首个受规范约束的新服务（git-cli runner）。
- `bin/tests/` 黑盒测试分层规范与新集成 test target `bin/tests/integration_git_cli.rs`（IT-03 建立 harness、最小凭据链路与往返断言，IT-10 追加认证边界与凭据零泄漏断言，IT-12 追加失败路径与 liveness 断言）。
- `src/notification/dispatcher.rs` 的并发投递用例（`#[cfg(test)] mod tests` 内的新 focused 用例，不是新 cargo test target）。
- `.env.test.example` 的变量补齐与 `export` 语义修复。
- `.github/workflows/config-validation.yml` 与 `.github/workflows/git-protocol-smoke.yml` 的执行面与触发面扩展。
- `docs/refactoring/test-infra.md`（新建）、`docs/refactoring/integration.md` P2 清单与覆盖矩阵收口、`docs/plan/plan-long.md` PT-01 状态同步。

### 非目标

- 不追求覆盖率数字；本计划只为后续 PT 提供验收承载。
- 不重写既有测试：`bin/tests/integration_vault.rs`（含已落地的 `integration_config_hot_reload`）与 notification 模块既有集成测试保持不动。
- 不重复 `scripts/git_protocol_smoke.sh` 已覆盖的正路径广度矩阵；本计划只补它在结构上无法提供的部分（见 GAP-02）。
- Git 协议专项矩阵的完整用例面（SSH / LFS 通道纳入 `bin/tests/` 分层、Mega `tests/` 夹具审计移植）属 PT-04，见 DEFER-IT-01/02。
- orion-server、fake webhook receiver 等后续 PT 的拓扑接入只定义规范，不预先实现，见 DEFER-IT-04。
- 真多进程黑盒 claim 竞争矩阵属 PT-09，见 DEFER-IT-03。

### 成功定义

- 新服务接入 docker-compose 测试栈有书面标准流程（`docs/refactoring/test-infra.md`），既有 rustfs（S3-compatible）扩展已登记，git-cli runner 是首个按规范落地的服务。
- 真实 `git` 客户端对测试进程启动的 monoengine HTTP 服务的**往返一致性、用例隔离、未认证 401、失败路径**进入 `cargo test --all` 门内。
- 并发 dispatcher 端到端投递基线用例落地，并与 Git CLI target 一起进入 CI 的执行面与触发面。
- `.env.test.example` 与 CI env 对齐，且 `source` 后可被 cargo 子进程继承。
- integration.md P2 清单每一项都有「已落地（证据）/ 书面关闭（含理由与承接编号）」结论——与 PT-01 完成判据（`docs/plan/plan-long.md:204`）的口径一致，**不允许以「延后」结案**；覆盖矩阵与实际 target 一致。
- 全部非延后任务 `Lifecycle=done` 且 `Acceptance=complete`；PT-01 完成判据逐条对照后在 `plan-long.md` 更新状态。

## 事实基线

> **本节整表均为开工前历史快照（核对日 2026-07-29）**，描述计划起草时的仓库状态，**不是**执行期收口后的 live 现状。执行后状态以各任务卡 **Lifecycle / Acceptance**、落地产物（`Implementation write set`）、以及 `docs/refactoring/integration.md`（IT-07）与 `plan-long.md` PT-01 行为准。各卡内的 `Current evidence` 表若未另行标注「执行后」，亦按**开工前快照**阅读，不得当作 HEAD 现状。所有行号和源码锚点仅作历史线索。
>
> **Live 对照（2026-07-30，与 `docs/refactoring/test-infra.md` / compose 对齐）：** 测试栈服务为 postgres / redis / mailpit / **rustfs** / **rustfs-init** / **git-cli**（`-p monoengine-it`；`git-cli` 需 `--profile git`）；RustFS 公开凭据为 `rustfs_it` / `rustfs_it_secret`（端口 `19000/19001`，桶 `testbucket`）；harness git pin = `git version 2.49.1`；CI smoke 宿主机 git/lfs pin = `2.54.0` / `3.7.1`。

| 类别 | 开工前事实（历史快照） | 证据 |
|---|---|---|
| 代码入口 | 黑盒进程集成测试的唯一 cargo target 是 `integration_vault`，另有共享 helper 模块 `bin/tests/common/mod.rs`（`write_bootstrap_config` / `write_full_config`，32 行） | `bin/tests/integration_vault.rs:13`（`mod common;`）、`bin/tests/common/mod.rs:1`、`:32` |
| 代码入口 | 可复用的黑盒编排：`VaultCliEnv` 夹具、`isolated_command` 命令构造、`integration_service_http_smoke`（启动真实 `service http`、断言 PostgreSQL、SIGINT 优雅退出） | `bin/tests/integration_vault.rs:77`、`:87`、`:600`、`:756` |
| 代码入口 | 热加载黑盒**已落地**：`integration_config_hot_reload` 覆盖白名单 `log.level` 热生效、非白名单 `monorepo.import_dir` 仅报 `restart_required_fields` 且旧配置继续服务（用 `/api/v1/tree/path-can-clone` 证明）、坏 TOML 被拒且服务不中断 | `bin/tests/integration_vault.rs:872` |
| 代码入口 | 热加载触发入口是**轮询文件 watcher，不是 SIGHUP、也不是 HTTP API**：`ConfigReloadWatcher` 监视 base + profile 文件，按固定间隔 tick 拉取，由 `service http` 启动时 spawn | `src/config/reload.rs:191`、`:223`（`poll_once`）、`:233`（`run_until_shutdown`）、`src/commands/service/mod.rs:20`（`CONFIG_RELOAD_POLL_INTERVAL`）、`:44-49`（`service http` 启动路径调用 `spawn_config_reload_watcher`）、`:131`（`tokio::spawn`） |
| 数据/状态 | 邮件 outbox claim 路径：dispatcher 每 tick 调 `fetch_pending_jobs`；storage 层并发 claim（含跨独立连接变体）与 stale `sending` 恢复已覆盖；**dispatcher 层无任何双实例并发投递用例** | `src/notification/dispatcher.rs:343`、`src/jupiter/storage/notification_storage.rs:639`、`:1254`、`:1313`、`:1372` |
| 数据/状态 | dispatcher 测试模块现有 **7 个** `integration_mail_dispatcher_*` 用例，无一涉及双实例；唯一并发断言是单实例内有界并行。核对命令：`rg -n 'async fn integration_mail_dispatcher' src/notification/dispatcher.rs` | `src/notification/dispatcher.rs:810`、`:960`、`:1010`、`:1066`、`:1123`、`:1181`、`:1228`；单实例并行 `:1339` |
| CLI 命令 | CI 直接覆盖 `config validate` 与 `config init`；`monoengine service http` 在 `config-validation.yml` 中没有任何直接调用，只经 `integration_vault` 间接覆盖；直接启动 `service http` 的是 `git-protocol-smoke.yml` | `.github/workflows/config-validation.yml:124`、`:130-132`、`:294`；`.github/workflows/git-protocol-smoke.yml:148-149`；三处注册见 `src/commands/mod.rs:34`、`:43`、`:55` |
| HTTP API | Git smart HTTP 的 receive-pack 路径**强制认证**（Bearer 优先、Basic 的 password 作 token 回退）；upload-pack 路径**认证可选**——同样解析身份并调用 `check_upload_pack_access`，是否允许匿名由 `git.anonymous_access` 决定。401 响应带 `WWW-Authenticate: Basic realm="Mega", Bearer realm="Mega"` | receive-pack：`src/contract/git_protocol/http.rs:64`（`check_push_permission`）、`:345`、`:355`；upload-pack：`:52-57`、`:209`、`:216-217`；401 构造 `:93`（`auth_failed`）、认证入口 `:123`（`git_http_auth`） |
| HTTP API | 真实 Git CLI 对 HTTP 通道的**正路径矩阵已在 CI 落地**：ls-remote / clone / fetch / protocol v2 fetch+ls-remote / shallow depth=1 / `--filter=blob:none` / branch push+delete / tag push+delete / LFS push+clone+pull+locks-list，全部对真跑的 `service http` 执行，用一次性 `access_token` 走 Basic Auth | `scripts/git_protocol_smoke.sh:246-268`；`.github/workflows/git-protocol-smoke.yml:120-131`、`:133-163`、`:165-175`；`docs/refactoring/protocol.md:789` |
| 配置项 | 测试环境变量经 `.env.test` / CI env 注入。CI 注入三项；`.env.test.example` 只有三行生效项且**全部是裸赋值**（`source` 后不会传入 cargo 子进程） | CI：`.github/workflows/config-validation.yml:288`、`:289`、`:290`；示例：`.env.test.example:1`、`:2`、`:3`（缺 `MAILPIT_API_URL` / `MAILPIT_SMTP_HOST` / `MAILPIT_SMTP_PORT`） |
| 配置项 | 仓库默认 `config/config.toml` 指向 `postgres://localhost:5432/mono` 与 `redis://127.0.0.1:6379`，与测试栈 `15432` / `16379` 不符——起 `service http` 必须覆盖数据面 | `config/config.toml:40`、`:208` |
| 错误类型 | `MegaError` 是**应用层主错误类型**，不是唯一对外错误契约：HTTP 边界另有 `ApiError`（可接收 `MegaError`、`anyhow::Error` 与 legacy `[code:xxx]` 文案），Git 协议边界另有 `ProtocolError`。无数值错误码注册表。本计划不新增任何错误变体 | `src/common/errors/mod.rs:28`（`pub enum MegaError`）、`src/common/errors/api.rs:41`（`pub struct ApiError`）、`:80`（`impl IntoResponse`）、`docs/errors.md:36`、`:38`、`:39` |
| 迁移 | `N/A`——本计划无 schema 变更，不新增迁移、不改 `migrations()` 注册列表 | `src/jupiter/migration/mod.rs:123`（`fn migrations()`，本计划不触碰） |
| 文档 | 集成测试策略事实源：测试分层定义、容器编排样例、P2 能力 gate 表、覆盖矩阵（开工前基线；IT-07 收口后见该卡 Current evidence） | `docs/refactoring/integration.md:111-143`、`:147-192`、`:423-429`、`:622-636`（行号为开工前锚点） |
| 文档 | integration.md 已在四处记录热加载黑盒落地，但 P2 表 `:426` 仍写成待办且措辞为「明确 SIGHUP 或 HTTP API」，与实际轮询 watcher 矛盾 | `docs/refactoring/integration.md:81`、`:107`、`:635`、`:655` vs `:426` |
| 测试 | CI 有 **2 个**跑测试的 workflow：`config-validation.yml`（305 行，`validate-config` job）与 `git-protocol-smoke.yml`（188 行，`git-protocol-smoke` job）。`claude-review.yml`（75 行）不跑测试，且只响应 issue/PR 事件、不由 push 触发 | `.github/workflows/config-validation.yml:294`、`:298-300`；`.github/workflows/git-protocol-smoke.yml:36-37`、`:175`；`.github/workflows/claude-review.yml:3-12`（`on:` 块） |
| 测试 | 两个 workflow 的 `paths:` 过滤器**都不包含** `bin/tests/**`，也不包含 `docker-compose.test.yml`；新增黑盒 target 默认不会触发任何 CI | `.github/workflows/config-validation.yml:5-15`、`:19-29`；`.github/workflows/git-protocol-smoke.yml:14-22`、`:26-34` |
| 工作区前置 | **`../orbit` 路径依赖当前不在位**：`Cargo.toml:71` 声明 `orbit-api`，`bin/Cargo.toml:18-19` 声明 `orbit` / `orbit-api`；sibling checkout 不存在，`cargo metadata --no-deps` 以 101 退出。**任何 cargo 门在补齐前都无法执行** | `Cargo.toml:71`、`bin/Cargo.toml:18-19`；核对命令与退出码见 DEP-01 |
| 工作区前置 | 本地 `.env.test` 不存在（仓库只提供 `.env.test.example`）。按 `AGENTS.md` 必须停止并询问，不得静默生成，也不得降级为不 source 的 `cargo test --all` | `AGENTS.md:95`、`:98`、`:102` |
| 工作区前置 | 版本面 parity 已满足（ER-08 预检通过）：两处均为 `0.1.166` | `Cargo.toml:6`、`bin/Cargo.toml:3` |
| 工作区前置 | 工作区没有任何 cargo feature，根 `Cargo.toml` 也没有 `default-members`，故不带 `-p` 的 cargo 命令只选中 `monoengine-core`。核对命令：`rg -n -e '^\[features\]' -e '^default-members' Cargo.toml bin/Cargo.toml`（零命中，按「Verification 判定口径」的退出码模板判定） | `Cargo.toml:1`（`[workspace]`）、`:4`（`[package]`）；`bin/Cargo.toml:1`（`[package]`） |
| 外部参照 | Mega 目标项目 `main` @ `d2b6d1c314ab750a06fd96cff85559cd685907ea`（#2157，核对日 2026-07-27），`mega/tests/` 协议夹具归 PT-04 | `docs/plan/plan-long.md:30`（完整 SHA，权威锚点）；`:62` 仅为审计快照记录，其中的 `d2b6d1c3` 是缩写 |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | 测试栈已有 5 个服务但**无书面登记规范**：rustfs + rustfs-init（`profiles: ["init"]` one-shot；承接原 MinIO 位）是测试栈 S3-compatible 扩展先例，后续服务（git runner、orion-server、fake receiver）没有标准登记方式 | 后续 PT 各自发明脚手架，重复且不可比；对象存储扩展曾缺书面登记（现以 rustfs 登记） | `docker-compose.test.yml` 的 `rustfs` / `rustfs-init` 服务块与 `networks.default`；反证：`docs/` 全目录无 `test-infra.md`（`ls docs/refactoring/`），`docs/refactoring/integration.md:111-143` 与 `:147-192` 是仅有的散落纪律 | IT-01、IT-02 |
| GAP-02 | 真实 Git CLI 覆盖是 **shell 脚本 + 独立 workflow 形态**，未纳入 `bin/tests/` 黑盒分层：不在 `cargo test --all` 门内、无法用 `source .env.test && cargo test` 本地跑、需手工先起服务；且矩阵**全部是正路径**，无失败路径、无未认证 401 断言、无 push 后字节级往返、无每用例隔离、无客户端版本固定 | PT-03 协议改动的回归网只在一条路径上；开发者跑本地强制门时对 git 协议零信号；runner 的 git 版本漂移可静默改变协议协商 | `scripts/git_protocol_smoke.sh:246-268`（全为正断言）、`:120-150`（push 后只删 ref、从不回读）、`:275-277`（push/LFS 默认 SKIP 且退出码 0）；`bin/tests/` 只有 `integration_vault.rs` 与 `common/mod.rs`；`.github/workflows/git-protocol-smoke.yml:68-75`（宿主机 git，版本只 echo 不断言）、`:172`（凭据内嵌 URL，故 receive-pack 401 分支无真实客户端覆盖） | IT-02、IT-03、IT-04、IT-10、IT-11、IT-12 |
| GAP-03 | **已关闭。** 热加载黑盒用例已落地并在 CI 执行；剩余的只是 integration.md 内部自相矛盾（P2 表仍把它写成待办，且措辞与实际触发机制不符） | — | 用例：`bin/tests/integration_vault.rs:872`；CI：`.github/workflows/config-validation.yml:294`；文档已记录落地：`docs/refactoring/integration.md:81`、`:107`、`:635`、`:655`；矛盾行：`:426` | 关闭；文档矛盾并入 IT-07 |
| GAP-04 | 并发 dispatcher 端到端投递无验证（storage 层并发 claim 与 stale 恢复已覆盖，是本缺口的上界对照） | 多部署下可能重复发送；PT-09 验收无承载 | 已覆盖上界：`src/jupiter/storage/notification_storage.rs:1254`、`:1313`、`:1372`；缺口：`src/notification/dispatcher.rs` 的 7 个 `integration_mail_dispatcher_*` 用例（`:810`、`:960`、`:1010`、`:1066`、`:1123`、`:1181`、`:1228`）无一涉及双实例，`:1339` 是单实例内并行 | IT-06 |
| GAP-05 | integration.md 三处漂移：(a) P2 能力表 5 行中 **4 行**仍写成待办（`config init` 行已标「已落地」），其中热加载行与同文件 `:81` / `:107` / `:635` 的「已落地」自相矛盾；(b) 内嵌 compose 样例只列 3 服务且无 `shm_size`，与实际 5 服务不符；(c) 覆盖矩阵无任何 Git 协议行，`git-protocol-smoke.yml` 完全未被记录 | 计划文档与测试现状不一致，后续 PT 无法引用；新人按内嵌样例会得到错误的测试栈 | (a) `docs/refactoring/integration.md:425`（已落地行）vs `:426`、`:427`、`:428`、`:429`（待办行）；(b) `:152-192` vs `docker-compose.test.yml:4`、`:38`、`:56`；(c) `:624-636` 无 git 行，仅 `docs/refactoring/protocol.md:789` 有记录 | IT-03、IT-06、IT-07 |
| GAP-06 | `.env.test.example` 与 CI env 不对齐且**语义失效**：缺 `MAILPIT_API_URL` / `MAILPIT_SMTP_HOST` / `MAILPIT_SMTP_PORT`，且生效行全部是裸赋值——`source .env.test && cargo test` 不会把变量传给 cargo 子进程 | 无法证明示例中的变量确实被子进程继承：现有用例对 DB / Redis / Mailpit 地址都有指向测试栈的默认值，因此 target 全绿可能是**默认值兜底造成的假绿**，而不是示例生效 | `.env.test.example:1-3`、`:5-12`；CI 对照 `.github/workflows/config-validation.yml:288-290`；默认值兜底：`bin/tests/integration_vault.rs:693-701`（DB / Redis）、`:1606-1612`（`MAILPIT_API_URL`）、`:1620-1628`（`MAILPIT_SMTP_HOST` / `MAILPIT_SMTP_PORT`） | IT-08 |
| GAP-07 | `../orbit` sibling checkout 缺失，整个 workspace 的 cargo 命令不可执行 | 本计划所有 Verification 命令当前均不可运行 | `Cargo.toml:71`、`bin/Cargo.toml:18-19`；`cargo metadata --no-deps` → exit 101 | DEP-01（开工前置，非任务） |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | 承接 PT-01（DEP-02）；SB-03 与本计划同源互补 | 日期计划索引已登记本文件（`docs/plan/plan-long.md:744` 节、`:103` PT-01 行）；完成后按 PT-01 完成判据（`:200-204`）逐条复核并更新状态（IT-07） |
| `docs/refactoring/integration.md` | 测试策略事实源（DEP-04） | 本计划是其 Phase 4（P2 gate）的执行层；IT-01 插入指向 `test-infra.md` 的链接，IT-03/IT-06 各自登记本卡的覆盖矩阵行，IT-07 收口 P2 清单与内嵌 compose 样例 |
| `docs/refactoring/test-infra.md` | 本计划新建（IT-01 产物），测试基建规范的单一事实源 | IT-01 创建；IT-02 登记 git-cli 服务；IT-04 登记 CI 入口 |
| `docs/refactoring/protocol.md` | Git CLI smoke 用例建议清单来源（DEP-04） | IT-03 在其 smoke 清单标注「最小集已进 cargo target」；完整清单归 PT-04 |
| `AGENTS.md` / `README.md` | 工程约束基线 | 遵守 ER-07 + GC-12；登记漂移（`README.md` Contributing 的 `git commit -s -S` 在本仓双重不可执行；`AGENTS.md` 无提交/签名指引且含过期事实），作为独立文档修复项承接，本计划不修订 |

## 评审结论与修订记录

计划成稿前必须从以下维度做一次自审；如果有阻断项，先修计划再开工。

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | PT-01 是 plan-long 排定的下一个执行任务，基建先行可解锁 PT-03/04/06/09 验收 | 无 |
| 可行性 | 任务按「规范 → 环境 → 示范负载 → P2 用例 → CI → 收口 → 发布」拆分，均可独立交付 | 无 |
| 任务卡粒度 | 本次迁移拆出 IT-08（`.env.test.example` 轴自 IT-02 分离）、IT-09（发布点）、IT-10（认证边界自 IT-03 分离）、IT-11（git 客户端版本固定自 IT-04 分离）、IT-12（不存在仓库的失败路径与 liveness 自 IT-10 分离）；IT-06 删除预授权缺陷修复轴；IT-05 关闭并降级为非任务卡（已落地）。最终卡集 11 张，全部卡的 AC/VER 按模板原文口径重新计数并落在上限内；无 L/XL 卡；无未登记的合并发布；无 `EX-*` waiver | 按 `G-*` 拆分/关闭并登记「修订历史」 |
| 依赖与顺序 | DAG 无环；补 `IT-08 -> IT-03`、`IT-08 -> IT-06`、`IT-03 -> IT-10`、`IT-03 -> IT-06`、`IT-06 -> IT-04`、`IT-10 -> IT-12`、`IT-12 -> IT-04`、`IT-04 -> IT-11`、`IT-11 -> IT-07`、`IT-07 -> IT-09`；旧的多源边 `IT-04、IT-05、IT-06 -> IT-07` 已按 `A -> B` 格式拆开。`docs/refactoring/integration.md` 的四个写者（IT-01/IT-03/IT-06/IT-07）已由单链串行化 | 重写「实施顺序」并新增「依赖登记表」 |
| 完整性 | 纯测试基建，无 DB schema / API / 配置项变更；每张行为卡自带其覆盖矩阵同步（G-02），文档同步覆盖 `test-infra.md`、`integration.md`、`protocol.md`、`plan-long.md`，另含 `.env.test.example` 与两个 workflow | 把 integration.md 矩阵行并回 IT-03 / IT-06 |
| 安全性 | git-cli 容器不注入 secret，工作目录为隔离可写卷；测试 token 经 credential helper 注入并有零命中守卫；CI 沿用 `::add-mask::` | 无 |
| 功能正确性 | IT-06 的断言基于 claim 语义（恰好一次投递），不涉及业务状态机变更；暴露缺陷按 ER-10 派生 `FIX-*` 卡 | 修订 ADR-IT-03 的 Consequences |
| 接口兼容 | 不改公开接口；workflow 变更不影响本地产出物；`.env.test.example` 只影响开发者环境 | 无 |
| 数据流与控制流 | IT-06 断言同一 DB 下两 dispatcher 实例无重复发送、无丢失、stale 恢复在并发下成立 | 无 |
| 性能与容量 | 新用例受 wall-clock 预算约束（见「性能与容量摘要」），防止 CI 时长失控 | 无 |
| 可靠性与容错 | compose 服务失败必须 `down -v` 清理；黑盒测试进程崩溃不得泄漏容器卷与端口；暂时性错误按 ER-10 有界重试 | 无 |
| 可维护性 | `docs/refactoring/test-infra.md` 是新服务的唯一登记入口，禁止绕规范加服务；integration.md 只放链接不复制内容（GC-02） | 无 |

### 修订历史

计划成稿后的每次规范性变更（任务卡拆分/合并、依赖调整、发布边界变化、决策反转）都必须在此登记一行；G-09 的拆分同步以本表为闭环凭证。

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-07-29 | 模板 v2 生效（`docs/plan/plan-template.md:5`） | 整份迁移到模板 v2：新增「修订历史」「依赖登记表」「发布分组与并发窗口」「任务卡粒度规则 G-01..G-11」「推荐拆分维度」「粒度反模式速查」「字段全局默认与例外」「任务卡粒度审计表」「Verification 判定口径」；执行检查条目改为稳定 `ER-01..ER-12`；GC-02/04/05/06/07/09/12 采用 v2 措辞；Review log 改 6 列；「兼容与文档收口」7→8 项；「完成判据」7→10 项 | `N/A` | 全文 |
| 2026-07-29 | 现状核对（GC-01 / ER-02） | 事实基线整表重写：旧表 10 行中 5 行锚点错误、4 行结论已被现实推翻（热加载黑盒已落地、Git smart HTTP 已有真实客户端矩阵、CI 有两个测试 workflow、compose 有 5 服务）。新增「错误类型」「迁移」「工作区前置」行；GAP-03 关闭，新增 GAP-06、GAP-07 | `N/A` | 事实基线、当前缺口、全部卡的 Current evidence |
| 2026-07-29 | 现状核对（GC-01） | IT-05 关闭：热加载黑盒用例已于计划成稿前落地并在 CI 执行，原卡 4 条 AC 逐条被现有用例满足；编号降级为「已关闭编号记录」，不再是任务卡 | `IT-05 → 关闭` | 实施顺序、测试矩阵、追溯表、里程碑 M2、风险登记、性能摘要、粒度审计表 |
| 2026-07-29 | 粒度门禁（G-01 / G-09） | IT-02 拆出 IT-08：`.env.test.example` 的变量补齐与 `export` 语义修复是独立可恢复的行为轴，且是所有 `source .env.test` 门的前置 | `IT-02 → IT-02, IT-08` | 实施顺序、依赖登记表、追溯表、粒度审计表 |
| 2026-07-29 | 粒度门禁（G-07 / ER-04） | 新增 IT-09 发布点：IT-07 是终点 `docs` 卡，`no-release` 卡必须具名承载其 C 组覆盖与 D 组，原计划无此承载点 | `N/A → IT-09` | 实施顺序、发布分组与并发窗口、里程碑 M3、完成判据 |
| 2026-07-29 | 粒度门禁（G-01 / G-03，codex review R5） | IT-03 拆出 IT-10：按独立谓词重新计数后原卡 AC ≥12，超 implementation 上限；按「安全轴」切分——IT-03 保留往返一致性与用例隔离，IT-10 承接认证边界与失败路径。两卡写集在 `bin/tests/integration_git_cli.rs` 相交，已补 `IT-03 -> IT-10` 顺序边 | `IT-03 → IT-03, IT-10` | 实施顺序、并发声明、发布窗口顺序、Phase 1、追溯表、测试矩阵、里程碑 M1、风险登记、粒度审计表 |
| 2026-07-29 | 粒度门禁（G-03，codex review R5） | 全部卡的 AC / Verification 按「独立 pass/fail 谓词」与「独立验证门」重新计数并改写：ER-04 的强制门（fmt / clippy / 全量测试 / 表面门 / C 组发布动作 / D 组远端门）从各卡 `Verification` 移除，只保留本卡特有判据；模糊描述改为可直接复制的命令，IT-06 的新用例定名 | `全部卡` | 各卡 AC / Verification / Granularity、粒度审计表 |
| 2026-07-29 | 规则冲突（G-04，codex review R5） | 删除计划自设的「G-04 计数口径补记」：模板 v2 明确把本卡自己的测试文件排除在计数外，计划无权改写分母。IT-03 / IT-06 / IT-10 的 `landing`/`prod-files` 相应改为 `0/0`，`scope` 仍为 `S` | `IT-03, IT-06, IT-10` | 字段全局默认与例外、粒度审计表 |
| 2026-07-29 | 规则冲突（G-02，codex review R5） | IT-03 与 IT-06 各自把本行为轴的 `docs/refactoring/integration.md` 覆盖矩阵行并回本卡写集与 AC，不再交给 IT-07；IT-07 相应缩减。新增写集相交由 `IT-03 -> IT-06` 顺序边消解 | `IT-03, IT-06, IT-07` | 实施顺序、各卡写集与 AC、粒度审计表 |
| 2026-07-29 | 规则冲突（ER-10 / G-01） | IT-06 删除「若发现缺陷：修复 + 回归用例 + 版本 patch +1」的预授权修复轴，改为按 ER-10 派生 `FIX-IT-06-01` 卡并置本卡 `blocked`；ADR-IT-03 Consequences 同步改写 | `IT-06` | ADR-IT-03、里程碑 M2、故障恢复矩阵、风险登记 |
| 2026-07-29 | 粒度门禁（G-05 / G-10） | IT-04 落点从「或新建 workflow」收敛到具名两文件，并吸收 IT-06 的 CI 验收（G-10 允许的「合并到唯一集成卡」）；IT-05 的 CI 验收随卡关闭而删除 | `IT-04` | 实施顺序、追溯表、IT-06 AC |
| 2026-07-29 | 现状核对（ER-01，codex review R5） | 新增 DEP-01（`../orbit` sibling 缺失阻断全部 cargo 门）与 DEP-09（`.env.test` 须由使用者确认后提供）；Phase 0 进入条件改为引用 DEP-09，解除「IT-08 在 Phase 0 内却被要求先完成」的不可满足条件；Phase 1 进入条件改为 IT-01 `locally-accepted + review PASS`（其 `complete` 依赖后继 IT-02 的 C 覆盖） | `N/A` | 依赖登记表、Phase 0、Phase 1、风险登记、故障恢复矩阵 |
| 2026-07-29 | 现状核对（codex review R5） | 事实基线四处更正：upload-pack 为「认证可选 + `check_upload_pack_access`」而非「忽略 auth 结果」；`MegaError` 改述为「应用层主错误类型」并列出 `ApiError` / `ProtocolError`；dispatcher 用例数由 8 更正为 7；P2 待办行由 5 更正为 4。GAP-06 的影响改述为「默认值兜底可能造成假绿」 | `N/A` | 事实基线、当前缺口、IT-06 / IT-07 / IT-08 的 Current evidence |
| 2026-07-29 | 粒度门禁（G-01 / G-03，codex review R6） | IT-04 拆出 IT-11：原卡同时承担「CI 执行面 + 触发面」与「git 客户端版本固定」两个可独立恢复的行为轴，AC 按独立谓词计已超上限。IT-11 承接版本固定与 job summary 输出，与 IT-04 在 `git-protocol-smoke.yml` 写集相交，已补 `IT-04 -> IT-11` 顺序边 | `IT-04 → IT-04, IT-11` | 实施顺序、并发声明、发布窗口顺序、Phase 2、追溯表、里程碑 M2、风险登记、粒度审计表 |
| 2026-07-29 | 规则冲突（ER-04，codex review R6） | ER-04 A 组的表面 focused 门（各卡自身的 `cargo test -p ... --test <target>` / `-p monoengine-core '<mod::tests>'`）从全部卡的 `Verification` 移出——它们由 `Task type` 与改动表面自动命中，不计入 G-03；各卡 `Verification` 只保留本卡特有的守卫与手工证据，VER 计数相应下调 | `全部卡` | 各卡 Verification / Granularity、粒度审计表 |
| 2026-07-29 | 规则冲突（G-05，codex review R6） | 五条 Verification 命令被判定为不可直接执行或存在幽灵验收，已重写：IT-03 的 fail-closed 守卫改为断言退出码 + 预期原因 + 拒绝 `SKIP` 输出；IT-10 的 token 守卫改为由 Verification shell 生成并导出 token、`tee` 输出、扫描真实工作目录；IT-08 的键集合守卫由 `diff` 相等改为 `comm` 子集判定；IT-04 的 `paths:` 守卫改为对两个 workflow 逐项断言且要求同时出现在 `pull_request` 与 `push` 两个块；IT-07 的两条比对命令改为限定行区间与测试属性提取 | `IT-03, IT-04, IT-07, IT-08, IT-10` | 各卡 Verification |
| 2026-07-29 | 现状核对（codex review R6） | 三处事实更正：`integration_multichannel_notification` **已落地**（`bin/tests/integration_vault.rs:1008`，integration.md `:18`/`:105`/`:636` 均已标记），webhook / slack 渠道亦已实现，故删除 DEFER-IT-05 并把 IT-07 的该行结论改为「已落地」；crate 级 `#[allow(...)]` 基线为 `src/lib.rs:1`、`src/vault/core.rs:1`、`src/vault/utils/seal.rs:1`（原写 `src/main.rs`，该文件不存在）；完整 Mega SHA 锚点改为 `plan-long.md:30` | `IT-07, IT-09` | 事实基线、非目标与延后项、依赖登记表 DEP-06 |
| 2026-07-29 | 依赖闭合（G-06，codex review R6） | IT-03 / IT-06 / IT-08 原写「从 IT-08 消费 `.env.test`」，但 IT-08 永久不创建该文件。三卡改为直接登记 `DEP-09`；IT-08 只作为示例格式前置，不再被当作 `.env.test` 的提供者 | `IT-03, IT-06, IT-08` | 各卡 Dependencies、依赖登记表、粒度审计表 |
| 2026-07-29 | 现状核对（GC-02，codex review R6） | IT-01 的 AC 补上「替换」语义：`docs/refactoring/integration.md:111-143` 的既有分层正文必须改为指向 `test-infra.md` 的链接与摘要，仅新增链接无法满足单一事实源 | `IT-01` | IT-01 AC |
| 2026-07-29 | 一致性（codex review R6） | M0 完成条件改为「本地产物 + review PASS」（IT-01 是 `no-release`，其提交发生在 IT-02，原写法在 Phase 0 不可满足）；M2 回滚集补 IT-04 的 `docs/refactoring/test-infra.md` CI 登记 | `N/A` | 里程碑验收与回滚 |
| 2026-07-29 | 现状核对（codex review R7） | 四处锚点更正：`plan-long.md:178` 是 **SB-03** 的完成判据，PT-01 的完成判据实际在 `:200-204`，改锚（R8 复审发现依赖登记表的 DEP-02/DEP-03 两行当时**未同步**，已于 R8 修复轮补齐）；DEP-03 的完整 Mega SHA 锚点改为 `plan-long.md:30`（`:62` 只是缩写快照）；IT-11 的 `Current evidence` 删除对「IT-01 未来 AC」的引用，改为仓库现状锚点 | `N/A` | 与其它计划的关系、依赖登记表、IT-07、IT-11 |
| 2026-07-29 | 依赖闭合（G-06，codex review R7） | `DEP-09`（`.env.test` 的提供）实际是**每张会跑 ER-04 C 组的卡**都需要的前置，选择性登记在部分卡上不闭合。改为提升到「本计划的 ER-01 现状补记」作为计划级前置，各卡 `Dependencies` 不再逐张登记 | `全部实现卡` | ER-01 补记、依赖登记表、各卡 Dependencies 与 Granularity、粒度审计表 |
| 2026-07-29 | 规则冲突（G-07 / ER-08，codex review R7） | IT-09 原称「本计划唯一发布点」，与 IT-02/03/04/06/08/10/11/12 各自 `independent` 发布矛盾；版本判据「相对 `0.1.166` patch 递增」也与前序多次 bump 冲突。改为「IT-07 的收口承载发布卡」，版本判据改为「相对本卡进入发布窗口时 `Cargo.toml` 的权威版本 `patch +1`」 | `IT-09` | IT-09、发布分组与并发窗口、里程碑 M3 |
| 2026-07-29 | 粒度门禁（G-01 / G-03，codex review R7） | IT-10 拆出 IT-12：认证边界与「不存在仓库的失败路径 + liveness」是两个可独立恢复的轴，且合并后 AC 超上限。两卡在 `bin/tests/integration_git_cli.rs` 写集相交，已补 `IT-10 -> IT-12` 顺序边 | `IT-10 → IT-10, IT-12` | 实施顺序、并发声明、发布窗口顺序、Phase 1、追溯表、里程碑 M1、粒度审计表 |
| 2026-07-29 | 完整性（codex review R7） | IT-11 原描述承诺同时固定 git 与 git-lfs，但 AC/Verification 只覆盖 git。补齐 git-lfs 的 pin、实际版本断言与文档一致性守卫 | `IT-11` | IT-11 AC / Verification / Granularity |
| 2026-07-29 | 幽灵验收（G-05，codex review R7） | 八条守卫命令重写：统一加 `set -euo pipefail`、`source .env.test &&` 串联、`tee` 前置 `pipefail` 并单独检查测试退出码、服务停启用 `trap` 恢复并校验、`rg` 退出码 1 与 >1 显式区分、比较前先断言两侧非空、集合比较改为**双向**差集（IT-07 审计表逐字段与覆盖矩阵、IT-08 键集合、IT-09 allow 基线）、残留守卫改用 compose project label 与本次分配端口清单精确判定 | `IT-02, IT-03, IT-06, IT-07, IT-08, IT-09, IT-10, IT-11` | 各卡 Verification |
| 2026-07-29 | 粒度门禁（G-03，codex review R7） | 按模板的「独立 pass/fail 谓词」口径继续拆分含复合断言的 AC 行：IT-01 与 IT-07 到 `20/20`，IT-09 到 `11/12`；IT-04 的 `paths:` 判据改为「集合等于本卡 `Deliverables` 声明的固定 allowlist」这一单一集合相等谓词（allowlist 内容移入 `Deliverables`）；IT-08 的键可见性改为「声明键集合与子进程 `env` 可见键集合相等」单一谓词 | `IT-01, IT-04, IT-07, IT-08, IT-09` | 各卡 AC / Granularity、粒度审计表 |
| 2026-07-29 | 现状核对（codex review R8） | 三处证据更正：DEP-02 / DEP-03 两行的锚点在 R7 轮实际未同步，本轮补齐（`:200-204` 与 `:30`）；IT-03 的 `scripts/git_protocol_smoke.sh:275-277` 改述为「push/LFS 可选矩阵未开启时的 SKIP」，依赖缺失分支另锚到 `:33-35`、`:38-41`；IT-12 的 401 覆盖锚点改为单测 `src/contract/git_protocol/http.rs:444-449`，并说明「不存在仓库 clone」路径当前无覆盖 | `N/A` | 依赖登记表、IT-03、IT-12 |
| 2026-07-29 | 规则冲突（ER-04，codex review R8） | IT-09 的 `Verification` 删除 B 组聚合守卫、release note/兼容证据与 C 组版本 parity 门（三者由 ER-04 自动强制），只保留 crate 级 allow 基线这一本卡特有守卫，`VER` 由 `4/12` 改为 `1/12` | `IT-09` | IT-09、粒度审计表、追溯表 |
| 2026-07-29 | 幽灵验收（G-05，codex review R8） | 六条守卫再修：IT-02 的卷/网络守卫合为一次整体判定并逐个检查 docker 命令退出码（`VER` 5→4）；IT-03 的端口守卫区分 `pgrep`/`ss`/`rg` 的失败与零命中；IT-03 与 IT-06 的服务恢复失败改为计入守卫最终判定（不再被 `trap` 里的 fallback echo 吞掉）；IT-04 的两份 allowlist 改为在守卫命令内用 `printf` 内联构造，不再依赖预置的 `/tmp` 文件；IT-07 的覆盖矩阵守卫收窄到「只核对本计划新增的 target 与 focused 用例」并限定在覆盖矩阵区间内解析；IT-08 的 AC 与命令口径对齐为「声明集合 ⊆ 可见集合」「示例 ↔ allowlist 双向相等」「CI 键 ⊆ 示例键」 | `IT-02, IT-03, IT-04, IT-06, IT-07, IT-08` | 各卡 Verification、粒度审计表 |
| 2026-07-29 | 一致性（codex review R8） | IT-06 / IT-10 / IT-12 的 D 组不再宣称「IT-04 落地后变为适用」——这三卡排在 IT-04 之前，后续 workflow 变更只能是 IT-04 自身的 D 组证据，不得追溯改变前置卡已完成的 `Acceptance`；追溯表删除 IT-05 行以与「最终卡集 11 张」的声明一致；测试矩阵的 `integration_git_cli` 命令补回 `source .env.test` 与 `--test-threads=1`；本节自审与 ADR-IT-02 同步 IT-12 的存在 | `IT-06, IT-10, IT-12` | 各卡 D 组字段、追溯表、测试矩阵、评审结论、ADR-IT-02 |
| 2026-07-29 | 依赖闭合（G-05 / G-06，codex review R9） | IT-03 的 push 往返用例无法在缺少凭据链路时独立验收（receive-pack 强制认证，而 token 注入原本交给后继的 IT-10），依赖方向自相矛盾。改为：**IT-03 自带最小的 token seed 与 credential helper 注入 helper**（其 push AC 的前提），IT-10 复用该 helper、只承接 401 挑战头与凭据零泄漏断言，AC 由 `8/8` 降为 `5/8` | `IT-03, IT-10` | IT-03 Description 与 Current evidence、IT-10 Description / Out of scope / AC / Granularity、粒度审计表 |
| 2026-07-29 | 粒度门禁（G-03，codex review R9） | 两处 AC 复合断言按模板口径拆开：IT-08 的「键集合相等**且**覆盖 CI 三键」拆为两条（`6/8` → `7/8`）；IT-12 的「错误消息稳定**且**可断言」拆为两条（`4/8` → `5/8`） | `IT-08, IT-12` | 两卡 AC / Granularity、粒度审计表 |
| 2026-07-29 | 幽灵验收（G-05，codex review R9） | IT-08 的 CI 键提取范围收窄到「Run integration tests」步骤的 `env:` 块——原命令按缩进匹配全文件，会把 `ORBIT_CHECKOUT_TOKEN`（`config-validation.yml:42`）也算进来，导致守卫必然失败；IT-12 删除不成立的零命中审计命令（原 pattern 会命中 `integration_vault.rs` 的 secret 文案），改为用「`bin/tests/` 目录内容 + 脚本矩阵全为正断言」直接证明该路径无覆盖 | `IT-08, IT-12` | 两卡 Verification 与 Current evidence |
| 2026-07-29 | 一致性（codex review R9） | Phase 2 不再称 IT-09 执行「唯一发布点动作」，与「其余实现卡各自独立发布」对齐；热加载 spawn 锚点改为实际调用 `src/commands/service/mod.rs:44-49` 与 `tokio::spawn` `:131`；IT-09 的「无 artifact 上传」补独立零命中证据；IT-11 的现状改述为「只显式安装 git-lfs，git 用 runner 自带版本，两者均未 pin」 | `N/A` | 事实基线、Phase 2、IT-09、IT-11 |
| 2026-07-29 | 一致性（codex review R10） | 三处遗留自相矛盾清除：适用范围与 IT-03 的 `Out of scope`/`Security` 不再把 token 注入交给 IT-10（seed 与注入 helper 归 IT-03，IT-10 只做 401 挑战头与零泄漏审计）；GAP-02 的「计划动作」补齐 IT-11、IT-12；IT-05 关闭记录的 spawn 锚点改为 `service/mod.rs:44-49` 与 `:131` | `IT-03, IT-10` | 适用范围、当前缺口、IT-03、IT-05 关闭记录 |
| 2026-07-29 | 幽灵验收（G-05，codex review R10） | IT-10 与 IT-12 的守卫都会「跑整个既有 target」而不定向本卡新增用例，未实现也可能通过；IT-10 的 `rg` 默认跳过隐藏目录，证明不了 `.git/config` 无 token；IT-12 复用固定 `liveness.txt`，上一轮的 `200` 可被当轮当作证据。已修复：两卡的新增用例分别以 `integration_git_cli_auth_` 与 `integration_git_cli_failpath_` 前缀具名并在守卫中过滤运行；IT-10 追加 `--hidden --no-ignore -a` 扫描与逐个 clone 的 `.git/config` 检查；IT-12 改用每轮唯一的 `MONOENGINE_IT_LIVENESS_FILE`、运行前清空，并断言 `status=200 pid=<n>` 且该 PID 仍存活 | `IT-10, IT-12` | 两卡 Description 与 Verification |
| 2026-07-29 | 术语精确化（codex review R10） | IT-09 的「crate 级 `#[allow]`」改称「顶层 inner `#![allow(...)]`」：三处基线中只有 `src/lib.rs:1` 是 crate-level attribute，`src/vault/core.rs:1` 与 `src/vault/utils/seal.rs:1` 是子模块文件的 inner module attribute | `IT-09` | IT-09 AC / Verification / Current evidence |
| 2026-07-29 | 幽灵验收（G-05，codex review R11） | 五处假绿再修：IT-02 的健康守卫由 `rg '^git-cli .*healthy'` 改为 `docker inspect` 取 Health 状态并与 `healthy` **完全相等**比较（原正则同样匹配 `unhealthy`）；IT-03 的端口守卫改为在守卫内以每轮唯一的 `MONOENGINE_IT_PORTS_FILE` 运行并先删除，杜绝复用上一轮 `ports.txt`；IT-09 的 allow 守卫正则由 `[a-z_]+` 放宽为捕获任意 `#![allow(...)]` 语法（原正则漏掉 `clippy::`、多 lint 与含空格写法）；IT-10 的守卫改为断言**两个**具名用例均跑到 `ok`、工作目录每轮唯一、且必须存在真实 `.git/config` 才执行泄漏扫描（否则空目录会让零命中扫描变成空洞通过） | `IT-02, IT-03, IT-09, IT-10` | 四卡 Verification、IT-03 Description 与 AC 措辞 |
| 2026-07-29 | 一致性（codex review R11） | IT-02 的 D 组不再宣称「IT-04 落地后在后续卡中变为适用」——IT-02 排在 IT-04 之前，后续触发面扩展只产生 IT-04 自身的 D 组证据，口径与 IT-06 / IT-10 / IT-12 统一 | `IT-02` | IT-02 的 D 组字段 |
| 2026-07-29 | 幽灵验收（G-05，codex review R12） | 三处守卫再修：IT-03 的残留进程判定由 `pgrep -af "monoengine.*service http"` 改为按本轮 `MONOENGINE_IT_PIDS_FILE` 逐 PID `kill -0`（原写法会命中承载同样字符串的父 `bash -c`，干净状态也无法通过）；IT-10 的逐 `.git/config` 扫描补上 `else` 分支——原结构在 `if` 之后取 `$?` 得到的是 `if` 的返回值 0，会把期望的零命中误报为 `rg exit 0`；IT-12 的 liveness 改为**用例在 teardown 前**断言并写证据、shell 在测试退出后**反向**断言该 PID 已消失（原写法要求服务在 `cargo test` 退出后仍存活，与 IT-03 的零残留验收直接冲突，只有泄漏进程时才能通过） | `IT-03, IT-10, IT-12` | 三卡 Description / AC / Verification |
| 2026-07-29 | 契约对齐（codex review R12） | PT-01 完成判据（`docs/plan/plan-long.md:204`）只接受「落地 / 关闭（含理由）」，而本计划的成功定义、Phase 2 退出条件与 IT-07 的两条 AC 允许以「延后」结案，会出现「IT-07 可验收但 PT-01 未达标」。四处统一收紧为「落地 / 书面关闭（含理由与承接编号）」 | `IT-07` | 成功定义、Phase 2 退出条件、IT-07 AC、里程碑 M3 |
| 2026-07-29 | 一致性（codex review R12） | IT-07 新增一条 AC 与一条守卫：`plan-long.md:750` 的日期计划索引仍写 `IT-01..IT-07`，必须更新为最终 11 张卡集并注明 IT-05 已关闭，且以机械比对守卫防止再次漂移（`VER` 2/20 → 3/20）；IT-02 的全部 compose 命令统一加显式 `-p monoengine-it`，残留查询用同一项目名过滤，避免 `COMPOSE_PROJECT_NAME` 覆盖导致查错项目 | `IT-02, IT-07` | IT-02 Description 与 Verification、IT-07 AC / Verification / Granularity、粒度审计表 |
| 2026-07-29 | 回归修复（codex review R13） | **R12 修复引入的回归**：上一轮只给 IT-02 的 compose 命令加了 `-p monoengine-it`，而 ER-01 补记、IT-03、IT-06、测试矩阵与故障恢复矩阵仍用默认 project，会起出第二套占用相同端口的栈、并让 fail-closed 守卫停错容器。已把项目名统一为 `-p monoengine-it` 并写进 ER-01 补记 | `IT-02, IT-03, IT-06` | ER-01 补记、各卡 Verification、测试矩阵、故障恢复矩阵 |
| 2026-07-29 | 执行期（codex review IT-02 + ER-07） | ① 登记 `EX-01`（sign-off-only，vault unseal 不可用）；② `git-cli` 改 `profiles: ["git"]`，避免 host 网络进入默认 `up` 拖垮 Docker Desktop 数据面；③ IT-02 AC/VER 同步为 `--profile git`；④ 文档澄清 `monoengine-test-net` 固定名下两套 project 不可并存 | `IT-02` | waiver 表、IT-02、docker-compose.test.yml、test-infra.md |
| 2026-07-29 | 执行期（Linux-only） | 集成测试 / `git-cli` harness **目标 OS = Linux**；删除 Windows/macOS 宿主机 git 兼容路径与相关实现（`taskkill`、非 Linux `temp_dir`、自动非 Linux fallback）；宿主机 git 仅 `MONOENGINE_IT_ALLOW_HOST_GIT=1` 本地实验；同步 ADR-IT-01、test-infra / integration / IT-01–IT-03 措辞 | `IT-01, IT-02, IT-03` | ADR、checklist AC、风险表、实现与文档 |
| 2026-07-29 | 执行期（RustFS） | 测试栈 S3-compatible 对象存储由 MinIO 切换为 **RustFS**（`rustfs` / `rustfs-init`）；端口保持 `19000/19001`；初版曾沿用历史凭据字符串 `minioadmin`（已于 2026-07-30 改为 `rustfs_it` / `rustfs_it_secret`，见同日「文档/合同对齐」） | `IT-01, IT-02` 相关登记 | docker-compose.test.yml、test-infra.md、integration.md、plan、workflow |
| 2026-07-29 | 执行期（IT-04 / IT-11） | IT-04：CI 执行面接入 `integration_git_cli` 与 IT-06 focused 用例、allowlist A/B 触发面、git-cli mkdir+UID、`-p monoengine-it --profile git`；IT-11：宿主机 git `2.54.0` / git-lfs `3.7.1` pin 与 job summary | `IT-04, IT-11` | workflows、test-infra.md |
| 2026-07-29 | 执行期（IT-07） | P2 清单收口：3 项已落地、2 项书面关闭（`DEFER-IT-07` 全局 disabled vs system_required；`DEFER-IT-08` GCS gate）；删除 integration.md 内嵌 compose/CI 样例；`plan-long.md` 索引与状态同步；已发布实现卡 Lifecycle 回填；复审 P2：IT-12 重定义为保留根 failpath（`DEFER-IT-12`）、IT-04 双 workflow 执行面措辞、compose `-p monoengine-it` 全覆盖、事实校准/依赖矩阵/IT-01 VER 证据对齐 | `IT-07` | integration.md、plan-long.md、本文件 |
| 2026-07-30 | 执行期（D 组收口） | `git-protocol-smoke` 在 v0.1.174/175 因 `timeout-minutes: 30` 于 cargo-native gate 中取消；v0.1.177 前滚：`--release` 复用构建 + `timeout-minutes: 60`。远端绿：`config-validation` [30466831819](https://github.com/gitmono-dev/monoengine/actions/runs/30466831819)（v0.1.174）、`git-protocol-smoke` [30479301244](https://github.com/gitmono-dev/monoengine/actions/runs/30479301244)（v0.1.177，含 pin summary）。IT-03/IT-04/IT-11 → `done`/`complete`；`plan-long.md` PT-01 → 已完成 | `IT-03, IT-04, IT-11` | 本文件、plan-long.md、git-protocol-smoke.yml（已随 v0.1.177） |
| 2026-07-30 | 文档/合同对齐 | ① ADR-IT-01 与现行实现对齐：harness = compose `git-cli`（`2.49.1`），smoke 宿主机 git/lfs 已由 IT-11 pin（`2.54.0`/`3.7.1`），删除「版本不固定」过期表述；② RustFS 公开测试凭据由历史 `minioadmin` 改为 `rustfs_it` / `rustfs_it_secret`（compose、`.env.test.example`、smoke、test-infra、ER-11） | `N/A` | ADR-IT-01、test-infra.md、compose、`.env.test.example`、integration_vault、plan-template ER-11 |

## 已决议设计决策

实现时若需偏离本节，必须先修改计划并说明原因，不得在代码中静默改语义。

### ADR-IT-01: Git CLI 由 docker-compose 固定版本的 runner 容器提供

- **Status:** Accepted（2026-07-30 修订：与 IT-11 / 现行 CI 对齐——smoke 宿主机 git 已 pin；RustFS 凭据改为 `rustfs_it`）
- **Context:** 真实 Git CLI 矩阵需要版本确定的 `git` 客户端。cargo-native harness（`integration_git_cli`）以 compose `git-cli`（pin `git version 2.49.1`）为**唯一验收 runner**。CI 的 `git-protocol-smoke.yml` 另用宿主机 git / git-lfs 跑脚本矩阵，并由 IT-11 **固定**为 `2.54.0` / `3.7.1`（解析版本号断言 + job summary）；二者是不同入口，不得混用或互相冒充。
- **Decision:** 在 `docker-compose.test.yml` 增加 `git-cli` 服务，使用固定 tag（必要时固定 digest）的轻量镜像。两项机制必须显式实现：①**网络**——该服务需能访问测试进程在宿主 `127.0.0.1` 高位端口上的 monoengine 服务；`network_mode: host` 与文件现有的 `networks.default`（`docker-compose.test.yml` 末尾 `networks.default`）在同一服务上不可共存，服务定义必须单独声明网络模式并在规范中写明；②**工作目录**——静态声明的 compose 服务无法 bind-mount 测试进程运行时才创建的临时目录，因此挂载一个**固定共享宿主路径**（`${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}`），由用例在其下分配每用例子目录并负责清理。
- **Alternatives considered:** 宿主机 git（**不**作为 harness 验收路径；smoke 脚本入口已 pin，但仍与 compose pin 解耦。集成 harness 目标 OS 为 Linux，compose `git-cli` 是唯一验收 runner；宿主机 git 仅允许显式 `MONOENGINE_IT_ALLOW_HOST_GIT=1` 的 Linux 本地实验）；testcontainers.rs（新依赖，违反 integration.md「不要为了集成测试新增不必要依赖」，拒绝）；GitHub Actions `services:` 语法（与本地 `docker compose` 路径分裂，拒绝）。
- **Consequences:** 集成测试与 `git-cli` harness **只保证 Linux**；无 Windows/macOS 兼容验收路径。镜像 tag/digest 升级是显式 PR 动作；共享工作目录是跨用例可见的宿主路径，用例必须自行隔离子目录，不得假定独占。`profiles: ["git"]` 仍避免在非目标开发机上把 host 网络拉进默认栈。
- **Revisit when:** PT-04 需要 SSH 通道（sshd 拓扑）或矩阵规模显著扩大时。

### ADR-IT-02: Git CLI 黑盒用例放 `bin/tests/integration_git_cli.rs`，编排只用 std + 现有依赖

- **Status:** Accepted（2026-07-29 修订：`bin/tests/common/mod.rs` 已存在，取消「必要时抽取」的条件表述）
- **Context:** 黑盒进程测试的既有落点是 `bin/tests/`（`integration_vault.rs`），编排方式为 `std::process::Command` + `CARGO_BIN_EXE_monoengine`；共享 helper 模块 `bin/tests/common/mod.rs` 已存在（`write_bootstrap_config` / `write_full_config`）。
- **Decision:** 新建 `bin/tests/integration_git_cli.rs`，复用 `VaultCliEnv` / `isolated_command` 的编排模式，由测试进程启动 `service http`（高位端口、临时 `base_dir`/`cache_dir`、隔离 DB），在 **Linux** 上调用 compose `git-cli` 执行用例（宿主机 git 仅 `MONOENGINE_IT_ALLOW_HOST_GIT=1` 本地实验）。**本卡新增的 git-cli helper 落在 `bin/tests/common/git_cli.rs`，仅由 `integration_git_cli` 经 `#[path]` 引入**（避免编译进 `integration_vault`）；`common/mod.rs` 只保留指向说明。**不修改 `bin/tests/integration_vault.rs`**，该文件不进任何本计划卡的写集。
- **Alternatives considered:** 写入 `integration_vault.rs`（文件职责膨胀，拒绝）；引入 assert_cmd / pexpect 类依赖（非必要，拒绝）。
- **Consequences:** 新 target 的用例默认 `--test-threads=1` 串行（端口与 DB 资源）；`integration_vault` 作为回归门单独运行，用于证明 helper 追加未破坏既有用例；IT-03、IT-10 与 IT-12 共写该 target 文件，必须串行（`IT-03 -> IT-10 -> IT-12`）。
- **Revisit when:** PT-04 扩展 SSH / LFS 通道时重审编排抽象是否仍够用。

### ADR-IT-03: 多实例 outbox 竞争用例以端到端并发 dispatcher 投递形态落地

- **Status:** Accepted（2026-07-29 修订：Consequences 改为 ER-10 的 `FIX-*` 派生协议）
- **Context:** storage 层并发 claim 已有覆盖（`concurrent_claim_only_allows_one_sender`、独立连接变体、stale 恢复，`src/jupiter/storage/notification_storage.rs:1254`、`:1313`、`:1372`）；未覆盖的是两个完整 dispatcher 实例对同一 DB 的端到端投递竞争（重复发送风险）。真多进程黑盒成本高。
- **Decision:** 在模块集成测试层对同一测试 DB 运行两个完整 dispatcher 实例，并发执行 outbox 投递循环（真实 Mailpit），断言每封邮件恰好投递一次（无重复发送、无丢失），且 stale `sending` 恢复语义在并发下仍成立。
- **Alternatives considered:** 真多进程黑盒（留给 PT-09，DEFER-IT-03）；重复 storage 层 claim 测试（已有覆盖，无增量证据，拒绝）。
- **Consequences:** **若用例暴露 dispatcher 层重复发送缺陷，不得在 IT-06 内就地修生产代码**——按 ER-10 新建 `FIX-IT-06-01` 卡、加 `FIX-IT-06-01 -> IT-06` 依赖边、IT-06 置 `blocked`，修复卡自行承担版本动作。用例形态不覆盖进程级崩溃窗口。
- **Revisit when:** PT-09 落地真多实例部署时。

## 全局工程约束

以下约束对本文所有任务生效。任务条目不再逐条重复，违反任一项即视为任务未完成。

- **GC-01 现状核实前置:** 每个任务开工前重新核对计划、相关开发文档、当前代码和测试。如果已实现，则任务改为补测试、补文档、更新状态或关闭，不重复实现。
- **GC-02 单一事实源:** entity 定义、配置解析、API schema、权限策略、错误类型和共享 helper 必须有单一事实源。禁止 CLI handler、HTTP handler、migration 和测试 fixture 各自复制等价逻辑。
- **GC-03 Mega 目标项目与 monoengine 扩展边界:** 直接从 Mega 移植的代码表面必须标注来源和差异；monoengine-only 表面必须说明替代方案、用户影响和机器接口。
- **GC-04 输出与错误契约:** 用户可见错误使用 `MegaError` 稳定变体并同步 `docs/errors.md`。HTTP 状态码、JSON 响应、CLI 退出码和人读输出必须分别验收。注意 `MegaError` 目前**没有**数值错误码注册表，变体→HTTP 状态的映射只存在于 `src/common/errors/api.rs` 的转换实现里；改动该映射必须同时更新 `docs/errors.md` 或在卡内说明为何不需要。
- **GC-05 文档同步:** 命令、配置、HTTP API 或公开行为变化必须同步对应 `docs/` 下文档、`config/config.toml` 注释与 `README.md`。本仓 `docs/` 为中文单语，无 EN/zh 双份要求；OpenAPI 由 `utoipa` 在运行时聚合、**磁盘上没有落盘的 spec 文件**，因此 API schema 证据只能取自运行中的 `/api/openapi.json`。
- **GC-06 测试覆盖:** 新增 entity / storage / migration 必须附带 `#[cfg(test)] mod tests`，使用 `test_db_connection` + `apply_migrations` 集成测试。新增 CLI 子命令必须附解析测试，并覆盖 `builtin()` / `builtin_exec()` / `load_mode()` 三处注册。新增集成 test target 直接在 `bin/tests/<name>.rs` 建文件（cargo 自动发现，无需 `[[test]]` 声明），但必须同步本计划「测试矩阵」与 `docs/refactoring/integration.md` 的覆盖矩阵。
- **GC-07 安全默认值:** 未满足认证、授权（Cedar）、路径归属、schema 版本、对象闭包或 secret redaction 前置时默认 fail-closed。任何 fail-open 必须有显式用户选择、日志和测试。已知现状：Cedar guard 当前使用硬编码 permit-all 且 `EntityStore` 启动时为空，任何依赖「授权已生效」的验收判据都必须先验证该前提，不得假定。
- **GC-08 原子性与恢复:** 修改 DB 事务、redis 状态、对象存储、配置、vault secret 或发布状态时，必须定义事务边界、幂等键、崩溃窗口和回滚/前滚策略。
- **GC-09 并发与资源生命周期:** DB 连接池、redis 连接、文件句柄、异步任务队列和临时目录必须有释放/恢复语义；测试不得依赖未隔离的全局状态（改环境变量的测试必须使用 `src/config/testing.rs` 的 `env_lock` / `EnvVarGuard`）。
- **GC-10 性能预算:** HTTP 热路径、DB 查询、对象存储读写、Git 协议操作和后台任务不得引入无界扫描、无界内存或 N+1 DB/网络调用。需要时写出数据规模和断言。
- **GC-11 生产 panic 禁止:** 生产路径不得新增裸 `unwrap()`、`expect()`、`panic!()`；必须用 `MegaResult`、`anyhow::Context` 或领域错误返回可操作信息。
- **GC-12 精确提交:** 提交前只 `libra add <相关路径>`（本仓由 libra 管理，无 `.git`，任何 `git` 命令在本仓不可用），不得使用 `commit -a`。发现无关脏状态时保留并报告，不得清理、重置或混入提交。

## 执行检查必备需求（强制）

任一要求未满足，对应任务不得标记完成。条目使用稳定 ID，正文引用时用 ID 而不是序号，便于后续插入条目而不破坏交叉引用。

1. **ER-01 开工前安全检查:** 必须完成下列四项，缺一不可。
   - `libra status --short --branch`（`--branch` 才会输出 `## <branch>...<upstream>` 行；不带它只有文件状态），确认当前分支与计划指定分支一致、工作区脏状态、目标文件是否已有无关改动。若目标文件已有未确认用户改动，先报告并避免覆盖。已知现状：本仓 upstream 行可能显示 `## main...origin/main [gone]`，而 `origin/main` ref 实际存在，因此**不要**把 status 的 ahead/behind 当作推送判据。
   - 确认路径依赖 `../orbit` 已就位（`Cargo.toml` 的 `orbit-api` 与 `bin/Cargo.toml` 的 `orbit` 均为 path 依赖）。缺失时任何 `cargo` 命令都会在依赖解析阶段失败，必须先补齐 sibling checkout，不得把该失败当作代码缺陷排查。
   - 确认 `.env.test` 是否存在（仓库只提供 `.env.test.example`，`.env.test` 本身被忽略）。缺失时按 `AGENTS.md` 的规定停下来确认，**不得**静默降级为不 source 的 `cargo test --all`。
   - 确认需要的测试服务是否已启动：`docker compose -p monoengine-it -f docker-compose.test.yml up -d --wait`（Postgres `15432`、Redis `16379`、Mailpit `11025/18025`、RustFS `19000/19001`；RustFS 桶初始化需要额外的 `--profile init run --rm rustfs-init`；git-cli 需要 `--profile git`）。Postgres 缺失会让相关用例直接 panic 而不是跳过。
2. **ER-02 先核对后实现:** 刷新本任务相关源码锚点、文档锚点、测试 target 和外部参照 revision，再决定实现、补测、补文档、关闭或降级。
3. **ER-03 粒度门禁:** 开工前按粒度规则 `G-*` 逐条复核本任务卡，并逐字段核对该卡的 `Granularity` 摘要行。若核对后发现范围已扩大（新增行为轴、AC/Verification 超限、scope 升到 L、写集与其它在跑任务重叠），先修改计划拆卡再开工，不得在实现中静默扩张任务范围。
4. **ER-04 每卡验收门:** 门由 **A 表面 focused 门**（按实际改动的表面）+ **B 类型门**（按 `Task type`）+ **C 发布收口门**（覆盖要求对所有非延后卡生效，执行归属见下）+ **D 远端后置门**（有不可本地复现的 CI 语义时）四组组成，**所有适用行累加**，全部通过才算验收。权威口径分层：`AGENTS.md`「Required Checks Before Submitting Code Changes」的三门（`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all`）是**任何会提交的改动**的完成契约，模板不得削弱；`AGENTS.md` 另强制 `cargo build` 与 `cargo build --tests` 均 0 错误 0 警告。任务卡指定的 focused 用例是在此之上的**附加**门，用来证明本卡行为，不是三门的替代品。权威口径变更时必须同批同步 `AGENTS.md`、`docs/plan/README.md`，以及**采用本模板当前版本的计划**（存量计划按「模板版本与迁移政策」处理，不因本条被追认为违规）。

   **门不计入条目上限：** 本条列出的门是全局强制门，**不计入**任务卡 `Verification` 的 G-03 条目计数；`Verification` 只登记本卡特有的判据（指定用例、新增守卫、手工证据）。

   **两个正交状态字段（都在任务卡登记）:**
   - `Lifecycle`（执行生命周期）：`pending` | `in-progress` | `blocked`（ER-10 的越界故障置此值）| `done`。
   - `Acceptance`（验收状态）：空 | `locally-accepted` | `remote-pending`（仅当本卡有适用的 D 组远端后置门）| `complete`。与 `AGENTS.md` 的完成契约对齐 —— 任何改动只有三门全绿才算 done，因此：
     - `locally-accepted` = 本卡适用的 **A 组 + B 组**门已过，但本卡的 **C 组覆盖**（自行执行或从承接卡继承，见下）尚未取得。此状态下**不得**对外报告「完成 / done」。
     - `remote-pending` = A/B 已过且 C 组覆盖已取得（含一次三门全绿的运行，其被测树状态包含本卡最终变更），但本卡适用或继承的 **D 组**远端后置门尚未全绿。此状态同样**不得**报告完成。
     - `complete` = 本卡的 **A + B** 已过、**C 组覆盖**已取得、且适用或继承的 **D 组**已全绿。无 D 时 C 覆盖到手即可 `complete`；有 D（含继承的 D）时必须先经 `remote-pending`。
     - 唯一状态转移路径：A/B 通过 → `locally-accepted` → ER-05 review PASS → 取得 C 组覆盖 →（无 D：`complete`；有 D：`remote-pending` → D 全绿 → `complete`）。
   - 两者独立取值：`blocked` 卡的 `Acceptance` 可以已是 `locally-accepted` 甚至 `complete`（例如变更已被三门覆盖，但仍卡在外部前置）。`Lifecycle=done` 必须以 `Acceptance=complete` 为前提；`blocked` 必须先回到 `in-progress` 并完成剩余动作才能进入 `done`，**不允许**从 `blocked` 直接标 `done`。计划完成门另要求所有非延后任务都到 `done`（见「完成判据」）。`Granularity` 里的 `complete=yes` 是 G-02 的结构完整性判据，与本字段无关，不可混用。

   **C 组覆盖与执行归属（每张非延后卡都必须取得 C 覆盖，但不都自己执行）:**
   - **独立发布卡（`Release boundary = independent`）与发布点卡（`release` / `family release point`）**：自行执行完整 C 组门。
   - **`family child`**：不 bump、不构建发布产物、不推送，**继承**其家族唯一发布点的 C 覆盖——前提是该发布点的三门运行其被测树状态包含本子卡的最终变更；同时继承该发布点适用的 D 组。
   - **`no-release` 卡（`docs` / `audit` / `spike` / `handoff`）**：**继承**任务卡显式声明的承载发布点（或计划收口点）的 C 覆盖与其 D 组；该承载点必须在卡内写明 ID，不得留空。
   - 继承 D 组的卡同样要经过 `remote-pending`，直到被继承的 D 组证据全绿。
   - **不存在「用零命中守卫替代三门」的通道**——零命中守卫只用于证明这类卡未改代码，不改变其在取得 C 覆盖前仍是 `locally-accepted` 的事实。

   门分四组，全部适用者累加：**A 表面 focused 门**（按实际改动的表面，每个表面唯一命中一行，命中几行加几行）+ **B 类型门**（按 `Task type` 取一行）+ **C 发布收口门**（由会推送的卡执行，其覆盖可被 `family child` / `no-release` 卡继承）+ **D 远端后置门**（只在存在不可本地复现的 CI 语义时适用）。A/B/C 是本地可完成的门；D 只能在推送之后取得证据，因此**不阻塞** `locally-accepted` 与 ER-05 的 review 顺序。

   **A 表面 focused 门**（覆盖全部合法生产表面；`family child` 复用同一映射）：

   | 实际改动的表面 | focused 门 |
   |---|---|
   | `monoengine-core` 纯单元逻辑（测试写在 `src/**` 的 `#[cfg(test)]` 里） | `source .env.test && cargo test -p monoengine-core '<mod::path::tests>'` |
   | `monoengine-core` 需要真实 Postgres 的逻辑（storage / entity / migration / config secret） | 先起测试栈，再 `source .env.test && cargo test -p monoengine-core '<mod::path::tests>'`；用例必须走 `test_db_connection` + `apply_migrations` |
   | 进程级黑盒 / CLI 集成行为（`bin/tests/**`） | `source .env.test && cargo test -p monoengine --test <target> -- --test-threads=1 [<filter>]`（`-p monoengine` 不可省略：默认选中的是 `monoengine-core`，它没有集成 target） |
   | `bin/src/**`（composition root：orbit provider 注入、global allocator、`parse` 分发；本仓只有 `bin/src/main.rs`） | `source .env.test && cargo test -p monoengine --test <target> -- --test-threads=1`（经 `CARGO_BIN_EXE_monoengine` 黑盒覆盖真实二进制）**加** `cargo clippy -p monoengine --all-targets -- -D warnings`（不可省略：全局 clippy 门不覆盖 `bin` 包，见下） |
   | CLI 解析与注册（`src/cli.rs`、`src/commands/**`） | `cargo test -p monoengine-core 'cli::tests'` + `cargo test -p monoengine-core 'commands::'`；新增/改名子命令必须同时断言 `builtin()`、`builtin_exec()`、`load_mode()` 三处 |
   | HTTP API 路由 / handler / OpenAPI 注解（`src/api/**`、`src/server/http_server.rs`） | `cargo test -p monoengine-core 'api::'` + 启动服务后拉取 `/api/openapi.json` 的 sanitized 证据（无落盘 spec，只能取运行时输出） |
   | `src/callisto/**`、`src/jupiter/migration/**` | `migrations()` 注册列表已登记的断言 + `apply_migrations(&db, true)` 集成用例；若该迁移的 `down` 是 no-op，必须在 `Rollback mode` 写 `forward-only`，不得声称可回滚 |
   | `config/config.toml`、`src/config/**` | config 校验链本地等价：`cargo run -p monoengine -- --config config/config.toml config validate`，按改动追加 `config init --output <tmp> --force`、`config validate --deny-warnings`、`--profile <name> config validate --show-sources`，以及坏配置的非零退出与 secret 不泄漏断言 |
   | Git 协议 / LFS（`src/ceres/protocol/**`、`src/contract/git_protocol/**`、`src/ceres/lfs/**`、`src/api/router/lfs_router.rs`、`src/server/http_server.rs`） | `scripts/git_protocol_smoke.sh` 本地等价，完整前置以 `.github/workflows/git-protocol-smoke.yml` 现场读取为准（本格只给骨架，不是快照事实源）：① `docker compose -p monoengine-it -f docker-compose.test.yml up -d --wait postgres redis`；② `cargo build --release -p monoengine`；③ **起 `service http` 前必须覆盖数据面**——仓库默认 `config/config.toml` 指向 `postgres://localhost:5432/...` 与 `redis://127.0.0.1:6379`，与测试栈的 `15432` / `16379` 不符，用默认配置起服务必然连不上；须 `source .env.test` 或显式导出 `MEGA_DATABASE__DB_URL`（建议独立 smoke 库）、`MEGA_REDIS__URL`、`MEGA_BASE_DIR`，并按 workflow 用同一 `MEGA_BASE_DIR` 预置 `mail.password` secret（mail 启动路径 fail-closed，缺 secret 时进程直接退出且不绑定端口）；④ 起服务后轮询 `/api/openapi.json` 就绪；⑤ 只读用例（ls-remote / clone / fetch / protocol v2 / shallow / blob:none）匿名即可通过（`git.anonymous_access` 默认 `true`），此时 `MONOENGINE_HTTP_REPO_URL=http://127.0.0.1:9000/` 足够；⑥ **push / tag / LFS 用例需要鉴权**：receive-pack 无有效 Mono access token 一律 401，而脚本只能通过 URL 传凭据，因此必须先向 smoke 库 `access_token` 表播种一次性 token，并写成 `http://<user>:<token>@127.0.0.1:9000/`（token 按 ER-11 脱敏，不得进验收证据）；漏做这一步会让全部 push/tag/LFS 用例以 401 失败，**不得**判为协议实现回归；⑦ **LFS 卡必须同时设 `MONOENGINE_GIT_SMOKE_PUSH=1 MONOENGINE_GIT_SMOKE_LFS=1`**——LFS 矩阵嵌在 push 分支内，只设 `MONOENGINE_GIT_SMOKE_LFS=1` 时脚本只打印 `SKIP: HTTP LFS push/clone also requires MONOENGINE_GIT_SMOKE_PUSH=1` 且仍以退出码 0 结束，该 skip **不得**作为 LFS 证据（属「Verification 判定口径」禁止的幽灵验收）；LFS 验收必须断言输出出现 `PASS: HTTP LFS push and clone` 且末行 summary 为 `0 failed`，并预装 `git-lfs`（缺失直接判 FAIL） |
   | Cedar 策略与守卫（`src/contract/policy/**`） | 对应单元/集成用例 + 明确断言当前 permit-all 与空 `EntityStore` 前提是否被本卡改变 |
   | 仓库配置与 CI（`Cargo.toml` / `bin/Cargo.toml` 非版本行、`rustfmt.toml`、`docker-compose.test.yml`、`scripts/**`、`.github/workflows/**`） | 受影响 CI job 的本地等价命令，按下文「仓库配置与 CI 展开规则」现场提取；不可本地复现的部分归 D 组 |
   | 只改文档 / 索引（无代码、无配置） | 无表面 focused 门，只走 B 组的结构与链接门 |

   **Rust 行的修饰规则（不是独立表面）:** env 约束与线程约束是上面几条 Rust 行的**修饰条件**，不构成独立表面：先按测试归属唯一选中 `-p monoengine-core` 或 `-p monoengine --test <target>` 中的一行，再把该 target 实际需要的 env 变量与线程约束**并入同一条命令**，例如 `source .env.test && cargo test -p monoengine --test integration_vault -- --test-threads=1`（`source .env.test &&` 前缀不可省略）。当前工作区**没有任何 cargo feature**（两个包的 `[features]` 均不存在），因此 `--all-features` 不改变本仓编译内容，不要用它冒充一条独立的 focused 门；它只保留在 clippy 全局门里。

   **默认包选择（本仓事实，直接影响门的覆盖面）:** 根 `Cargo.toml` 同时是 workspace 根与 root package `monoengine-core`，且**没有** `default-members`，因此不带 `-p` 的 cargo 命令只选中 `monoengine-core`——`cargo build`、`cargo build --tests`、`cargo clippy --all-targets --all-features -- -D warnings` 均**不覆盖 `bin` 包**；只有 `cargo +nightly fmt --all --check` 与 `source .env.test && cargo test --all` 覆盖整个 workspace。所以改动 `bin/src/**` 的卡必须显式写 `-p monoengine`，否则该改动不会被任何 clippy 门检查（`bin/src/main.rs` 仍会被 `cargo test -p monoengine --test <target>`、`cargo run -p monoengine`、`cargo build --release -p monoengine` 编译，只是不会被 lint）。

   **不得**为了凑一条 Rust 命令而制造与本卡无关的用例；也不得因为「C 组已有全量测试」就跳过 A 组某个表面的行。

   **仓库配置与 CI 展开规则:** 受影响 job 必须**从本卡实际改动的那个 workflow 文件现场提取**。模板不保存 job→命令的映射快照（必然漂移）。两步：

   ① 列出 job，且必须限定在 `jobs:` 节点内（裸 `rg "^  [a-z...]:"` 会把 `on:` 下的 `push`/`pull_request`、`permissions:` 下的 `contents` 误报为 job）：

   ```bash
   awk '/^jobs:/{in_jobs=1; next} in_jobs && /^[^[:space:]]/{exit} in_jobs && /^  [A-Za-z0-9_-]+:/{print FNR ":" $0}' .github/workflows/<file>.yml
   ```

   ② 读取受影响 job 的**完整定义**——`permissions`、`strategy` / `matrix`、job 与 step 级 `env`、`if`、`working-directory`、`run`、`uses`、`with`——再据此写判据。只看 `run:` 不够：`claude-review.yml` 当前**没有任何 `run:` 步骤**，其行为完全由 action 定义。分流：
   - **可本地复现**的步骤 → 抄成命令进 A 组，保留其 `env`、`working-directory`、线程与 URL 约束。**依赖 GitHub checkout 的步骤不得照抄**（本仓无 `.git`）：`actions/checkout` 对应的本地事实是「当前工作树即 checkout」，私有 `orbit` 仓的 checkout 对应「sibling `../orbit` 已就位」（见 ER-01）。`${RUNNER_TEMP}` 换成本地临时目录，`>> "$GITHUB_ENV"` 换成同一 shell 会话内的 `export`。
   - **不可本地复现**的语义（`::add-mask::`、secret 存在性校验、私有仓 token checkout、`anthropics/claude-code-action`）→ 不得凭空造命令，归入下文 **D 组远端后置门**。

   参考：2026-07-29 快照下 `.github/workflows/` 只有三个文件、每个文件恰好一个 job——`claude-review.yml` → `claude-review-with-tracking`（仅 `issue_comment` / `pull_request_review*` / `issues` 事件触发，**不由 push 触发**，且 100% action-only）、`config-validation.yml` → `validate-config`、`git-protocol-smoke.yml` → `git-protocol-smoke`（后两者均为 `pull_request` + `push: branches:[main]`，且都带 `paths:` 过滤器）。仅核对日快照，判定一律以现场读取的 workflow 文件为准。

   **B 类型门**（按 `Task type` 取一行）：

   | Task type | 类型门 |
   |---|---|
   | `implementation` / `migration` / `removal` | 无额外类型门（由 A 组 + C 组构成完整验收；`family child` 自跑 A 组 + fmt/clippy，C 覆盖继承自家族唯一发布点） |
   | `docs` / `audit` / `handoff` | 结构与链接门：本卡产物文件存在且章节完整、内部链接与 `file:line` 锚点可解析、新引入的 `docs/*.md` 路径全部真实存在（本仓已有多处悬空文档引用，不得新增）、`libra status --short --branch` 无越界改动。这三类**必须保持 no-code / no-config**：一旦发现需要改动代码或配置，不得「就地升级门」，必须先按 ER-03 把卡重分类为 `implementation` / `migration` / `removal`，同步 `Release boundary`、`Version increment`、`Release write set`，重跑粒度门后再执行 |
   | `spike` | 产物门：结论文档或 ADR 已落盘、go/no-go 已判定、承接卡已登记；**allowlist diff 门**——`libra status --short --branch` 的全部变更必须落在本卡 `Deliverables` 声明的产物内，且生产表面零改动（至少覆盖 `src/**`、`bin/src/**`、`bin/tests/**`、`config/**`、`scripts/**`、`Cargo.toml`、`bin/Cargo.toml`、`Cargo.lock`、`rustfmt.toml`、`docker-compose.test.yml`、`.github/workflows/**`），用「Verification 判定口径」的退出码模板逐条守卫 |
   | `release` | 聚合守卫（本组引入的全部新守卫用例）+ release note / 兼容证据 |

   **C 发布收口门（由会推送的卡执行，顺序强制）:** ① 版本面 parity 预检（ER-08）→ ② 按 `Version increment` bump **两处** `version`（`Cargo.toml` 的 `monoengine-core`、`bin/Cargo.toml` 的 `monoengine`）+ 让工具链刷新 `Cargo.lock` 的两个对应条目（不手改）→ ③ 在**已 bump 的状态**上跑 `AGENTS.md` 三门（fmt、clippy、`source .env.test && cargo test --all`）→ ④ `cargo build` 与 `cargo build --tests` 均 0 错误 0 警告；需要可执行产物时另跑 `cargo build --release -p monoengine` → ⑤ `libra add <相关路径>` + `libra commit -s -m`（ER-07 的签名预检与提交后校验）→ ⑥ 推送并确认 branch ref：`libra push origin main` 成功且远端 ref 已更新 → ⑦ **tag 与 release artifact：默认 `N/A`**。本仓当前**没有任何 release 流水线**：三个 workflow 都不由 tag 触发，没有 artifact 上传，仓库也没有任何已存在的 tag。因此不得凭空写「推 tag 触发发布」；确需产出用户可获取产物时，必须先用独立卡引入 release workflow 并同步修订本模板。

   三门必须覆盖 bump 后的最终状态——bump 与 `Cargo.lock` 刷新本身可能引入格式、lint 或编译回归，`cargo build` 不能替代 clippy 与全量测试。

   **C / D 边界（唯一口径）:** C 组**截止到已验证的 branch 推送**；推送之后由远端流水线产生的一切证据全部归 **D 组**。任务卡必须为每个 D 组项登记：workflow 文件、job 名、**触发事件与 ref**、**paths 过滤器是否命中本卡改动**、以及判据。

   **D 远端后置门（post-push，仅在有不可本地复现的 CI 语义时适用）:**
   - 本仓 D 组的实际内容是：`config-validation.yml` 与 `git-protocol-smoke.yml` 在 `push: branches:[main]` 上的远端结论——**当且仅当本卡改动命中各自的 `paths:` 过滤器**；以及这两个 workflow 中本地无法复现的部分（私有 `gitmono-dev/orbit` token checkout、`::add-mask::` 日志脱敏、secret 存在性校验）。
   - `claude-review.yml` **不由 push 触发**（只响应 issue/PR 评论与 review 事件），因此直推 `main` 的卡不得把它登记为 D 组项；只有走 PR 流程的卡才可能命中。
   - 若本卡改动不命中任何 `paths:` 过滤器，D 组写 `N/A` 并说明「改动路径不在两个 workflow 的 paths 列表内」，不得虚构一个远端门。
   - D 组**不属于**本地验收，不阻塞 `locally-accepted`，也不改变 ER-05「先本地验收再 review」与 C 组「review 通过后才提交推送」的顺序。
   - 只有当适用的 D 组门也全绿，该卡的 `Acceptance` 才能到 `complete`；此前停在中间态 `remote-pending`。
   - D 组失败一律**前滚修复**（新提交 / 新版本），不得回退已推送提交；修复卡按 ER-10 的越界规则处理。

   「完成判据」的计划级门是最后一次总检查，不替代每张会推送的卡各自跑过的发布收口门。
5. **ER-05 代码 review 闭环:** 实现和本地验收完成后进行代码 review；review 问题修复后重跑相关验收，直到 review 明确给出 `PASS`。P0/P1 必须关闭，不得以「residual risk 已接受」替代 `PASS`（仅 P2 可由具名责任人书面接受）。
6. **ER-06 文档与兼容同步:** 涉及公开行为的任务必须同步用户文档、开发文档、`config/config.toml` 示例、错误契约、运行时 OpenAPI 证据和测试矩阵。
7. **ER-07 Libra-native 工作流与提交签名:** 本仓库使用 Libra 工作流：`libra status`、`libra add <相关路径>`、`libra commit -s -m "<scope>: <summary>"`、`libra push origin main`。不要把仓库当普通 Git 仓库处理——本仓没有 `.git`，任何 `git` 命令都会失败。
   - `libra commit` **没有** `-S`（Libra 未暴露 Git 的正向 `-S`/`--gpg-sign`），`-s` 只添加 `Signed-off-by`。签名策略优先级为 `--no-gpg-sign`（最高）> `commit.gpgSign`（Git 级联，`false` 直接关闭签名）> `vault.signing` 默认（为 `true` 且 vault unseal key 可用时签名）。
   - 预检必须**先读 `commit.gpgSign`**（`libra config get commit.gpgSign`），未设置时再回退 `libra config get vault.signing`；`commit.gpgSign=false` 时不得当作「已启用签名」。
   - 每次提交后强制校验：`libra cat-file -p HEAD | rg -q '^gpgsig'`。校验失败不得推送。
   - **本仓已知现状（2026-07-29 核对）:** `commit.gpgSign` 未设置、`vault.signing=true`，但近期历史提交**均不带 `gpgsig` 头**。这说明「配置为签名」与「实际产生签名」之间存在缺口。首次执行本条时若校验失败，必须先查明原因（vault 未 unseal、签名密钥不可用等）并记录，不得静默跳过；在查明修复前若确需以 sign-off-only 方式提交，必须在「字段全局默认与例外」的 waiver 表登记 `豁免项 = ER-07 签名要求` 的 `EX-*`（含 Approver、Review round、证据、有效期）。「事实基线」最多链接该 `EX-*`，不构成独立授权路径。
   - **与 `README.md` / `AGENTS.md` 的已知漂移及优先级（必须按此执行）:** `README.md` 的 Contributing 一节写的是「Sign your commits (`git commit -s -S …`)」——该命令在本仓**双重不可执行**（没有 `.git`；`libra commit` 也没有 `-S`）。`AGENTS.md` 则**完全没有**提交/分支/签名指引，同时含若干过期事实（把本仓描述为单一 binary crate、`src/main.rs`、`src/common/config/loader.rs`、`cargo test` 作为全量测试、子命令 executor 签名）。计划执行时**以 ER-07 + GC-12 为准**：`libra add <相关路径>` → `libra commit -s -m` →（按上三条做签名预检与提交后 `gpgsig` 校验）。这些漂移应作为独立的文档修复项承接，不得在计划执行中两套并行。
   - 提交信息沿用本仓已观察到的两种既有风格：发布类改动用 `v<version>: <summary>`；文档/测试/CI 类改动用 `<type>(<scope>): <summary>`。
8. **ER-08 版本与发布:** 版本权威源是根 `Cargo.toml` 的 `version`（包 `monoengine-core`）。发布前先做版本面 parity 预检：`Cargo.toml` 与 `bin/Cargo.toml`（包 `monoengine`）的 `version` 必须一致；**不一致时先建立修复卡对齐，禁止直接 bump**。对齐后按任务卡的 `Version increment` 递增并同步这两处，让工具链刷新 `Cargo.lock` 中两个包的版本条目，其余步骤与顺序按 ER-04 的「发布收口门」执行（bump 后必须重跑三门）。开工时须重新核对版本面文件数量是否仍为两处。
   - `Version increment` 取值：`patch`（默认）| `minor` | `major` | `N/A`。
   - 删除公开 surface、破坏兼容的 schema/协议变更必须用 `minor` 或 `major`，且递增级别由 ADR + 兼容窗口证据决定，不得用 patch 夹带；家族卡（G-08）的递增级别写在唯一发布点卡上，子卡为 `N/A`。
   - `docs` / `audit` / `spike` / `handoff` 卡为 `N/A`，但必须说明产物随哪次提交进入仓库。
   - 本仓没有安装脚本、没有 release artifact、没有 tag 自动化，因此「安装证据」「artifact 可获取性」默认写 `N/A`；不得照抄其它仓库的发布步骤。
9. **ER-09 push 失败策略:** 非 fast-forward 需要 pull/merge 后重新验收再推；认证、权限、网络或服务端失败不 blind retry，记录原因，待下一次修复/发布窗口处理。
10. **ER-10 内部服务错误（有界重试）:** Redis、Postgres、对象存储、SMTP、AI provider 等错误不得直接把任务宣告完成。先分类：确定性错误（4xx 参数/权限、schema 不符、编译或配置缺陷）不重试，按范围决定归属——只有当修复落在**本卡行为轴内**，且先更新本卡 `Acceptance criteria` / `Verification` / `Implementation write set` / `Granularity` 后**重跑 ER-03 的全部 `G-*` 仍然通过**（含 G-10 写集不与在跑卡新冲突）时，才作为本卡修复项就地修；任一条不满足就新建修复卡 `FIX-*`、加一条 `FIX-* -> 当前卡` 的依赖边，并把当前卡置为 `blocked`，不得为了「顺手修完」突破粒度；暂时性错误（超时、5xx、限流、网络中断）按指数退避重试，并写明**最大尝试次数与总时间预算**（计划未另行规定时默认 ≤ 5 次、总计 ≤ 30 分钟）。超预算后把任务置为 `blocked` 并记录 sanitized 证据与升级对象，不得静默空转。发布类动作（push）不自动重试，按 ER-09 处理。
11. **ER-11 证据卫生:** 验收证据不得保存 secret、API key、token、PII、未脱敏 transcript、绝对私有路径或原始 tool payload。需要留存时只写 sanitized summary。测试栈的口令（如 `mono_test_password`、`smtp-test-password`、RustFS 测试凭据 `rustfs_it` / `rustfs_it_secret`）虽为公开测试值，记录时同样按脱敏处理。
12. **ER-12 并发边界与串行发布:** 并发只适用于**实现与 review 阶段**：只有 `Implementation write set` 不相交（G-10）的卡可以并发推进。**发布动作一律串行，且由单一发布者执行**：
    - 计划必须在「发布分组与并发窗口」声明发布者（哪个 Agent/人负责 C 组的 bump、构建、提交、推送，以及推送后跟踪 D 组远端证据）。同一时刻只允许一个卡处于「已 bump 未完成推送」状态。
    - 进入发布前重新读取 `Cargo.toml` 权威版本（ER-08 的 parity 预检），按顺序做完整套发布动作后才轮到下一张卡。
    - **禁止多 Agent 并发发布。** 本仓库当前**没有**仓库级发布锁：`libra push origin main` 推送的是本地 `main` 的整个 ref tip，无法只发布一条协调记录，也无法在 push 之外提供 CAS 仲裁；靠纯文档约定实现的 lease 无法验证，属于未经实现验证的协议。若某计划确实需要并发发布，必须先用独立 ADR + 独立计划落地一个仓库级发布锁（含原子认领、fence 校验、超时回收、崩溃恢复与测试），并在本计划以 `DEFER-*` 登记；在该机制落地并通过验收之前，一律按本条串行执行。
    - 并发实现期间仍受 I–R 约束（G-10）：发布者持有发布窗口时，其它卡不得修改 `Release write set` 内的文件。

### 本计划的 ER-01 现状补记（核对日 2026-07-29）

- ER-01 子项 2（`../orbit` 路径依赖）**当前不满足**：sibling checkout 不存在，`cargo metadata --no-deps` 以 101 退出（`failed to load manifest for workspace member .../bin`）。这是依赖解析失败，不是代码缺陷；开工前必须按 DEP-01 补齐，否则本计划全部 Verification 命令不可执行。
- ER-01 子项 3（`.env.test`）**当前不满足**：仓库只提供 `.env.test.example`，本地 `.env.test` 不存在。按 `AGENTS.md:102` 停下来确认，不得静默生成、不得降级为不 source 的 `cargo test --all`。该文件的提供是**计划级外部前置 DEP-09**：本计划每一张会执行 ER-04 C 组（`source .env.test && cargo test --all`）的卡都依赖它，因此不在各卡 `Dependencies` 中逐张登记，统一由本条与 Phase 0 进入条件约束。示例文件本身的缺陷由 IT-08 修复，修复后须按 DEP-09 重新生成 `.env.test`。
- ER-01 子项 4（测试服务）：**本计划的全部 compose 命令统一使用显式项目名 `-p monoengine-it`**（`docker compose -p monoengine-it -f docker-compose.test.yml ...`），残留查询按同一项目名做 label 过滤；不得依赖默认项目名或 `COMPOSE_PROJECT_NAME`，否则会起第二套占用相同端口的栈、并让 fail-closed 守卫停错容器。服务集为 postgres `15432`、redis `16379`、mailpit `11025/18025`、rustfs `19000/19001`（桶初始化需 `--profile init run --rm rustfs-init`），IT-02 落地后追加 git-cli。

## 实施顺序

依赖边格式：`A -> B` 表示 A 必须先于 B。依赖图必须无环，且每条边都指向具体任务卡而不是整个 Phase（G-06）；任务卡拆分后本节必须同步更新。

- `IT-01 -> IT-02`
- `IT-02 -> IT-03`
- `IT-08 -> IT-03`
- `IT-03 -> IT-10`
- `IT-08 -> IT-06`
- `IT-03 -> IT-06`
- `IT-06 -> IT-04`
- `IT-10 -> IT-12`
- `IT-12 -> IT-04`
- `IT-04 -> IT-11`
- `IT-01 -> IT-07`
- `IT-11 -> IT-07`
- `IT-06 -> IT-07`
- `IT-07 -> IT-09`

`IT-05` 已关闭（见「修订历史」），其原有边 `IT-01 -> IT-05` 与 `IT-05 -> IT-07` 一并移除。旧的多源边 `IT-04、IT-05、IT-06 -> IT-07` 不符合 `A -> B` 单边格式，已拆为独立边。

写集串行化说明（G-10）：`docs/refactoring/test-infra.md` 的四个写者（IT-01、IT-02、IT-04、IT-11）由 `IT-01 -> IT-02 -> IT-03 -> IT-10 -> IT-12 -> IT-04 -> IT-11` 链串行；`docs/refactoring/integration.md` 的四个写者（IT-01、IT-03、IT-06、IT-07）由 `IT-01 -> IT-02 -> IT-03 -> IT-06 -> IT-07` 链串行；`bin/tests/integration_git_cli.rs` 的三个写者由 `IT-03 -> IT-10 -> IT-12` 串行；`.github/workflows/git-protocol-smoke.yml` 的两个写者由 `IT-04 -> IT-11` 串行。

### 依赖登记表

本计划外的一切依赖关系（其它日期计划、外部服务、人工审批、上游 revision），以及本计划向外移交的范围，都必须在此登记后才能被任务卡引用（G-06）。计划内任务之间的依赖直接写任务 ID，不进本表。

`direction` 区分方向：`incoming` = 本计划等待外部产物；`outgoing` = 本计划把范围移交给别的计划（此时 Owner 是接收方，「超时与失败策略」写接收方未接手时的回落处理）。

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-01 | `incoming` | 工作区前置 | sibling `../orbit` checkout（`orbit` / `orbit-api` 路径依赖） | porrychen | 两处路径依赖可解析；判据 = `cargo metadata --no-deps` 退出码 0 | `Cargo.toml:71`、`bin/Cargo.toml:18-19`；2026-07-29 核对：sibling 目录不存在，退出码 101 | **阻断全部 cargo 门**。未就位前所有实现卡停在 `blocked`；不得改用不 source 的 `cargo test`、不得跳过门、不得把该失败当作代码缺陷排查 |
| DEP-09 | `incoming` | 审批 / 人工前置 | 本地 `.env.test` 的提供（仓库忽略该文件，`AGENTS.md:102` 要求缺失时停下来确认）。**本计划内没有任何任务卡创建该文件**——IT-08 只修正 `.env.test.example` 的格式与键集合，不产出 `.env.test` | porrychen | 文件存在，且 `source .env.test` 后 `MEGA_DATABASE__DB_URL`、`MEGA_REDIS__URL`、`MAILPIT_API_URL` 在子进程可见 | `AGENTS.md:95`、`:98`、`:102`；`.env.test.example:1-3` | **这是计划级前置**：每一张会执行 ER-04 C 组的卡都依赖它，因此不在各卡 `Dependencies` 中逐张登记；未提供前全部实现卡与 `release` 卡停在 `blocked`；**不得**由任务静默生成该文件。IT-08 完成后必须按新示例重新生成一次并复核本判据 |
| DEP-02 | `incoming` | 跨计划 | `plan-long.md` PT-01（本计划承接其落地） | plan-long 维护者 | PT-01 完成判据文本稳定可逐条引用 | `docs/plan/plan-long.md:103`（PT-01 行）、`:200-204`（PT-01 完成判据；注意 `:178` 是上一节 SB-03 的完成判据，不可混用）、`:744`（日期计划索引节） | 判据变更时 IT-07 必须按新判据重新对照；不得按旧判据宣告 PT-01 完成 |
| DEP-03 | `incoming` | 上游 revision | Mega 目标项目 `main` @ `d2b6d1c314ab750a06fd96cff85559cd685907ea` | 移植参照（只读） | pinned revision 源码可读 | `docs/plan/plan-long.md:30`（完整 SHA，权威锚点）；`:62` 仅为审计快照记录，其中只有缩写 `d2b6d1c3`（核对日 2026-07-27） | revision 漂移时只更新引用与核对日；对 Mega 的历史描述不得追认为已实现证据 |
| DEP-04 | `incoming` | 事实源 | `docs/refactoring/integration.md`、`docs/refactoring/protocol.md` | 文档维护者 | 被引用章节锚点可解析 | `docs/refactoring/integration.md:111-115`（测试分层→test-infra 链接）、`:354-364`（P2 收口表）、`:504-520`（覆盖矩阵）；`docs/refactoring/protocol.md:789` | 锚点漂移时开工日刷新（ER-02），不得沿用过期行号作为证据 |
| DEP-05 | `outgoing` | 跨计划 | SSH / LFS 通道纳入 `bin/tests/` 分层与 CI 必跑（DEFER-IT-01、DEFER-IT-02） | `plan-long.md` PT-04 | PT-04 立卡并写明承接范围 | `docs/plan/plan-long.md:106`（PT-04 行） | PT-04 未接手时保持现状脚本形态（`scripts/git_protocol_smoke.sh`），本计划不重做、也不把脚本覆盖记为分层覆盖 |
| DEP-06 | `outgoing` | 跨计划 | 真多进程投递竞争矩阵（DEFER-IT-03） | `plan-long.md` PT-09 | PT-09 立卡并写明承接范围 | `docs/plan/plan-long.md:111`（PT-09 行，已列「多实例黑盒矩阵缺失」） | 未接手时缺口保持登记状态，不得降级为「已覆盖」 |
| DEP-07 | `outgoing` | 跨计划 | orion-server / fake webhook receiver 拓扑接入（DEFER-IT-04） | `plan-long.md` PT-06、PT-09 | 对应功能落地时按 IT-01 规范登记服务 | `docs/plan/plan-long.md:108`（PT-06 行）、`:111`（PT-09 行） | 未接手时规范空转但不失效；后续接入仍必须走 `test-infra.md` 登记，不得绕行 |
| DEP-08 | `incoming` | 外部服务 | git-cli 容器镜像固定 tag/digest 的可拉取性 | 镜像上游 | `docker compose -p monoengine-it -f docker-compose.test.yml pull git-cli` 成功且容器内 `git --version` 等于固定值 | IT-02 开工日记录镜像名与 digest（sanitized） | 拉取失败按 ER-10 有界重试（≤5 次、总计 ≤30 分钟）；超预算把 IT-02 置 `blocked` 并记录原因，不得改用漂移版本或宿主机 git 冒充 |

### 发布分组与并发窗口

默认每张卡独立发布（G-07）。只有需要合并发布或需要显式并发/串行窗口时才登记本表；登记项必须在任务卡 `Release boundary` 中被引用。

| ID | 成员 | 唯一发布点 | 窗口规则 | 失败回滚顺序 | 理由 |
|---|---|---|---|---|---|
| — | — | — | — | — | 本计划**无** `REL-*` 登记：每张实现卡都是独立发布切片（G-07 默认），不存在必须同时上线的不可分割变更。两张 `docs` 卡（IT-01、IT-07）与 `release` 卡（IT-09）之间是 `C/D coverage from` 的承载关系，不是家族卡（G-08） |

**并发声明:** 只有 `{IT-01, IT-08}` 可并发（实现写集不相交）；其余全部串行。所有 I–I 相交均已由依赖边消解：`docs/refactoring/test-infra.md`（IT-01 → IT-02 → IT-04 → IT-11）、`docs/refactoring/integration.md`（IT-01 → IT-03 → IT-06 → IT-07）、`bin/tests/integration_git_cli.rs`（IT-03 → IT-10 → IT-12）、`.github/workflows/git-protocol-smoke.yml`（IT-04 → IT-11）。（G-10）

**发布者:** `porrychen`（唯一发布者，负责 C 组的 bump、构建、提交、推送，以及推送后跟踪 D 组远端证据。更换执行者必须先更新本行再发布）。（ER-12：发布一律串行，禁止多 Agent 并发发布）

**发布窗口顺序:** `IT-08` → `IT-01`（`no-release`，产物随 IT-02 提交）→ `IT-02` → `IT-03` → `IT-10` → `IT-12` → `IT-06` → `IT-04` → `IT-11` → `IT-07`（`no-release`，产物随 IT-09 提交）→ `IT-09`。同一时刻只允许一张卡处于「已 bump 未完成推送」状态。

并发执行时另需满足：实现写集不相交（G-10）。

### Phase 0: 规范与环境冻结

**目标:** 建立测试基建规范的单一事实源（IT-01），并修复 `.env.test.example` 使所有 `source .env.test` 门真正生效（IT-08）。

**进入条件:**

- DEP-01 已就位（`cargo metadata --no-deps` 退出码 0）——当前不满足，未补齐前本 Phase 不得开工。
- DEP-09 已就位：使用者已确认并提供本地 `.env.test`（可先按现状示例生成；IT-08 完成后按新示例重新生成一次）。
- `libra status --short --branch` 确认分支与工作区脏状态，目标文件无未确认的用户改动。

**退出条件:**

- IT-01 产物 `docs/refactoring/test-infra.md` 已落盘、B 组结构与链接门通过、review 结论为 `PASS`（其 `Acceptance` 停在 `locally-accepted`，`complete` 由 IT-02 的 C 组覆盖提供）。
- IT-08 `Acceptance=complete`，且按新示例重新生成的 `.env.test` 经 `source` 后变量在子进程可见。

### Phase 1: Git CLI runner 示范与 cargo-native 最小矩阵

**目标:** 按规范落地首个受登记约束的扩展服务（git-cli runner，IT-02），交付 `bin/tests/integration_git_cli.rs` 的往返一致性与隔离 harness（IT-03），追加认证边界断言（IT-10），再追加不存在仓库的失败路径与 liveness 断言（IT-12）。

**进入条件:**

- IT-01 产物已落盘且 review `PASS`；IT-08 `Acceptance=complete`。
- ADR-IT-01 的共享工作目录与网络模式修订已确认可实现。

**退出条件:**

- git-cli 服务健康、版本被断言、已按 checklist 登记；`down -v` 后零残留。
- `integration_git_cli` 本地绿（含 IT-10 的认证用例与 IT-12 的失败路径用例），且 `integration_vault` 回归绿。

### Phase 2: 并发投递基线、CI 收口与发布

**目标:** 落地并发 dispatcher 端到端投递基线（IT-06），把本计划的新集成 target 与 core focused 用例接入 CI 的执行面与触发面（IT-04），固定 git 客户端版本（IT-11），收口 integration.md 与 plan-long（IT-07），由 IT-09 作为 IT-07 的收口承载发布卡完成最后一个发布切片（本计划其余实现卡各自独立发布，IT-09 不是它们的发布点）。

**进入条件:**

- IT-06 需 IT-03；IT-04 需 IT-12 与 IT-06；IT-11 需 IT-04；IT-07 需 IT-01、IT-11、IT-06；IT-09 需 IT-07。计划级前置 DEP-01 与 DEP-09 对全部卡生效。

**退出条件:**

- 新集成 target 与 core focused 用例在 CI 执行面内，触发面覆盖其写集，且 git 客户端版本已固定；两个 workflow 的 job 均绿（D 组证据）。
- integration.md P2 清单每项有「落地 / 书面关闭（含理由与承接编号）」结论（PT-01 完成判据不接受「延后」），覆盖矩阵与实际 target 一致。
- PT-01 完成判据逐条满足，`plan-long.md` 状态更新；IT-09 推送完成且远端 ref 已更新。

## 任务卡

任务 ID 使用稳定前缀 `IT`。编号被引用后不重排；拆分出的新卡在所属 Phase 末尾追加新编号，因此**编号顺序 ≠ 执行顺序**，执行以「实施顺序」的依赖边和各卡 `Dependencies` 为准。废弃的编号保留并标记替代关系。

### 任务卡粒度规则（强制）

粒度是任务卡质量的第一判据：卡过大则无法 review、无法回滚、无法交给单个 Agent 完成；卡过碎则实现、测试与文档脱节，且每片都要付一次发布成本。新增或修改任务卡时必须逐条满足下列 `G-*` 规则。任一条不满足即为「粒度不合格」，必须在开工前拆分或合并，并同步实施顺序、依赖登记表、发布分组、追溯表、测试矩阵、里程碑、风险表的任务归属和修订历史。

- **G-01 单一行为轴与恢复模式（上限）:** 一张卡只承担一个**可独立恢复**的行为轴——该卡失败或需要撤回时，存在**单一已声明的恢复动作**，执行后系统停在一个自洽状态，不留半吊子中间态。这里的「恢复」不等于「一次 revert」：不可逆变更同样合格，只要恢复路径是单一且已写明的。禁止把「schema 变更 + 存储层接入 + API 暴露」「删 A + 删 B + 删 C」「新增能力 + 顺带重构既有实现」压进同一张卡。快速判据：`Description` 中出现两个以上并列的「并且 / 同时 / 顺带 / 以及」，先按「推荐拆分维度」拆。恢复形式必须在 `Rollback mode` 字段声明为四种模式之一：
  - `revert`：纯本地代码/文档变更，一次 revert 即完整撤销（默认）。
  - `forward-only`：已产生不可逆数据或迁移（Postgres schema、对象存储、vault secret），只能前滚修复；必须写出数据不变量、恢复验证命令和用户影响，**不得**为了凑「可 revert」而设计不安全的 down migration。注意本仓有相当一部分既有迁移的 `down` 是空实现（开工时以实际迁移文件为准），且运行期没有任何 `migrate down` 入口——`src/jupiter/migration/runner.rs` 只暴露 `Migrator::up` 与破坏性的 `Migrator::refresh`。因此涉及既有迁移的卡默认按 `forward-only` 处理，除非本卡自己实现并验证了真实的 `down`。
  - `compensating`：对外部服务已有副作用（远端写入、邮件发送、外部对象删除），撤销靠补偿动作；必须写出补偿命令与幂等键。
  - `immutable-release`：已推送的提交不可撤回，只能前滚新版本；必须写出降级指引与兼容窗口。
- **G-02 完整可交付（下限）:** 一张卡必须是一个自洽的可验收增量。同一行为轴的实现、测试、文档与索引同步是**同一张卡**的验收内容，禁止拆成「实现卡 / 补测试卡 / 补文档卡」。只有当被拆出的部分本身就是独立可恢复的行为轴（G-01 意义上：有单一已声明的恢复动作——独立迁移、独立 deprecation 收口、独立性能门、独立 API 切面、跨计划移交）时才允许单独成卡。
- **G-03 条目上限与计数口径:** 上限按 `Task type` 取值，计数按**独立判据**而非行数。ER-04 的强制门（fmt / clippy / 全量测试 / 表面门）**不计入**本条计数，`Verification` 只登记本卡特有的判据：

  | Task type | AC 上限 | Verification 上限 |
  |---|---|---|
  | `implementation` / `migration` / `removal` | 8 | 8 |
  | `spike` | 8 | 8 |
  | `docs` / `audit` / `handoff` | 20 | 20 |
  | `release` | 12 | 12（只计聚合守卫、release note、兼容证据等本卡特有项） |

  计数细则：
  - AC 按「独立 pass/fail 谓词」计。一条 checklist 内用「且 / 并且 / 以及 / 同时」连接的多个可分别失败的断言按多条计；嵌套子列表逐项计；表格行逐行计。
  - Verification 按「独立验证门」计。判据是「是否构成一次独立的通过/失败判定」：环境准备前缀（`source .env.test`、`MEGA_*=…` 赋值、`docker compose … up -d --wait`、`export`）与其后的命令合计为**一门**；一条命令中的多个 `--test` target 分别计；`&&` 串联两个都会独立判定的验收命令按两门计；手工证据按项计。
  - 超限视为多轴信号，必须拆卡，**不得**通过合并长句、塞进表格或改写成「等等」来规避。
  - 文档 / 审计 / 索引-only 卡的条目是清单项、不构成独立行为轴，故适用 20 条上限；这类卡仍受 G-01 约束，且必须在任务卡 `Deliverables` 字段登记产物范围（具体文件清单）——这是常规登记，不是例外，无需进 waiver 表。需要突破本表上限时，只能在 waiver 白名单登记 `EX-*`（具名审批），不得私自改写分母。
- **G-04 规模上限（可计数）:** `Estimated scope` 的开工态只允许 `S` 或 `M`。`L`/`XL` 只能作为「必须再拆」的中间标注，计划成稿后不得存在 L/XL 卡。计数**只统计行为实现落点与生产文件**，不统计「随附同步集」：
  - **计入**：承载本卡行为变更的生产代码落点与文件（`src/**`、`bin/src/**`、`config/**`、`scripts/**` 等）。
  - **不计入（随附同步集）**：本卡自己的测试文件、按 GC-05/ER-06 强制同步的文档集（`docs/**`、`README.md`、`config/config.toml` 注释）、以及 ER-08 的版本面两处。这些是每张卡的固定成本，不构成粒度信号；但仍要在写集字段中如实列出（文档/测试进 `Implementation write set`，版本面两处进 `Release write set`）。
  - **仓库根文件**（`Cargo.toml`、`bin/Cargo.toml`、`rustfmt.toml`、`docker-compose.test.yml` 等）按「单个文件」计，不各占一个落点；若某张卡的行为变更**就发生在**根文件本身（例如改 `docker-compose.test.yml` 的服务拓扑），则该文件计为一个落点。
  - `S`：≤ 2 个行为落点、≤ 3 个生产文件，无 schema / 协议 / 公开接口变更。
  - `M`：≤ 4 个行为落点、≤ 12 个生产文件，最多一处公开行为或接口变化，仍是单一行为轴。
  - 超出 `M` 的计数即为 L：默认必须拆分。确实不可拆的机械变更（全仓重命名、批量删除、格式化）可在「字段全局默认与例外」的 waiver 白名单中登记 `EX-*`（需具名审批人与 review 轮次），写明为何不可拆、如何 review、如何恢复；此时该卡 `Estimated scope` 写 `L-exception:EX-<n>`，这是全文唯一允许出现 `L` 字样的形式，`XL` 永不允许。
  - 把 `src/`、`bin/`、`docs/` 或仓库根算作「一个落点」是规避行为，按「粒度反模式速查」的「落点注水」处理。
- **G-05 Agent 可独立执行:** 一张卡必须能在不阅读其它卡正文的前提下被执行：`Current evidence` 给出可核对的 `file:line` 锚点，`Acceptance criteria` 自洽可判定，`Verification` 是可直接复制执行的确切命令，`Dependencies` 只引用「依赖登记表」中的 `DEP-*` / 任务 ID。禁止「见上文」「同上一卡」式跨卡隐式约定；确属跨卡共享的约定要提升为全局工程约束或 ADR。
- **G-06 依赖闭合且无环:** 依赖必须有向无环。本计划内依赖直接引用任务 ID；跨计划与外部前置必须先在「依赖登记表」登记为 `DEP-*` 再引用，不得在卡内自由描述。互相等待、循环依赖、以及「等某个 Phase 整体完成」都是拆分错误——把依赖收敛到具体前置卡。「实施顺序」的依赖边与各卡 `Dependencies` 必须一致；不一致时以「实施顺序」为准并当场修正卡片。
- **G-07 发布切片对齐（按任务类型）:** 默认「一张卡 = 一个发布切片」（独立 review + ER-04 门 + 版本 + 提交 + 推送）。适用范围按 `Task type`（G-11）区分：`implementation` / `migration` / `removal` 必须走完整发布切片；`docs` / `audit` / `spike` / `handoff` 卡不 bump 版本，`Release boundary` 写 `no-release` 并说明其产物随哪次提交进入仓库；`release` 卡本身就是发布点（家族卡的唯一发布点必须是 `release` 卡，见 G-08）。任何「多卡合并发布」都是例外，必须在「发布分组与并发窗口」登记 `REL-*`：成员、唯一发布点、窗口期禁止插入的内容、失败时的逆序回滚顺序。例外必须先修订计划并通过 review 才可开工，**不得**在开工时凭笔记临时合并。
- **G-08 家族卡（不可分割变更的唯一出路）:** 当一次公开 surface 删除、或 schema 与 reader 必须同时上线这类变更确实无法切成可独立发布的切片时，用「家族卡」表达：拆成多张各自 review、各自通过全部适用 ER-04 门、各自本地提交的子卡，共用一个唯一发布点卡；**该发布点卡的 `Task type` 必须是 `release`**（不引入新行为，只做版本、构建、聚合守卫与发布证据），以保证它在 ER-04 的 B 组中唯一命中 `release` 行。家族内子卡仍受除 G-07 外的全部 `G-*` 约束；子卡 `Release boundary` 写 `family child`，发布点卡写 `family release point`，家族边界与「不推送窗口」写进 `REL-*` 登记。
- **G-09 拆分协议:** 拆分已被引用的卡时，原编号保留给主轴，新子卡在所属 Phase 末尾追加新编号，不重排既有编号。原卡必须写明「拆出 `<ID>`、`<ID>`」，新卡写明「自 `<ID>` 拆出」，并同步实施顺序、依赖登记表、「发布分组与并发窗口」、追溯表、测试矩阵、里程碑、风险表，以及「修订历史」中的一行（日期、原因、原卡、新卡、受影响引用）。
- **G-10 写集与并发:** 写集分三类，每张卡必须声明前两类（第三类由 ER-12 统一定义，卡内不重复）：
  - **`Implementation write set`（I）**：承载本卡行为的代码、测试、文档文件。
  - **`Release write set`（R）**：ER-08 的版本面两处（`Cargo.toml`、`bin/Cargo.toml`）+ `Cargo.lock`。对所有发布卡相同；`family child` 与 `no-release` 卡写 `N/A`（它们不 bump、不推送）。
  - **协调写集（C）**：计划级的发布顺序与窗口记录（「发布分组与并发窗口」的发布者、发布顺序、`REL-*` 登记）。由 ER-12 的单一发布者串行维护，**不计入**任何卡的 I 或 R，也不参与并发判定。

  冲突规则：
  - **I–I 相交** → **禁止并发，无豁免通道**（G-10 不在 waiver 白名单内）：只有两个合法出路——补一条顺序依赖边，或把相交部分合并到唯一集成卡。
  - **I–R 相交**（某卡把 `Cargo.toml`、`bin/Cargo.toml` 等当行为落点，而它同时属于别的卡的 R）→ 在**已声明的串行发布窗口**内（ER-12），该窗口对 R 内文件是写锁：其它卡不得在此期间修改这些文件，必须等窗口结束或补顺序边。
  - **R–R 相交** → 由 ER-12 的串行发布窗口顺序化，不构成并发禁止条件。
  - `Files likely touched` 是估计值，并发判定以 `Implementation write set` 为准。
- **G-11 任务类型:** 每张卡必须声明 `Task type`，不同类型适用不同粒度口径：
  - `implementation`：默认类型，全部 `G-*` 条款全量适用。
  - `migration`：数据/schema 迁移，`Rollback mode` 通常为 `forward-only`，必须有 up/down 或前滚验证与故障注入用例，并在 `src/jupiter/migration/mod.rs` 的 `migrations()` 列表登记顺序。
  - `removal`：公开 surface 删除，通常进入家族卡（G-08），必须先有 deprecation 窗口证据。
  - `spike`：探索/验证，**不得**改动生产代码。必须写出待回答的问题、时间箱、产物（结论 + ADR 或缺口登记）、go/no-go 退出标准与后续承接卡；不适用 G-04 的文件计数，`Estimated scope` 按时间箱判定：`S` ≤ 0.5 人日、`M` ≤ 2 人日，超出即拆成多个问题或直接转 ADR / `implementation` 卡。
  - `audit` / `docs`：只读核对或文档收敛，按 G-03 的文档-only 口径执行。规模上限按**产物文件数或人日**判定（不适用 G-04 的生产文件计数）：`S` ≤ 5 个产物文件或 ≤ 0.5 人日；`M` ≤ 15 个产物文件或 ≤ 2 人日；超出即拆卡。随代码卡强制同步的文档仍按 G-04 的随附同步集处理，不计入这里。
  - `release`：发布点卡，不引入新行为，只做版本、构建、聚合守卫与发布证据。
  - `handoff`：跨计划移交，默认 `no-release`。**移入**（本计划承接他人）在「依赖登记表」登记 `direction: incoming`；**移出**（本计划把范围交给别的计划或 `plan-long.md` 的 PT 项）登记 `direction: outgoing`，并写明接收方、移交日期、本计划不再重做的部分，以及接收方未接手时的回落处理。

#### 推荐拆分维度

超限卡按下列维度之一切开；切完每片仍须独立满足 G-01（单一行为轴 + 已声明的恢复模式）。

| 维度 | 切法 | 典型结果 |
|---|---|---|
| 数据 / 状态轴 | entity + migration → storage 写入与幂等 → 读取投影与恢复 | 3 张卡 |
| 协议轴 | 协议版本与协商 → 容量 / 背压与性能门 → 消费端接入 | 3 张卡 |
| 表面轴 | 后端 service/storage → 机器接口（HTTP JSON / OpenAPI / 错误映射） → CLI 或客户端接入 | 2–3 张卡 |
| 生命周期轴 | 新实现上线 → 默认切换 → deprecation shim → 物理删除 | 按发布窗口分卡 |
| 安全轴 | 身份与请求边界（Cedar / token） → 路径与对象归属 → 敏感信息 redaction | 每轴一卡 |
| 清理轴 | 公开 surface 删除（家族卡） → 内部模块退场 → 依赖摘除 | 家族卡 + 普通卡 |

#### 粒度反模式速查

| 反模式 | 症状 | 处理 |
|---|---|---|
| 巨型卡 | `Estimated scope` = L；AC > 8；Description 含多个并列目标 | 按「推荐拆分维度」拆分（G-01/G-03/G-04） |
| 碎片卡 | 「补测试」「补文档」「改个字段名」单独成卡 | 合并回所属行为轴（G-02） |
| 多轴伪装 | 把多条 AC 合成一条长句、塞进表格或写「等等」以压到 8 条以内 | 按独立谓词还原计数后重新判定（G-03） |
| 落点注水 | 把 `src/` 或仓库根算作「一个落点」以保住 S/M | 按目录级落点重新计数（G-04） |
| 隐式依赖 | Description 写「按 X 卡的约定」而 X 卡未交付该约定 | 写进本卡，或提升为全局约束 / ADR（G-05） |
| 幽灵验收 | `Verification` 只写 `cargo test --all`，或零命中守卫不区分 `rg` 退出码 `1` 与 `>1` | 指定 package/target 与 test fn；按「Verification 判定口径」的退出码模板重写（G-05） |
| 悬空依赖 | `Dependencies` 写「Phase N 完成」或自由描述外部前置 | 收敛到具体前置卡 ID / `DEP-*`（G-06） |
| 假回滚 | 已推送或已迁移数据的卡仍写「一次 revert 撤销」 | 按实际选 `forward-only` / `compensating` / `immutable-release`（G-01） |
| 并发冲撞 | 两张无依赖的卡实现写集相交 | 只有两条出路：补顺序边，或合并到唯一集成卡（G-10 不可豁免）。版本面争用不算并发冲突，由 ER-12 的串行发布窗口处理 |
| 顺手合并 | 多张卡凭开工笔记合成一次发布 | 登记为 `REL-*` 家族卡，或拆回独立发布切片（G-07/G-08） |

#### 字段全局默认与例外

计划在本节声明字段的全局默认值后，任务卡中**取默认值的字段可以整行省略**，或写 `Inherited`；只有偏离默认的字段才在卡内展开并在下表登记。`Task type`、`Lifecycle / Acceptance`、`Rollback mode`、`Implementation write set`、`Version increment`、`C/D coverage from`、`Granularity` 摘要行是每卡必填，不可省略；`Release write set` 可写 `Inherited` 或 `N/A`；`Deliverables` 对 `docs` / `audit` / `spike` / `handoff` 卡必填。

- **Release boundary 默认:** `independent`（每张实现卡一个发布切片）
- **Task type 默认:** `implementation`
- **Rollback mode 默认:** `revert`
- **Migration and rollback 默认:** `N/A`：本计划无 schema 迁移，不新增 migration、不改 `migrations()` 注册列表
- **Security and privacy 默认:** 继承 GC-07、GC-11；测试凭据与测试栈口令按 ER-11 脱敏
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 按 GC-05 同步相关 `docs/` 文档与 `config/config.toml` 示例

**G-04 计数口径（照模板执行，本计划不做任何改写）:** 承载行为变更的生产文件才计入 `landing` / `prod-files`；本卡自己的测试文件（`bin/tests/**`、`#[cfg(test)] mod tests`）、按 GC-05/ER-06 强制同步的文档集、以及 ER-08 的版本面两处属「随附同步集」，一律不计入。因此本计划中 IT-01、IT-03、IT-06、IT-07、IT-09、IT-10、IT-12 的计数为 `0/0`，`scope` 由 G-04 的其余判据（无 schema / 协议 / 公开接口变更）判为 `S`；这些卡的粒度信号主要由 G-03 的 AC / VER 上限提供。

**ER-04 门与 `Verification` 的边界（本计划统一口径）:** ER-04 的 A 表面门（各卡自身表面的 `cargo test -p monoengine --test <target>` 或 `cargo test -p monoengine-core '<mod::path::tests>'`、`.github/workflows/**` 的受影响 job 本地等价命令）、B 类型门、C 发布收口门、D 远端后置门**都不进入任务卡的 `Verification`**，也不计入 G-03。它们由 `Task type`、实际改动表面与 `Release boundary` 自动命中；D 组另在各卡的「D 组远端后置门」字段登记。各卡 `Verification` 只登记本卡特有的守卫命令与手工证据。

**默认覆盖**（不是例外，只是取了非默认值，无需审批）：

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| IT-01 | `Task type` / `Release boundary` / `Version increment` | `docs` / `no-release` / `N/A`：纯规范文档，不引入代码或配置变更，产物随 IT-02 的提交进入仓库 |
| IT-07 | `Task type` / `Release boundary` / `Version increment` | `docs` / `no-release` / `N/A`：纯文档收口，产物随 IT-09 的提交进入仓库 |
| IT-09 | `Task type` / `Rollback mode` / `Implementation write set` | `release` / `immutable-release` / `N/A`：发布点卡不引入新行为；已推送提交不可撤回，只能前滚新版本 |

**规则 waiver（`EX-*`，需具名审批）**：可豁免的规则是**白名单**，只有下表三项；`G-01`、`G-02`、`G-05`、`G-06`、`G-07`、`G-08`、`G-09`、`G-10`、`G-11` **永不可豁免**（它们是可 review、可恢复、可并发的前提）。

| 可豁免项 | 允许的理由范围 |
|---|---|
| G-03 条目上限 | 清单型产物（文档 / 审计 / 索引）确实需要超过本类上限，且已写明产物文件清单 |
| G-04 规模上限（`L-exception`） | 不可拆的机械变更：全仓重命名、批量删除、格式化 |
| ER-07 签名要求 | 仓库策略层面的具名豁免（sign-off-only） |

| 例外 ID | 任务（或 `ALL/<作用域>`） | 豁免项 | 理由与补偿措施 | Approver | Review round | 证据 | 有效期 |
|---|---|---|---|---|---|---|---|
| EX-01 | `ALL/release` | ER-07 签名要求 | `vault.signing=true` 且已配置 `vault.gpg.pubkey`，但本机未提供 vault unseal key（`LIBRA_VAULT_UNSEAL_KEY` unset），`libra commit -s` 只能产出 Signed-off-by、无法写入 `gpgsig`。按 ER-07 已排查该缺口；本计划发布窗口内允许 sign-off-only 推送。补偿：每次提交仍强制 `-s`；恢复 unseal 后撤销本豁免并恢复 `gpgsig` 校验 | porrychen（本会话确认） | plan-exec 2026-07-29 | `libra config get vault.signing` → true；`libra cat-file -p HEAD` 无 `gpgsig`；`LIBRA_VAULT_UNSEAL_KEY` unset | 至 vault unseal 恢复或本计划全部卡关闭（以先到者为准） |

#### 任务卡粒度审计表

计划成稿与每次规范性修订后填一次，逐卡汇总各卡 `Granularity` 行，便于机械核对与脚本校验。判定规则：任一列不达标即不得开工；`AC` / `VER` / `scope` 列超限时必须带 `@EX-ID` 或 `L-exception:EX-n`，且该 `EX-*` 必须同时满足：存在于 waiver 表、`任务` 列等于引用它的卡（或显式写 `ALL/<作用域>` 的计划级豁免）、`豁免项` 等于被超限的那条规则、理由落在白名单、且仍在有效期内。任一条不满足仍判为不达标。

本表只收录任务卡。最终卡集为 `IT-01`、`IT-02`、`IT-03`、`IT-04`、`IT-06`、`IT-07`、`IT-08`、`IT-09`、`IT-10`、`IT-11`、`IT-12`（共 11 张）。`IT-05` 已关闭并降级为「已关闭编号记录」，不是任务卡，故不参与粒度审计（见「修订历史」）。

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| IT-01 | docs | 测试基建规范的单一事实源建立 | revert test-infra.md 与 integration.md 两处改动，回到规范散落状态 | yes | yes | 20/20 | 1/20 | 0/0 | S | none | 序列化于 IT-02、IT-07 | no-release | N/A | N/A |
| IT-02 | implementation | 测试栈拓扑新增受规范登记的 git-cli runner | revert docker-compose.test.yml 的 git-cli 服务与 test-infra.md 的登记条目，测试栈回到 5 服务 | yes | yes | 8/8 | 4/8 | 1/1 | S | IT-01,DEP-08 | 序列化于 IT-01 | independent | N/A（拆出 IT-08） | N/A |
| IT-03 | implementation | Git CLI 对 HTTP 通道的往返一致性与用例隔离 | revert 新增的 integration_git_cli.rs、common/git_cli.rs，以及 protocol.md / integration.md 的登记行，回到脚本-only 覆盖 | yes | yes | 8/8 | 4/8 | 0/0 | S | IT-02,IT-08 | 序列化于 IT-01 | independent | N/A（拆出 IT-10） | N/A |
| IT-04 | implementation | 本计划测试用例在 CI 上的执行面与触发面覆盖 | revert config-validation.yml、git-protocol-smoke.yml 与 test-infra.md 的 CI 登记，回到既有触发面 | yes | yes | 8/8 | 2/8 | 1/2 | S | IT-12,IT-06 | 序列化于 IT-01 | independent | N/A（拆出 IT-11） | N/A |
| IT-06 | implementation | 同一 DB 上两个 dispatcher 实例的 exactly-once 投递门 | revert dispatcher.rs 的测试模块新增块与 integration.md 的矩阵行 | yes | yes | 8/8 | 2/8 | 0/0 | S | IT-08,IT-03 | 序列化于 IT-03 | independent | N/A | N/A |
| IT-07 | docs | 本计划的文档与索引收口（P2 定性 + 内嵌样例对齐 + PT-01 状态） | revert integration.md、plan-long.md、plan-20260727.md 三处改动 | yes | yes | 20/20 | 3/20 | 0/0 | S | IT-01,IT-11,IT-06 | 序列化于 IT-06 | no-release | N/A | N/A |
| IT-08 | implementation | 测试环境变量示例与 CI env 对齐且 source 后可被子进程继承 | revert .env.test.example 单文件 | yes | yes | 7/8 | 3/8 | 1/1 | S | none | no-overlap | independent | IT-02 | N/A |
| IT-09 | release | IT-07 的收口承载发布卡 | 前滚新 patch 版本，不回退已推送提交 | yes | yes | 11/12 | 1/12 | 0/0 | S | IT-07 | no-overlap | independent | N/A | N/A |
| IT-10 | implementation | Git smart HTTP 认证边界与凭据零泄漏在真实客户端下的断言 | revert integration_git_cli.rs 中本卡追加的认证用例块 | yes | yes | 5/8 | 1/8 | 0/0 | S | IT-03 | 序列化于 IT-03 | independent | IT-03 | N/A |
| IT-11 | implementation | CI 中 git 客户端版本的确定性 | revert git-protocol-smoke.yml 的 pin 与 summary 改动，以及 test-infra.md 的版本登记 | yes | yes | 8/8 | 2/8 | 1/1 | S | IT-04 | 序列化于 IT-04 | independent | IT-04 | N/A |
| IT-12 | implementation | Git CLI 失败路径不拖垮服务 | revert integration_git_cli.rs 中本卡追加的失败路径用例块 | yes | yes | 5/8 | 1/8 | 0/0 | S | IT-10 | 序列化于 IT-10 | independent | IT-10 | N/A |

#### Verification 判定口径

- **零命中守卫必须区分「无命中」与「命令失败」。** `rg` 的退出码是 `0` = 有命中、`1` = 零命中、`>1` = 执行失败（非法正则、路径不存在、I/O 错误）。`! rg …` 和 `if rg …; then exit 1; fi` 都会把 `>1` 误判为通过，**不得**单独作为证据。裸 `rg …; rc=$?` 也不行——在 `set -e` 下零命中会让脚本在取 `$?` 前就退出。使用下列模板：

  ```bash
  if rg -n "<pattern>" <paths>; then
    echo "FAIL: forbidden pattern found"; exit 1
  else
    rc=$?
    if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi
    echo "OK: zero hits"
  fi
  ```

- 「只允许 allowlist 命中」类守卫必须逐条比对固定 allowlist，并在任务记录中附命中 diff。
- 仅用于定位符号的 `rg` 必须注明「锚点定位用，非判据」。
- 本任务新增的 test fn / 场景过滤必须标 `(new)`，并确保其归属明确：`monoengine-core` 的 `#[cfg(test)]` 用例写 `-p monoengine-core '<mod::path::tests::fn>'`；集成用例写 `-p monoengine --test <target>`，新 target 直接在 `bin/tests/<target>.rs` 建文件（cargo 自动发现），并同步测试矩阵与 `docs/refactoring/integration.md` 覆盖矩阵。
- `cargo test --all` 不能替代任务指定用例（ER-04）；反过来，指定用例也不能替代计划完成前的全量门（见「完成判据」）。
- 依赖真实服务的用例必须在 Verification 里写清前置：Postgres 缺失会让相关用例 **panic**（不是跳过），Mailpit 在 `monoengine-core` 侧是优雅跳过、在 `bin/tests` 侧是硬断言。把「跳过」当成「通过」属于幽灵验收。

### Task IT-01: 测试基建规范（双层职责 + docker-compose 拓扑扩展登记）

**Task type:** `docs`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（执行期收口，2026-07-29）

**Description:** 新建 `docs/refactoring/test-infra.md`，把当前散落在 `docs/refactoring/integration.md` 与各测试文件中的测试基建纪律收敛为单一事实源：模块集成测试与黑盒进程测试的分层职责、fixture 生命周期、docker-compose 新服务的登记 checklist、客户端版本确定性规则，并把测试栈的 S3-compatible 对象存储（**RustFS** `rustfs` + `rustfs-init`）登记为工作样例。为满足 GC-02，`integration.md` 既有的 `## 测试分层` 正文（`:111-143`）必须被**删除并替换**为指向新规范的链接与一句摘要，而不是并存。本卡唯一行为轴是「建立规范的单一事实源」，不实现任何新服务。

**Out of scope:**

- git-cli 服务本身的实现 —— 由 IT-02 承接。
- CI 入口登记 —— 由 IT-04 承接；git / git-lfs 版本 pin 值的登记 —— 由 IT-11 承接。
- `integration.md` 的 P2 清单与内嵌 compose 样例收口 —— 由 IT-07 承接。
- 各测试用例的覆盖矩阵行 —— 由各自的行为卡承接（IT-03、IT-06）。
- orion-server / fake webhook receiver 的拓扑接入 —— 尚未排期，重启条件见 DEP-07。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 分层规范正文当前住在 integration.md，`docs/refactoring/` 下不存在 `test-infra.md` | `docs/refactoring/integration.md:111`（`## 测试分层`）、`:111-143`（正文）、`:147-192`（容器编排，内嵌 YAML 样例） |
| compose 已有 5 个服务，其中 rustfs + rustfs-init 是 S3-compatible 对象存储扩展先例 | `docker-compose.test.yml` 的 `rustfs` / `rustfs-init`（`profiles: ["init"]`、`depends_on: service_healthy`） |
| 固定 tag 纪律已有先例但无成文要求 | `docker-compose.test.yml:3`、`:18`、`:28`、`:39`、`:57` |

**Acceptance criteria:**

- [x] `docs/refactoring/test-infra.md` 已创建。
- [x] 文档含「双层测试职责」章节。
- [x] 文档含「fixture 生命周期」章节。
- [x] 文档含「新服务登记 checklist」章节。
- [x] 文档含「客户端版本确定性规则」章节。
- [x] checklist 含「镜像固定 tag 或 digest」一项。
- [x] checklist 含「端口绑定 `127.0.0.1`」一项。
- [x] checklist 含「使用高位端口」一项。
- [x] checklist 含「healthcheck 定义」一项。
- [x] checklist 含「`down -v` 清理」一项。
- [x] checklist 含「零残留判据」一项。
- [x] checklist 含「secret 只经 stdin/env 注入」一项。
- [x] checklist 含「`::add-mask::` 脱敏」一项。
- [x] checklist 含「目标 OS = Linux」一项（不要求跨平台降级路径）。
- [x] 文档以 rustfs 为对象给出一份填好的登记条目。
- [x] 该样例写明 `profiles: ["init"]` 的 one-shot 语义。
- [x] 该样例写明 `depends_on: service_healthy` 约束。
- [x] `docs/refactoring/integration.md` 中原 `## 测试分层` 的正文已被删除。
- [x] 该位置改为指向 `test-infra.md` 的链接与一句摘要（GC-02）。
- [x] 文档写明「新增服务必须先按 checklist 登记评审，禁止绕规范加服务」。

**Verification:**

- [x] 以 rustfs + rustfs-init 为对象实跑一遍登记 checklist，产出的登记条目逐项与 `docker-compose.test.yml` 的 `rustfs` / `rustfs-init` 对照可核对（手工证据，sanitized 记录于 Review log 的 `E-IT-01` 行）

**Dependencies:** 无（G-06；计划级前置 DEP-01、DEP-09 见「本计划的 ER-01 现状补记」）

**Deliverables:** `docs/refactoring/test-infra.md`（新建）；`docs/refactoring/integration.md` 中替换后的链接段。

**Implementation write set:** `docs/refactoring/test-infra.md`、`docs/refactoring/integration.md`（删除 `## 测试分层` 正文并替换为链接段）（G-10）

**Release write set:** `N/A`（`no-release` 卡）

**Files likely touched:** `docs/refactoring/test-infra.md`、`docs/refactoring/integration.md`

**Docs and compatibility impact:** 即任务本身；无用户可见命令、配置或 API 变化

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** 规范须包含 secret 注入与脱敏纪律（stdin/env、`::add-mask::`、ER-11 证据卫生）

**Performance budget:** `Inherited`

**Estimated scope:** `S`（2 个产物文件 ≤ 5，`docs` 口径）（G-04）

**Version increment:** `N/A`（产物随 IT-02 的提交进入仓库）（ER-08）

**Release boundary:** `no-release`

**C/D coverage from:** `IT-02`（继承其 C 组覆盖与 D 组；IT-02 的三门运行必须包含本卡最终变更）（ER-04）

**Granularity:** `type=docs; axis=测试基建规范的单一事实源建立; recovery=revert test-infra.md 与 integration.md 两处改动，回到规范散落状态; complete=yes; self-contained=yes; AC=20/20; VER=1/20; landing=0; prod-files=0; scope=S; deps=none; writeset=序列化于 IT-02、IT-07; release=no-release; split-from=N/A; exception=N/A`

### Task IT-02: docker-compose.test.yml 增加 git-cli runner 服务

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（执行期收口，2026-07-29）

**Description:** 本卡的全部 compose 命令统一使用显式项目名 `-p monoengine-it`，残留查询用同一项目名做 label 过滤，避免 `COMPOSE_PROJECT_NAME` 等环境覆盖导致守卫查错项目而空洞通过。按 IT-01 的登记 checklist 在 `docker-compose.test.yml` 增加 `git-cli` 服务：固定 tag（必要时固定 digest）的轻量 git 镜像、可访问宿主 `127.0.0.1` 高位端口的网络模式、挂载固定共享宿主工作目录 `${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}`、healthcheck，并在 `test-infra.md` 完成登记。本卡唯一行为轴是「测试栈拓扑新增一个受规范登记的 git-cli runner」，不修改既有五个服务。**拆出 `IT-08`**（`.env.test.example` 修复）。

**Out of scope:**

- `.env.test.example` 的变量补齐与 `export` 修复 —— 由 IT-08 承接（自本卡拆出）。
- 使用该服务的测试用例 —— 由 IT-03、IT-10、IT-12 承接。
- CI 中 git 客户端的版本 pin —— 由 IT-11 承接。
- sshd / orion-server / fake receiver 服务 —— 尚未排期，重启条件见 DEP-05、DEP-07。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| compose 当前有 5 个服务：postgres / redis / mailpit / rustfs / rustfs-init，无 git 客户端服务 | `docker-compose.test.yml` 的 postgres / redis / mailpit / rustfs / rustfs-init 服务块 |
| 固定 tag 纪律已有先例 | `docker-compose.test.yml:3`、`:18`、`:28`、`:39`、`:57` |
| 文件已声明 `networks.default`，与单服务 `network_mode: host` 不可共存（ADR-IT-01） | `docker-compose.test.yml:69-71` |
| 当前 CI 用宿主机 git 且不断言版本 | `.github/workflows/git-protocol-smoke.yml:68-75` |

**Acceptance criteria:**

- [x] `git-cli` 服务的镜像使用固定 tag 或 digest（不含 `latest`）。
- [x] `docker compose -p monoengine-it -f docker-compose.test.yml --profile git up -d --wait` 后 `git-cli` 的健康状态为 healthy。
- [x] 容器内 `git --version` 的输出等于 `docs/refactoring/test-infra.md` 中登记的固定版本字符串。
- [x] git-cli 容器可访问宿主 `127.0.0.1` 高位端口上的 HTTP 服务。
- [x] `docker compose -p monoengine-it -f docker-compose.test.yml --profile git down -v` 后本 compose project 无残留卷且无残留网络。
- [x] `docs/refactoring/test-infra.md` 含按 checklist 填好的 `git-cli` 登记条目，并写明共享工作目录 `${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}` 的约定。
- [x] `git-cli` 使用 `profiles: ["git"]`，默认（无 profile）`up -d --wait` **不**启动该服务。
- [x] 登记条目写明 `network_mode: host`，并声明目标 OS 为 Linux（宿主机 git 仅 `MONOENGINE_IT_ALLOW_HOST_GIT=1` 本地实验，非验收路径）。

**Verification:**

- [x] 健康状态断言（读取容器 Health 状态并与字符串 `healthy` **完全相等**比较——子串匹配会把 `unhealthy` 误判为通过）：`bash -c 'set -euo pipefail; mkdir -p "${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}"; chmod 1777 "${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}"; docker compose -p monoengine-it -f docker-compose.test.yml --profile git up -d --wait; cid=$(docker compose -p monoengine-it -f docker-compose.test.yml ps -q git-cli); [ -n "$cid" ] || { echo "FAIL: git-cli container not found"; exit 1; }; st=$(docker inspect --format "{{.State.Health.Status}}" "$cid"); [ "$st" = "healthy" ] || { echo "FAIL: git-cli health is [$st], expected exactly [healthy]"; exit 1; }; echo "OK: healthy"'`
- [x] 版本确定性断言（pin 值从规范登记确定性提取，两侧先断言非空）：`bash -c 'set -euo pipefail; PIN=$(rg -N -o "git-cli 固定版本[：:] *\`([^\`]+)\`" -r "\$1" docs/refactoring/test-infra.md | head -1); ACTUAL=$(docker compose -p monoengine-it -f docker-compose.test.yml exec -T git-cli git --version); [ -n "$PIN" ] || { echo "FAIL: pin not registered in test-infra.md"; exit 1; }; [ -n "$ACTUAL" ] || { echo "FAIL: empty git --version"; exit 1; }; [ "$ACTUAL" = "$PIN" ] || { echo "FAIL: got [$ACTUAL] pinned [$PIN]"; exit 1; }; echo "OK: $PIN"'`
- [x] 连通性断言（**动态分配端口**避免撞上既有服务；确认探针确实绑定并存活；用随机 nonce 做**身份断言**，确保响应确实来自本探针；探针必被回收）：`bash -c 'set -euo pipefail; port=$(python3 -c "import socket;s=socket.socket();s.bind((\"127.0.0.1\",0));print(s.getsockname()[1]);s.close()"); nonce="probe-$(od -An -N8 -tx1 /dev/urandom | tr -d " \n")"; d=$(mktemp -d); printf "%s" "$nonce" > "$d/index.html"; ( cd "$d" && exec python3 -m http.server "$port" --bind 127.0.0.1 >/dev/null 2>&1 ) & PROBE=$!; trap "kill $PROBE 2>/dev/null; rm -rf $d" EXIT; ok=0; for i in $(seq 1 25); do if curl -sf "http://127.0.0.1:$port/" >/dev/null; then ok=1; break; fi; sleep 0.2; done; [ "$ok" -eq 1 ] || { echo "FAIL: probe never bound on port $port"; exit 1; }; kill -0 "$PROBE" 2>/dev/null || { echo "FAIL: probe process died"; exit 1; }; got=$(docker compose -p monoengine-it -f docker-compose.test.yml exec -T git-cli sh -c "wget -qO- http://127.0.0.1:$port/"); [ "$got" = "$nonce" ] || { echo "FAIL: got [$got], expected the probe nonce"; exit 1; }; echo "OK: git-cli reaches this host probe on port $port"'`
- [x] `down -v` 零残留守卫 + 默认栈不启 git-cli（卷与网络合为一次整体判定；每个 docker 命令的退出码都显式检查；按 compose project label 精确判定；另断言无 profile 时 `ps -q git-cli` 为空）：`bash -c 'set -uo pipefail; docker compose -p monoengine-it -f docker-compose.test.yml --profile git down -v || { echo "FAIL: down -v exited non-zero"; exit 1; }; vols=$(docker volume ls -q --filter "label=com.docker.compose.project=monoengine-it") || { echo "ERROR: docker volume ls failed"; exit 2; }; nets=$(docker network ls -q --filter "label=com.docker.compose.project=monoengine-it") || { echo "ERROR: docker network ls failed"; exit 2; }; if [ -n "${vols}${nets}" ]; then echo "FAIL: leftover volumes=[$vols] networks=[$nets]"; exit 1; fi; docker compose -p monoengine-it -f docker-compose.test.yml up -d --wait || { echo "FAIL: default up failed"; exit 1; }; gid=$(docker compose -p monoengine-it -f docker-compose.test.yml ps -q git-cli || true); [ -z "$gid" ] || { echo "FAIL: git-cli started without --profile git"; exit 1; }; echo "OK: zero leftover and git-cli not in default stack"'`

**Dependencies:** `IT-01`（消费其登记 checklist 与固定版本登记格式）、`DEP-08`（镜像可拉取性）（G-06）

**Deliverables:** `Inherited`

**Implementation write set:** `docker-compose.test.yml`、`docs/refactoring/test-infra.md`（登记该服务）（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `docker-compose.test.yml`、`docs/refactoring/test-infra.md`

**Docs and compatibility impact:** `test-infra.md` 的服务登记条目

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** git-cli 服务不注入任何 secret；工作目录必须是隔离**可写**卷（git 需创建 checkout、`.git` 元数据与提交），只读挂载仅适用于纯 fixture 数据

**Performance budget:** compose 全栈 `up --wait` 总耗时不超过现状 +30s

**Estimated scope:** `S`（1 个行为落点、1 个生产文件）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（其三门运行须覆盖 IT-01 的文档变更）（ER-04）

**D 组远端后置门:** `N/A` —— `docker-compose.test.yml` 与 `docs/refactoring/test-infra.md` 均不在任一 workflow 的 `paths:` 内（`.github/workflows/config-validation.yml:5-15`、`:19-29`；`.github/workflows/git-protocol-smoke.yml:14-22`、`:26-34`）。本卡在自身发布窗口内 D 组恒为 `N/A`：`IT-04` 排在本卡之后，其后续把 `docker-compose.test.yml` 加入触发面只产生 **IT-04 自己的** D 组证据，**不追溯改变本卡已完成的 `Acceptance`**。

**Granularity:** `type=implementation; axis=测试栈拓扑新增受规范登记的 git-cli runner; recovery=revert docker-compose.test.yml 的 git-cli 服务与 test-infra.md 的登记条目，测试栈回到 5 服务; complete=yes; self-contained=yes; AC=8/8; VER=4/8; landing=1; prod-files=1; scope=S; deps=IT-01,DEP-08; writeset=序列化于 IT-01; release=independent; split-from=N/A（拆出 IT-08）; exception=N/A`

### Task IT-03: `bin/tests/integration_git_cli.rs` —— Git CLI HTTP 往返一致性与用例隔离

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（D 组 `git-protocol-smoke` 绿于 v0.1.177 / [30479301244](https://github.com/gitmono-dev/monoengine/actions/runs/30479301244)，2026-07-30）

**Description:** 新建黑盒集成 test target `integration_git_cli`：测试进程以 `CARGO_BIN_EXE_monoengine` 启动 `service http`，每个用例分配互不相同的高位端口、独立临时 `base_dir`/`cache_dir` 与隔离 DB（模式参照 `integration_service_http_smoke`），用 compose `git-cli`（Linux 目标 OS；宿主机 git 仅 `MONOENGINE_IT_ALLOW_HOST_GIT=1` 本地实验）自建远端仓库并完成 clone → 提交 → push → 再 clone 的往返。用例的共享工作根取自 `${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}`，每用例在其下分配唯一子目录；用例把本次分配的端口写入 `${MONOENGINE_IT_PORTS_FILE}`、把本轮启动的每个 `service http` 子进程 PID 写入 `${MONOENGINE_IT_PIDS_FILE}`（两者都是 Verification shell 指定的每轮唯一路径），供守卫**按本轮实际 PID 与端口**精确判定，不做全局文本匹配。**因为 receive-pack 强制认证（`src/contract/git_protocol/http.rs:345`、`:355`），本卡必须自带完成 push 所需的最小凭据链路**：向本用例的隔离 DB seed 一个一次性 access token（取自 `MONOENGINE_IT_SEED_TOKEN`，缺省时用例自行随机生成），并经 credential helper 或 `http.extraHeader` 注入 git、不写入 remote URL。该 helper 落在 `bin/tests/common/git_cli.rs`（经 `#[path]` 仅编入本 target），由 IT-10 复用；IT-10 只承接 401 挑战头与凭据零泄漏的断言。依赖缺失时必须以非零退出失败并打印可断言的原因串 `git-cli runner unavailable`；只有显式 `MONOENGINE_IT_SKIP_GIT_CLI=1` 才允许跳过（该 opt-out 语义写在此处，不单列为 AC）。本卡唯一行为轴是**往返一致性与用例隔离**；不重复 `scripts/git_protocol_smoke.sh` 已覆盖的正路径广度矩阵。新增的 git-cli helper 落在 `bin/tests/common/git_cli.rs`（经 `#[path]` 仅编入本 target），**不修改 `bin/tests/integration_vault.rs`**（ADR-IT-02）。**拆出 `IT-10`**（认证边界）。

**Out of scope:**

- 未认证 401 与 `WWW-Authenticate` 挑战头断言、以及凭据零泄漏审计 —— 由 IT-10 承接（自本卡拆出）。**注意：token 的 seed 与注入 helper 本身归本卡**，IT-10 只复用。
- 不存在仓库的失败路径与 liveness —— 由 IT-12 承接。
- SSH / LFS 通道 —— 由 PT-04 承接（DEFER-IT-01、DEFER-IT-02，见 DEP-05）。
- 正路径广度矩阵（ls-remote / fetch / protocol v2 / shallow / blob:none）—— 永久非目标：已由 `scripts/git_protocol_smoke.sh:246-252` 覆盖，重复实现无增量证据。
- CI 接入 —— 由 IT-04 承接。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 黑盒编排模式（启动 service、SIGINT 清理）已有实现可参照 | `bin/tests/integration_vault.rs:600`、`:756` |
| 共享 helper 模块已存在（32 行） | `bin/tests/common/mod.rs:1`、`:32` |
| 现有脚本的 push 用例只 push 后删 ref、从不回读，故无往返一致性断言 | `scripts/git_protocol_smoke.sh:120-150` |
| receive-pack 强制认证，因此任何 push 用例都必须自带凭据链路 | `src/contract/git_protocol/http.rs:64`（`check_push_permission`）、`:345`、`:355` |
| 脚本使用固定端口 9000、固定库与固定基址，无每用例隔离 | `.github/workflows/git-protocol-smoke.yml:133-163` |
| 脚本的 `SKIP:` 分支只对应「push / LFS 可选矩阵未开启」，此时以退出码 0 结束；真正的依赖缺失（缺 URL 或缺 `git`）走 `:33-41` 以退出码 2 失败。因此脚本形态下**没有**「runner 不可用时以可断言原因串失败」的语义，本卡需自建 | SKIP 分支 `scripts/git_protocol_smoke.sh:275-277`；依赖缺失分支 `:33-35`、`:38-41` |
| 覆盖矩阵当前无任何 Git 协议行 | `docs/refactoring/integration.md:504-520`（IT-03 后已有 `integration_git_cli` 行；本证据为开工前缺口） |

**Acceptance criteria:**

- [x] 用例自建远端仓库后的 HTTP clone 结果与预置 fixture 逐字节一致。
- [x] push 到 monorepo `refs/cl/*` 后：默认 tip 的再次 clone 仍与种子树一致；对新建 `refs/cl/*` tip 做定向 fetch 得到的工作树与 push 前工作树逐字节一致。
- [x] 连续运行本 target 两遍均全绿（证明用例之间无端口、临时目录或 DB 状态泄漏）。
- [x] 本 target 运行结束后，`${MONOENGINE_IT_PIDS_FILE}` 中记录的每个 PID 均已不存在。
- [x] 本 target 运行结束后其记录在 `${MONOENGINE_IT_PORTS_FILE}` 的高位端口均已释放。
- [x] 依赖缺失时本 target 以非零退出码失败。
- [x] 依赖缺失时本 target 的输出含 `git-cli runner unavailable`。
- [x] `docs/refactoring/integration.md` 覆盖矩阵新增 `integration_git_cli` 行（GC-06、G-02）。

**Verification:**

- [x] 隔离连跑守卫（AC3 证据；两遍合计一个通过/失败判定）：`bash -c 'set -euo pipefail; source .env.test; for i in 1 2; do cargo test -p monoengine --test integration_git_cli -- --test-threads=1 || { echo "FAIL: run $i"; exit 1; }; done; echo "OK: two consecutive clean runs"'`
- [x] 残留进程与端口守卫（**证据文件每轮唯一且运行前删除**，杜绝复用上一轮结果；残留进程按本轮 PID 清单逐个 `kill -0` 判定，**不做 `pgrep` 全局文本匹配**——那会命中承载同样字符串的父 shell；`ss` / `rg` 的退出码区分「无命中」与「命令失败」）：`bash -c 'set -uo pipefail; W="${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}"; export MONOENGINE_IT_PORTS_FILE="$W/ports-$$.txt"; export MONOENGINE_IT_PIDS_FILE="$W/pids-$$.txt"; rm -f "$MONOENGINE_IT_PORTS_FILE" "$MONOENGINE_IT_PIDS_FILE"; source .env.test || exit 1; cargo test -p monoengine --test integration_git_cli -- --test-threads=1 || { echo "FAIL: target failed"; exit 1; }; [ -s "$MONOENGINE_IT_PORTS_FILE" ] || { echo "FAIL: ports file not produced by this run"; exit 1; }; [ -s "$MONOENGINE_IT_PIDS_FILE" ] || { echo "FAIL: pids file not produced by this run"; exit 1; }; while read -r pid; do if kill -0 "$pid" 2>/dev/null; then echo "FAIL: service pid $pid still alive"; exit 1; fi; done < "$MONOENGINE_IT_PIDS_FILE"; while read -r p; do out=$(ss -ltnH "sport = :$p") || { echo "ERROR: ss failed for port $p"; exit 2; }; printf "%s" "$out" | rg -q .; rrc=$?; if [ "$rrc" -eq 0 ]; then echo "FAIL: port $p still bound"; exit 1; fi; [ "$rrc" -eq 1 ] || { echo "ERROR: rg exit $rrc"; exit 2; }; done < "$MONOENGINE_IT_PORTS_FILE"; echo "OK: no leftover process or port"'`
- [x] fail-closed 守卫（断言退出码 + 原因串 + 拒绝 `SKIP`；**服务恢复失败计入本守卫的最终判定**，不被 `trap` 吞掉）：`bash -c 'set -uo pipefail; docker compose -p monoengine-it -f docker-compose.test.yml stop git-cli || { echo "FAIL: cannot stop git-cli"; exit 1; }; source .env.test; src=$?; out=""; trc=0; if [ "$src" -eq 0 ]; then out=$(cargo test -p monoengine --test integration_git_cli -- --test-threads=1 2>&1); trc=$?; fi; docker compose -p monoengine-it -f docker-compose.test.yml start git-cli; rst=$?; v=0; [ "$src" -eq 0 ] || { echo "FAIL: source .env.test exited $src"; v=1; }; [ "$trc" -ne 0 ] || { echo "FAIL: expected non-zero exit from target"; v=1; }; printf "%s" "$out" | rg -q -F "git-cli runner unavailable"; r1=$?; [ "$r1" -eq 0 ] || { echo "FAIL: missing expected reason (rg exit $r1)"; v=1; }; printf "%s" "$out" | rg -q -F "SKIP"; r2=$?; if [ "$r2" -eq 0 ]; then echo "FAIL: SKIP printed without opt-out"; v=1; elif [ "$r2" -ne 1 ]; then echo "ERROR: rg exit $r2"; v=1; fi; [ "$rst" -eq 0 ] || { echo "FAIL: git-cli restart failed (exit $rst)"; v=1; }; [ "$v" -eq 0 ] && echo "OK: fail-closed"; exit "$v"'`
- [x] 手工证据：用例输出中的 `git --version` 与 monoengine 版本（sanitized 记录于 Review log 的 `E-IT-03` 行）

**Dependencies:** `IT-02`（消费 git-cli 服务与共享工作目录约定）、`IT-08`（消费修正后的 `.env.test.example` 键集合）（G-06）

**Deliverables:** `Inherited`

**Implementation write set:** `bin/tests/integration_git_cli.rs`（新增）、`bin/tests/common/git_cli.rs`（新增 helper）、`bin/tests/common/mod.rs`（指向说明）、`docs/refactoring/protocol.md`（smoke 清单标注最小集已进 cargo target）、`docs/refactoring/integration.md`（覆盖矩阵新增本 target 行）（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `bin/tests/integration_git_cli.rs`、`bin/tests/common/git_cli.rs`、`bin/tests/common/mod.rs`、`docs/refactoring/protocol.md`、`docs/refactoring/integration.md`

**Docs and compatibility impact:** `docs/refactoring/protocol.md` 的 smoke 清单标注；`docs/refactoring/integration.md` 的覆盖矩阵行

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** 测试仓库内容无 secret；服务日志沿用既有脱敏。本卡 seed 的一次性 token 只经 credential helper 注入、不写 remote URL、不落盘持久化；其**零泄漏审计**由 IT-10 承接（本卡不重复登记守卫）

**Performance budget:** 本 target wall-clock ≤ 3 分钟（含服务启动与两轮 clone）

**Estimated scope:** `S`（0 个生产落点：产物全部是本卡自己的测试文件与随附同步文档，按 G-04 属随附同步集）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（ER-04）

**D 组远端后置门:** **适用** —— 本卡改 `docs/refactoring/protocol.md`，命中 `git-protocol-smoke.yml:22`（`pull_request`）与 `:34`（`push`）的 `paths:`。登记：workflow=`.github/workflows/git-protocol-smoke.yml`、job=`git-protocol-smoke`、事件=`push`、ref=`main`、paths 命中=是、判据=job 结论为 success。

**Granularity:** `type=implementation; axis=Git CLI 对 HTTP 通道的往返一致性与用例隔离; recovery=revert 新增的 integration_git_cli.rs、common/git_cli.rs，以及 protocol.md / integration.md 的登记行，回到脚本-only 覆盖; complete=yes; self-contained=yes; AC=8/8; VER=4/8; landing=0; prod-files=0; scope=S; deps=IT-02,IT-08; writeset=序列化于 IT-01; release=independent; split-from=N/A（拆出 IT-10）; exception=N/A`

### Task IT-04: CI 执行面与触发面接入本计划的测试用例

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（D 组双 job 绿：`validate-config` [30466831819](https://github.com/gitmono-dev/monoengine/actions/runs/30466831819)、`git-protocol-smoke` [30479301244](https://github.com/gitmono-dev/monoengine/actions/runs/30479301244)；超时前滚见 v0.1.177，2026-07-30）

**Description:** 把本计划新增的**一个集成 test target**（`integration_git_cli`）与**一个 `monoengine-core` focused 用例**（IT-06 的双实例投递用例）接入 CI 的执行面，并把两个 workflow 的 `paths:` 触发面收敛到本卡 `Deliverables` 中声明的两份固定 allowlist。落点具名固定：`.github/workflows/config-validation.yml` 是 **`cargo test` 主入口**（`integration_vault`、`integration_git_cli`、IT-06 focused 与其它 monoengine-core 集成均在此跑）；`.github/workflows/git-protocol-smoke.yml` 扩触发面（allowlist B），并**额外**跑 `integration_git_cli` 作为协议路径上的二次执行面（与 shell smoke 同 job，不是第三个 `cargo test` workflow）。本卡唯一行为轴是「本计划测试用例在 CI 上的执行面与触发面覆盖」，恢复动作是 revert 这两个 workflow 文件与 `test-infra.md` 的 CI 登记。**拆出 `IT-11`**（git 客户端版本确定性）。

**Out of scope:**

- git / git-lfs 版本 pin 与 job summary 输出 —— 由 IT-11 承接（自本卡拆出）。
- 新建第三个 workflow —— 永久非目标：`config-validation.yml` 已是 `cargo test` 主入口；`git-protocol-smoke.yml` 上的 `integration_git_cli` 是协议路径二次门，另起第三个 workflow 会分裂事实源（GC-02）。
- `claude-review.yml` —— 永久非目标：只响应 issue/PR 事件、不由 push 触发，与本计划门禁无关，不得登记为 D 组项。
- 热加载用例的 CI 接入 —— 已落地（`.github/workflows/config-validation.yml` 已执行 `integration_vault`），无动作。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `config-validation.yml` 是 `cargo test` 主入口（含本计划 target / focused） | `.github/workflows/config-validation.yml`（`integration_vault` / `integration_git_cli` / IT-06 focused） |
| `git-protocol-smoke.yml` 在协议路径上二次执行 `integration_git_cli`（非唯一 `cargo test` 入口） | `.github/workflows/git-protocol-smoke.yml`（`integration_git_cli` 步骤） |
| 两 workflow 的 `pull_request`/`push` `paths:` 已收敛到 allowlist A/B（含 `bin/tests/**` / `docker-compose.test.yml` 等） | `.github/workflows/config-validation.yml`；`.github/workflows/git-protocol-smoke.yml` |
| `down -v` 清理已在 `if: always()` 下 | `.github/workflows/config-validation.yml`；`.github/workflows/git-protocol-smoke.yml` |

**Acceptance criteria:**

- [x] `config-validation.yml` 的测试步骤包含 `cargo test -p monoengine --test integration_git_cli -- --test-threads=1`。
- [x] `config-validation.yml` 的测试步骤包含 IT-06 的 `monoengine-core` focused 用例命令。
- [x] `config-validation.yml` 的 `pull_request` `paths:` 集合等于本卡 `Deliverables` 中的 allowlist A。
- [x] `config-validation.yml` 的 `push` `paths:` 集合等于 allowlist A。
- [x] `git-protocol-smoke.yml` 的 `pull_request` `paths:` 集合等于本卡 `Deliverables` 中的 allowlist B。
- [x] `git-protocol-smoke.yml` 的 `push` `paths:` 集合等于 allowlist B。
- [x] `config-validation.yml` 的 `down -v` 清理步骤仍在 `if: always()` 条件下。
- [x] `git-protocol-smoke.yml` 的 `down -v` 清理步骤仍在 `if: always()` 条件下。

**Verification:**

- [x] allowlist A 双向集合相等守卫（期望集合由本命令内联构造，无需任何预置文件）：`bash -c 'set -euo pipefail; wf=.github/workflows/config-validation.yml; printf "%s\n" ".github/workflows/config-validation.yml" "config/**" "src/cli.rs" "src/commands/config.rs" "src/config/**" "src/contract/vault/integration/vault_core.rs" "docs/refactoring/config.md" "docs/refactoring/mail.md" "docs/refactoring/vault.md" "README.md" "bin/tests/**" "docker-compose.test.yml" ".env.test.example" "src/notification/**" "src/jupiter/storage/notification_storage.rs" | sort -u > /tmp/a.want; for blk in pull_request push; do sed -n "/^  ${blk}:/,/^  [a-z_]*:\$/p" "$wf" | rg -N -o "^      - \"(.*)\"" -r "\$1" | sort -u > /tmp/a.have.$blk; d=$(diff /tmp/a.want /tmp/a.have.$blk || true); [ -z "$d" ] || { echo "FAIL: $blk paths differ from allowlist A"; echo "$d"; exit 1; }; done; echo "OK: allowlist A matches both blocks"'`
- [x] allowlist B 双向集合相等守卫（同上，期望集合内联构造）：`bash -c 'set -euo pipefail; wf=.github/workflows/git-protocol-smoke.yml; printf "%s\n" ".github/workflows/git-protocol-smoke.yml" "scripts/git_protocol_smoke.sh" "src/ceres/protocol/**" "src/contract/git_protocol/**" "src/ceres/lfs/**" "src/api/router/lfs_router.rs" "src/server/http_server.rs" "docs/refactoring/protocol.md" "docker-compose.test.yml" "src/ceres/pack/**" "src/jupiter/storage/git_db_storage.rs" "src/jupiter/storage/mono_storage.rs" "src/jupiter/storage/lfs_db_storage.rs" | sort -u > /tmp/b.want; for blk in pull_request push; do sed -n "/^  ${blk}:/,/^  [a-z_]*:\$/p" "$wf" | rg -N -o "^      - \"(.*)\"" -r "\$1" | sort -u > /tmp/b.have.$blk; d=$(diff /tmp/b.want /tmp/b.have.$blk || true); [ -z "$d" ] || { echo "FAIL: $blk paths differ from allowlist B"; echo "$d"; exit 1; }; done; echo "OK: allowlist B matches both blocks"'`

**Dependencies:** `IT-12`（`integration_git_cli` 的用例集已完整）、`IT-06`（focused 用例名已确定）（G-06）

**Deliverables:** 两份固定 allowlist 文件内容——
`/tmp/it04-allow-a.txt`（`config-validation.yml`）：既有 10 条（`.github/workflows/config-validation.yml`、`config/**`、`src/cli.rs`、`src/commands/config.rs`、`src/config/**`、`src/contract/vault/integration/vault_core.rs`、`docs/refactoring/config.md`、`docs/refactoring/mail.md`、`docs/refactoring/vault.md`、`README.md`）+ 新增 5 条（`bin/tests/**`、`docker-compose.test.yml`、`.env.test.example`、`src/notification/**`、`src/jupiter/storage/notification_storage.rs`），共 15 条。
`/tmp/it04-allow-b.txt`（`git-protocol-smoke.yml`）：既有 8 条（`.github/workflows/git-protocol-smoke.yml`、`scripts/git_protocol_smoke.sh`、`src/ceres/protocol/**`、`src/contract/git_protocol/**`、`src/ceres/lfs/**`、`src/api/router/lfs_router.rs`、`src/server/http_server.rs`、`docs/refactoring/protocol.md`）+ 新增 5 条（`docker-compose.test.yml`、`src/ceres/pack/**`、`src/jupiter/storage/git_db_storage.rs`、`src/jupiter/storage/mono_storage.rs`、`src/jupiter/storage/lfs_db_storage.rs`），共 13 条。

**Implementation write set:** `.github/workflows/config-validation.yml`、`.github/workflows/git-protocol-smoke.yml`、`docs/refactoring/test-infra.md`（登记 CI 入口）（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `.github/workflows/config-validation.yml`、`.github/workflows/git-protocol-smoke.yml`、`docs/refactoring/test-infra.md`

**Docs and compatibility impact:** `test-infra.md` 的 CI 入口登记

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** `::add-mask::` 覆盖全部测试凭据；orbit checkout token 纪律不变；新增触发路径不得引入 secret 暴露面（ER-11）

**Performance budget:** CI job 增量时长 ≤ 5 分钟（两个 workflow 合计）

**Estimated scope:** `S`（1 个行为落点 `.github/workflows/`、2 个生产文件）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（ER-04）

**D 组远端后置门:** **适用（两个 job）**：① workflow=`.github/workflows/config-validation.yml`、job=`validate-config`、事件=`push`、ref=`main`、paths 命中=是（`:20`）、判据=job success 且新增的两条测试命令均执行；② workflow=`.github/workflows/git-protocol-smoke.yml`、job=`git-protocol-smoke`、事件=`push`、ref=`main`、paths 命中=是（`:27`）、判据=job success。另含两处本地无法复现的语义：`::add-mask::` 脱敏与私有 `orbit` token checkout。`claude-review.yml` 不登记（不由 push 触发）。

**Granularity:** `type=implementation; axis=本计划测试用例在 CI 上的执行面与触发面覆盖; recovery=revert config-validation.yml、git-protocol-smoke.yml 与 test-infra.md 的 CI 登记，回到既有触发面; complete=yes; self-contained=yes; AC=8/8; VER=2/8; landing=1; prod-files=2; scope=S; deps=IT-12,IT-06; writeset=序列化于 IT-01; release=independent; split-from=N/A（拆出 IT-11）; exception=N/A`

### 已关闭编号 IT-05: 热加载黑盒用例 `integration_config_hot_reload`（非任务卡）

**Status:** **关闭（已落地，2026-07-29 核对确认）。** 本条目**不是任务卡**，不参与「任务卡粒度审计表」、实施顺序与完成判据的任务统计；编号保留不再复用。原计划的该卡在成稿前即已由 `bin/tests/integration_vault.rs:872` 的 `integration_config_hot_reload` 实现，并经 `.github/workflows/config-validation.yml:294` 在 CI 执行。按 GC-01，不重复实现。

| 原 AC | 落地证据 |
|---|---|
| 白名单字段热生效、不重启、有日志证据 | 断言 `"config reload watcher applied changed config"`，reload report 含 `log.level` |
| 非白名单变更标记 restart-required、旧快照继续服务 | 断言 `monorepo.import_dir` 出现在 `restart_required_fields`，并用 `/api/v1/tree/path-can-clone` 证明旧 `import_dir` 仍在服务 |
| 非法值回滚、服务不中断 | 写入非法 TOML 后断言 `"config reload watcher rejected changed config"` 且服务仍 200 |
| 接入 CI | `.github/workflows/config-validation.yml:294` |

原卡遗留的开工问题「触发机制是 SIGHUP 还是 HTTP API」已核实：**两者都不是**，是**轮询文件 watcher**（`src/config/reload.rs:191`、`:223`、`:233`，由 `src/commands/service/mod.rs:44-49` 调用、`:131` 处 `tokio::spawn`，间隔常量在 `:20`），用例通过重写 profile 文件触发。该事实由 **IT-07** 承接，用于修正 `docs/refactoring/integration.md:426` 仍写「明确 SIGHUP 或 HTTP API」的措辞。

### Task IT-06: 并发 dispatcher 端到端投递基线用例

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（执行期收口，2026-07-29）

**Description:** storage 层并发 claim 已有覆盖，剩余缺口是端到端并发 dispatcher 投递。在 `src/notification/dispatcher.rs` 的 `#[cfg(test)] mod tests` 内新增用例 `integration_mail_dispatcher_two_instances_deliver_exactly_once`：对同一测试 DB 运行两个完整 dispatcher 实例，并发执行 outbox 投递循环（真实 Mailpit）。本卡唯一行为轴是「同一 DB 上两个 dispatcher 实例的 exactly-once 投递门」。**若用例暴露 dispatcher 层缺陷，不得在本卡内修生产代码**：按 ER-10 新建 `FIX-IT-06-01`、加 `FIX-IT-06-01 -> IT-06` 边、本卡置 `blocked`（ADR-IT-03）。

**Out of scope:**

- 生产代码缺陷修复 —— 由 ER-10 派生的 `FIX-IT-06-01` 承接（用例暴露缺陷时才建卡）。
- storage 层 claim 语义测试 —— 永久非目标：已覆盖（`src/jupiter/storage/notification_storage.rs:1254`、`:1313`、`:1372`），重复无增量证据。
- 真多进程黑盒竞争矩阵 —— 由 PT-09 承接（DEFER-IT-03，见 DEP-06）。
- SMTP 成功后、`mark_job_sent` 前崩溃的 at-least-once 窗口 —— 需 idempotency key，见 `DEFER-IT-13`（用例注释亦指向 `FIX-IT-06-01` 若另行追）。
- CI 接入 —— 由 IT-04 承接（G-10 允许的「合并到唯一集成卡」）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| storage 层并发 claim（含独立连接变体）与 stale 恢复已覆盖 | `src/jupiter/storage/notification_storage.rs:1254`、`:1313`、`:1372` |
| dispatcher 拉取路径 | `src/notification/dispatcher.rs:343`（`fetch_pending_jobs`） |
| dispatcher 测试模块现有 7 个 `integration_mail_dispatcher_*` 用例，无一涉及双实例 | `src/notification/dispatcher.rs:810`、`:960`、`:1010`、`:1066`、`:1123`、`:1181`、`:1228` |
| 唯一并发断言是单实例内有界并行 | `src/notification/dispatcher.rs:1339` |
| 多实例竞争被 mail.md 明确列为遗留缺口 | `docs/refactoring/mail.md:193` |
| 覆盖矩阵当前无本用例行 | `docs/refactoring/integration.md:504-520`（IT-06 后已有 focused 行；本证据为开工前缺口） |

**Acceptance criteria:**

- [x] N 封邮件由 2 个 dispatcher 实例并发投递后，Mailpit 收到的邮件数恰好为 N。
- [x] Mailpit 中不存在同一 outbox 记录被投递两次的邮件。
- [x] 全部 outbox 记录的终态为已发送。
- [x] 用例结束后无残留 `pending` 状态的 outbox 记录。
- [x] 并发下被 stale `sending` 恢复的任务（**SMTP 投递前**造 stale、无先验投递）不产生第二封邮件。
- [x] 用例连续 10 轮全部通过。
- [x] Mailpit 不可达时用例以非零退出结束。
- [x] `docs/refactoring/integration.md` 覆盖矩阵新增本用例行（GC-06、G-02）。

**Verification:**

- [x] 10 轮稳定性守卫（十轮合计一个通过/失败判定）：`bash -c 'set -euo pipefail; source .env.test; for i in $(seq 1 10); do cargo test -p monoengine-core "notification::dispatcher::tests::integration_mail_dispatcher_two_instances_deliver_exactly_once" || { echo "FAIL at round $i"; exit 1; }; done; echo "OK: 10 rounds"'`
- [x] Mailpit fail-closed 守卫（**服务恢复失败计入本守卫的最终判定**）：`bash -c 'set -uo pipefail; docker compose -p monoengine-it -f docker-compose.test.yml stop mailpit || { echo "FAIL: cannot stop mailpit"; exit 1; }; source .env.test; src=$?; trc=0; if [ "$src" -eq 0 ]; then cargo test -p monoengine-core "notification::dispatcher::tests::integration_mail_dispatcher_two_instances_deliver_exactly_once"; trc=$?; fi; docker compose -p monoengine-it -f docker-compose.test.yml start mailpit; rst=$?; v=0; [ "$src" -eq 0 ] || { echo "FAIL: source .env.test exited $src"; v=1; }; [ "$trc" -ne 0 ] || { echo "FAIL: case passed with Mailpit down"; v=1; }; [ "$rst" -eq 0 ] || { echo "FAIL: mailpit restart failed (exit $rst)"; v=1; }; [ "$v" -eq 0 ] && echo "OK: fail-closed"; exit "$v"'`

**Dependencies:** `IT-08`（消费修正后的 `.env.test.example` 键集合）、`IT-03`（`docs/refactoring/integration.md` 写集串行化）（G-06）

**Deliverables:** `Inherited`

**Implementation write set:** `src/notification/dispatcher.rs`（仅 `#[cfg(test)] mod tests` 内新增用例与 helper）、`docs/refactoring/integration.md`（覆盖矩阵新增本用例行）（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `src/notification/dispatcher.rs`、`docs/refactoring/integration.md`

**Docs and compatibility impact:** `docs/refactoring/integration.md` 的覆盖矩阵行

**Rollback mode:** `revert`（`#[cfg(test)]`-only 变更，无 schema、无迁移）（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** `Inherited`

**Performance budget:** 单轮用例 wall-clock ≤ 2 分钟；claim 查询不得退化为全表扫描（GC-10）

**Estimated scope:** `S`（0 个生产落点：变更全部在本卡自己的 `#[cfg(test)]` 测试模块与随附同步文档内）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（ER-04）

**D 组远端后置门:** `N/A` —— `src/notification/**` 与 `docs/refactoring/integration.md` 当前均不在任一 workflow 的 `paths:` 内。本卡在自身发布窗口内 D 组恒为 `N/A`：`IT-04` 排在本卡之后，其后续的触发面扩展只能产生 **IT-04 自己的** D 组证据，**不能追溯改变本卡已完成的 `Acceptance`**。

**Granularity:** `type=implementation; axis=同一 DB 上两个 dispatcher 实例的 exactly-once 投递门; recovery=revert dispatcher.rs 的测试模块新增块与 integration.md 的矩阵行; complete=yes; self-contained=yes; AC=8/8; VER=2/8; landing=0; prod-files=0; scope=S; deps=IT-08,IT-03; writeset=序列化于 IT-03; release=independent; split-from=N/A; exception=N/A`

### Task IT-07: integration.md P2 收口、内嵌样例对齐与 plan-long 同步

**Task type:** `docs`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（IT-09 C 覆盖，2026-07-29）

**Description:** 逐项收口 `docs/refactoring/integration.md` 的 P2 清单与内嵌 compose 样例，消除其与实际测试现状的漂移，并按 PT-01 完成判据（`docs/plan/plan-long.md:200-204`）逐条复核后更新 `plan-long.md` 与本计划自身的索引表。本卡唯一行为轴是「本计划的文档与索引收口」。各测试用例的覆盖矩阵行由其行为卡自带（IT-03、IT-06），本卡不代劳。

**Out of scope:**

- 任何代码或配置变更 —— 永久非目标：本卡是 `docs` 类，一旦需要改代码必须按 ER-03 重分类后再执行。
- `test-infra.md` 的内容本身 —— 由 IT-01 承接（本卡只确认 integration.md 的链接存在）。
- `integration_git_cli` 与并发投递用例的覆盖矩阵行 —— 由 IT-03、IT-06 各自承接（G-02）。
- `README.md` / `AGENTS.md` 的漂移修订 —— 尚未排期，作为独立文档修复项承接（见「与其它计划的关系」）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| P2 收口表（IT-07 后） | `docs/refactoring/integration.md:354-364`：`config init`/热加载/多渠道扇出已落地；`DEFER-IT-07`/`DEFER-IT-08`/`DEFER-IT-10` 书面关闭 |
| 热加载触发为轮询文件 watcher | 同表热加载行；实现锚点 `src/config/reload.rs` / `src/commands/service/mod.rs:44-49`、`:131` |
| 多渠道通知黑盒已落地 | `bin/tests/integration_vault.rs:1008`；覆盖矩阵 `docs/refactoring/integration.md:504-520` |
| 内嵌 compose / CI 样例已删除 | `docs/refactoring/integration.md:119-127`、`:407-414` 改为链接 `docker-compose.test.yml` 与真实 workflow |
| plan-long 日期计划索引已登记本文件 | `docs/plan/plan-long.md:744`（索引节）、`:103`（PT-01 行含本文件链接与最终卡集） |
| PT-01 的完成判据在 `:200-204`；`:178` 是上一节 SB-03 的完成判据，不可混用 | `docs/plan/plan-long.md:200`（`### 完成判据` 标题）、`:202-204`（三条判据） |

**Acceptance criteria:**

- [x] P2 表 `config init` 行保持「已落地」。
- [x] `config init` 行补上指向 `integration_config_init_creates_safe_skeleton_and_validates` 的证据链接。
- [x] P2 表热加载行改为「已落地」。
- [x] 热加载行附 `bin/tests/integration_vault.rs:872` 证据。
- [x] 热加载行删除「明确 SIGHUP 或 HTTP API」措辞。
- [x] 热加载行写明触发机制为轮询文件 watcher。
- [x] P2 表多渠道通知行改为「已落地」。
- [x] 多渠道通知行引用 `bin/tests/integration_vault.rs:1008`。
- [x] P2 表用户通知设置 API 行结论为「已落地」或「书面关闭（含理由与承接编号）」之一（PT-01 完成判据不接受「延后」）。
- [x] P2 表对象存储 SecretRef 行结论为「已落地」或「书面关闭（含理由与承接编号）」之一。
- [x] `docs/refactoring/integration.md:152-192` 的内嵌 compose YAML 已删除。
- [x] 该位置改为指向 `docker-compose.test.yml` 的链接（GC-02）。
- [x] `docs/refactoring/integration.md` 中不再保留与 `docker-compose.test.yml` 冲突的服务清单描述。
- [x] `docs/plan/plan-long.md` 的 PT-01 状态按 `:200-204` 的完成判据逐条核对后更新。
- [x] `docs/plan/plan-long.md:750` 日期计划索引的「范围」列由 `IT-01..IT-07` 更新为本计划最终的 11 张卡集，并注明 `IT-05` 已关闭为非任务卡。
- [x] 本计划「追溯表」与最终卡集一致。
- [x] 本计划「测试矩阵」与最终 target 集一致。
- [x] 本计划「任务卡粒度审计表」与各卡 `Granularity` 行逐字段一致。
- [x] 本计划「修订历史」包含成稿后的全部规范性变更。
- [x] 本计划「里程碑验收与回滚」的回滚动作与各卡最终 `Implementation write set` 一致。

**Verification:**

- [x] 粒度审计表逐字段双向比对（拒绝重复任务行、断言每行恰好「任务 ID + 14 个数据字段」、任务 ID 集合双向相等，再逐字段比较）：`bash -c 'set -euo pipefail; python3 - <<"PY"
import re,sys
t=open("docs/plan/plan-20260727.md").read()
sec=t.split("#### 任务卡粒度审计表")[1].split("#### Verification 判定口径")[0]
COLS=["type","axis","recovery","complete","self-contained","AC","VER","landing/prod","scope","deps","writeset","release","split-from","exception"]
bad=[]; rows={}
for ln in sec.splitlines():
    if not ln.startswith("| IT-"): continue
    c=[x.strip() for x in ln.strip("|").split("|")]
    if len(c)!=15: bad.append((c[0] if c else "?","column-count",len(c),15)); continue
    if c[0] in rows: bad.append((c[0],"duplicate-audit-row")); continue
    rows[c[0]]=c[1:]
cards={}
for m in re.finditer(r"### Task (IT-\d+):[\s\S]*?\*\*Granularity:\*\* `([^`]+)`", t):
    cards[m.group(1)]=dict(kv.split("=",1) for kv in [p.strip() for p in m.group(2).split(";")] if "=" in kv)
if set(rows)!=set(cards): bad.append(("id-set",sorted(set(rows)^set(cards))))
norm=lambda x: re.sub(r"\s+","",x)
for cid in sorted(set(rows)&set(cards)):
    g=cards[cid]
    want=[g["type"],g["axis"],g["recovery"],g["complete"],g["self-contained"],g["AC"],g["VER"],
          g["landing"]+"/"+g["prod-files"],g["scope"],g["deps"],g["writeset"],g["release"],g["split-from"],g["exception"]]
    for k,(a,b) in enumerate(zip(rows[cid],want)):
        if norm(a)!=norm(b): bad.append((cid,COLS[k],a,b))
print("FAIL",bad) if bad else print("OK: audit table matches every Granularity line, no dup/short rows")
sys.exit(1 if bad else 0)
PY'`
- [x] 覆盖矩阵比对（**按 Markdown 表格行解析**，只认独立数据行，不接受名称仅出现在说明文字里；并核对 target 文件名与 focused test fn 真实存在）：`bash -c 'set -euo pipefail; python3 - <<"PY"
import re,sys,os
doc=open("docs/refactoring/integration.md").read()
m=re.search(r"## 覆盖矩阵([\s\S]*?)(?=\n## |\Z)", doc)
if not m: print("FAIL: coverage matrix section not found"); sys.exit(1)
rows=[l for l in m.group(1).splitlines() if l.strip().startswith("|") and not re.match(r"^\|[-\s|]+\|$", l.strip())]
cells=[[c.strip() for c in l.strip().strip("|").split("|")] for l in rows]
first=[c[0] if c else "" for c in cells]
def has_row(name): return any(name in f for f in first)
bad=[]
if not has_row("integration_git_cli"): bad.append("no data row for integration_git_cli")
if not has_row("integration_mail_dispatcher_two_instances_deliver_exactly_once"): bad.append("no data row for the dispatcher focused case")
if not os.path.exists("bin/tests/integration_git_cli.rs"): bad.append("target file bin/tests/integration_git_cli.rs missing")
if "fn integration_mail_dispatcher_two_instances_deliver_exactly_once" not in open("src/notification/dispatcher.rs").read():
    bad.append("focused test fn not found in src/notification/dispatcher.rs")
print("FAIL",bad) if bad else print("OK: both objects have their own matrix data row and exist in code")
sys.exit(1 if bad else 0)
PY'`
- [x] 日期计划索引与最终卡集**双向**比对（只取 `## 日期计划索引` 节内含 `plan-20260727.md` 的表格行，不命中总览表 PT-01 行；索引里的任务 ID 集合必须与计划的任务卡集合完全相等——多余或过期 ID 同样判 FAIL；IT-05 的关闭说明单独断言）：`bash -c 'set -euo pipefail; python3 - <<"PY"
import re,sys
long=open("docs/plan/plan-long.md").read()
sec=re.search(r"## 日期计划索引([\s\S]*?)(?=\n## |\Z)", long)
if not sec: print("FAIL: date-plan index section not found"); sys.exit(1)
idx=[l for l in sec.group(1).splitlines() if "plan-20260727.md" in l and l.strip().startswith("|")]
if not idx: print("FAIL: date-plan index row not found"); sys.exit(1)
row=idx[0]
cells=[c.strip() for c in row.strip().strip("|").split("|")]
# Markdown index columns: 日期计划 | PT | 状态 | 范围 | ...
if len(cells) < 4: print("FAIL: index row has fewer than 4 cells"); sys.exit(1)
scope=cells[3]
plan=open("docs/plan/plan-20260727.md").read()
cards=set(re.findall(r"^### Task (IT-\d+):", plan, re.M))
ids=set(re.findall(r"IT-\d+", scope))
closed={"IT-05"}
bad=[]
extra, missing = sorted(ids-cards-closed), sorted(cards-ids)
rng=re.search(r"IT-(\d+)\.\.IT-(\d+)", scope)
if rng: bad.append("index still uses a range form %s; list the explicit card set" % rng.group(0))
if missing: bad.append("cards missing from index scope cell: %s" % missing)
if extra: bad.append("stale/extra ids in index scope cell: %s" % extra)
if "IT-05" not in scope: bad.append("index scope cell does not mention the closed IT-05")
print("FAIL",bad) if bad else print("OK: index card set == plan card set, IT-05 noted")
sys.exit(1 if bad else 0)
PY'`

**Dependencies:** `IT-01`（链接目标存在）、`IT-11`（CI 侧最后一张实现卡，其结论可引用）、`IT-06`（最后一个写 `integration.md` 的行为卡，写集串行化）（G-06）

**Deliverables:** `docs/refactoring/integration.md`、`docs/plan/plan-long.md`、`docs/plan/plan-20260727.md` 三文件的收口版本。

**Implementation write set:** `docs/refactoring/integration.md`、`docs/plan/plan-long.md`、`docs/plan/plan-20260727.md`（G-10）

**Release write set:** `N/A`（`no-release` 卡）

**Files likely touched:** 同上

**Docs and compatibility impact:** 即任务本身

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** `Inherited`

**Performance budget:** `Inherited`

**Estimated scope:** `S`（3 个产物文件 ≤ 5，`docs` 口径）（G-04）

**Version increment:** `N/A`（产物随 IT-09 的提交进入仓库）（ER-08）

**Release boundary:** `no-release`

**C/D coverage from:** `IT-09`（继承其 C 组覆盖与 D 组）（ER-04）

**D 组远端后置门:** `N/A` —— 三个文件均不命中任一 workflow 的 `paths:`；由 IT-09 的 D 组结论统一承接。

**Granularity:** `type=docs; axis=本计划的文档与索引收口（P2 定性 + 内嵌样例对齐 + PT-01 状态）; recovery=revert integration.md、plan-long.md、plan-20260727.md 三处改动; complete=yes; self-contained=yes; AC=20/20; VER=3/20; landing=0; prod-files=0; scope=S; deps=IT-01,IT-11,IT-06; writeset=序列化于 IT-06; release=no-release; split-from=N/A; exception=N/A`

### Task IT-08: `.env.test.example` 变量补齐与 `export` 语义修复

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（执行期收口，2026-07-29）

**Description:** 自 `IT-02` 拆出。`.env.test.example` 当前的生效行是裸赋值，`source .env.test && cargo test` 不会把变量传给 cargo 子进程；同时缺少 CI 注入的 `MAILPIT_API_URL` 与用例读取的 `MAILPIT_SMTP_HOST` / `MAILPIT_SMTP_PORT`。由于现有用例对这三类地址都有指向测试栈的默认值兜底，当前无法证明示例是否生效。本卡唯一行为轴是「测试环境变量示例与 CI env 对齐且 `source` 后可被子进程继承」。本卡的固定 allowlist 见 `Deliverables`。**不新增** `MEGA_BASE_DIR`（用例各自分配临时目录）。

**Out of scope:**

- 生成本地 `.env.test` —— 永久非目标：该文件被忽略且按 `AGENTS.md:102` 必须由使用者确认后提供（计划级前置 DEP-09），本卡只改示例。
- 移除测试用例中的默认值兜底 —— 尚未排期：默认值使本地开箱可用，移除会改变既有用例行为，属独立行为轴。
- git-cli 服务本身 —— 由 IT-02 承接（本卡自其拆出）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 示例文件 3 行生效项全为裸赋值；注释块中的 rustfs/S3 项同样是裸赋值 | `.env.test.example:1`、`:2`、`:3`、`:5-12` |
| CI 注入三项，示例缺 `MAILPIT_API_URL` | `.github/workflows/config-validation.yml:288`、`:289`、`:290` |
| 用例读取 `MAILPIT_API_URL` / `MAILPIT_SMTP_HOST` / `MAILPIT_SMTP_PORT`，且三者均有默认值兜底 | `bin/tests/integration_vault.rs:1606-1612`、`:1620-1628` |
| DB / Redis 地址同样有默认值兜底，故 target 全绿不能证明示例生效 | `bin/tests/integration_vault.rs:693-701` |
| compose 暴露的 Mailpit 端口 | `docker-compose.test.yml:30`（SMTP `11025`）、`:31`（HTTP `18025`） |

**Acceptance criteria:**

- [x] `.env.test.example` 的全部生效行使用 `export KEY=value` 形式。
- [x] 文件的生效键集合与 `Deliverables` 中的固定 allowlist 完全相等（双向无差集）。
- [x] 该 allowlist 覆盖 `.github/workflows/config-validation.yml:288-290` 在「Run integration tests」步骤注入的三个测试环境键。
- [x] `MAILPIT_API_URL` 的取值指向 `docker-compose.test.yml:31` 暴露的 HTTP 端口 `18025`。
- [x] `MAILPIT_SMTP_HOST` 的取值为 `127.0.0.1`。
- [x] `MAILPIT_SMTP_PORT` 的取值等于 `docker-compose.test.yml:30` 暴露的 SMTP 端口 `11025`。
- [x] `source` 本文件后，文件声明的每个键都出现在子进程 `env` 的输出中（声明集合 ⊆ 可见集合）。

**Verification:**

- [x] 继承语义断言（直接 source 本卡产物，不依赖 `.env.test`；断言「声明集合 ⊆ 子进程可见集合」，与 AC 口径一致）：`bash -c 'set -euo pipefail; cp .env.test.example /tmp/it08.env; declared=$(rg -N -o "^export ([A-Z_]+)=" -r "\$1" /tmp/it08.env | sort -u); [ -n "$declared" ] || { echo "FAIL: no exported keys declared"; exit 1; }; visible=$(bash -c ". /tmp/it08.env; env" | rg -N -o "^([A-Z_]+)=" -r "\$1" | sort -u); miss=$(comm -23 <(echo "$declared") <(echo "$visible")); [ -z "$miss" ] || { echo "FAIL: declared but not exported: $miss"; exit 1; }; echo "OK: every declared key is visible in the child env"'`
- [x] 裸赋值零命中守卫（拒绝任何未以 `export` 开头的非注释生效赋值行——只提取 `export KEY=` 无法发现残留或新增的裸赋值，AC1 会假绿；按退出码模板区分零命中与命令失败）：`bash -c 'set -uo pipefail; if rg -n "^[[:space:]]*[A-Za-z_][A-Za-z0-9_]*=" .env.test.example; then echo "FAIL: bare assignment(s) found; every effective line must start with export"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: no bare assignments"; fi'`
- [x] 键集合比对（示例 ↔ 固定 allowlist 双向相等；CI env 键为示例键的子集）：`bash -c 'set -euo pipefail; printf "MEGA_DATABASE__DB_TYPE\nMEGA_DATABASE__DB_URL\nMEGA_REDIS__URL\nMAILPIT_API_URL\nMAILPIT_SMTP_HOST\nMAILPIT_SMTP_PORT\n" | sort -u > /tmp/it08.want; rg -N -o "^export ([A-Z_]+)=" -r "\$1" .env.test.example | sort -u > /tmp/it08.have; d=$(diff /tmp/it08.want /tmp/it08.have || true); [ -z "$d" ] || { echo "FAIL: example keys differ from allowlist"; echo "$d"; exit 1; }; sed -n "/- name: Run integration tests/,/^        run:/p" .github/workflows/config-validation.yml | rg -N -o "^          ([A-Z_]+): " -r "\$1" | sort -u > /tmp/it08.ci; [ -s /tmp/it08.ci ] || { echo "FAIL: could not extract the integration-test env block"; exit 1; }; extra=$(comm -23 /tmp/it08.ci /tmp/it08.have); [ -z "$extra" ] || { echo "FAIL: CI test-env keys absent from example: $extra"; exit 1; }; echo "OK: example == allowlist (both ways) and the integration-test env keys are a subset"'`

**Dependencies:** 无（G-06；计划级前置 DEP-01、DEP-09 见「本计划的 ER-01 现状补记」）

**Deliverables:** `.env.test.example` 的固定键 allowlist（六项）：`MEGA_DATABASE__DB_TYPE`、`MEGA_DATABASE__DB_URL`、`MEGA_REDIS__URL`、`MAILPIT_API_URL`、`MAILPIT_SMTP_HOST`、`MAILPIT_SMTP_PORT`。

**Implementation write set:** `.env.test.example`（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `.env.test.example`

**Docs and compatibility impact:** 开发者环境示例；无用户可见命令或配置项变化

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** 示例文件只含公开测试口令，记录到验收证据时仍按 ER-11 脱敏

**Performance budget:** `Inherited`

**Estimated scope:** `S`（1 个行为落点、1 个生产文件）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（ER-04）

**D 组远端后置门:** `N/A` —— `.env.test.example` 当前不在任一 workflow 的 `paths:` 内（IT-04 会把它加入 `config-validation.yml` 的触发面）。

**Granularity:** `type=implementation; axis=测试环境变量示例与 CI env 对齐且 source 后可被子进程继承; recovery=revert .env.test.example 单文件; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=none; writeset=no-overlap; release=independent; split-from=IT-02; exception=N/A`

### Task IT-09: IT-07 的收口承载发布卡

**Task type:** `release`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（执行期收口发布，2026-07-29）

**Description:** 本计划最后一个发布切片，不引入任何新行为，用于把 IT-07 这张 `no-release` 卡的产物带入仓库并为其提供 C 组覆盖与 D 组结论。**本计划的其它实现卡各自独立发布**，本卡不是它们的发布点。ER-04 C 组的 7 步（parity 预检 → 按本卡进入发布窗口时 `Cargo.toml` 的权威版本 `patch +1` 并同步两处 + 刷新 `Cargo.lock` → 在已 bump 状态跑三门 → `cargo build` 与 `cargo build --tests` 0 错 0 警告 → `libra add` + `libra commit -s -m "v<version>: <summary>"` 含 ER-07 签名预检与提交后 `gpgsig` 校验 → `libra push origin main` 并确认远端 ref → tag/artifact 记 `N/A`）由 ER-04 强制执行，不重复登记为本卡 `Verification`。`release` 类型门要求的聚合守卫与 release note / 兼容证据同属 ER-04 的 B 组，按本计划「字段全局默认与例外」的统一口径**不进入本卡 `Verification`**；本卡 `Verification` 只登记 crate 级 allow 基线这一本卡特有守卫。

**Out of scope:**

- 任何行为变更 —— 永久非目标：`release` 卡按 G-11 不引入新行为。
- 其它实现卡的发布 —— 永久非目标：IT-02/03/04/06/08/10/11/12 各自 `independent` 发布，不并入本卡。
- 推 tag 或产出 release artifact —— 永久非目标（本仓无 release 流水线）；确需时必须先用独立卡引入 release workflow 并同步修订模板。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 版本面两处当前一致（本计划开工前基线），parity 预检通过 | `Cargo.toml:6`、`bin/Cargo.toml:3`（均为 `0.1.166`） |
| 三个 workflow 均**不由 tag 触发** | `.github/workflows/config-validation.yml:3-29`（`on:` 块）、`.github/workflows/git-protocol-smoke.yml:3-34`、`.github/workflows/claude-review.yml:3-12` |
| 三个 workflow 均**无 artifact 上传**：对 `.github/workflows/` 搜索 `actions/upload-artifact` 为零命中（核对时按「Verification 判定口径」的退出码模板区分零命中与命令失败） | `.github/workflows/` 全目录零命中（2026-07-29 核对） |
| 提交信息既有风格：发布类用 `v<version>: <summary>` | `libra log` 中的 `bc79bd8`（`v0.1.166: run git-protocol-smoke on LFS code changes`） |
| 顶层 inner `#![allow(...)]` 的既有基线共 3 处；其中只有 `src/lib.rs:1` 是 crate-level attribute，另两处是子模块文件的 inner module attribute | `src/lib.rs:1`（`#![allow(dead_code)]`）、`src/vault/core.rs:1`、`src/vault/utils/seal.rs:1`（均为 `#![allow(unused_assignments)]`） |

**Acceptance criteria:**

- [ ] `Cargo.toml` 与 `bin/Cargo.toml` 的 `version` 一致。
- [ ] 该版本相对**本卡进入发布窗口时** `Cargo.toml` 的权威版本为 `patch +1`。
- [ ] `Cargo.lock` 中两个包的版本条目已随之更新。
- [ ] `Cargo.lock` 由工具链刷新，无手工编辑痕迹。
- [ ] 提交通过 `libra cat-file -p HEAD | rg -q '^gpgsig'` 校验。
- [ ] 远端 `main` ref 已更新到本次提交。
- [ ] 顶层 inner `#![allow(...)]` 命中集合与既有基线三处完全相等（既无新增也无删除；其中只有 `src/lib.rs:1` 是 crate-level attribute，另外两处是子模块文件的 inner module attribute）。
- [ ] tag 记为 `N/A`。
- [ ] tag 的 `N/A` 附理由（本仓无 tag 触发的 workflow）。
- [ ] release artifact 记为 `N/A`。
- [ ] artifact 的 `N/A` 附理由（本仓无 artifact 上传）。

**Verification:**

- [ ] 顶层 inner allow 集合相等守卫（双向差集；捕获**任意** `#![allow(...)]` 语法——含 `clippy::` 前缀、多 lint 与内部空格；扫描范围覆盖完整的 `src/` 与 `bin/`——`bin/tests/*.rs` 每个都是独立 crate root，本计划会在其中新增文件，只扫 `bin/src` 会漏掉；`rg` 退出码显式处理）：`bash -c 'set -uo pipefail; rg -N --with-filename -o "^#!\[allow\(.*\)\]" src bin > /tmp/it09.now.raw; rc=$?; if [ "$rc" -ne 0 ] && [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; sort -u /tmp/it09.now.raw > /tmp/it09.now; printf "src/lib.rs:#![allow(dead_code)]\nsrc/vault/core.rs:#![allow(unused_assignments)]\nsrc/vault/utils/seal.rs:#![allow(unused_assignments)]\n" | sort -u > /tmp/it09.base; d=$(diff /tmp/it09.base /tmp/it09.now || true); [ -z "$d" ] || { echo "FAIL: crate-level allow set changed"; echo "$d"; exit 1; }; echo "OK: allow set unchanged"'`

**Dependencies:** `IT-07`（其产物随本卡提交进入仓库）（G-06）

**Deliverables:** 提交与推送证据（sanitized）：版本号、提交摘要、远端 ref 更新确认。

**Implementation write set:** `N/A`（不引入行为变更）（G-10）

**Release write set:** `Cargo.toml`、`bin/Cargo.toml`、`Cargo.lock`

**Files likely touched:** `Cargo.toml`、`bin/Cargo.toml`、`Cargo.lock`

**Docs and compatibility impact:** `N/A`（不改公开行为）

**Rollback mode:** `immutable-release`（已推送提交不可撤回，只能前滚新版本）（G-01）

**Migration and rollback:** `N/A`；降级指引：本卡不改运行时行为，回退需求由前滚一个新 patch 版本满足，无兼容窗口要求

**Security and privacy:** 提交与推送证据按 ER-11 脱敏；不得在日志中打印凭据

**Performance budget:** `N/A`

**Estimated scope:** `S`（0 个行为落点、0 个生产文件）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（并为 IT-07 提供承载）（ER-04）

**D 组远端后置门:** 开工日按最终写集重新核对；版本面三文件当前不命中任何 `paths:`，预期 `N/A`。若本卡提交同时携带 IT-07 的文档改动，仍不命中两个 workflow 的 `paths:`。

**Granularity:** `type=release; axis=IT-07 的收口承载发布卡; recovery=前滚新 patch 版本，不回退已推送提交; complete=yes; self-contained=yes; AC=11/12; VER=1/12; landing=0; prod-files=0; scope=S; deps=IT-07; writeset=no-overlap; release=independent; split-from=N/A; exception=N/A`

### Task IT-10: Git CLI 认证边界断言

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（执行期收口，2026-07-29）

**Description:** 自 `IT-03` 拆出。IT-03 已经交付「seed 一次性 token + credential helper 注入」的最小凭据链路（其 push 往返用例依赖它），本卡在同一 target 上追加**认证边界**用例：未携带 token 的 push 必须被拒并给出可协商的挑战头；并把 IT-03 的凭据链路提升为**可审计的零泄漏保证**——token 不得出现在 remote URL、测试输出或共享工作目录的任何文件中。本卡复用 IT-03 的 helper，不重复实现 seed 与注入。本卡新增的用例统一以 `integration_git_cli_auth_` 前缀命名（至少 `integration_git_cli_auth_push_without_token_returns_401_challenge` 与 `integration_git_cli_auth_token_never_leaks` 两个 `(new)` 用例），便于守卫定向运行。本卡唯一行为轴是**Git smart HTTP 认证边界与凭据零泄漏在真实客户端下的断言**。本卡与 IT-03 共写同一 target 文件，必须串行（`IT-03 -> IT-10`）。**拆出 `IT-12`**（不存在仓库的失败路径与 liveness）。

**Out of scope:**

- 往返一致性、用例隔离基础设施与 token seed / 注入 helper —— 由 IT-03 承接（本卡复用，不重复实现）。
- 不存在仓库的 clone 失败与 liveness 探针 —— 由 IT-12 承接（自本卡拆出）。
- 服务端认证逻辑本身的修改 —— 永久非目标：本卡只断言现状；若断言暴露服务端缺陷，按 ER-10 新建 `FIX-IT-10-01` 并置本卡 `blocked`。
- SSH 通道的认证矩阵 —— 由 PT-04 承接（DEFER-IT-01，见 DEP-05）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| receive-pack 强制认证，401 响应带 `WWW-Authenticate` 挑战头 | `src/contract/git_protocol/http.rs:93`、`:345`、`:355` |
| CI 凭据永远内嵌在 remote URL，故 401 分支无真实客户端覆盖 | `.github/workflows/git-protocol-smoke.yml:172` |
| 脚本矩阵全为正断言，无认证失败用例 | `scripts/git_protocol_smoke.sh:246-268` |
| 脱敏当前只靠 GitHub 的 `::add-mask::`，不是测试断言 | `.github/workflows/git-protocol-smoke.yml:81-84`、`:121` |

**Acceptance criteria:**

- [x] 未携带 token 的 push 返回 HTTP 401。
- [x] 该 401 响应包含 `WWW-Authenticate` 头。
- [x] seed token 不出现在任何 remote URL 中。
- [x] seed token 不出现在本 target 的捕获输出中。
- [x] seed token 不出现在共享工作目录下的任何文件中。

**Verification:**

- [x] 认证边界 + token 零泄漏守卫（**定向运行本卡新增用例**；token 由本命令生成并导出；**变更 `MONOENGINE_IT_GIT_WORKDIR` 后必须 `--force-recreate` `git-cli`**，因 compose bind-mount 仅在创建容器时解析；**退出前（含失败）用 trap 恢复默认工作目录并 recreate**，避免后续 IT-12 / 全量 target 挂到错目录；`pipefail` + 单独检查测试退出码；扫描含隐藏文件与被忽略文件，并显式检查每个 clone 的 remote URL）：`bash -c 'set -euo pipefail; DEFAULT_W="${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}"; restore_git_cli(){ export MONOENGINE_IT_GIT_WORKDIR="$DEFAULT_W"; mkdir -p "$DEFAULT_W"; chmod 1777 "$DEFAULT_W"; docker compose -p monoengine-it -f docker-compose.test.yml --profile git up -d --force-recreate --wait git-cli >/dev/null; }; trap restore_git_cli EXIT; export MONOENGINE_IT_SEED_TOKEN="it10-$(od -An -N12 -tx1 /dev/urandom | tr -d " \n")"; export MONOENGINE_IT_GIT_WORKDIR="/tmp/monoengine-it-git-$$"; rm -rf "$MONOENGINE_IT_GIT_WORKDIR"; mkdir -p "$MONOENGINE_IT_GIT_WORKDIR"; chmod 1777 "$MONOENGINE_IT_GIT_WORKDIR"; docker compose -p monoengine-it -f docker-compose.test.yml --profile git up -d --force-recreate --wait git-cli; source .env.test; set -o pipefail; cargo test -p monoengine --test integration_git_cli -- --nocapture --test-threads=1 integration_git_cli_auth_ 2>&1 | tee /tmp/it10-output.log; trc=${PIPESTATUS[0]}; [ "$trc" -eq 0 ] || { echo "FAIL: auth cases exited $trc"; exit "$trc"; }; for c in integration_git_cli_auth_push_without_token_returns_401_challenge integration_git_cli_auth_token_never_leaks; do rg -q -e "${c} \.\.\. ok" -e "test .*${c} \.\.\. ok" /tmp/it10-output.log || { echo "FAIL: named case ${c} did not run to ok"; exit 1; }; done; cfgs=$(find "$MONOENGINE_IT_GIT_WORKDIR" -name config -path "*/.git/*"); [ -n "$cfgs" ] || { echo "FAIL: no .git/config produced — nothing to scan, leak check would be vacuous"; exit 1; }; for cfg in $cfgs; do if rg -n -F "$MONOENGINE_IT_SEED_TOKEN" "$cfg"; then echo "FAIL: token in $cfg"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg exit $rc on $cfg"; exit 2; fi; fi; done; if rg -n -F --hidden --no-ignore -a "$MONOENGINE_IT_SEED_TOKEN" /tmp/it10-output.log "$MONOENGINE_IT_GIT_WORKDIR"; then echo "FAIL: token leaked"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi'`

**Dependencies:** `IT-03`（消费其 test target 与服务编排 helper；写集相交，必须串行）（G-06）

**Deliverables:** `Inherited`

**Implementation write set:** `bin/tests/integration_git_cli.rs`（追加认证边界用例块）（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `bin/tests/integration_git_cli.rs`

**Docs and compatibility impact:** `N/A`（覆盖矩阵行已由 IT-03 建立，本卡用例属同一 target 行）

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** seed token 由 Verification shell 生成、只经 credential helper 注入并有零命中守卫；token 值按 ER-11 脱敏，不进验收证据

**Performance budget:** 本卡用例 wall-clock ≤ 1 分钟

**Estimated scope:** `S`（0 个生产落点：变更全部在本卡自己的测试文件内）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（ER-04）

**D 组远端后置门:** `N/A` —— 本卡只改 `bin/tests/**`，当前不命中任一 workflow 的 `paths:`；本卡在自身发布窗口内 D 组恒为 `N/A`：IT-04 排在本卡之后，其后续把 `bin/tests/**` 加入触发面只产生 IT-04 自己的 D 组证据，**不追溯改变本卡已完成的 `Acceptance`**。

**Granularity:** `type=implementation; axis=Git smart HTTP 认证边界与凭据零泄漏在真实客户端下的断言; recovery=revert integration_git_cli.rs 中本卡追加的认证用例块; complete=yes; self-contained=yes; AC=5/8; VER=1/8; landing=0; prod-files=0; scope=S; deps=IT-03; writeset=序列化于 IT-03; release=independent; split-from=IT-03; exception=N/A`

### Task IT-11: CI 中 git 与 git-lfs 客户端版本的确定性

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（D 组 `git-protocol-smoke` 绿于 v0.1.177 / [30479301244](https://github.com/gitmono-dev/monoengine/actions/runs/30479301244)，pin step success + job summary，2026-07-30）

**Description:** 自 `IT-04` 拆出。`git-protocol-smoke.yml` 当前只显式安装 `git-lfs`，`git` 直接用 hosted runner 自带的版本；两者都不 pin 且只 `echo` 版本、不做断言，因此 runner 镜像升级会静默改变协议协商行为。本卡把 **git 与 git-lfs 两者**的版本固定（apt 版本 pin 或等价机制），在任一版本漂移时让 job 失败，并把两个版本输出写入 job summary；两个 pin 值同时登记进 `docs/refactoring/test-infra.md` 的「客户端版本确定性规则」章节，与 IT-02 的容器版本登记共用同一格式。本卡唯一行为轴是「CI 中 git 客户端版本的确定性」。

**Out of scope:**

- CI 执行面与触发面 —— 由 IT-04 承接（本卡自其拆出）。
- git-cli 容器的版本固定 —— 由 IT-02 承接（容器侧），本卡只管 CI 宿主机侧。
- `config-validation.yml` —— 永久非目标：该 workflow 不安装 git 客户端。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| workflow 只显式安装 `git-lfs`（`apt-get install -y git-lfs`），`git` 来自 hosted runner 自带环境或包依赖；两者均未 pin，`git --version` 与 `git lfs version` 只 echo、不断言 | `.github/workflows/git-protocol-smoke.yml:68-75` |
| LFS 用例要求 `git-lfs` 存在，缺失直接判 FAIL | `scripts/git_protocol_smoke.sh:185` |
| 版本输出当前不进 job summary（该 step 只有 `run:`，无 `$GITHUB_STEP_SUMMARY` 写入） | `.github/workflows/git-protocol-smoke.yml:68-75` |

**Acceptance criteria:**

- [x] `git-protocol-smoke.yml` 使 job 内的 git 版本被固定（显式安装 pin 版本，或断言 runner 自带版本等于 pin 值）。
- [x] 安装后实际 `git --version` 与 git pin 值不符时 job 以失败结束。
- [x] `git-protocol-smoke.yml` 安装 git-lfs 时使用固定版本。
- [x] 安装后实际 `git lfs version` 与 git-lfs pin 值不符时 job 以失败结束。
- [x] `git --version` 的输出写入 job summary。
- [x] `git lfs version` 的输出写入 job summary。
- [x] `docs/refactoring/test-infra.md` 登记该 git pin 值。
- [x] `docs/refactoring/test-infra.md` 登记该 git-lfs pin 值。

**Verification:**

- [x] git pin 一致性守卫（workflow 声明值与规范登记值必须相同；两侧先断言非空）：`bash -c 'set -euo pipefail; WF=$(rg -N -o "MONOENGINE_CI_GIT_VERSION: *\"?([^\"\n]+)\"?" -r "\$1" .github/workflows/git-protocol-smoke.yml | head -1); DOC=$(rg -N -o "CI git 固定版本[：:] *\`([^\`]+)\`" -r "\$1" docs/refactoring/test-infra.md | head -1); [ -n "$WF" ] || { echo "FAIL: git pin missing in workflow"; exit 1; }; [ -n "$DOC" ] || { echo "FAIL: git pin missing in test-infra.md"; exit 1; }; [ "$WF" = "$DOC" ] || { echo "FAIL: git pin mismatch [$WF] vs [$DOC]"; exit 1; }; echo "OK: git pin $WF"'`
- [x] git-lfs pin 一致性守卫（同上）：`bash -c 'set -euo pipefail; WF=$(rg -N -o "MONOENGINE_CI_GIT_LFS_VERSION: *\"?([^\"\n]+)\"?" -r "\$1" .github/workflows/git-protocol-smoke.yml | head -1); DOC=$(rg -N -o "CI git-lfs 固定版本[：:] *\`([^\`]+)\`" -r "\$1" docs/refactoring/test-infra.md | head -1); [ -n "$WF" ] || { echo "FAIL: git-lfs pin missing in workflow"; exit 1; }; [ -n "$DOC" ] || { echo "FAIL: git-lfs pin missing in test-infra.md"; exit 1; }; [ "$WF" = "$DOC" ] || { echo "FAIL: git-lfs pin mismatch [$WF] vs [$DOC]"; exit 1; }; echo "OK: git-lfs pin $WF"'`

**Dependencies:** `IT-04`（写集在 `git-protocol-smoke.yml` 与 `test-infra.md` 上相交，必须串行）（G-06）

**Deliverables:** `Inherited`

**Implementation write set:** `.github/workflows/git-protocol-smoke.yml`、`docs/refactoring/test-infra.md`（登记两个 pin 值）（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `.github/workflows/git-protocol-smoke.yml`、`docs/refactoring/test-infra.md`

**Docs and compatibility impact:** `test-infra.md` 的客户端版本登记

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** `Inherited`

**Performance budget:** 版本 pin 不得显著延长安装步骤（≤ 现状 +60s）

**Estimated scope:** `S`（1 个行为落点 `.github/workflows/`、1 个生产文件）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（ER-04）

**D 组远端后置门:** **适用** —— workflow=`.github/workflows/git-protocol-smoke.yml`、job=`git-protocol-smoke`、事件=`push`、ref=`main`、paths 命中=是（`:27`，本卡改该 workflow 自身）、判据=job success 且 summary 含 pin 后的 `git --version` 与 `git lfs version`。

**Granularity:** `type=implementation; axis=CI 中 git 客户端版本的确定性; recovery=revert git-protocol-smoke.yml 的 pin 与 summary 改动，以及 test-infra.md 的版本登记; complete=yes; self-contained=yes; AC=8/8; VER=2/8; landing=1; prod-files=1; scope=S; deps=IT-04; writeset=序列化于 IT-04; release=independent; split-from=IT-04; exception=N/A`

### Task IT-12: Git CLI 失败路径与服务存活断言

**Task type:** `implementation`（G-11）

**Lifecycle / Acceptance:** `done` / `complete`（执行期收口，2026-07-29）

**Description:** 自 `IT-10` 拆出。在 `bin/tests/integration_git_cli.rs` 上追加**失败路径**用例（统一以 `integration_git_cli_failpath_` 前缀命名，至少 `integration_git_cli_failpath_clone_missing_repo_keeps_service_alive` 这一个 `(new)` 用例）：对 **`parse_git_protocol_path` 拒绝的保留根路径**（落地路径 `/third-party.git/`，在仓库查找之前即失败）发起 HTTP clone，断言客户端以非零退出结束、错误消息稳定可断言，且该失败**之后** `service http` 进程仍存活。选用保留根而非「形态合法但仓库不存在」的路径，是因为 monorepo 对任意合法路径会广告空仓库，无法形成稳定的 missing-repo 客户端失败。用例**在自身 teardown 之前**发起探针，并把结果写入 `${MONOENGINE_IT_LIVENESS_FILE}`（由 Verification shell 指定的每轮唯一路径），格式为 `status=<http-code> pid=<service-pid>`；teardown 仍照常结束该进程，因此守卫在测试退出后反向断言该 PID 已消失。本卡唯一行为轴是**失败路径不拖垮服务**，与 IT-10 的认证边界是两个可独立恢复的轴。本卡与 IT-03、IT-10 共写同一 target 文件，必须串行（`IT-10 -> IT-12`）。

**Out of scope:**

- 认证边界（401 / `WWW-Authenticate` / token 注入）—— 由 IT-10 承接（本卡自其拆出）。
- 往返一致性与用例隔离 —— 由 IT-03 承接。
- 「形态合法但仓库不存在」的 clone 失败矩阵 —— 永久非目标于本卡（monorepo 空仓库广告语义；见 `DEFER-IT-12`，由 PT-04 承接）。
- 服务端错误消息的改写 —— 永久非目标：本卡只断言现状；若断言暴露服务端缺陷，按 ER-10 新建 `FIX-IT-12-01` 并置本卡 `blocked`。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 脚本矩阵 19 个 `run_case` 全为正断言，无任何失败路径用例 | `scripts/git_protocol_smoke.sh:246-268` |
| CI 的就绪探针只在 smoke **之前**跑一次，失败后不再复检服务存活 | `.github/workflows/git-protocol-smoke.yml:155-163` |
| 401 分支的现有覆盖是**进程内单测** `auth_failed_returns_401_response`（只断言状态码与挑战头，不经真实客户端） | 单测 `src/contract/git_protocol/http.rs:444-449`（`auth_failed_returns_401_response`）；生产分支 `:93`（`auth_failed`） |
| IT-12 落地用例以保留根 `/third-party.git/` 作为失败路径（`parse_git_protocol_path` 拒绝），非形态合法 missing repo | `bin/tests/integration_git_cli.rs`（`integration_git_cli_failpath_clone_missing_repo_keeps_service_alive`） |

**Acceptance criteria:**

- [x] 对保留根路径（`/third-party.git/`）的 HTTP clone 以非零退出码结束。
- [x] 该 clone 的错误消息稳定（同样输入重复运行得到相同消息）。
- [x] 用例对该错误消息的固定子串做断言。
- [x] 用例在自身 teardown **之前**对失败后的服务发起 liveness 探针并得到 HTTP 200。
- [x] 用例 teardown 之后，该 `service http` 进程已退出（与 IT-03 的零残留验收一致）。

**Verification:**

- [x] 失败后存活守卫（**定向运行本卡新增用例**；证据文件每轮唯一且运行前清空，杜绝复用上一轮的 `200`。liveness 由**用例在 teardown 之前**断言并写证据；shell 在测试退出后**反向**断言该 PID 已消失，以免与 IT-03 的「零残留进程」验收冲突）：`bash -c 'set -euo pipefail; W="${MONOENGINE_IT_GIT_WORKDIR:-/tmp/monoengine-it-git}"; export MONOENGINE_IT_LIVENESS_FILE="$W/liveness-$$.txt"; rm -f "$MONOENGINE_IT_LIVENESS_FILE"; source .env.test; cargo test -p monoengine --test integration_git_cli -- --nocapture --test-threads=1 integration_git_cli_failpath_ 2>&1 | tee /tmp/it12-output.log; trc=${PIPESTATUS[0]}; [ "$trc" -eq 0 ] || { echo "FAIL: failpath cases exited $trc"; exit "$trc"; }; rg -q -e "integration_git_cli_failpath_clone_missing_repo_keeps_service_alive \.\.\. ok" /tmp/it12-output.log || { echo "FAIL: named failpath case did not run to ok"; exit 1; }; [ -s "$MONOENGINE_IT_LIVENESS_FILE" ] || { echo "FAIL: liveness evidence not produced by this run"; exit 1; }; rg -q "^status=200 pid=[0-9]+$" "$MONOENGINE_IT_LIVENESS_FILE" || { echo "FAIL: expected \"status=200 pid=<n>\" recorded before teardown"; exit 1; }; pid=$(rg -N -o "pid=([0-9]+)" -r "\$1" "$MONOENGINE_IT_LIVENESS_FILE"); if kill -0 "$pid" 2>/dev/null; then echo "FAIL: service pid $pid leaked past teardown"; exit 1; fi; echo "OK: alive during the failure path, cleaned up afterwards"'`

**Dependencies:** `IT-10`（消费其 target 与服务编排；写集相交，必须串行）（G-06）

**Deliverables:** `Inherited`

**Implementation write set:** `bin/tests/integration_git_cli.rs`（追加失败路径用例块）（G-10）

**Release write set:** `Inherited`

**Files likely touched:** `bin/tests/integration_git_cli.rs`

**Docs and compatibility impact:** `N/A`（覆盖矩阵行已由 IT-03 建立，本卡用例属同一 target 行）

**Rollback mode:** `revert`（G-01）

**Migration and rollback:** `Inherited`

**Security and privacy:** `Inherited`

**Performance budget:** 本卡用例 wall-clock ≤ 30 秒

**Estimated scope:** `S`（0 个生产落点：变更全部在本卡自己的测试文件内）（G-04）

**Version increment:** `patch`（ER-08）

**Release boundary:** `independent`

**C/D coverage from:** `self`（ER-04）

**D 组远端后置门:** `N/A` —— 本卡只改 `bin/tests/**`，当前不命中任一 workflow 的 `paths:`；本卡在自身发布窗口内 D 组恒为 `N/A`：IT-04 排在本卡之后，其后续把 `bin/tests/**` 加入触发面只产生 IT-04 自己的 D 组证据，**不追溯改变本卡已完成的 `Acceptance`**。

**Granularity:** `type=implementation; axis=Git CLI 失败路径不拖垮服务; recovery=revert integration_git_cli.rs 中本卡追加的失败路径用例块; complete=yes; self-contained=yes; AC=5/8; VER=1/8; landing=0; prod-files=0; scope=S; deps=IT-10; writeset=序列化于 IT-10; release=independent; split-from=IT-10; exception=N/A`

## 测试矩阵

本计划新增**一个**集成 test target（`integration_git_cli`，由 IT-03 建立，IT-10 与 IT-12 扩充）与**一个** `monoengine-core` focused 用例（`notification::dispatcher::tests::integration_mail_dispatcher_two_instances_deliver_exactly_once`，IT-06）。除此之外不新增任何 cargo test target。

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | 本计划不新增生产逻辑单测；若 IT-06 派生 `FIX-IT-06-01`，由该卡补 claim 语义单测 | `cargo test -p monoengine-core 'jupiter::storage::notification_storage'` |
| 集成（DB） | 并发 dispatcher 端到端投递（IT-06；`test_db_connection` + 真实 Mailpit） | `docker compose -p monoengine-it -f docker-compose.test.yml up -d --wait && source .env.test && cargo test -p monoengine-core 'notification::dispatcher::tests::integration_mail_dispatcher_two_instances_deliver_exactly_once'` |
| 集成（进程） | 真实 Git CLI 黑盒（IT-03 往返与隔离、IT-10 认证边界、IT-12 失败路径）；既有黑盒回归含已落地的 `integration_config_hot_reload` 与 `integration_multichannel_notification` | `source .env.test && cargo test -p monoengine --test integration_git_cli -- --test-threads=1`；`source .env.test && cargo test -p monoengine --test integration_vault -- --test-threads=1` |
| CLI | 本计划不新增子命令；既有解析与三处注册测试保持不动 | `cargo test -p monoengine-core 'cli::tests'` |
| HTTP API | Git smart HTTP 的未认证 401 与 `WWW-Authenticate` 由真实客户端断言（IT-10）；失败路径后的 liveness 由 IT-12 断言 | `source .env.test && cargo test -p monoengine --test integration_git_cli -- --test-threads=1` |
| 迁移 | `N/A`（无 schema 变更，不改 `migrations()` 注册列表） | `N/A` |
| 配置 | 本计划不改配置项；`.env.test.example` 的继承语义由 IT-08 的子进程断言验证 | IT-08 Verification 第 1 条（`. /tmp/it08.env` 后逐键 `env` 断言） |
| Git 协议 | 既有脚本矩阵保持绿（触发面由 IT-04 扩展、客户端版本由 IT-11 固定）；cargo-native 最小集由 IT-03 / IT-10 / IT-12 落地 | `scripts/git_protocol_smoke.sh` 本地等价（ER-04 A 组 Git 协议行）+ `source .env.test && cargo test -p monoengine --test integration_git_cli -- --test-threads=1` |
| 安全 | seed token 不进任何测试输出与工作目录（IT-10 零命中守卫）；CI 日志 `::add-mask::` 覆盖（IT-04 D 组） | IT-10 Verification 第 1 条 + CI 日志人工核对（sanitized 记录） |
| 性能 | 新用例 wall-clock 预算（IT-03 ≤3min、IT-10 ≤1min、IT-12 ≤30s、IT-06 单轮 ≤2min） | 用例输出时间戳 |
| live/gated | 真实外部服务：Postgres / Redis / Mailpit / git-cli 容器；缺失时必须 FAIL，不得跳过 | `docker compose -p monoengine-it -f docker-compose.test.yml up -d --wait` 前置 + IT-03 的 fail-closed 守卫、IT-06 AC7 与其 Verification 第 2 条 |

## 追溯表

本表只收录任务卡（11 张）。已关闭编号 `IT-05` 不是任务卡，其落地证据见「已关闭编号 IT-05」条目，不进本表。

| 任务 | 来源/证据 | monoengine 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| IT-01 | GAP-01；`docs/refactoring/integration.md:111-143`、`docs/refactoring/integration.md:147-192`；`docker-compose.test.yml` 的 `rustfs` / `rustfs-init` | `docs/refactoring/test-infra.md`（新建） | `docs/refactoring/integration.md` 的 `## 测试分层` 正文替换为链接段 | 手工证据：rustfs 登记条目走查 |
| IT-02 | ADR-IT-01；`docker-compose.test.yml:2`、`docker-compose.test.yml:17`、`docker-compose.test.yml:27`、`docker-compose.test.yml` 的 `rustfs` / `rustfs-init`、`docker-compose.test.yml:69` | `docker-compose.test.yml` | `docs/refactoring/test-infra.md` 服务登记 | `docker compose -p monoengine-it -f docker-compose.test.yml up -d --wait` + `exec -T git-cli git --version` |
| IT-03 | ADR-IT-02；GAP-02；`bin/tests/integration_vault.rs:600`、`bin/tests/integration_vault.rs:756`；`bin/tests/common/mod.rs:1`；`scripts/git_protocol_smoke.sh:120` | `bin/tests/integration_git_cli.rs`（新建） | `docs/refactoring/protocol.md` smoke 清单标注；`docs/refactoring/integration.md` 覆盖矩阵行 | `cargo test -p monoengine --test integration_git_cli -- --test-threads=1` |
| IT-04 | GAP-02；`.github/workflows/config-validation.yml:294`、`.github/workflows/config-validation.yml:5`、`.github/workflows/config-validation.yml:19`；`.github/workflows/git-protocol-smoke.yml:14`、`.github/workflows/git-protocol-smoke.yml:26` | `.github/workflows/config-validation.yml`、`.github/workflows/git-protocol-smoke.yml` | `docs/refactoring/test-infra.md` CI 入口登记 | 两个 workflow 的触发面守卫 + D 组两 job 绿 |
| IT-06 | GAP-04；ADR-IT-03；`src/jupiter/storage/notification_storage.rs:1254`、`src/jupiter/storage/notification_storage.rs:1313`、`src/jupiter/storage/notification_storage.rs:1372`；`src/notification/dispatcher.rs:343`、`src/notification/dispatcher.rs:1339`；`docs/refactoring/mail.md:193` | `src/notification/dispatcher.rs`（`#[cfg(test)]`） | `docs/refactoring/integration.md` 覆盖矩阵行 | `cargo test -p monoengine-core 'notification::dispatcher::tests::integration_mail_dispatcher_two_instances_deliver_exactly_once'` |
| IT-07 | GAP-03、GAP-05；`docs/refactoring/integration.md:354-364`、`:119-127`、`:407-414`、`:504-520`；`docs/plan/plan-long.md:103`、`docs/plan/plan-long.md:200-204`、`docs/plan/plan-long.md:744` | `docs/refactoring/integration.md`、`docs/plan/plan-long.md`、`docs/plan/plan-20260727.md` | 即任务本身 | 覆盖矩阵机械比对 + 粒度审计表比对 |
| IT-08 | GAP-06；`.env.test.example:1`；`.github/workflows/config-validation.yml:288`；`bin/tests/integration_vault.rs:693`、`bin/tests/integration_vault.rs:1606`、`bin/tests/integration_vault.rs:1620` | `.env.test.example` | 开发者环境示例 | 继承语义子进程断言 + CI 键子集守卫 |
| IT-09 | ER-04 C 组；ER-08；`Cargo.toml:6`、`bin/Cargo.toml:3` | `Cargo.toml`、`bin/Cargo.toml`、`Cargo.lock` | `N/A` | crate 级 allow 基线守卫 |
| IT-10 | GAP-02；`src/contract/git_protocol/http.rs:93`、`src/contract/git_protocol/http.rs:345`、`src/contract/git_protocol/http.rs:355`；`.github/workflows/git-protocol-smoke.yml:172` | `bin/tests/integration_git_cli.rs`（追加认证用例） | `N/A`（矩阵行由 IT-03 建立） | token 零命中守卫 |
| IT-12 | GAP-02；`scripts/git_protocol_smoke.sh:246-268`、`.github/workflows/git-protocol-smoke.yml:155-163`、`src/contract/git_protocol/http.rs:93` | `bin/tests/integration_git_cli.rs`（追加失败路径用例） | `N/A`（矩阵行由 IT-03 建立） | 失败后存活守卫 |
| IT-11 | GAP-02；`.github/workflows/git-protocol-smoke.yml:68`、`scripts/git_protocol_smoke.sh:185` | `.github/workflows/git-protocol-smoke.yml` | `docs/refactoring/test-infra.md` 客户端版本登记 | pin 值一致性守卫 + D 组 job 绿 |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | Phase 0 退出：IT-01 产物落盘且 review `PASS`（`Acceptance=locally-accepted`，其提交发生在 IT-02）、IT-08 `Acceptance=complete`，DEP-01 与 DEP-09 已就位 | IT-01 本地产物 + review 结论；IT-08 的提交与继承语义断言输出（sanitized） | `revert`：撤销 `docs/refactoring/test-infra.md`、`docs/refactoring/integration.md` 的链接行、`.env.test.example`，无生产影响 |
| M1 | Phase 1 退出：git-cli 服务登记且容器版本被断言（IT-02）、`integration_git_cli` 本地绿（IT-03 + IT-10 + IT-12） | 指定用例通过记录（sanitized）+ 镜像与版本登记 | `revert`：按四卡完整 `Implementation write set` 撤销——`docker-compose.test.yml` 的 git-cli 服务、`docs/refactoring/test-infra.md` 的登记条目、`bin/tests/integration_git_cli.rs`、`bin/tests/common/mod.rs` 的 helper 追加、`docs/refactoring/protocol.md` 的标注、`docs/refactoring/integration.md` 的矩阵行；恢复后测试栈回到 5 服务、git 覆盖回到脚本-only |
| M2 | IT-06 并发投递基线落地；IT-04 完成 CI 执行面与触发面扩展；IT-11 固定 git 客户端版本，两个 job 绿 | 指定用例通过记录 + D 组远端证据（sanitized 链接） | `revert` 本地：按三卡完整 `Implementation write set` 撤销——`src/notification/dispatcher.rs` 的测试块、`docs/refactoring/integration.md` 的矩阵行、`.github/workflows/config-validation.yml`、`.github/workflows/git-protocol-smoke.yml`、以及 IT-04 与 IT-11 在 `docs/refactoring/test-infra.md` 中的 CI 入口与版本 pin 登记；已推送后 D 组失败一律**前滚修复**（ER-04），不回退已推送提交 |
| M3 | IT-07 收口完成（P2 清单全部为「落地 / 书面关闭」，无「延后」结案）、IT-09 发布点完成，PT-01 完成判据（`docs/plan/plan-long.md:202-204`）逐条满足 | `plan-long.md` 状态更新 + 推送证据（sanitized） | `immutable-release`：只能前滚新 patch 版本 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| `../orbit` sibling 缺失（DEP-01） | 全部 cargo 命令以 101 退出 | 补齐 sibling checkout 后重跑 ER-01 四项；未补齐前相关卡置 `blocked` | 把依赖解析失败当作代码缺陷排查；降级为不 source 的 `cargo test` |
| 本地 `.env.test` 缺失（DEP-09） | 需要 source 的门无法运行 | 按 `AGENTS.md:102` 停下来向使用者确认后由其提供；IT-08 完成后重新生成一次 | 由任务静默生成该文件；跳过 source 直接跑 `cargo test --all` |
| compose 服务启动失败 / 健康检查超时 | 停止的容器 | `docker compose -p monoengine-it -f docker-compose.test.yml down -v` 后按 ER-10 有界重试（≤5 次、≤30 分钟）；超预算置 `blocked` 并记录镜像/网络原因 | 残留卷或网络进入下次运行 |
| 黑盒测试进程崩溃（`service http` 未退出） | 监听中的子进程、临时目录 | 测试 teardown 杀进程组并清理临时目录；重跑前用 IT-03 的残留进程守卫确认端口释放 | 僵尸进程占用高位端口导致后续用例假败 |
| IT-06 暴露 dispatcher 投递缺陷 | 该用例失败 | 按 ER-10 新建 `FIX-IT-06-01`、加 `FIX-IT-06-01 -> IT-06` 边、IT-06 置 `blocked` | 在 IT-06 内就地修生产代码；删除、弱化或 `#[ignore]` 用例以通过验收 |
| IT-10 暴露服务端认证缺陷 | 该用例失败 | 按 ER-10 新建 `FIX-IT-10-01`、加 `FIX-IT-10-01 -> IT-10` 边、IT-10 置 `blocked` | 在 IT-10 内就地改 `src/contract/git_protocol/http.rs` |
| IT-12 暴露失败路径缺陷（服务被拖垮或错误消息不稳定） | 该用例失败 | 按 ER-10 新建 `FIX-IT-12-01`、加 `FIX-IT-12-01 -> IT-12` 边、IT-12 置 `blocked` | 在 IT-12 内就地改生产代码 |
| CI 凭据 / token 失效 | job 失败 | 记录原因，不 blind retry（ER-09）；待下一次修复窗口处理 | 在日志中打印 secret 排查 |
| git-cli 镜像拉取失败（DEP-08） | job 或本地栈起不来 | 按 ER-10 有界重试；超预算置 `blocked` 并记录 | 改用漂移版本或用宿主机 git 冒充固定版本 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| `../orbit` sibling 缺失导致全部 cargo 门不可执行 | 高：计划无法开工 | DEP-01 列为开工阻断前置（ER-01 子项 2）；未就位前所有实现卡停在 `blocked` | ALL |
| 本地 `.env.test` 缺失导致所有 source 门不可执行 | 高：Phase 0 无法退出 | DEP-09 列为人工前置；IT-08 完成后重新生成并复核 | ALL |
| `network_mode: host` 仅在 Linux 可用；非目标 OS 不得冒充验收 | 中：误在非 Linux 开发机启 `--profile git` 或用宿主机 git 冒充 pin | ADR-IT-01：目标 OS = Linux；compose `git-cli` 为唯一验收 runner；宿主机 git 仅显式 opt-in；`profiles: ["git"]` 避免默认栈误启 host 网络 | IT-02、IT-03 |
| git-cli 镜像 tag/digest 供应不稳定 | 低：拉取失败 | DEP-08 登记；ER-10 有界重试，超预算置 `blocked` | IT-02 |
| 并发投递用例暴露既有缺陷 | 中：范围外溢 | ADR-IT-03 已改为 ER-10 的 `FIX-*` 派生协议，禁止本卡内预授权修复；禁止弱化用例 | IT-06 |
| 认证边界或失败路径断言暴露服务端缺陷 | 中：范围外溢 | 同上，派生 `FIX-IT-10-01` / `FIX-IT-12-01`，本卡只断言不修 | IT-10、IT-12 |
| 新用例拖慢 CI | 低：反馈变慢 | wall-clock 预算；新增一个集成 target 与一个 focused 用例，且串行执行 | IT-03、IT-10、IT-12、IT-06 |
| CI 触发面扩展后 job 频次上升 | 低：CI 成本 | IT-04 的 `paths:` 逐条评审，只加与本计划用例相关的路径 | IT-04 |
| apt 版本 pin 在 runner 镜像升级后不可用 | 中：`git-protocol-smoke` job 装不上 pin 版本 | IT-11 的 pin 机制须允许显式升级（改 pin 值 + 同步 `test-infra.md` 登记），失败按 ER-10 有界重试后置 `blocked` | IT-11 |
| 共享工作目录跨用例可见导致互相污染 | 中：用例假败或假绿 | 用例在共享根下分配唯一子目录并负责清理；`--test-threads=1` 串行；IT-03 AC3 用连跑两遍证明无泄漏 | IT-03 |
| 测试用例的默认值兜底掩盖 `.env.test` 未生效 | 中：假绿 | IT-08 用独立子进程逐键断言 `export` 语义，不依赖用例是否通过 | IT-08 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| compose 全栈 `up --wait`（含 git-cli） | O(镜像拉取 + 健康检查) | 每次 CI job 一次 | ≤ 现状 +30s | CI 日志时间戳 |
| `integration_git_cli` 全 target（IT-03 + IT-10 + IT-12 用例） | O(服务启动 + 隔离用例 × N) | 每次运行一次；AC3 要求连跑两遍 | ≤ 3 分钟（IT-03）+ ≤ 1 分钟（IT-10）+ ≤ 30 秒（IT-12） | 用例输出时间戳 |
| IT-06 双实例投递用例 | O(N 封邮件 × 2 dispatchers) | 单轮一次；稳定性验收连跑 10 轮 | 单轮 ≤ 2 分钟；claim 查询无全表扫描 | 用例输出 + GC-10 审查 |
| CI job 增量时长（两个 workflow 合计） | O(新增用例执行) | 每次 push / PR | ≤ 5 分钟 | CI 日志时间戳 |

## 兼容与文档收口

- [ ] `docs/errors.md` 已同步，或说明 `N/A`。
- [ ] `docs/refactoring/*.md` 相关文档已同步，或说明 `N/A`。
- [ ] `config/config.toml` 示例配置与注释已同步，或说明 `N/A`。
- [ ] 运行时 OpenAPI（`/api/openapi.json`）证据已取得，或说明 `N/A`（本仓无落盘 spec 文件）。
- [ ] `README.md` / `AGENTS.md` 相关工程约束已同步或登记漂移，或说明 `N/A`。
- [ ] `src/callisto/`、`src/jupiter/storage/` 与 `src/jupiter/migration/`（含 `migrations()` 注册列表）已同步，或说明 `N/A`。
- [ ] 新引入的 `docs/*.md` 引用全部指向真实存在的文件，或说明 `N/A`。
- [ ] `plan-long.md` 日期计划索引或 PT/SB 状态已同步，或说明 `N/A`。

## Review log

Result 只允许 `PASS` 或 `FAIL`。`FAIL` 必须列出 P0/P1 条目并在下一轮复审关闭；P2 可由具名责任人书面接受为 residual risk，但不改变本轮 `FAIL` 记录（ER-05）。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R1 | 全部改动（codex review --uncommitted，2026-07-27） | `FAIL` | 3×P1，均已关闭：GC-12 改用 `libra add`（模板 + 本计划）；IT-03 补 receive-pack 认证 fixture 且依赖缺失在门禁环境默认失败；执行检查补全量 `cargo test --all` 门禁 | 3×P2 已修复：`.env.test` 缺失时停止并询问；示例变量补齐列入任务卡；IT-06 重定位为端到端并发 dispatcher 投递 | codex review R1 输出；R2 复审关闭 |
| R2 | 全部改动（codex review --uncommitted，2026-07-27） | `FAIL` | 3×P1，均已关闭：git-cli 工作卷改为隔离可写（只读仅限 fixture）；IT-05/IT-06 验收补 CI 接入；IT-04 触发路径补 `src/ceres/protocol/**` 及路由/storage 路径 | 2×P2 已修复：`.env.test.example` 补齐变量并统一 `export` 声明；plan-long 工作区基线更正 | codex review R2 输出；R3 复审关闭 |
| R3 | 全部改动（codex review --uncommitted，2026-07-27） | `PASS` | 无 | 无 | codex review R3 输出（no findings） |
| R4 | 模板 v2 整份迁移 + 事实基线刷新（自审，2026-07-29） | `FAIL` | 自审遗漏被 R5 检出：AC/VER 按 bullet 行数而非独立谓词计数、G-04 计数口径被计划自行改写、B/C/D 组门混入卡片 `Verification`、Phase 0 进入条件不可满足 | — | 结论由 R5 推翻，见下一行 |
| R5 | 模板 v2 迁移全文（`codex exec` 结构与事实评审，2026-07-29） | `FAIL` | 21×P1，全部已关闭：① IT-05 降级为「已关闭编号记录」非任务卡；② 全部卡 AC 按独立谓词重算并改写（IT-03 因超限拆出 IT-10）；③ Verification 按独立门重算；④ ER-04 的 B/C/D 组门移出卡片 `Verification`；⑤ 模糊描述改为可复制命令、IT-06 用例定名；⑥ IT-08 幽灵验收改为直接 source 本卡产物 + 子进程逐键断言；⑦ 删除计划自设的 G-04 计数口径，`landing`/`prod-files` 改为 `0/0`；⑧ IT-03/IT-06 各自收回 `integration.md` 矩阵行（G-02）；⑨ Phase 0 进入条件改为引用 DEP-09；⑩ Phase 1 进入条件改为 `locally-accepted + review PASS`；⑪ 发布者具名为 `porrychen`；⑫ 粒度审计表逐字段对齐 `Granularity`；⑬–⑯ 事实基线四处更正（upload-pack 认证语义、`MegaError` 定位、dispatcher 用例数 7、P2 待办行 4）；⑰ GAP-06 影响改述为默认值兜底假绿；⑱ 补齐缺失的 `file:line` 与 commit pin；⑲「两个新 target」改为「一个集成 target + 一个 core focused 用例」；⑳ IT-09 补 `release` 类型门的聚合守卫与兼容证据；㉑ M1 回滚补齐完整写集 | 1×P2 已修复：追溯表的 `file:2/17/27` 简写展开为完整 `file:line` 锚点 | `codex exec` R5 输出；R6 复审 |
| R6 | R5 全部 P1/P2 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 24×P1，全部已关闭：① 全部卡 AC/VER 按更严的独立谓词口径重算；② IT-04 因多轴 + 超限拆出 IT-11（git 客户端版本确定性）；③ ER-04 的 A 表面门从各卡 `Verification` 移出并新增统一口径说明；④ 五条 Verification 命令重写（IT-03 fail-closed、IT-10 token 守卫、IT-08 键集合 `comm` 子集、IT-04 双 workflow 触发面、IT-07 两条比对）；⑤ IT-02 版本断言去掉占位符，改为从 `test-infra.md` 确定性提取；⑥ IT-03/IT-06/IT-08 的 `.env.test` 依赖改挂 `DEP-09`；⑦ IT-09 AC 重构到 `8/12`、补 `release` 类型门的聚合守卫与兼容证据；⑧ crate 级 allow 基线更正为 `src/lib.rs:1`、`src/vault/core.rs:1`、`src/vault/utils/seal.rs:1`；⑨ `integration_multichannel_notification` 已落地，删除 DEFER-IT-05 并改 IT-07 结论；⑩ IT-01 补「替换 integration.md 分层正文」的 GC-02 语义；⑪ M0 改为「本地产物 + review PASS」；⑫ M2 回滚补 `test-infra.md` 登记 | 1×P2 已修复：完整 Mega SHA 锚点改为 `docs/plan/plan-long.md:30` | `codex exec` R6 输出；R7 复审 |
| R7 | R6 全部 P1/P2 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 20×P1，全部已关闭：① `plan-long.md:178` 实为 SB-03 判据，PT-01 判据在 `:200-204`，四处改锚；② DEP-03 完整 SHA 锚点改 `plan-long.md:30`；③ `DEP-09` 提升为计划级前置，不再选择性登记在部分卡；④ IT-09 不再自称「唯一发布点」，版本判据改为「相对进入发布窗口时的权威版本 `patch +1`」；⑤ IT-11 补齐 git-lfs 的 pin、断言与文档守卫；⑥ IT-11 `Current evidence` 删除对未来 AC 的引用；⑦ IT-10 拆出 IT-12（失败路径与 liveness）；⑧ 八条守卫命令重写（`set -euo pipefail`、`source .env.test &&`、`tee` 前 `pipefail` 且单独检查退出码、`trap` 恢复服务并校验、`rg` 退出码 1 与 >1 区分、比较前断言两侧非空）；⑨ IT-07 的两条比对改为**双向**集合差且覆盖全部 14 个字段、严格按测试属性识别用例；⑩ IT-08 键集合改为示例↔allowlist 双向相等 + CI 键覆盖；⑪ IT-09 allow 守卫改为集合相等（既拒新增也拒删除）；⑫ 残留守卫改用 compose project label 与用例记录的端口清单精确判定；⑬ 含复合断言的 AC 行继续拆分（IT-01/IT-07 到 `20/20`，IT-09 到 `11/12`），IT-04 的 `paths:` 判据改为对 `Deliverables` 中固定 allowlist 的单一集合相等谓词 | 2×P2 已修复：测试矩阵的 fail-closed 交叉引用改指 IT-06 AC7 与其 Verification 第 2 条；残留卷/进程守卫收窄到本 compose project 与本次端口清单 | `codex exec` R7 输出；R8 复审 |
| R8 | R7 全部 P1/P2 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 12×P1，全部已关闭：① IT-09 的 `Verification` 移除 B/C 组项，`VER` 改 `1/12`；② IT-02 卷/网络守卫合并并逐个检查 docker 退出码；③ IT-03 端口守卫区分 `pgrep`/`ss`/`rg` 失败与零命中；④ IT-03 与 IT-06 的服务恢复失败计入守卫判定；⑤ IT-04 的两份 allowlist 改为守卫内 `printf` 内联构造；⑥ IT-07 覆盖矩阵守卫收窄到本计划新增 target 与 focused 用例；⑦ IT-08 的 AC 与命令口径对齐（⊆ / 双向相等分别写清）；⑧ IT-03 的 `SKIP` 证据改述并补依赖缺失分支锚点；⑨ **DEP-02/DEP-03 锚点在 R7 轮实际未同步，本轮补齐**，修订历史同步更正；⑩ IT-06/IT-10/IT-12 的 D 组不再宣称被 IT-04 追溯改变；⑪ IT-12 的 401 覆盖锚点改为单测 `http.rs:444-449` 并说明不存在仓库路径无覆盖；⑫ 追溯表删除 IT-05 行 | 2×P2 已修复：评审结论与 ADR-IT-02 同步 IT-12 与最新依赖边；测试矩阵的 `integration_git_cli` 命令补回 `source .env.test` 与 `--test-threads=1` | `codex exec` R8 输出；R9 复审 |
| R9 | R8 全部 P1/P2 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 6×P1，全部已关闭：① IT-03 收回 token seed 与注入 helper（原本交给依赖它的 IT-10，导致 IT-03 的 push AC 不可独立验收），IT-10 相应降为 `AC=5/8`；② IT-08 AC 拆分为 `7/8`；③ IT-08 的 CI 键提取收窄到「Run integration tests」步骤的 `env:` 块（原命令会命中 `ORBIT_CHECKOUT_TOKEN` 而必然失败）；④ IT-12 删除不成立的零命中审计命令，改用目录内容与脚本矩阵直接举证；⑤ IT-12 AC 拆分为 `5/8`；⑥ Phase 2 不再称 IT-09 为「唯一发布点」 | 3×P2 已修复：热加载 spawn 锚点改为 `service/mod.rs:44-49` 与 `:131`；IT-09 的「无 artifact 上传」补零命中证据；IT-11 现状改述为「只显式安装 git-lfs，git 用 runner 自带版本」 | `codex exec` R9 输出；R10 复审 |
| R10 | R9 全部 P1/P2 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 4×P1，全部已关闭：① IT-03 的 `Out of scope`/`Security` 仍把 token 注入交给 IT-10，与 R9 的归属修订矛盾，已统一为「seed 与注入归 IT-03」；② 适用范围仍写 IT-10 承接失败路径、GAP-02 动作漏 IT-11/IT-12，已补齐；③ IT-10 守卫改为定向运行 `integration_git_cli_auth_` 前缀的 `(new)` 用例，并补 `--hidden --no-ignore -a` 扫描与逐 clone `.git/config` 检查；④ IT-12 守卫改为定向运行 `integration_git_cli_failpath_` 前缀用例，证据文件每轮唯一且运行前清空，并断言 `status=200 pid=<n>` 与该 PID 存活 | 2×P2 已修复：IT-05 关闭记录的 spawn 锚点改为 `service/mod.rs:44-49` 与 `:131`；IT-09 的「crate 级 allow」改称「顶层 inner `#![allow(...)]`」并说明三处的性质差异 | `codex exec` R10 输出；R11 复审 |
| R11 | R10 全部 P1/P2 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 5×P1，全部已关闭：① IT-02 健康守卫的 `.*healthy` 正则同样匹配 `unhealthy`，改为 `docker inspect` 精确比较；② IT-02 的 D 组去掉「后续变为适用」的追溯表述；③ IT-03 端口守卫改用每轮唯一且运行前删除的 `MONOENGINE_IT_PORTS_FILE`；④ IT-09 的 allow 守卫正则放宽到任意 `#![allow(...)]` 语法（原写法漏 `clippy::`、多 lint、含空格）；⑤ IT-10 守卫改为断言两个具名用例均跑到 `ok`、工作目录每轮唯一、并要求存在真实 `.git/config` 后才执行泄漏扫描 | 无 P2 | `codex exec` R11 输出；R12 复审 |
| R12 | R11 全部 P1 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 5×P1，全部已关闭：① IT-03 残留进程守卫改为按本轮 PID 清单逐个 `kill -0`（`pgrep` 全局匹配会命中父 shell，干净状态也过不了）；② IT-10 的逐 `.git/config` 扫描补 `else` 分支，修正在 `if` 之后取 `$?` 导致零命中被误报为 `rg exit 0` 的结构错误；③ IT-12 的 liveness 改为用例在 teardown 前断言、shell 事后反向断言 PID 已消失（原写法与 IT-03 的零残留验收冲突）；④ 成功定义 / Phase 2 退出 / IT-07 AC / M3 统一收紧为「落地 / 书面关闭」，与 PT-01 完成判据 `plan-long.md:204` 对齐，不再允许「延后」结案；⑤ IT-07 补 AC 与守卫，要求把 `plan-long.md:750` 的索引范围由 `IT-01..IT-07` 更新为最终 11 张卡集并注明 IT-05 已关闭 | 1×P2 已修复：IT-02 的 compose 命令统一显式 `-p monoengine-it`，残留查询按同一项目名过滤 | `codex exec` R12 输出；R13 复审 |
| R13 | R12 全部 P1/P2 修复后的复审（`codex exec`，2026-07-29） | `FAIL` | 7×P1，全部已关闭：① **R12 引入的回归**——compose project 名只在 IT-02 加了 `-p`，与 ER-01/IT-03/IT-06 不一致，已全局统一为 `-p monoengine-it`；② IT-02 连通性守卫改为动态端口 + 探针存活确认 + nonce 身份断言；③ IT-07 审计脚本拒绝重复行、断言列数、ID 集合双向相等；④ IT-07 覆盖矩阵守卫改为解析表格数据行并核对 target 文件与 test fn 存在；⑤ IT-07 索引守卫改为双向集合相等并拒绝区间写法；⑥ IT-08 增加裸赋值零命中守卫（`VER` 2/8 → 3/8）；⑦ IT-09 的 allow 扫描扩到完整 `src/` 与 `bin/`（`bin/tests/*.rs` 是独立 crate root） | 无 P2 | `codex exec` R13 输出；R14 复审 |
| R14 | R13 全部 P1 修复后的复审（`codex exec`，2026-07-29） | `PASS` | 无 | 无 | `codex exec` R14 输出（P0/P1/P2 均为「无」）；本轮为模板 v2 整份迁移的收口评审 |
| E-IT-01 | IT-01 执行期 VER（登记 checklist 实跑，2026-07-29） | `PASS` | 无 | 无 | 以 `rustfs`/`rustfs-init` 对照 `docker-compose.test.yml`：固定 tag、`127.0.0.1` 高位端口、healthcheck、`profiles: ["init"]` one-shot、`depends_on: service_healthy`、`down -v`/零残留、secret 经 env、目标 OS=Linux；条目已写入 `docs/refactoring/test-infra.md`；`integration.md` `## 测试分层` 改为链接摘要 |
| E-IT-03 | IT-03 执行期 VER（版本手工证据，2026-07-29） | `PASS` | 无 | 无 | sanitized：compose 固定镜像 `alpine/git:v2.49.1`（digest 见 `docker-compose.test.yml` / `test-infra.md`）；容器 `git --version` 与 pin 相等（`git version 2.49.1`）；测试二进制 `monoengine 0.1.169`（IT-03 发布窗口）。本地版本手工证据；D 组后由 E-D-01 收口 |
| E-D-01 | IT-03/IT-04/IT-11 D 组远端证据（2026-07-30） | `PASS` | 无 | 无 | sanitized：`validate-config` success [30466831819](https://github.com/gitmono-dev/monoengine/actions/runs/30466831819)（v0.1.174）；`git-protocol-smoke` success [30479301244](https://github.com/gitmono-dev/monoengine/actions/runs/30479301244)（v0.1.177，23m44s，含 cargo-native gate + shell smoke + git `2.54.0` / git-lfs pin step）；v0.1.174/175 的 smoke 为 30m timeout cancel，非业务失败 |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-IT-01 | SSH 通道纳入 `bin/tests/` 黑盒分层与 CI 必跑 | SSH 通道已有脚本形态（`scripts/git_protocol_smoke.sh:254-262`），延后的是把它纳入分层与必跑门；依赖测试栈新增 sshd 服务设计，超出最小矩阵 | PT-04 启动 | `plan-long.md:106`（PT-04 行）；DEP-05 |
| DEFER-IT-02 | LFS 通道纳入分层与 Mega `tests/` 夹具审计移植 | LFS 已有脚本形态（`scripts/git_protocol_smoke.sh:265-269`，opt-in），延后的是分层归属与夹具移植，属协议专项矩阵范围 | PT-04 启动 | `plan-long.md:106`（PT-04 行）；DEP-05 |
| DEFER-IT-03 | 真多进程黑盒投递竞争矩阵 | 成本高；进程内双 dispatcher 已覆盖端到端投递语义，未覆盖的只有进程级崩溃窗口 | PT-09 多实例部署落地 | `plan-long.md:111`（PT-09 行）；DEP-06 |
| ~~DEFER-IT-05~~ | ~~`integration_multichannel_notification`~~ | **已撤销（2026-07-29）**：该黑盒用例早已落地（`bin/tests/integration_vault.rs:1008`），webhook / slack 渠道亦已实现（`src/notification/channels`），原延后理由不成立。编号保留不再复用；文档结论由 IT-07 改为「已落地」 | — | — |
| DEFER-IT-04 | orion-server / fake webhook receiver 拓扑接入 | 对应功能未存在；按 IT-01 规范随功能落地 | PT-06 / PT-09 启动 | `plan-long.md:108`、`:111`；DEP-07 |
| DEFER-IT-06 | 仓库级发布锁（原子认领、fence 校验、超时回收、崩溃恢复） | ER-12 要求：本仓无发布锁机制，纯文档 lease 无法验证；本计划按串行发布执行，不需要并发发布 | 确有并发发布需求时，先立独立 ADR + 独立计划 | 未指派（新计划） |
| DEFER-IT-07 | 全局 `enabled=false` 不得压制 `system_required` 通知 | IT-07 收口时核对：`should_send` 在检查 `system_required` 之前因全局 disabled 短路；用户偏好 API 已落地，但原「系统必发不可关闭」验收方向未完全满足。`notification.md` 仍写「system_required 可强制」，修复本缺口时须同步修订该文档 | notification 模块改进计划立卡修复 `should_send` 顺序/语义并修订 `docs/refactoring/notification.md` | `docs/plan/plan-20260727.md` 本表；`docs/refactoring/integration.md` P2 用户通知行 |
| DEFER-IT-08 | 对象存储 GCS 凭据 / 真实后端黑盒 gate | IT-07 收口时 S3/RustFS 路径已落地；GCS 未做，超出本计划 RustFS 合同 | orbit/GCS 后端验收排期时补黑盒 | `docs/refactoring/integration.md` P2 对象存储行；`orbit.md` |
| DEFER-IT-10 | 次渠道（Slack/webhook/in-app）投递失败的独立 retry | 多渠道扇出正路径已落地；`process_email_job` 对次渠道失败只打日志、不改变主邮件 job 状态，无 per-channel retry 队列 | PT-09 多渠道可靠性收尾 | `src/notification/dispatcher.rs`；`docs/refactoring/integration.md` P2 多渠道行；`plan-long.md` PT-09 |
| DEFER-IT-12 | 形态合法但仓库不存在的 HTTP clone 失败矩阵 | IT-12 以保留根 `/third-party.git/`（`parse_git_protocol_path` 拒绝）覆盖「失败路径不拖垮服务」；monorepo 对任意合法路径广告空仓库，无法形成稳定的 missing-repo 客户端失败 | PT-04 协议夹具与失败矩阵扩展 | `bin/tests/integration_git_cli.rs`；`plan-long.md` PT-04 行 |
| DEFER-IT-13 | SMTP 成功后、`mark_job_sent` 前崩溃的 at-least-once 重投 | IT-06 只覆盖「SMTP 前」stale `sending` 恢复；crash-after-SMTP 需 idempotency key，超出本卡写集（见用例注释 / `FIX-IT-06-01`） | PT-09 或独立 FIX 卡落地 idempotency | `src/notification/dispatcher.rs` IT-06 用例；`plan-long.md` PT-09 |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [x] 所有任务卡满足粒度规则 `G-*`：无未登记的 L 例外、无 XL 卡、无碎片卡、无未登记的合并发布例外、实现写集冲突均已消解；「任务卡粒度审计表」已填齐。
- [x] 所有非延后任务的 acceptance criteria 已满足，且 `Lifecycle=done` **且** `Acceptance=complete`（ER-04）。任何停在 `remote-pending` 的卡都必须先取得其 D 组远端后置门的绿色证据。任何仍为 `blocked` 的任务都必须先解除阻塞（`blocked` → `in-progress` → 完成剩余动作 → `done`）或按 `DEFER-*` 正式延后，不得带着 `blocked` 通过完成门。
- [x] 所有任务的 Verification 命令已运行并记录结果。
- [x] **计划完成门（区别于每卡 focused gate）**：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all` 全绿，且 `cargo build`、`cargo build --tests` 均 0 错误 0 警告（`AGENTS.md` 强制）；不得新增 crate 级 `#[allow(...)]`。
- [x] 必要的 docs/配置/错误契约/测试矩阵更新已完成。
- [x] 必要的 migration、rollback、failure-recovery 验证已完成；每张卡的 `Rollback mode` 都已被实际验证或记录为不可验证的原因。
- [x] 代码 review 最终结论为 `PASS`，P0/P1 全部关闭；仅 P2 residual risk 允许保留，且有具名接受人。
- [x] 如有发布要求，版本面两处一致、构建、提交、推送证据已完成（ER-08）；无 release artifact 要求时明确写 `N/A` 及原因。
- [x] 「修订历史」已记录成稿后的全部规范性变更（G-09）。
- [x] `plan-long.md` 相关 PT/SB 状态或日期计划索引已同步，或明确 `N/A`。
